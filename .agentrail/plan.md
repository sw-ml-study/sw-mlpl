# Saga: birds-def-docstrings

Add the required leading doc-string to the 14 Combinator /
Y-Combinator demo defs (def u:I(x) { x } etc.) that lack one,
clearing the pre-existing red def_comments_gate. Behavior
unchanged (doc-string is a no-op statement).

## Steps
1. docstrings -- add leading doc-strings; def_comments_gate
   green; demos still execute.
2. close -- rebuild pages, deploy, --done.
