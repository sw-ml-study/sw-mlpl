Saga E from docs/viz-ir-plan.md. Op derivation view inside the attention inspector. Walk back through stepMeshes from the currently selected tensor, collect named ancestors via regex on the RHS of each producer line, render the chain as clickable steps in the dialog. Each step jumps to that tensor. v0 ships plain HTML+CSS (no MathJax); MathJax lazy-load is a follow-up.