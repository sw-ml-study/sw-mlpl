User direction: lookup tables and listings order alphabetically (grouping kept only where it clearly aids discovery, alphabetical WITHIN groups; time-ordered logs like :experiments stay chronological; short narrative/pedagogical tables exempt). Apply to :builtins / the :ask cheat-sheet (runtime sort per group), the lang-reference builtin tables and Value Kinds table, and the usage-guide command table; fix the stale scatter row found during the audit; document the ordering rule.