Saga B from docs/viz-ir-plan.md. First viz renderer: pattern-detect 'A = softmax(...)' on a rank-2 square tensor in stage3d.js's __stage3d_add_step, attach viz: { kind: 'attention', attention: {...} } to userData, register an SVG attention-heatmap renderer via window.__viz_register('attention', fn). JS-side this saga; Rust port deferred to saga F (D3 upgrade restructures the renderer anyway). Demo proof: run demos/attention.mlpl, click A sculpture, see a 6x6 SVG heatmap with row-sum-to-1 colored cells using the viridis palette already used by the svg('heatmap') builtin.