Saga C from docs/viz-ir-plan.md, retargeted to vit_attention_pattern_multihead.mlpl. Reason: tiny_lm hides attention weights inside the chain (no clickable sculpture); the vit multi-head demo already produces a rank-3 [4, 17, 17] tensor via 'A = attention_weights(mdl, seq)' that the user can click. BPE token plumbing for tiny_lm-style models requires Rust eval changes and is split out into a future saga (queued in next-prompt). This saga: JS detection of '<var> = attention_weights(' on rank-3 tensors, head-selector dropdown in the renderer, integer indices for tokens.