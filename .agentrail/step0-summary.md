Saga bootstrap: extension-arrays-handles, from the demo-extensions blocker survey + user priority (the interactive native-3D demo needs the live extension boundary, not the renderer). Design: extend the scalar-only ExtValue with Array + Handle; marshal both layers (mlpl-eval/ext_marshal + mlpl-extension-cabi/marshal); the ABI already declares AbiArrayView/DTypeTag/AbiHandle (tags 6/7) but rejects them today.