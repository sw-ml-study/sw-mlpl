engram_stats(e, ids) / engram_stats(e, h, ids) builtin returning a record: rows_addressed / unique_rows / collisions from the new mlpl_engram_core::addressing_stats (frozen-contract collision counting: distinct contexts sharing a row per (order,head) region; repetition is not collision), nonzero_rows / max_row_norm memory health, gate_mean / gate_max via the eager forward whose gate now rides along (engram_forward returns (out, gate)). Catalog + lang-reference rows, help-completeness green. 6 new eval tests + 2 core contract tests, all engram suites green. Paydown: fncall_engram args split; warnings 340->339.