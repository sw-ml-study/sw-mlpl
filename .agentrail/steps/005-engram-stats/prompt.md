E3: engram_stats(e, ids) builtin. Report for a given engram + id vector: rows_addressed (total (t,order,head) lookups), unique_rows, collisions (distinct n-gram contexts sharing a slot -- count via the same ngram_hashes contract), plus memory-table health: nonzero_rows and max row L2 norm. Optionally accept (e, h, ids) to also report mean/max gate activation via the eager forward. Return a Value::Record so fields are addressable (stats.unique_rows), and render nicely in :describe-style output. Place eval in mlpl-eval-models (new model_engram_stats.rs; keep modules <=4 fns), dispatch alongside apply_engram in fncall_models/fncall_engram, math (collision counting) in mlpl-engram-core if it strengthens the frozen contract (hash_tests pin it). TDD: fixed-seed tests pinning exact counts for a known id set incl. a forced collision (small slots), record-field access, gate stats on a fresh engram (sigmoid(-2) ~ 0.1192 mean), error surface (non-engram model, bad ids). Scoped: cargo test -p mlpl-eval --test engram_stats_tests plus engram suites; mlpl-engram-core tests if touched; clippy/fmt eval (+engram component if touched); sw-checklist ratchet applies.