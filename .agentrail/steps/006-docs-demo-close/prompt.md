Final step of extensions-event-loop. Document what shipped and close the saga. Add user-facing docs for the Port + applet surface -- port_send/port_poll(port[,limit])/port_recv, on/off/run, and run_applet_with_host -- in the appropriate reference doc, WHAT and HOW only, no saga/step/date framing (per the user-facing-docs rule). Add a worked, headless-runnable MLPL applet example under examples/ (an on/run handler applet that folds state + submits commands, driven by a documented host). Update the demo-extensions upstream-contract note (docs/companion-demo-extensions.md / downstream-unblock-plan.md): B6 event-loop primitives shipped (Port, on/off/run, bounded poll, launch inversion, provider contract) with the interactive interpreted demo now unblocked at the host-primitive level; note the provider plugs in as the Rust UI-host closure. Update the wiki Documentation-Errata. Queue any follow-ons in docs/future-sagas-queue.md: the responsive-worker escalation notes, pub/sub topics, connect-mode main-thread handoff, and the mlpl-eval-env / mlpl-extension-cabi crate splits (both at module ceilings). Keep sw-checklist fails/warns decreasing. Then run agentrail complete with --done to close the saga.