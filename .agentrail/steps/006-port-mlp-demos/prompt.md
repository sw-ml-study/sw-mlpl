Phase 5: Port demos/moons_mlp.mlpl and demos/tiny_mlp.mlpl to the new DSL. Each demo should define its model as a one-line chain[...] expression, train it inside train { adam(loss, params(model), ...); loss }, and keep the existing decision-boundary rendering. The ported demo source must be shorter than the v0.6 form (measured in non-blank source lines). Verify both via mlpl-repl -f. Commit via /mw-cp.