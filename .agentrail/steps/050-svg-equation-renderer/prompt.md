Build Tier 1 math rendering: a self-contained, offline, high-contrast equation renderer that shows a Unicode math string as an SVG visual, working in BOTH the web playground (inline SVG) and the native REPL (SVG cache-file). Design: render a text equation -> SVG <text> using the existing viz scaffold palette (light #cdd6f4 glyphs on dark #1e1e2e); support multi-line (one line per newline) and simple sub/superscripts. Prefer reusing the svg() surface as svg(text, "equation") (branch eval_svg on a string arg0 for the equation type) to avoid a new builtin + catalog/coverage churn; fall back to a dedicated equation(text) builtin only if the string-arg path is too invasive. TDD: renderer unit tests assert SVG structure (<text>, fill, font, line count); an eval test for the surface. Add a small showcase (the convolution / moving-average equation). Docs: lang-reference + glossary + catalog note the 'equation' render. Rebuild binaries; redeploy DEV/nightly ONLY (no version bump, no stable). sw-checklist: retire >=5 FAIL if the surface grows; design new modules to the gate.