Tokenizers step 003 shipped. New language construct: for <ident> in <expr> { body }. Mirrors train/repeat semantics: each iteration binds ident to a rank-(r-1) slice of source's axis 0, runs body, captures final value into last_rows in the env. Non-axis-0 labels inherited on slices so labels(row) = 'feat' inside the loop. Implementation: new Token::For + Token::In keywords, Expr::For AST variant, new stmts.rs parser module (hosts parse_for, parse_annotated_assign, parse_braced_body shared with train/repeat), new eval_for.rs evaluator module. Compile path (mlpl-lower-rs) + grad.rs both reject Expr::For as Unsupported. 9 new tests covering sum-per-row, rank reduction, rank-1 source, label inheritance, rank-0 error, empty source, parser surface, legacy repeat/train regression, and lower-rs rejection. 655 total tests pass. sw-checklist +1 FAIL (mlpl-parser crate module count, same FAIL kind as other crates) -- traded to avoid stricter per-file budgets.