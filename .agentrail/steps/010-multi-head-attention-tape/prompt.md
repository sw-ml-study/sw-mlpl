Saga 29 step 010 (per docs/milestone-vit.md Phase 3): multi-head attention on the tape. crates/mlpl-eval/src/model_tape.rs currently rejects h > 1 (and rank-3 + multi-head also rejects in step 008 due to this). Replace the rejection with a per-head lowering: reshape [B, T, d] -> [B, T, h, d/h], transpose to [B, h, T, d/h], per-head scaled-dot-product softmax + matmul, transpose back, reshape, output projection. Backward: every primitive in the lowering already has a tape rule, so the multi-head backward is just the same backward with the head axis present. Gradcheck parity vs the single-head path on a d=16, h=4 fixture (where multi-head with a specific weight choice equals single-head -- standard sanity check). Also unblock the B=200 demo: replace the O(B^2) concat chain with a single batched-stack tape op so the full-200 pets_tiny demo runs in seconds. Update contracts/eval-contract for both changes.