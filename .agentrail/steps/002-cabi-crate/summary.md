New mlpl-extension-cabi crate: canonical #[repr(C)] V1 boundary (model) + register_c_extension host adapter that validates a provider descriptor, wraps each C invoke trampoline in a boxed closure marshaling scalar ExtValue<->AbiValue (panics contained, non-zero status->ExtError), and registers into the safe registry. TDD: 8 crate tests via a synthetic extern-C provider + 2 interpreter integration tests (cabi_demo:answer()->42).