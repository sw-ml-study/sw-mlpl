User regression follow-up: their playground page is served from http://large12:6466 (not the server's own 6464 origin), so every /v1 request incl. the devices probe is CORS-blocked -- the server's --cors-allow takes exactly ONE origin. Make --cors-allow accept a comma-separated origin list (tower-http AllowOrigin::list), TDD the parsing, rebuild+restart the 6464 server with the user's origins added (keep original flags), verify with curl Origin headers and a playwright run replicating the cross-origin page.