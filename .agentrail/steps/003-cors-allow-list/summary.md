Root-caused the persistent CUDA-demos-disabled report: the user's page origin was not in the server's single-origin CORS allow (only http://localhost:9957), so the /v1/devices probe was CORS-blocked regardless of retries. Made --cors-allow a comma-separated origin list (TDD, tower-http AllowOrigin::list), documented the exact-match rule, restarted the 6464 server allowing localhost/large12 x 9957/6466, verified per-origin with curl and cross-origin end-to-end with playwright (page on :6466 -> connect :6464 -> CUDA demos enable).