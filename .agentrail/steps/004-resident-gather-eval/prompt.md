E5 step 4 per plan: evaluate replacing the host-built selection one-hot with a device gather (DeviceShapeOps could grow gather_rows via mlx take_axis) IF a resident scatter-add backward is available in vendored mlx-rs (check ops/indexing indexmut surface); otherwise KEEP the selection-matmul (already resident with resident scatter-add backward via sel^T matmul) and instead cache the uploaded selection leaf across steps when ids are unchanged (fixed-context training re-uploads it redundantly every step). Measure both candidates with the seam counters and the engram seam test; ship the winner; document the decision honestly in the step summary. Scoped gates, commit.