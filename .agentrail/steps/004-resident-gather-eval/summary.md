Gather evaluation complete, selection-matmul retained with recorded evidence: mlx-rs has no scatter_add (IndexMut is scatter-replace) so device gather would break backward residency; sel^T matmul already IS a resident exact scatter-ADD (new duplicate-address parity test pins it); selection upload measured at ~0.3% of a 7.3ms warm step so the caching alternative was rejected too. Rationale documented on engram_tape's doc comment; sparse gather deferred to the sparse-rows saga with a real scatter-add kernel.