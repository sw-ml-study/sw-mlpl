F14: constant constructors fill/zeros/ones now usable inside grad as constant leaves (same eager-then-leaf mechanism as F5's stop-gradient builtins), so a loss can scale by a constant mask or add a constant bias built inline. Added is_const_ctor_builtin routed through eval_stop_gradient. Verified grad(sum(W*fill([2],3.0)),W)==[3,3], W*ones/W+zeros grad to ones. Backward-compatible (only previously-rejected names now succeed). grad_builtin_tests 27/27. lang-reference extended. sw-checklist 38/562 unchanged. Rebuilt repl/build; mlpl-serve rebuild batched to relay-close. Pushed.