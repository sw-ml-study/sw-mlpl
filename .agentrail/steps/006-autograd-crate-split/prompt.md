Refactor spike (b): split the mlpl-autograd crate to clear its ceilings before the remaining grad-surface work (softmax-axis RS2, transpose_axes RS3, pow-general). Current FAILs: tensor_ops.rs (8 fns), tensor_reduce.rs (9), backward_shape.rs (8) all exceed max 7; crate at 7 modules (ceiling). Options: extract sibling crates (e.g. mlpl-autograd-backward for backward.rs+backward_shape.rs, mlpl-autograd-ops for tensor_ops/tensor_reduce/tensor_shape) OR split the fat modules by responsibility across a lower module count. Goal: retire >=3 module-fn FAILs and get the crate under the module ceiling so the next feature can add a NodeKind/method/backward arm without a new FAIL. Pure refactor: no behavior change; the full autograd + eval grad suites must stay green. Measure sw-checklist before/after and target a net reduction in failed count. Rebuild repl/build release+debug.