Metal ON: mlx-rs features [accelerate]->[accelerate, metal] in all four mlx crates; metal_device_tests proves the default device is the GPU. Metal exposed a concurrency hazard (SIGSEGV under parallel submissions) fixed with the process-wide mlpl_mlx_rt::mlx_op_lock (held by every op + both gpu_step entries) and per-crate MLX_TEST_LOCKs in forward/model tests. Repaired latent bit-rot (lora_mlx_demo_tests env_api import; cross_entropy 51-line FAIL split via validated_targets). BASELINE RECORDED in docs/benchmarks.md: Metal is 10-30x WORSE than Accelerate on the per-op path (tiny_lm_train_step 0.008x vs CPU) -- kernel-launch latency dominates at demo scale, proving persistent tensors + per-step graphs (steps 2-5) are the entire win. All parity suites green on GPU. Warnings 339->338.