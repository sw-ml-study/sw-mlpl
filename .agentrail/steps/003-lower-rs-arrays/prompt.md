Phase 2 cont.: extend mlpl-lower-rs to cover the array surface. Lower ArrayLit (including nested), FnCall for the phase 1 primitives (iota, shape, rank, reshape, transpose, reduce_add), and variable bindings via Assign + Ident. Emit let-bindings in the generated Rust that match MLPL's variable semantics. Reshape/transpose use the mlpl-rt wrappers so label propagation (Saga 11.5) comes for free. TDD: lower and compile 'x = iota(6); y = reshape(x, [2, 3]); transpose(y)' and assert the runtime result matches what the interpreter produces for the same source.