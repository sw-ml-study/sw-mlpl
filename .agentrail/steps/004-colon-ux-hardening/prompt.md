User-reported astonishments: (1) :history() errors 'unknown function: history' -- when :name(...) fails and name is a REPL command, hint that commands take no parentheses; (2) :describe :disp says 'not a bound variable, model, or built-in' -- strip a leading colon and describe the builtin; (3) :list :disp -- hint that :list is for u: functions and point at :describe for builtins; (4/5/6) :help <unknown-topic> falls through to evaluating the remainder as MLPL (':help :disp' prints the BuiltinRef, ':help disp' says undefined variable, ':help :disp()' calls disp) -- unknown help topics must print the topic list, never evaluate. TDD; cover web local, serve, and terminal repl surfaces.