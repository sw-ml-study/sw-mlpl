Lower take and floor to the compile-to-Rust path (wc's first two of four). Confirm from the interpreter map: take supports take/3 = take(a, offset, length) (a bounded slice) and possibly take/2; reproduce its EXACT slice/clamp semantics. floor/1 is elementwise math (likely in mlpl-runtime-math). Both are pure DenseArray/math -- add a runtime primitive in mlpl-rt (mirror the local shape/rank/tally style, or re-export a pure runtime fn) and wire into the fncall REGISTRY (take/3 needs an emitter passing the array via lower_darr + offset/length as DenseArray scalars; floor fits a unary elementwise shape). Add dispatch_coverage_tests Builtin variants. TDD RED first with gated MLPL_BUILD_TESTS=1 e2es: take(iota(10), 2, 3) -> the 3-element slice; floor over a fractional array. Hold sw-checklist. After push, refresh target/{release,debug} mlpl-repl + mlpl-build. Commit source before agentrail complete.