env-types-out landed: new mlpl-eval-state crate (types component, 4 modules) hosts Interrupt, OptimizerState, ExperimentRecord/ParamShape, and the whole GPU seam (GpuEnv, GpuAdamStep, AdamHp, DemoLayout, LoraNames, OnceLock registry). mlpl-eval deleted its three gpu modules (100 -> 97) and keeps every public path via shims + re-exports; cuda/mlx-eval imports unchanged. Verified with targeted test files per the newly refined scoped-testing policy plus cpu+cuda clippy. Ratchet 292 -> 291.