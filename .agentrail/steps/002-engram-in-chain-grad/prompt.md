E3 step 2: tape path for the in-chain Engram so train/adam works on a full model expression. Extend apply_model_tape (components/models-write/crates/mlpl-models-tape/src/apply.rs) with the same ids-threading rule as eager apply_chain: a Chain child of variant Engram receives the chain's ORIGINAL input as ids (evaluated eagerly, ids are not differentiated) and the running tape node as h, lowered via the existing engram_tape (layers.rs) with EngramInputs built like grad_calls_engram.rs does. Also mirror the eager errors (bare Engram on tape stays unsupported with the clear message). TDD in mlpl-eval tests (new engram_chain_grad_tests.rs): (a) grad of a loss through chain(embed, attn-block, e, head) matches grad through the manually composed apply_engram expression (same seeds/params); (b) frozen-base training: freeze all non-engram layers, train N adam steps -- base params bit-identical after, engram memory rows move ONLY where addressed (unaddressed rows stay exactly zero), loss decreases; (c) gradcheck-style finite-difference spot check on one addressed memory row through the chain. Scoped tests: cargo test -p mlpl-eval --test engram_chain_grad_tests --test engram_grad_tests --test engram_chain_tests plus tiny_lm_train/freeze test files if optimizer surface touched; clippy/fmt in components/eval and components/models-write; sw-checklist ratchet applies.