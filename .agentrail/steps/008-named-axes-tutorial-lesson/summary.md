Saga 11.5 step 008 shipped. New 'Named Axes' tutorial lesson inserted between Matrices and Linear Algebra in apps/mlpl-web/src/tutorial.rs (index 5, 13 example snippets smoke-tested via mlpl-repl -f). Model Composition lesson updated: X annotated as [batch, feat] with a labels(apply(mdl, X)) example showing batch flowing through the forward pass. Plus drive-by fix: DenseArray::map now preserves labels (was dropping them, which stripped labels through every activation in the Model DSL). One-line change + regression test. 561 tests passing. Pages rebuilt.