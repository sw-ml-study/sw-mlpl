Second :ask hallucination round (after the grounding step): the model copied 'mlpl> line => <DenseArray...>' transcript echoes into its runnable example (user pasted them, parser threw UnexpectedCharacter '>'), and still built a model inside def u:. Harden: (1) explicit no-echo rule -- example code must be raw lines only, the => output format in the session context is a display artifact; (2) a STRICT RULES RECAP appended at the very END of the system prompt (recency placement for weak models) restating positional-only args, top-level-only models/train, and raw-lines-only; native tests pin both; rebuild pages.