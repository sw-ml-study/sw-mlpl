Reusable dynamic loader: mlpl-extension-loader::load_c_extension(path) dlopens a provider cdylib (libloading, confined to this crate), resolves the sw_mlpl_extension_v1 entry, validates + register_c_extension's the descriptor (reusing existing struct_size/abi_version checks), returns the namespace name, and holds the Library mapped for the process (process-global vec, no dlclose v1) so the descriptor fn pointers stay valid. Marked unsafe with a documented safety contract. TDD: load mlpl-ext-testdylib -> namespace testext + testext:answer dispatches to 42.0; bad path is a clean Err. Checklist 44/523 held; new crate clean.