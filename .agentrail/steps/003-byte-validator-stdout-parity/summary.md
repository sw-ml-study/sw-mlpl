Added a loud-reject array_to_bytes validator to mlpl-rt-value (rank<=1, cells integer 0..=255, interpreter-parity error messages) and rewired compiled write_stdout to validate (reject not truncate) and return ok(count)/err(msg), propagating write/flush errors. Generated main now prints via CVal Display (handles Result results; scalars unchanged). rt-value unit tests + 2 gated e2e (valid bytes -> ok+write; 256 -> err, no truncation); full 12-test e2e suite green. sw-checklist HOLD 554/49/521.