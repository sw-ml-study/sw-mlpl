Built-in layer weight access + bias-free/eps options (user-approved 2026-09-22; asks from microgpt-mlpl, also serves reasoning-from-scratch pretrained-weight loading). (0) BUG: params(model) is documented in lang-reference ('flat list of parameter names owned by a model') but is 'unknown function' in mlpl-repl -- it exists only inside the optimizers (collect_params / ModelSpec::params). Ship it (string-list return; note string-lists are second-class -- pick the representation other name-list builtins use) or fix the doc; decide + pin with a test. (1) get_param(model, role) / set_param(model, role, value): address a built-in layer's weights by ROLE name (attention: Wq Wk Wv Wo; linear: W b; embed: table; rms_norm/layer_norm gamma/beta as applicable; for chains, define how a sub-layer is addressed, e.g. walk to the first layer owning the role or accept a layer index -- design it, document it). set_param shape-checks and errors naming the expected shape; it must persist across user-function frames like optimizer writes (grad::set_trained_param) and invalidate any cached resident copies (optim_state.resident_witness / gen_state caches -- check). Today weights are only reachable through hidden generated names like __attn_Wq_0 (model-counter dependent) -- do not document those. Document orientation: x[T,d] @ W[in,out]; multi-head: head h = contiguous columns h*dk..(h+1)*dk (so PyTorch [out,in] weights need one transpose). (2) rms_norm(dim, eps): optional eps (default stays 1e-8 so existing results are unchanged). (3) bias-free linear: optional arg (e.g. linear(in, out, seed, 0) or a named variant -- choose, keep existing 3-arg behavior) so Llama/Qwen-style projections and MLPs need no hand-written matmul. TDD: set_param then apply equals the hand-written matmul forward; get_param round-trip; shape-mismatch error; set_param'd attention trains under adam; rms_norm eps changes output as expected; bias-free linear has params(model) without b and param count d_in*d_out. Check the Model DSL crate module/function budgets before adding (split first if needed). lang-reference rows; q-and-a answer to microgpt-mlpl (their default mode can switch to causal_attention/rms_norm/linear and parity mode can set_param microgpt-rs weights). Rebuild release+debug mlpl-repl.