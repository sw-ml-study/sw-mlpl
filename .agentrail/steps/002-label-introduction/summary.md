Saga 11.5 step 002 shipped. label(x, ["a", "b"]) builtin attaches labels to an array; new annotation syntax x : [a, b] = expr desugars to a label() FnCall so the evaluator has one codepath. Parser gains a Colon token and parse_annotated_assign; DenseArray gains with_labels returning LabelsRankMismatch on disagreement. 17 new tests green (array with_labels, parser desugar, eval round-trip + annotation). All quality gates pass; sw-checklist net -2 FAILs (no new failures introduced). Pages rebuilt.