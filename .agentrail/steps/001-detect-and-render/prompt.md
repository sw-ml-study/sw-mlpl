1. In stage3d.js __stage3d_add_step, regex-detect '<var> = softmax(' on event.label when shape is [N, N] and values present. Set userData.viz = { kind: 'attention', attention: { weights, q, k, queryTokens: [0..N], keyTokens: [0..N], causal: false } }. 2. SVG attention-heatmap renderer: viridis palette (matches mlpl-viz svg/heatmap_grid.rs ramp); axis labels on top + left; hover tooltip showing weight value; row sums shown as a small bar on the right. 3. window.__viz_register('attention', renderer). 4. Demo proof: open attention.mlpl, click A, see a 6x6 colored grid.