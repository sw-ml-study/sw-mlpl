Saga 29 step 018: generic CSS-spinner running indicator wired into every demo-runner and REPL-submit line, so blocking WASM evals show a visible 'still alive' signal instead of a frozen page. New EntryKind::Running variant + entry_render Running arm + @keyframes spin CSS (animation runs on browser compositor, continues during JS blocking). running_message() returns line-kind-tailored captions (train -> 30-90s warning, predict_batch/apply/attention_weights -> 'forward pass through trained model', svg() -> 'rendering visualization', generic -> 'evaluating...'). Wired into schedule_demo_line AND make_submit_batch (refactored to async Timeout-yielding via new process_next_eval helper). Extracted push_running_marker + replace_running_with_result helpers; moved :upload-command helpers to new crate::upload_cmd sibling module to keep handlers.rs under the 500-line budget. 30 web tests pass; sw-checklist 157 (== parent). Pages rebuilt.