Step 013 extracted embed_table + estimate_train to new mlpl-models-inspect sub-crate (5 modules, each <=3 fns). Added HasStrings trait to mlpl-env-traits (needed for estimate's device-throughput override). Bundled mlpl-eval-side wrappers into model_inspect.rs (replaces model_embed_table.rs + model_estimate.rs). estimate.rs split into 3 helper files (estimate + estimate_compute + estimate_walk) to keep each <=4 fns. collect_stats shrunk from 26 LOC to 22 via Stats::default().