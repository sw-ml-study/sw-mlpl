Resident optimizer shipped: ONE tape per step under device(mlx) (was N tapes -- one per param), weights seeded from a pointer-witnessed resident cache, adam/momentum moments live as TensorHandles in OptimizerState.resident and never leave the device, update math composed lazily (new UnaryKind::Sqrt), one download per param per step refreshes the host mirror for eager readers. FOUND+FIXED a real CPU optimizer bug the parity test exposed: the per-param tape rebuild gave later params gradients computed at ALREADY-UPDATED earlier params (sequential semantics artifact); both CPU optimizers now use batched step-start gradients (also ~Nx fewer CPU forwards per step). Full mlpl-eval release suite + web-demos suite green under the semantics fix; new resident_optimizer_tests pin residency, 3-step trajectory parity, frozen-base engram training. Warnings 338->340 (+2 structural, spike owed).