Saga 29 step 022: built a markdown-ish renderer (apps/mlpl-web/src/path_body/) so learning-path step bodies render as readable HTML with paragraphs, bullet lists, ordered lists, inline code/bold/emph -- not one wall of run-on text. Wired into Note + Glossary render sites in paths_view.rs. CSS: path-step now scrolls (max-height 70vh, overflow-y auto) so long content doesn't push the page around; new path-body-* styles match the theme. 6 new unit tests cover empty/paragraph/bullet/ordered/multi-line cases. sw-checklist 157 (== parent). Pages rebuilt.