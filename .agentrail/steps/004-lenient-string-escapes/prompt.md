RS-escape (../reasoning-from-scratch): MLPL string literals hard-error on unknown backslash escapes (escape_char in components/syntax-lex/crates/mlpl-lex-string/src/lib.rs recognizes only \" \\ \n \t \r and returns UnexpectedCharacter otherwise). LaTeX/math data (\frac, \in, \boxed, \pi, \int) and the downstream's \; normalization all fail to lex. Fix: make an UNKNOWN escape lenient (Python-style) -- keep the backslash literally, so \i -> backslash+i. Implement multibyte-safely: change escape_char to return Option<char> (Some for known escapes, None otherwise); in handle_escape, on None push a literal backslash and return the position AT the escaped char so the main loop reprocesses it through the UTF-8 path (do NOT cast the byte to char). Known escapes (\n\t\r\"\\) unchanged. TDD: string literals with \frac/\in/\;/\pi lex to the literal backslash-prefixed text; \n/\t/\" still map to their control chars; a multibyte char after a backslash round-trips. Add tests in mlpl-lex-string (tests/) and an eval-level test. Rebuild repl/build release+debug (no serve/grad change).