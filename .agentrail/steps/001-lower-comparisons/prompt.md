Lower the elementwise comparison + logic builtins in the compile-to-Rust path so eq/2 and the family stop being 'unsupported construct'. Mirror the arithmetic binop lowering in mlpl-lower-rs/src/lower.rs (Expr::BinOp via ApplyBinopExt::apply_binop with a per-op closure) OR add a runtime comparison primitive re-exported from mlpl-rt (analogous to the mlpl-runtime-bits family) -- pick whichever matches the interpreter's implementation. Lower the exact set the interpreter supports (eq, ne, lt, le, gt, ge, and, or, not, equal -- confirm the names/arities from the interpreter dispatch). Match interpreter elementwise 0.0/1.0 semantics + broadcasting; add them to the fncall REGISTRY and the dispatch_coverage_tests Builtin enum. TDD RED first with a gated MLPL_BUILD_TESTS=1 e2e computing eq/lt/and/not and printing 0/1 results. Hold or lower sw-checklist (exception if test-only holds). Commit source before agentrail complete.