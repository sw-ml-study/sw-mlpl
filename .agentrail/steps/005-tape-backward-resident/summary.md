Resident backward shipped: NodeData.grad is a TensorHandle; core backward formulas (elementwise equal-shape, rank-2 matmul, activations with constant-free tanh'/sigmoid' + Gtz relu mask, sum/mean via DeviceOps::full, softmax) run as lazy device compositions in the new resident_backward module; root seeds device-side; accumulate handles all residency mixes with exact f64 for pure-host. Structural + CE backward stay CPU-exact with re-upload on accumulate. Tests caught a live hazard (non-resident tapes uploading when a backend was registered -- now gated on tape.resident + pinned). 13-op backward parity suite, grads-stay-resident assertion, tiny-LM mixed chain, all mlx + CPU suites green. Warnings 336->338 (+6 structural, 4 retired, cheap retirements exhausted -- paydown spike formally owed per the queue).