Write docs/bit-ops-design.md: the fixed-width-integer bit-op surface for MLPL's f64 arrays. Decide and document: integer domain (non-negative, exact 0..2^53; reject negative/non-integer/NaN/inf/out-of-range loudly), width parameters (1..=53; masks keep low width bits), LSB-first bit order (bit i = 2^i; from_bits inverse of bits), element-wise with scalar broadcast, width-aware shl(x,k,width) that masks (avoids the >2^53 exactness trap) vs plain shr(x,k), and the op set: band/bor/bxor/bnot/popcount/shl/shr/bmask/bits/from_bits. Note Hamming distance = popcount(bxor(a,b)). Placement rationale (pure -> runtime-array, universal). Commit.