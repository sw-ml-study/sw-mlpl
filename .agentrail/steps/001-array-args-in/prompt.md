Open the extension boundary to ARRAY ARGUMENTS (B4 send direction, for native3d:set_lines). (1) Extend the safe boundary mlpl_extension_abi::ExtValue with an Array variant carrying a dtype (u8/i64/f32/f64), shape (Vec<usize>, rank 1-8, row-major contiguous), and the flat data. (2) mlpl-eval/src/ext_marshal.rs::to_ext: accept a Value::Array of rank>=1 (currently only rank-0 scalars/strings pass) and build ExtValue::Array; keep rank-0 mapping to I64/F64 as-is. (3) mlpl-extension-cabi/src/marshal.rs: marshal ExtValue::Array -> AbiValue with tag DenseArray(6) + an AbiArrayView (dtype, rank, contiguous data slice, shape ptr, strides) that the C invoke fn can read; enforce dtype/rank<=8/size<=16MiB and reject others with a clear error (not a panic). TDD RED first: extend the existing extension_cabi_dispatch_tests.rs pattern with a repr(C) provider  (arity 1) that reads an f32 [2,3] AbiArrayView of 1..6 and returns F64(21.0); assert MLPL  == 21 in ONE call. Also a reject test (unsupported dtype/rank). Scope tests to the touched crates (mlpl-extension-abi, mlpl-extension-cabi, mlpl-eval). clippy/fmt those crates; repo-wide sw-checklist; ratchet both axes down or exception. Do NOT rebuild the release binary yet.