Step 001 (array-args-in): dense arrays now cross the C extension boundary in the SEND direction (B4). New ExtDtype dtype module (u8/i64/f32/f64 with pure width/wire_tag/encode_le/byte_strides); ExtValue::Array variant; ext_marshal sends rank>=1 Value::Array; new marshal_array (dispatcher + validate + owned-holder AbiArrayView that outlives the invoke call). A repr(C) sum_array provider reads an f64 [2,3]/[6] and returns 21 in one call; over-rank rejected. wire_tag pinned to DTypeTag by a test. All code correctly formatted, net-zero ratchet (44/522) via loose-coupling refactor (dtype methods, orchestrator/assembler split) -- no formatting bypasses.