Step 022 retired the fetch_dataset.rs Module-Function-Count FAIL (13 fns > 7) by splitting the inline #[cfg(test)] mod into 4 sibling test files declared via #[path = ...] from fetch_dataset.rs. Each file <=4 fns = PASS no warn. Helpers in fetch_dataset_tests_helpers.rs exposed as pub(super) so sibling test mods can import them. Bonus: extracted assert_decode_fields helper inside archive.rs which retired the decode_directory_to_record 31-LOC warn. STRICT RATCHET MET: -1 FAIL, -1 WARN, no exception trailer.