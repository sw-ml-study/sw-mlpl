F5: index/mask builtins (argmax, one_hot, eq, gt, lt, argtop_k) now usable inside grad as stop-gradient constants. eval_stop_gradient computes their value from the current forward values of args (tracks params across adam steps) via the canonical eager mlpl_runtime::call_builtin and inserts a non-tracked leaf; gradient flows through surrounding ops to selected values, never the mask. Enables an inline Switch-style top-1 router mask: grad(sum(one_hot(argmax(R,1),E)*R),R) == the mask, verified in release binary == [[0,1,0],[1,0,0]]. Additive guard ahead of the match in eval_tensor_fncall (previously these hit the reject); backward-compatible. Tests: grad_builtin_tests 23/23 (+2). lang-reference documents the stop-gradient family. sw-checklist 38/562 unchanged (grad.rs already a held module-fn FAIL). Rebuilt release+debug binaries. Pushed.