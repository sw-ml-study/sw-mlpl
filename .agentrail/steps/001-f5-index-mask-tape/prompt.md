F5: let index/mask builtins (one_hot, argmax) be used inside a traced loss by treating them as stop-gradient (constant) nodes on the tape -- forward value is the eager result, no gradient through indices. Enables a top-1 router mask computed inside the loss. TDD: grad(sum(one_hot(argmax(W), k) * W), W) succeeds and matches the eager mask. Backward-compatible additive tape arms. See docs/sw-mlpl-findings.md F5.