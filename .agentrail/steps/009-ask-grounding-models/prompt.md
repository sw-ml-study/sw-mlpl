The :ask Ollama assistant hallucinated keyword-argument syntax (linear(in=..., out=...)) and a model built+trained inside a def u: body -- both invalid (no kwargs exist; the Model DSL and train blocks are top-level-only, verified empirically: models cannot be created in, passed to, or trained inside u: functions). Strengthen the :ask system prompt's MLPL_SYNTAX cheat sheet with these two rules plus one canonical correct training example; move the prompt text consts to a non-wasm-gated module so a native test pins the anti-hallucination rules; rebuild pages.