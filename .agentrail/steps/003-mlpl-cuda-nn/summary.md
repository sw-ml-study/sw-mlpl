mlpl-cuda-nn: reductions (mean/argmax GPU; reduce_mul->CPU, candle has no prod), normalization (softmax/log_softmax via candle_nn), cross_entropy (GPU row-logsumexp + CPU gather). Activations turned out to be elementwise unary maps, so moved into mlpl-cuda-elementwise (exp/log/relu/sigmoid/tanh + neg via a unary! macro). All parity-tested vs CPU mlpl-rt on the RTX 5060 Ti within fp32 tol; warning-free + failure-free (baseline held); proper formatting, no LOC cramming (split cross_entropy into shape_nv/validate_targets/row_logsumexp).