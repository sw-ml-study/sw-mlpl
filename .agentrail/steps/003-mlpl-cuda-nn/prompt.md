Step (was 2b) of cuda-foundation: create mlpl-cuda-nn (in the components/cuda-rt/ workspace, depends on mlpl-cuda-rt) -- the nn op surface deferred from step 2: activations (exp/log/relu/sigmoid/tanh via candle Tensor ops + candle_nn::ops::sigmoid), reductions (mean via Tensor::mean over axes; argmax via Tensor::argmax; reduce_mul has NO candle prod op -> delegate to a CPU fold, documented like transpose), and softmax/log_softmax (candle_nn::ops) + cross_entropy (mirror mlpl-mlx-rt::reductions::cross_entropy: logsumexp - picked-logit mean, exact CPU-match validation of targets). Mirror the MLX semantics exactly (axis Option<usize> for reductions, usize for softmax; reductions drop the reduced-axis label; softmax preserves labels; cross_entropy returns unlabeled scalar). Respect the budgets: <=4 fns/module and <=4 modules/crate (warning-free) -- split into modules like activations.rs / reduce.rs / softmax.rs + a drop_axis helper; if it would exceed, add another sibling crate, do NOT cram. Each op parity-tested vs the CPU mlpl-rt reductions/activations within fp32 tol on THIS GPU. candle autograd thread-safety: forward-value tests can run in parallel. DISK: scope to cuda crates, clean -p after.