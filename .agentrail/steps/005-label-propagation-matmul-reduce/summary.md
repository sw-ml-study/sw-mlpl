Saga 11.5 step 005 shipped. matmul checks contraction axis labels (LabelMismatch on disagreement) and threads non-contracted labels through via matmul_labels helper. reduce_axis and argmax_axis drop the reduced axis's label inline. eval.rs adds an axis-name resolver branch: reduce_add/reduce_mul/argmax/softmax accept a string literal axis, looked up against the array's labels, then dispatched through the existing runtime path. 18 new tests green (9 array + 9 eval). All quality gates clean; sw-checklist unchanged FAIL set.