Update 'Game of Life (APL classic)': emphasize the ONE-LINE u:life step function as MLPL's answer to the APL2 one-liner; switch the stack reduction to the higher-order form reduce(:add, S, 0) to showcase builtin REFERENCES (the :name syntax -- a first-class function value, vs calling the builtin); add DEEP disp views: disp(S) of the [8,7,7] neighbor stack (labeled slice stack) and a rank-4 projection disp(reshape(S, [4, 2, 7, 7])) if disp renders rank 4 -- verify via probe first. Narration ties depth to APL2 completeness (beyond scalars/vectors/matrices). Probe-validate all lines; focused smoke test; pages rebuild.