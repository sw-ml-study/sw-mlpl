Add tab-completion to the web-UI REPL input (apps/mlpl-web). MVP scope: REPL only, simple popup for ambiguous matches. (1) Locate the REPL input handler in apps/mlpl-web/src (grep for repl.rs / textarea / onkeydown / current input handling). (2) Intercept Tab keydown with preventDefault. Compute the current prefix at the cursor position (alphanumeric / underscore / colon characters back to the previous whitespace). (3) Build a completion source from: REPL commands like :version / :wsid / :vars / :models / :experiments / :tags / :describe / :builtins / :introspect (use the same names the dispatcher in mlpl-eval/src/inspect.rs accepts); builtin names from mlpl_runtime::runtime_builtin_names(); language keywords (train, repeat, experiment, etc. -- enumerate from mlpl-parser if available); user-bound names from the live Environment (vars + models). (4) Behavior: unique match -> replace prefix in-place with the completion; multiple matches -> show a small floating popup near the cursor listing the candidates with click-to-pick. Arrow-key navigation in the popup is OUT OF SCOPE for the MVP (click-to-pick is enough). (5) Unit tests for the prefix-extraction + matching logic in pure Rust (no Yew DOM needed). (6) Update glossary with a 'Tab completion (REPL)' entry. (7) Pages rebuild required since apps/mlpl-web changed. (8) Other input areas (demo editor, tutorial try_it) are deferred -- file as a follow-up step if the MVP works well.