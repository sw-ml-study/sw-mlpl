REPL tab-completion MVP: Tab key in the web REPL input now resolves the token prefix at the cursor against REPL slash-commands + MLPL keywords + every runtime builtin. Unique match inserts inline; ambiguous match renders a row of click-to-pick chips below the input. New completion.rs module with 14 pure unit tests. mlpl-eval re-exports runtime_builtin_names from mlpl-runtime. Glossary entry added. Pages rebuilt.