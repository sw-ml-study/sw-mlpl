Phase 3: Shape-aware sculptures.

Replace placeholder boxes with shape-proportional meshes in stage3d.js: scalar = small sphere, vector [N] = horizontal bar (length proportional to N), matrix [M,N] = flat rectangle (M x N proportional), rank-3+ tensor [B,M,N] = stacked rectangles (B layers). Labels show 'name: [shape], rank R, N elements'.

Add D3.js (vendored or CDN) for per-element data-driven animation within sculptures -- not used yet in this step, just loaded and available. Pages rebuild required.