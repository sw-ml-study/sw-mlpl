Extracted mlpl-eval-core as a new foundation crate with 3 leaf modules (model.rs, metric_sink.rs, inspect_groups.rs). These had zero outbound crate:: imports and moved cleanly. mlpl-eval's lib.rs re-exports MetricSink, ModelSpec, and documented_builtin_names through the facade so every downstream import path is unchanged. Discovered the planned 4-way split needs more architectural work: the intercept layer (model_dispatch, model_clone, etc.) calls back into crate::eval::eval_expr, creating eval <-> model cycles that block moving those modules into sibling crates without further refactoring (trait inversion or per-module shim splits). Documented this in the commit body and the next-step prompt; subsequent steps will tackle it incrementally rather than as one big split. Tech-debt sweep bundled: -1 FAIL (compute_attn_weights 63->45 LOC via batched_attn_weights extraction), -1 warning (tokens_to_onehot extracted validate_token_id helper; parse_sse_stream absorbed err_outcome closure). sw-checklist: 151->150 failed, 452->451 warnings. Net-negative on both axes.