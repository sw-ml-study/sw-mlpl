Built the on-device MLX training kernel in mlpl-mlx-train and proved the LoRA fine-tune mechanism end-to-end. Added lora_linear (traceable LoRA forward x@(w+scale*(a@b))), train_steps (on-device forward+value_and_grad backward+MlxAdam loop returning the loss curve). Two gated tests pass on Apple Silicon: value_and_grad matches a finite-difference gradient over both adapters (parity) and MlxAdam collapses a frozen-base+rank-1-adapter loss. Discovered the interpreter forward is eager/CPU-materialized so it can't be value_and_grad-traced as-is -- the full model forward must be rebuilt in mlx_rs ops (step 007). Consolidated to a clean 4-module crate (0 warnings) and serialized the non-thread-safe value_and_grad tests on a crate-local lock.