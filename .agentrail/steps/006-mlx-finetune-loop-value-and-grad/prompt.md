Step 006 (approach A integration): wire mlpl-mlx-train into the real MLX LoRA fine-tune loop. Today the device("mlx") fine-tune builds a CPU autograd tape (mlpl-autograd) + runs CPU adam (mlpl-eval/src/grad_optim.rs). Goal: when env.device()=="mlx", the fine-tune STEP expresses its loss as an mlx_rs-traceable closure over the LoRA adapter params (the trainable set from lora()) and uses mlpl_mlx_train::loss_and_grads + MlxAdam instead of the CPU tape + CPU adam, so backward AND the optimizer run on-device. Key sub-problems: (1) get the LoRA adapter params as mlx_rs::Array (reuse mlpl-mlx-rt's dense<->mlx converters in common.rs); (2) build the forward (embed/attention/linear + loss) as a closure |&[Array]|->Result<Vec<Array>> -- this likely needs the model-DSL forward expressed against mlx_rs Arrays, the biggest piece, scope/split as needed (refactor up and out: new modules/crates freely); (3) keep moment buffers across steps in MlxAdam; (4) write adapter weights back into the Environment after each step. Parity-test the LoRA loss curve vs the CPU path within fp32 tol (extend lora_mlx_demo_tests.rs), then relabel the 'MLX LoRA fine-tune' demo from hybrid to true-GPU (lm.rs + the NOTE). Keep sw-checklist non-regressing; commit .agentrail with source. DISK: build scoped to mlx crates, clean after.