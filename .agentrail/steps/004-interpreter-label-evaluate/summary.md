Step 4 interpreter-label-evaluate: label/relabel/reshape_labeled now EVALUATE their name argument and accept a StrList, a comma-Str, or a variable holding either -- via the shared axis_adapter::axis_names_of. Removed the AST-only parse_axis_names. This closes the label-vs-reduce inconsistency: label(x,"a,b") and reduce(:add,x,["a","b"]) both work. Added a table-driven parity test (every label-form x reduce-form agrees; label accepts a computed name vector; reshape_labeled accepts comma-string; numeric/scalar name args still rejected). Regressions (builtin_eval/reduce_axes/pets_tiny) green. 38 failed / 557 warnings (no new fail). Backward-compatible.