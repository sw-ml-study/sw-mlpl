Shipped f32 + u8 wire dtypes on the MLX peer wire. New DTYPE_F32=1 + DTYPE_U8=2 constants; encode_tensor_as(arr, dtype) picks the on-disk encoding; decode_tensor dispatches on tag and lifts back to DenseArray (f64 internally). f32 cuts wire payload in half; u8 cuts to one-eighth (the headline win for image-tensor transport). Promotion ladder documented in module docs. 8 new tests cover round-trip precision, payload-size deltas, unknown-tag rejection, out-of-range u8 saturation. Drive-by fixed two cross-workspace drift issues in mlpl-mlx-serve (create_session InterruptMap arg + Value::BuiltinRef match arm). sw-checklist 138 held.