Saga 14 step 008 shipped with an honest speedup-gate miss. demos/tiny_lm_mlx.mlpl wraps Saga 13's tiny_lm.mlpl body in device("mlx") {} with identical seeds/hyperparams/dataset; loss curve matches CPU within fp32 tolerance (validated via a scaled micro-variant test V=60/d=16/T=8/3 steps). New mlpl-bench mlx_vs_cpu.rs Criterion harness on Apple Silicon measured: reshape_reduce_100x100 0.84x (MLX slower), tiny_lm_train_step 0.26x (MLX slower). 5x warm-path gate MISSED -- per the step-008 prompt's explicit fallback path, shipped the demo anyway with the honest number documented and deferred optimization to a follow-up step. Four bottlenecks diagnosed in docs/benchmarks.md 'Saga 14: MLX vs interpreter CPU' section: (1) f32/f64 round-trip on every op, (2) no graph fusion (every op .eval()s immediately), (3) Saga 14 step 006's tape re-materialization doubles MLX forward work, (4) Tiny LM inner dims d=16/32 make kernel-launch overhead dominate arithmetic -- same arch at d=256+ would flip ratio. Correctness unaffected: every MLX parity test across steps 001-007 continues to pass. Shipped: demo file, eval-side micro-variant validation test, bench harness, mlpl-bench mlx feature, mlpl-repl mlx feature, benchmarks.md section with bottleneck analysis + follow-up pointers. Quality gates: workspace tests clean default, mlx-feature tests clean, clippy both modes clean, markdown-checker clean, fmt clean, sw-checklist FAIL count unchanged (135). Commit 520a485 pushed.