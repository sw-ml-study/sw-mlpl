Phase 1: Introduce an axis-labeled shape type and thread it through Value::Array without changing any existing behavior. Add LabeledShape { dims: Vec<usize>, labels: Vec<Option<String>> } in mlpl-array or mlpl-core. Value::Array gains an optional labels field (or wraps in a small struct). Default construction leaves labels None. shape(x) continues to return the raw dim vector. New labels(x) built-in returns a string vector of axis names (empty string for positional axes). Zero behavior change: every existing demo and test must pass unchanged. TDD: add a labels() built-in test, then implement.