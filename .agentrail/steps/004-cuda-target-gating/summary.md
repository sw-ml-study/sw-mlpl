candle-core/candle-nn moved behind linux/x86_64 target tables in all 7 cuda crates; cuda feature now stubs cleanly on macOS (mirror of mlx-on-Linux). --all-features clippy verified green on macOS in cuda-rt, cuda-model, cuda-eval, eval, cli, serve. Fixed 3 pieces of latent mlx-gated bit-rot the change exposed: missing NodeKind::Rotate arm in materialize_tape_on_mlx, missing env_api prelude imports in train_mlx_tests + neural_thicket_mlx_demo_tests (both test files now RUN green with --features mlx, 7 tests incl CPU/MLX parity). Gate docs collapsed to --all-features everywhere. LINUX VERIFICATION PENDING: run cuda workspace suites on the CUDA box after the move back (note lives in the /mw-cp playbook).