Fixed grad constant-fold soundness bug: grad_purity walks u: bodies (recursion-safe), model idents count as params, so u:f(b) reading global W is no longer folded (sum case errors naming 'abs' instead of returning 1s; single case no longer blames W). Unsupported-form errors now name the form (record field access `r.w`; record-valued idents). Decision: adam/momentum_sgd (CPU + resident) ERROR when a listed non-frozen param gets no gradient (notices only reach mlpl-serve, invisible in REPL); documented in lang-reference. Split grad.rs (16 fns FAIL) into grad_entry/arith/scalars/params/optim_state. sw-checklist 34->33 FAIL, 574->571 WARN (exception trailer). 3 PRE-EXISTING mlpl-eval failures unrelated (auto_tag grad_tags..., typed_values_lesson step_4, help_completeness) reproduce on unmodified tree. Pushed ab858695; release+debug mlpl-repl rebuilt.