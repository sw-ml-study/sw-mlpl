Fix the grad constant-fold soundness bug (see plan.md step 1). grad_const::differentiably_uses_param only inspects call ARGUMENTS, so a u: call whose body reads a global param is eagerly folded to a constant: grad(u:f(b)+reduce_add(W), W) silently returns all 1s, and grad(u:f(b), W) wrongly blames the param. Make u: calls conservative (param-using, or walk body free idents), so the real tracing error surfaces; make the unsupported-form error name the form (e.g. record field access). Decide+document whether eval_grads_batch should warn when a param gets no gradient instead of silently zero-filling. TDD first: the sum case must error, not return 1s.