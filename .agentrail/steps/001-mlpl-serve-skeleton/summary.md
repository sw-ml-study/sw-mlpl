Shipped crates/mlpl-serve binary + library: REST surface for a long-running MLPL interpreter. POST /v1/sessions creates a session with a 32-alphanumeric bearer token; POST /v1/sessions/{id}/eval (auth required) lex+parse+eval_program_value against the session's Environment, returns {value, kind}; GET /v1/health returns status + CARGO_PKG_VERSION. Constant-time token compare via subtle::ConstantTimeEq. --bind 0.0.0.0 with --auth disabled refused at startup (ServerError::InsecureBind). 5 modules each under the 7-fn cap (auth/sessions/handlers/server/main). 10 mockito-style tokio integration tests cover the full surface including state persistence across eval calls, all three auth-failure paths, the eval-error 400 path, and the InsecureBind safety check.