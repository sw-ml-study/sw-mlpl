Per plan step 1: delete .github/workflows/ci.yml (no rustup on GitHub); build-pages.sh appends pages/build-info.json {commit, built_at (UTC iso), bundle, ledger summary} and injects <meta name=mlpl-build content="<commit> <built_at>"> into pages/index.html; README: drop the CI badge, keep pages-build-deployment, add shields dynamic-json badges (built_at + commit) reading the live build-info.json, with a one-line present-tense note that gates run locally before every commit. Verify build-info served locally. Gates; commit; push; deploy.