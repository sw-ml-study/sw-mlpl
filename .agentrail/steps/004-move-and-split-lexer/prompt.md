Move + split mlpl-lexer. After move, decompose into sibling crates: mlpl-lexer-token (Token, TokenKind), mlpl-lexer-error (ParseError + describe_kind), mlpl-lex-string (lex_string + utf8_char_len), mlpl-lex-number, mlpl-lex-punct (skip_whitespace + single_char_token + lex_builtin_ref), mlpl-lex-ident (lex_ident with keyword recognition), and mlpl-lexer (Lexer struct + next_token + lex entry). Each sibling depends only on its needs. mlpl-lexer becomes a thin orchestrator. Re-export Token, TokenKind, ParseError, describe_kind, lex from mlpl-lexer for downstream backward compat. Verify all 4 workspaces compile + tests pass.