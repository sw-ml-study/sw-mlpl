CNN Phase 5: autograd through windows. Backward of the sliding-window gather is scatter-ADD (overlapping windows accumulate). Follow the structural-op recipe (mirror take/rotate/patchify): (1) NodeKind::Windows{parent,orig_shape,sizes,strides} in mlpl-autograd-tape/tape.rs; (2) windows_backward scatter-add kernel in grad_kernels.rs (the forward gather() loop with += into a zeroed orig_shape buffer); (3) Tensor::windows forward-record in mlpl-autograd/tensor_shape.rs; (4) propagate_dense arm + prop_windows in backward_shape.rs; (5) 'windows' arm in mlpl-eval/grad.rs; (6) call_windows helper (parse array-valued sizes/strides) in grad_calls_shape.rs. TDD gradcheck-first: windows_gradcheck.rs copying rotate_gradcheck.rs (central difference, eps 1e-6, tol 1e-5). Rebuild binaries; docs; DEV/nightly only.