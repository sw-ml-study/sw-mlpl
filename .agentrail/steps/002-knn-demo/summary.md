K-Nearest Neighbors demo shipped in the new Classical ML category: one-matmul distance matrix (identity checked against pairwise_sqdist), 5-round argmax+mask vote, acc 1.0 vs 0.33 baseline; 3 pinning tests; README count 56. Every MLPL line validated against a live server first. Drive-by: fixed the quick smoke suite's 45-minute hang (LoRA fine-tunes missing from SKIP_DEMOS) and added per-demo wall-clock prints; suite now 28s.