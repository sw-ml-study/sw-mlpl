Step 2 of cuda-demo-parity: grad_optim_cuda_mlp + grad_optim_cuda_mlp_step in mlpl-eval (analogs of grad_optim_mlx_mlp + grad_optim_mlx_mlp_step). Recognize the board-policy MLP arch Chain[LinearLora, relu, LinearLora]; extract the 4 adapters + frozen bases (W1,b1,W2,b2) from the Environment as candle Tensors; build x/y; run loss_and_grads over mlpl-cuda-model::mlp_forward; adam-step the 4 adapters (moments in env.optim_state). Wire into eval_adam's CUDA branch AFTER the head-LoRA try_lora_adam (try MLP path next). Reuse the shared demo-layout recognition pattern; the MLP layout recognizer may need its own (mirror grad_optim_mlx_mlp). Respect module/fn budgets. Parity test (cuda_mlp_demo_tests.rs): device('cuda') MLP LoRA loss curve matches CPU within fp32 tol on the GPU. clippy clean. DISK: scope to eval+cuda crates.