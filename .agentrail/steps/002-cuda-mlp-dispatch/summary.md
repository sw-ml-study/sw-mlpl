cuda-demo-parity step 2: board-policy MLP fast path under device(cuda). grad_optim_cuda_mlp recognizes Chain[LinearLora,relu,LinearLora] (shared mlp_layout), runs candle loss_and_grads over mlpl-cuda-model::mlp_forward, adam-steps the 4 adapters on the GPU. Wired into eval_adam after the head-LoRA path. Parity test: device(cuda) MLP fine-tune matches CPU within fp32 tol on the RTX 5060 Ti; curve moves. clippy+fmt clean; sw-checklist held.