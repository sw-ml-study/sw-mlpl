Fix status badge stuck on 'Deploy Pending' on the stable site: status_badge.rs hardcoded the repo build-info URL to sw-mlpl/pages (dev), so the stable channel (published from mlpl-live) never matched. Add repo_build_info_url(served) picking mlpl-live for a stable channel stamp, else dev; inline running_commit to stay at 7 functions (crate already at both module + fn ceilings -- a new module or 2 new fns would FAIL). Regression test added.