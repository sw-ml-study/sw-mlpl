Fixed status badge stuck on Deploy Pending for the stable site. status_badge.rs hardcoded the repo build-info URL to sw-mlpl/pages (dev); added repo_build_info_url(served) that picks mlpl-live for a stable channel stamp, else dev; probe_status derives the URL from the served body. Kept status_badge.rs at 7 fns (folded channel+URL into one helper, inlined running_commit) since the crate is already at both the 7-module and 7-fn ceilings -- a new module or 2 new fns would each FAIL. Regression test repo_url_follows_the_served_channel. 4 badge tests + clippy clean; no new FAIL (41/541). Deployed BOTH channels (badge is in the WASM): dev pages (gh-pages 6d9ba205) + re-cut stable release (mlpl-live 0d87606 from 7b4545f2). Takes effect after CDN propagation + reload. This deploy also carries the BUG 1 scalar-broadcast fix into the browser playground.