Now that freeze_inner/unfreeze_inner are generic over HasModels + HasFrozen, extract them to a new sub-crate components/mlpl-session/crates/mlpl-models-freeze. Pattern: sub-crate exports freeze_inner + unfreeze_inner + a local FreezeError; mlpl-eval converts via From<FreezeError> for EvalError in a new error_from_freeze.rs sibling. The pub(crate) eval_freeze/eval_unfreeze wrappers stay in mlpl-eval and pass the eval_expr_resolver closure. Strict ratchet: this should net-negative because model_freeze.rs leaves mlpl-eval (-1 module from FAIL crate-mod-count) and the new crate is small (<=3 modules).