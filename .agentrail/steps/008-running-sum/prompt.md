User direction 2026-08-02: add running_sum(v) as the additive scan special case beside running_product -- dispatch + NAMES + catalog + lang-reference + glossary entry + tests; add the additive-scan line to the Thinking in Arrays demo and update the essay/apl2-parity wording (shipped special cases are now running_sum and running_product; scan(:op) remains the general form; running_max/min wait for scan). Pages rebuild + deploy; wiki errata note. No lowering arm (matches running_product).