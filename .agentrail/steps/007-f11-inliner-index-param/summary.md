F11: gather_rows index arithmetic over a nested user function's arguments now resolves. call_gather_rows evaluated its index via eager eval_expr (env only), which couldn't see traced fn args (start/count in the grad local scope) -> 'undefined variable'. New grad_const::eval_index_expr evaluates the non-differentiable index eagerly in a scope overlaid with the current grad bindings (snapshot->set values->eval->restore). Verified: nested u:second_pair->u:rows(M,1,2) gather_rows(M,start+range(count)) grads rows 1-2. Test grad_through_nested_user_fn_index_arithmetic; 32/32; F4 gather tests still green. sw-checklist 38/563 unchanged (helper in grad_const, already warning-level). Rebuilt binaries. Pushed.