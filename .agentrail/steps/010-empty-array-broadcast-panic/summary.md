S4: scalar + empty array no longer aborts the process. Root cause in broadcast_gather: total = out.iter().product().max(1) forced one iteration on an empty output (0-extent axis), indexing data()[0] on the empty operand. Dropped .max(1) (empty product is already 1 for the rank-0 scalar case). RED->GREEN broadcast_shape_tests empty cases (scalar+/-empty both orders, empty*empty, unit-broadcast-to-0, rank-2 [0,3]); existing scalar/unit tests unchanged; element-ops crate regression green. Verified in release repl: range(0)+1 -> [] exit 0, normal broadcasts unchanged. Report was fully sufficient (repro + exact site). sw-checklist 38/568 held. Binaries rebuilt repl/build release+debug, serve release.