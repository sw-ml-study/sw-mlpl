Fix demo-abstract-algebra BUG 1: an all-unit shape collapsed to rank 0 under scalar broadcast (apply_binop returned other.shape() when self was length-1, but a rank-0 scalar's shape is [], so [[0]]*1 gave [] not [1,1]). Fix: when both operands are single-element, keep the higher-rank shape; extract broadcast_apply to keep apply_binop under the LOC limit. Add crate + interpreter regression tests.