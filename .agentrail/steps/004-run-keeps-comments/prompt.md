User report: editor Run drops full-line # comments, so documents whose narrative lives in comments (docs/apl2-idioms.mlpl) lose it in the transcript. Fix group_statements to attach comment lines to the FOLLOWING statement group (leading overview comments included); trailing orphan comments drop. TDD; rebuild pages; push. Also: live-Pages staleness diagnosed -- push-triggered runs stopped after a failed deploy; manual dispatch issued; verify deploy at the end.