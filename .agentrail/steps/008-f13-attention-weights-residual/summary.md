F13: attention_weights now finds an Attention layer nested inside residual(chain(...)). extract_attn_weights' Chain arm only recursed into a directly-Attention child; a new contains_attention predicate lets it recurse into any child that contains attention (through Residual/Chain), threading the right activation. Fixed rather than documented (cheap). Verified: chain(residual(chain(rms,causal_attention)),linear) -> [5,5] weights, rows sum to 1. Test attention_weights_finds_layer_inside_residual_chain; regression vit 8/8, causal 6/6. sw-checklist 38 failed (held), 564 warnings (+1 for the predicate, exception). Rebuilt binaries. Pushed.