git mv all 11 runtime crates from crates/ to components/runtime/crates/. Update root workspace members. Add all 11 to components/runtime/Cargo.toml members. Update inter-crate paths (mlpl-runtime-core/math/etc depend on each other; siblings = '../mlpl-runtime-X'). Update mlpl-runtime's path to mlpl-array-ops-matmul (it depends on it via mlpl-runtime-array transitively, double-check). Update external consumer Cargo.toml refs: mlpl-eval, mlpl-cli, mlpl-bench, mlpl-serve, mlpl-reg, apps, components/mlpl-session/*, services. Verify all 4+ workspaces build and tests pass.