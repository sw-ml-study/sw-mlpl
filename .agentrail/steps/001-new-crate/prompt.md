Create components/web-viz-ir/ workspace + mlpl-web-viz-ir crate per docs/viz-ir-plan.md. Sub-modules: kind.rs, axis.rs, node.rs, attention.rs, sankey.rs. lib.rs facade only. All types derive Serialize. VizNode does NOT carry stats/values/elements (those stay on ShapeInfo); VizNode carries only the semantic payload. Wire viz3d: add Option<VizNode> field to ShapeInfo + viz-ir as a viz3d dep. stage3d.js renderInspectorBody: recognize userData.viz?.kind, falls through to current behavior when absent. Build pages + push.