Game of Life saga step 2: svg(frames, "life") SMIL grid animation render mode in the viz crates. Takes rank-3 [T,H,W] tensor, emits ONE self-contained SVG animating through T frames via SMIL (per-frame group opacity, staggered begin, repeatCount indefinite). T=1 degrades to heatmap-like static grid. TDD: golden test on a 2-frame blinker (frame groups + animate timing present), T=1 fallback test. Pure string assembly, native-testable. See docs/future-saga-game-of-life.md 'animated grid widget' section.