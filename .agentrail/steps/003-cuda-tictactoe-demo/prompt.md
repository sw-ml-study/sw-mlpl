Step 3 of cuda-demo-parity: the CUDA tic-tac-toe fine-tune demo. Mirror the MLX tic-tac-toe (MLPL engine: winner matmul + alpha-beta minimax + encode; self-play dataset; LoRA-fine-tune the board->move policy under device('cuda'); before/after win-rate vs random as a waffle). demos/ script + literate org page published on the GPU to pages/literate/ + a 'CUDA tic-tac-toe fine-tune' registry entry {requires_connect:true, device:Cuda} (catalog Demo in lm.rs + DEMO_CAPABILITIES + LITERATE_DOCS). Rebuild pages/ (build-pages.sh, now Linux-portable; restore pages/literate after rsync). Verify runnable on the connected CUDA peer. clippy clean; sw-checklist hold baseline.