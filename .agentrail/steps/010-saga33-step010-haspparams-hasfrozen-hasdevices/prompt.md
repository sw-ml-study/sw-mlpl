Continue mlpl-env-traits foundation by adding HasParams + HasFrozen + HasDevices traits across multiple sibling files in components/mlpl-session/crates/mlpl-env-traits/src/. Each trait file <=4 methods to PASS Module-Function-Count. Cap the crate at 4 modules total. Implement the new traits on Environment in mlpl-eval (likely split env_trait_impls.rs by trait family). Once these traits land, simple analyze fns like eval_freeze (only needs HasModels + HasFrozen) become extractable. Use sw-checklist: exception trailer for the scaffolding-only commit. Strict gate: net-negative if possible.