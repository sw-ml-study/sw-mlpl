Fixed the web-demo extractor: replaced the stale awk gen-web-demos.sh (aimed at the removed apps/mlpl-web/src/demos.rs) with a Rust gen-web-demos bin that reads the canonical DEMOS registry and writes each CPU-runnable demo with a doc header (name + intro + provenance), so extracted files carry docstrings that survive regeneration. Sanitized slugs (no stray path separators), CPU-only filter (skip connect/GPU/Ollama). Verified into a temp dir: 88 headered demos, no panic, output-preserving. Regenerating work/reg-rs/web-demos in place (14 stale -> 88 current) needs an external reg-rs re-seed -- left as a deliberate follow-up decision.