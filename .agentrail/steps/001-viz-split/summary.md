mlpl-viz split into a 4-crate family (core/marks/analysis + facade with gallery_overlay split); consumers untouched via facade re-exports; 12 test files green; downstream eval + wasm compile checks clean; lock sweep clean. sw-checklist 8->7 failed, 311->309 warnings. Also delivered alongside: the Game of Life APL2-parity analysis doc (validated live: works today via permutation matmuls; functional 9-neighborhood form preferred per the user's research notes) queued as a future saga.