TDD: wrote 8 failing tests in causal_attention_tests.rs (RED) covering param-set parity with attention, position-0 independence under later-position perturbation, lower-triangle invariance, a non-causal sanity contrast, gradcheck + finite-difference, and :describe rendering. Implemented by adding causal:bool to ModelSpec::Attention, folding eval_causal_attention into eval_attention(args, env, causal) dispatched from eval.rs with true/false literals (kept model_dispatch.rs at 7 fns), fused 1/sqrt(d_k) scaling with upper-triangle -1e9 fill in apply_attention, and added a non-trainable additive mask leaf in attention_single_head_tape so gradients flow through the existing matmul/mul/add/softmax primitives. Updated :describe and :builtins to render/list causal_attention. All 8 tests green, clippy clean, sw-checklist FAIL count unchanged from pre-step.