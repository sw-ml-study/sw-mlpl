D1: user-facing grad(expr, wrt) now fails loudly when the loss does not depend on wrt (grad() is None) instead of returning a silent all-zero gradient. eval_grad replaces the zeros fallback with a clear error naming the likely causes (loss computed eagerly, or wrong param). Scope is only the user-facing grad builtin; the optimizer path (eval_grads_batch) still returns zeros for untouched params (correct batched semantics). A Some-gradient containing zeros is unchanged. Verified in release binary; no regression across grad_builtin (25/25), gradient_flow/take/cross_entropy/multihead_attention_tape/engram_grad/model_dsl/engram_chain_grad. Test grad_wrt_param_not_in_loss_errors_loudly; lang-reference documents the error. sw-checklist 38/562 unchanged. Rebuilt binaries. Pushed.