Documented the shipped extension boundary value set (scalars + dense f64 arrays both directions + opaque native handles + structured record returns) in docs/companion-demo-extensions.md (A3 flipped to SHIPPED), marked extensions-arrays-handles (B4/B5) done in downstream-unblock-plan.md and future-sagas-queue.md, queued extensions-event-loop (B6) as the remaining gate for the live demo, and added a dated E-024 update to the wiki errata. Markdown clean, checklist 44/522 held (docs-only).