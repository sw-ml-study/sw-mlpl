Step 002: Latent space visualization in 3D.

After training the autoencoder, visualize the latent vectors in the 3D stage. The bottleneck is a small vector -- show it as a bar chart alongside the full input/output vectors to visually demonstrate compression.

This uses existing 3D pipeline -- the shapes naturally show the bottleneck (wide input -> narrow latent -> wide output). Pages rebuild required.