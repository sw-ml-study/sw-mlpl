Lower named reduce_add(x, names) via static Ctx.known_labels (LowerError when not static); extend label lowering (extract_label_list) to accept a constant-foldable StrList. Register in REGISTRY, add dispatch_coverage_tests Builtin variant, update CVAL_BUILTINS if needed, gated MLPL_BUILD_TESTS=1 e2e. See docs/unifying-plan.md section 5.