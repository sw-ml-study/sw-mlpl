Step 2 compiler-parity: the compile-to-Rust path now matches the interpreter on axis naming. reduce_add(x,"name") lowers by resolving the name to a position via Ctx.known_labels/labels_of at lower time (LowerError::NamedAxisNotStatic if labels not static; NoAxisNamed if absent) -- the deliberate compile-time-vs-runtime phase split. extract_label_list also accepts a comma-string literal, so label/relabel/reshape_labeled lower the comma form too. Kept fncall.rs at 7 fns by inlining the named-axis resolution (crate at its 7-module ceiling; a new module would trade fn-count fail for crate-module fail); Display arms one-lined to stay under the LOC budget. 4 TDD lower_labels_tests + a gated MLPL_LOWER_RS_COMPILE_TESTS=1 e2e that actually compiled reduce_add(reduce_add(M,"c"))->Rust and got 15 (ran green, 6.4s). Full lower-rs suite + clippy/fmt green; 38/558 unchanged.