Shipped tag propagation: ValueTags flow through arith/transpose/reshape/reductions/negation/identifier aliases at the assignment site. New crate::tag_propagate module (8 fns) with propagate (Result-returning, may TypeMismatch on domain mix) and infer (Option, best-effort, swallows errors). Producer rules from steps 002-003 take precedence -- for_assign runs first; propagate is the fallback when no producer matched. Domain mismatches (Logit + Probability, Loss + Logit, etc.) raise TypeMismatch with HINT_DOMAIN_MISMATCH naming the bridge ops. Loss survives reductions; Probability/Logit/etc. clear on reduction. transpose/reshape_labeled/label/relabel preserve; reshape clears. Contract doc shipped at contracts/eval-contract/tag-propagation.md. 15 tests pass. Inline-binop-inside-builtin-args checking deferred (e.g. cross_entropy(L+P, Y) does not raise yet -- predicate consumer sees the binop as untagged via infer's error-swallowing); future step can lift if educationally needed.