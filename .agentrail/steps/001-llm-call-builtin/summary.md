Shipped llm_call(url, prompt, model) -> string language-level builtin. Pure HTTP path in mlpl-runtime/src/llm_builtins.rs (3 fns: call_ollama, resolve_url, parse_response); eval-side dispatcher in crates/mlpl-eval/src/llm_dispatch.rs (1 fn). Design deviation from plan: split try_call/validate_args into the eval crate where Expr/env access lives, rather than runtime. Same boundary as Saga 22's revised layout. 8 mockito-based integration tests cover happy path, URL auto-append, full-URL passthrough, non-2xx status, missing response field, invalid JSON, wrong arity, non-string args.