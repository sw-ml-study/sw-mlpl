Root-caused + fixed the connect MLX telemetry stall AND a model-binding regression. (1) macOS CPU monitor scraped top -l 2 (~1s block) -> live sparklines frozen; replaced with Mach host_statistics tick counters (120ms window), verified /v1/stats advances during an in-flight eval. (2) env-table split dropped the models case from the bare-Ident dispatch, so m=chain/lora bound only for apply(m,x) -- bare m / is_model / user-fns referencing a global model errored 'undefined variable'; added the get_model dispatch case. Both fixed in current source, reproduced natively; local :6464 server rebuilt (--features mlx) + restarted; regression tests added (mach-tick live check; model_ident_resolution_tests). sw-checklist 575/2/386 held.