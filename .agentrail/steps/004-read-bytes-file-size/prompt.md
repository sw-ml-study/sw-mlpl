Add read_bytes/1 (whole file), read_bytes/3 (offset,length; non-negative integer args; EOF-clamped) and file_size/1 to the compiler runtime (mlpl-rt-value or a runtime IO module), returning CVal::Result (ok(rank-1 array)/ok(scalar)/err), path arg a Str. DESIGN + implement the compiled sandbox-root mechanism (a compiled binary has no Environment; resolve from cwd or a build/runtime path, documented). Register handlers in the fncall registry (add Emit variants or a shared reader handler). TDD + gated e2e (write a temp file, read its bytes+size back through a compiled program). Hold sw-checklist.