Fix upstream-asks #16: a multi-line playground entry renders its code inside the comment span. render_input_line applied split_inline_comment (a per-LINE contract) to the whole statement GROUP, so a leading full-line '# comment' split at the first # and swallowed the code after it. Add split_group_lines (per-line split) to the tutorial crate beside split_inline_comment; render_input_line maps over it, first line gets the mlpl> prompt. entry.rs is at 7 fns so no new fn there. TDD.