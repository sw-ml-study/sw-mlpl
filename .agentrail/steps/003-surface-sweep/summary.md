Step 3 surface-sweep: routed the single-axis labeled reduce family (reduce_add/reduce_mul/argmax/softmax) through a shared name-literal gate (axis_name_of) so it accepts the bracketed spelling ['b'] identically to 'b', matching reduce and label; multiple names error clearly (single-axis). compress/drop are runtime numeric-axis-only (never had a named path) -- out of scope, noted. 2 TDD tests; axis_reduce green; eval clippy(5m36s)/fmt green. 38/559 (no new fail; +1 module-fn warning from the helper).