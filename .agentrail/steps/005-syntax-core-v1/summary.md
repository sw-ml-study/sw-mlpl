Created docs/syntax-core-v1.md -- the v1 syntax design spike. Covers numeric literals, identifiers, array literals ([bracket, comma]), infix operators (+,-,*,/ with precedence), named function calls (reshape, transpose, shape, rank, iota, reduce_add, reduce_mul), assignment (x = expr), line comments (#), statement separation (newline/semicolon), whitespace rules, and 10 worked examples. Deliberately boring scripting syntax; APL symbols deferred.