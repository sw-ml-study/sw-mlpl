device('cuda') now computes on the GPU through the interpreter (RTX 5060 Ti). New mlpl-eval 'cuda' feature pulls the 3 cuda crates; op-dispatch extracted to device_dispatch.rs with one op_dispatch! macro generating both try_mlx_dispatch + try_cuda_dispatch (MLX/CUDA share an op surface -- define once); dispatched_call routes env.device()=='cuda' to the CUDA crates via a cuda_ops facade. CPU fallback + generalized one-time warning (take_device_fallback_warning/device_available); to_device accepts cuda (extracted to device_to.rs). cuda_parity tests (linear/chain-MLP/tiny-LM apply inside device('cuda')) match CPU within fp32 tol on the GPU. clippy clean cuda+default. sw-checklist net -1 warning (502): extraction retired device.rs file-LOC warn + 8 pre-existing clippy format lints; failures held at 111.