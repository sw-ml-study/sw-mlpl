Saga 33 step 032: critical_dimensions viz + PCA_LOADINGS demo shipped (Phase 1b of the dim-reduction milestone). New svg type renders a [k, D] loadings heatmap colored by |loading[i, j]| with right-side per-PC variance-explained labels. 7 unit tests cover SVG emission, cell count, label formatting, validation. PCA_LOADINGS demo uses the same 3-cluster 5-D dataset as PCA_3D; the new pca_components / pca_variance_explained builtins from step 031 feed straight into the new viz. Pages rebuilt + pushed; deployed to github.io.