F6: a repeat N { ... } inside a traced user function (F2 inliner) now unrolls onto the tape instead of hitting the generic reject. grad_user::trace_body already threads a function's assignments through a mutable local scope, so a repeat body is walked N times over the same local map -- h = apply(block, h) accumulates across iterations exactly as eager. Count is a plain eager scalar (repeat_count helper). Verified in release binary: def u:recur(h){repeat 2{h=h+h} h}; grad(u:recur(W),W) == [4,4]. Backward-compatible (only the previously-erroring repeat-in-traced-fn path changes); grad_user stays at 3 fns (under gate). Test grad_through_repeat_in_user_function_unrolls; 24/24 green. lang-reference documents the unroll. sw-checklist 38/562 unchanged. Rebuilt binaries. Pushed.