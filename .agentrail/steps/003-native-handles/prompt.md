Third step of extension-arrays-handles (B5, the spine root). Add opaque native-handle values to the boundary so a provider can return a persistent resource (e.g. a viewer object) and accept it back on later calls. Wire ValueTag::NativeHandle=7 and AbiHandle through: to_ext/from_ext in ext_marshal, the cabi send (ext_to_abi) and receive (abi_to_ext) paths, and a host-side ExtValue::Handle carrying an opaque id/type-tag that MLPL passes around by reference without inspecting. TDD: a provider returns a handle from make_viewer(), MLPL stores it in a variable, passes it back to a second call (use_viewer(h)) which recovers the same resource; a foreign/stale handle is a clean err. Keep functions correctly formatted and under 25 lines by refactoring, never by whitespace tricks; keep sw-checklist fails/warns decreasing (the extension-cabi crate split is queued if the module ceiling blocks clean dispatch).