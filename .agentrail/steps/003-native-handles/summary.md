Native handles (B5 spine root): ExtValue::Handle + Value::ExtHandle wire opaque provider handles both directions (ValueTag::NativeHandle via AbiHandle union payload). Env sibling table + assignment/resolution/frame-scope plumbing mirroring device_tensors; non-forgeable (no MLPL constructor); provider validates, stale/foreign handles are clean errs. 4 TDD handle tests pass. Loose-coupling: ext_marshal send/recv split, ext_to_abi + bind_value split. Checklist 44 fail (held), 522 warn (-1). serve-core value_kind arm added (exhaustive) but crate does not link locally (ring, unrelated).