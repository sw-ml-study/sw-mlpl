Phase 1a of general namespacing: generalized first-class references from :u:name-only to any :namespace:name in the interpreter (lexer extend + eval inner-colon->UserFnRef). Unblocks call/each/partial-application/callbacks for library/provider namespaces (:result:zip, :math:double); bare :name stays a builtin ref; u: back-compat. Regression test + verified. Remaining namespacing parity (compiler lowering+mangling, reflection de-u:, modules/aliases, extension collision policy) scoped as later phases.