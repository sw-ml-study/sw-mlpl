Write docs/storage-layout-viz.md: the COLUMNAR storage-layout.json contract (fields, types, block_size, capacity) sw-tos emits, sw-mlpl role/boundary (array-programming middle), reference-script architecture (layout math + classify + assemble + drive native3d), capability gap analysis (parse_json handles columnar; one gap = vectorized string->RGBA classification), and the native3d invocation surface. Ship a small sample columnar storage-layout.json fixture under examples/viz/. Coordination artifact for the sw-tos + demo-extensions agents. See ../demo-extensions/docs/research.txt.