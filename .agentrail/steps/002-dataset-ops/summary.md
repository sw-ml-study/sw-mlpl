Tokenizers step 002 shipped. Five dataset-prep builtins in a new crates/mlpl-runtime/src/dataset_builtins.rs module: shuffle(x, seed), batch(x, size), batch_mask(x, size), split(x, frac, seed), val_split(x, frac, seed). All row-axis (axis 0) operations; axis-0 labels drop, other axes' labels preserve. shuffle uses Fisher-Yates via the existing xorshift64 PRNG. batch zero-pads the final short batch; batch_mask returns the matching valid-row mask. split+val_split use the same seed so caller gets disjoint training/validation chunks covering x. 14 new tests covering determinism, permutation correctness, padding math, disjoint coverage, and label propagation. All gates clean (646 tests, sw-checklist unchanged).