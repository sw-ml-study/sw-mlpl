Two ../emufpga upstream asks (safe subset): (1) length-1 array broadcasts like a scalar in apply_binop (elem_count()==1), NumPy/APL parity, both surfaces; (2) fix the misleading disp doc + hello-world comment (disp RETURNS a display string, does not write stdout -- use print for mid-program output). No disp runtime change (would double-break ~230 downstream files). Regression test + verified.