Dataflow renderer dashed back-edge (../emufpga recursion-as-rotation ask): a DFS marks any edge into a DFS ancestor (a recurrence, e.g. a rotating parameter store) as a back-edge; the longest-path ranking pass ignores back-edges so layering stays a strict DAG; back-edges render dashed, routed through a reserved bottom lane as a 'rewind' (drop out of the source, run back, rise into the target). Self-loops count. In mlpl-viz-flow: layout.rs gains back_edges DFS + forward-only rank + a reserved BACK_LANE; Positioned carries per-edge back flags; render.rs edge_path branches forward-elbow vs rewind + stroke-dasharray. No new caller channel (derived from from/to). TDD.