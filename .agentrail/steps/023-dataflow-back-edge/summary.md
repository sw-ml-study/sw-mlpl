Dataflow dashed back-edges for recurrence: layout.rs back_edges() DFS marks edges into a DFS ancestor (incl self-loops); rank() skips them so layering stays a strict DAG (a c->a recurrence no longer pushes a right); place() reserves a BACK_LANE; Positioned carries per-edge back flags; render.rs edge_path() routes back-edges dashed through the bottom lane as a rewind. No new caller channel (derived from from/to). 7 crate tests + eval dataflow_tests pass; verified e2e in fresh release binary (3 polylines, 1 dashed, columns x=24/224/424, height 132). Fresh release/debug mlpl-repl+mlpl-build rebuilt. Design doc updated. sw-checklist held 44 failed (exception; DFS kept in layout.rs to avoid an 8th-module Crate-Module FAIL, cost is one layout.rs Module-Fn warning, 538->539).