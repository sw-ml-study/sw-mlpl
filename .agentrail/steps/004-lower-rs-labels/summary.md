Compile-to-rust step 004 shipped. Label-aware lowering: label/relabel/reshape_labeled builtins produce with_labels runtime calls; annotation syntax auto-works via parser desugar; matmul lowers and performs a static contraction-axis check when both operands' labels are known at lower time (LowerError::StaticShapeMismatch for disagreement). Static label inference walks across Ident lookups, label/relabel/reshape_labeled construction, and transpose label swap -- enough to catch both matmul(a, b) and matmul(a, transpose(b)) cases. labels() stays Unsupported (REPL-only string builtin, doesn't fit DenseArray shape). 13 new string-match tests + 1 new end-to-end test (annotated matmul program compiles via rustc and outputs reduce_add = 30). Module split: lib.rs stays under budget by moving fncall match arms + label helpers to crates/mlpl-lower-rs/src/fncall.rs. All quality gates clean; sw-checklist unchanged baseline.