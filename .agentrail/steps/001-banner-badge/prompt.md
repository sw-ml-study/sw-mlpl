Splash/banner staleness badge: on load, fetch the page's OWN index.html (cache-busted, same origin) and compare its hashed bundle name against the running one -- fresh (up to date) / stale (newer build available; clicking reloads) / unknown (fetch failed: offline, airplane mode -- the reasonable default per user). On the github.io origin additionally poll the Pages build API once for building status (deploying badge, best effort, errors hidden). Pure comparison logic TDD'd; rebuild pages + deploy.