parse_json(s) per plan: hand-rolled recursive-descent JSON decoder (no serde_json Value dependency in the eval layer unless already present) returning ok(value)/err(message with byte position); mapping object->record, string->Str, number->scalar, homogeneous arrays->vector/string-list, mixed arrays err, bool->1/0, null->empty vector; unicode + escapes exact; round-trip property against event_json. Docs rows + pin. Run their gate expecting structured-event-consumption AVAILABLE. Gates; commit; push.