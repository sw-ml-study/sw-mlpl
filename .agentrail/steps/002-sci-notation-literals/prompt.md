RS5: the lexer must accept scientific-notation numeric literals: 1e-4, 1.5e3, 2E-10, 6.02e23, 1e10. Find the number-lexing code in mlpl-parser (lexer), extend it to parse an optional exponent (e/E, optional sign, digits) after the integer/fraction, and parse to f64. TDD: RED lexer + eval tests for the forms above and a no-regression check on existing integer/float/range (a..b) forms; ensure '1e' or '1e-' without digits is a clean error, and that member/range syntax (e.g. 1e where e is not exponent) is unaffected. Hold sw-checklist at baseline; rebuild repl/build (release+debug) after (no serve/grad change).