Saga 29 step 013: multi-head attention on the tape + Tensor::stack op. Replaced 'heads != 1' rejection in model_tape with full per-head lowering using reshape + take + stack; added DenseArray::stack and NodeKind::Stack with N-way backward (O(N) replacing the prior O(N^2) binary-concat chain on the rank-3 batch path). attention_weights now returns [B, heads, T, T] for rank-3 multi-head. 12 new tests (5 stack gradcheck + 7 multi-head tape: forward parity, gradcheck on Wq for rank-2 and rank-3, divisibility rejection). All 1423 workspace tests pass; sw-checklist 157 (=parent); docs (eval-contract, glossary +1) refreshed.