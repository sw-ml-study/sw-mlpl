Saga 29 step 014 (per docs/milestone-vit.md Phase 4; renumbered from step 010 in the original plan): demos/vit_multihead_thorough.mlpl. Build the multi-head version of the trained ViT on pets_tiny (or fetch_dataset('oxford_iiit_pet') in native, falling back to pets_tiny in WASM). Use attention(128, 4, ...) -- single transformer block with multi-head (now unlocked by step 013). Train on MLX with device('mlx') { ... } if MLX is available on the host; CPU fallback otherwise. Document the training loss curve and final accuracy in docs/benchmarks.md as the Apple-specific baseline (this is one of the last MLX-specific deliverables before the planned move to Arch + CUDA). Add the demo to apps/mlpl-web/src/demos.rs and rebuild pages/. NOTE: a longer multi-epoch ignored test is fine; the smoke test runs one short epoch.