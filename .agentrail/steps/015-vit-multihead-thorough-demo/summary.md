Saga 29 step 015: demos/vit_multihead_thorough.mlpl (200 adam steps, 4-head ViT on 20-image pets_tiny, wrapped in device('mlx'); CPU fallback verified at 285s wall-time, training accuracy 1.0). Added integration test (#[ignore]'d, >=70% accuracy gate). Documented in docs/benchmarks.md as Saga 29 baseline. UX polish on the browser-friendly multi-head demo: two new ProgressNotes (What is Happening for the train block, richer post-train explanation of the four attention maps), expanded takeaway covering heatmap axis semantics + expected post-training patterns, added labeled-image gallery so the demo now shows both per-head attention maps AND per-image actual/predicted overlays. Vision learning path (apps/mlpl-web/src/paths.rs) rewritten end-to-end: new Stack glossary step, multi-head pattern + trained-multi-head demo entries, stale 'What is missing' note replaced with 'Coming next: bring-your-own-image' and 'Beyond this path'. Glossary catch-up: new attention_weights and heatmap_grid entries, removed duplicate concat entry, count 274 -> 275. Plan.md gained a new 'Deferred primitives queue' section seeded from observed gaps (permute, gather, slice ranges, log_softmax, higher-axis concat, layer_norm, gelu, :upload x, load_image, StrList indexing). Pages rebuilt + pushed.