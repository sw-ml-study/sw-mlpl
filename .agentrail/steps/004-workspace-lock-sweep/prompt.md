Maintenance insert discovered in step 002: 13 component workspaces fail cargo metadata --locked (cuda-rt, dev-tools, eval-pets-tiny, eval-types, ml-helpers, models-llm, models-read, models-write, native-rt, session-infra, syntax-codegen, web-mode, web-viz-ir) -- same disease as the cli lock fixed 2026-07-25. Regenerate each lock, verify --locked passes in every workspace, and add a cheap guard (script or sw-checklist hook) running cargo metadata --locked per workspace so manifest changes can no longer silently strand downstream locks.