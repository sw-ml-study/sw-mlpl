Replace native-title-only demo picker hints with visibly rendered hover/focus tooltips. Add data-tooltip markup and CSS for group labels and all demo rows; wrap disabled buttons so their explanation remains hoverable/focusable despite disabled controls. Add source-level tests that pin rendered tooltip hooks and accessibility, run scoped gates, rebuild pages, and verify locally before returning to deployment.