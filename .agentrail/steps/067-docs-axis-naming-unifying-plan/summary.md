Wrote docs/unifying-plan.md: a type-safe, staged plan to unify axis-naming semantics across label/reduce/reshape_labeled and the interpreter/compiler. Grounded in a two-agent type map: Value::StrList and CVal::StrList already exist, so the bug is phase+ad-hoc acceptance (label AST-validates literal-only; reduce evaluates but rejects StrList; compiler tracks labels statically but has no named reduce). Design: shared pure AxisSpec/AxisNames + one resolver in mlpl-array, thin exhaustive Value/CVal->AxisSpec adapters, explicit compile-time-vs-runtime phase split, backward-compatible staging, table-driven parity test. Verified non-breaking against shipped demos/examples and the CNN blog post (every form in use is existing-valid; the erroring bracketed-reduce form appears only in the plan and research4.txt; no test pins the current rejection) and recorded that as an impact section.