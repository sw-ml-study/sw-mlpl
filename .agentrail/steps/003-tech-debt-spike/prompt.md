Dedicated sw-checklist paydown spike (no new features). Current baseline: 47 failed / 522 warnings. Goal: strictly and substantially lower BOTH -- target at least -5 FAILs AND -15 warnings in one focused commit (per CLAUDE.md ratchet policy). Prefer high-leverage structural moves that clear several checks at once: split a fat over-limit module in mlpl-eval into a sibling crate or sub-modules (clears File-LOC + Module-Function-Count + Crate-Module-Count together), and fully extract a few >50-line functions to <=25 lines (not just <50, so a FAIL becomes a PASS not a WARN). Good targets from sw-checklist -v: eval.rs eval_expr (445 lines -- the giant dispatch; split by Expr variant family into named helpers), grad_optim.rs eval_adam (176) / eval_momentum_sgd (87), json_decode.rs. Follow docs/code_metrics.md + docs/loose-coupling.md (phase separation, compose-don't-compress, define-once macros/tables). TDD safety: run the affected test files after each extraction; behavior must be identical. clippy/fmt both workspaces; verify sw-checklist shows the drop; include the sw-checklist trailer naming retired checks + new counts. Then resume the byte-io features (reinterpret next).