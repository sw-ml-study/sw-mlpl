clock_ms() native builtin in mlpl-runtime: monotonic elapsed milliseconds from a process-start epoch (OnceLock<Instant>), scalar DenseArray, no args. Register in builtins dispatch; catalog row; lang-reference + glossary rows noting it is native/connect only (not a browser builtin). TDD: monotonic non-decreasing, finite scalar, positive elapsed across a busy loop. Also write docs/demo-memory-upstream.md tracking all 5 demo-memory requests with status (clock DONE) and add queue entries for fixed-width-ints-bitops, packed-layouts, rng-streams. Gates; commit; push.