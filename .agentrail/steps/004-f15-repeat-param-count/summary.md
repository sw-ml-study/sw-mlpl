F15: a repeat whose count is bound to a function parameter now works inside a traced function. grad_user::repeat_count resolves the count through eval_tensor_expr (traced local scope first, then env), so repeat r { } with r an arg works, as do global/literal counts. F6 case unchanged; grad_user stays 3 fns. Test grad_through_repeat_with_parameter_bound_count; 28/28. sw-checklist 38/562 unchanged. Rebuilt binaries. Pushed.