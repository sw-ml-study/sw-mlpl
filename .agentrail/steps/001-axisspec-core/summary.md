Step 1 axisspec-core: added shared AxisSpec (Names|Indices) + AxisNames types and AxisSpec::resolve(&DenseArray)->Vec<usize> (parse/validate/resolve split: resolve_indices, resolve_names, reject_duplicates) plus AxisError. 9 TDD unit tests (names->positions, empty, missing name, no-labels, out-of-rank, duplicate by index and name). Anticipated the metric ceiling: mlpl-array is saturated (8th module would FAIL; dense/indexing/shape/box_display at the 7-fn max), so the shared cross-consumer concern went in a NEW narrow crate mlpl-axes rather than growing the foundational crate -- landed at HOLD (43/556) with zero new fails and zero new warnings.