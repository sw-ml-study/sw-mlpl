Per design + resolutions: call(f, args...) invoking UserFnRef (via the u: call path, arity errors naming the referenced fn) and BuiltinRef (routed like colon-calls; builtin's own arity errors, no extra frame per resolution 2); tutoring error for non-reference f; registry test mirroring the mlplunit fixture (record of refs, call through field access); unknown-referent structured error. TDD.