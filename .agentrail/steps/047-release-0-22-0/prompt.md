Cut the 0.22.0 release carrying C5 (compress label propagation) + C1 (windows, positions-leading axis order [out_y,out_x,C,kh,kw]). Bump all mlpl-* component-workspace package versions 0.21.0 -> 0.22.0 (surgical perl over mlpl-* Cargo.toml only; leave external roxmltree etc. at their versions); revalidate touched workspaces with --locked; rebuild mlpl-repl/mlpl-build release+debug; refresh CHANGES.md; deploy dev pages (build-pages + commit pages/ + push + deploy-pages) and cut stable (release-stable.sh); hand out cache-busting ?ts= review URLs for both surfaces.