Per design: mlpl-repl integration -- --source-dir flag, FsProvider (canonicalize + root containment + absolute/traversal rejection), chunked script evaluation in one env with path:line:column diagnostics from the SourceTable, default root = the root script's directory; subprocess TDD; acceptance = mlplunit's scripts/verify-native-include passes without host --include and check-capabilities flips native-static-include AVAILABLE.