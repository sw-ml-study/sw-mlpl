User-facing staleness fix: mlpl-serve's static mount sends no Cache-Control, so browsers heuristically cache index.html and keep running stale bundles for hours after a deploy (user saw days-old gating behavior). Add Cache-Control: no-cache to the static-dir responses (assets are content-hashed; conditional revalidation makes this cheap), TDD via a route test, rebuild+restart the 6464 server, verify headers live.