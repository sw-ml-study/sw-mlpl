Colon-expression routing fixed at all three layers: the parser now lowers :name(args) to a real call (was two statements -- the root of ':disp(g) says g undefined'); the server evals expression-shaped colon lines and hints on ':disp g'; the connected client routes non-local colon lines to the server session that owns the bindings. Parser + server tests, connected playwright e2e, docs updated.