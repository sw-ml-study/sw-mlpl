Error Handling demo (user-requested, after Life): three acts per docs/option-result-design.md. Act 1: the two planes -- err(...) value vs hard error side by side with disp. Act 2: the railway -- safe lens get returning err({kind, message}), is_ok branching, unwrap_or, get_value/get_error projections. Act 3: the bridges -- try/catch demoting an out-of-bounds take into a handled fill value; ? propagating through a u: pipeline. Validate every line via a focused smoke test (erroring lines asserted to error). Glossary entries Result + Error Handling; README counts; pages rebuild.