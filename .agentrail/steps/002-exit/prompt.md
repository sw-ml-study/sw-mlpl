Lower exit to the compile-to-Rust path. Interpreter parity (eval_script.rs eval_exit): exit(code) ends the process with an integer status; code must be a scalar integer in 0..=255 else a hard error. In compiled code, exit(code) should call std::process::exit(code as i32) (via a runtime fn in mlpl-rt-value proc.rs, e.g. fn exit(v: &CVal) -> ! reading the scalar, validating 0..=255, else panic/abort with a clear message). Wire the fncall lowering -- exit never returns (type !), so it needs its own Emit shape (not CvalIo, which expects a CVal return); a distinct Emit::Exit that emits rt::exit(&cval) is cleanest, and produces_cval must NOT list exit (it does not yield a CVal). Add the dispatch-coverage Exit variant. TDD RED first: a gated MLPL_BUILD_TESTS=1 e2e where a compiled program exit(3) yields process exit code 3 and a normal program yields 0. Hold or lower sw-checklist (exception if test-only holds). Commit source before agentrail complete.