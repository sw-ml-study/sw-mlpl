Explicitly authorized by the user from ../demo-extensions: implement recursive outbound MLPL record and packed-byte marshaling through the existing versioned C extension provider adapter so interpreted MLPL can call record-taking HTTP server and SQLite providers. Use focused Rust TDD for nested records, bytes, ownership, malformed values, and real provider invocation. Preserve ABI layout unless evidence requires a versioned change. Update current contract/docs and wiki errata if applicable; run the scoped mandatory gates and sw-checklist ratchet, commit, complete, push main, refresh CHANGES.md per repository policy.