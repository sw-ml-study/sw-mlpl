Fixed BUG 3 / upstream-asks #8 / mlpl-blockers B6: deep user-function recursion aborted the process (exit 134) with no MLPL error. Added a call-depth cap (env.call_depth already tracked) checked in run_body: exceeding it raises a catchable EvalError naming the function. Platform-aware: native 1000 (8MB stack), wasm 250 (~1MB). Verified in fresh release binary (4000-recursion errors cleanly at 1000, 500 works). Tests on a 256MB thread (debug frames >> release). lang-reference documents the limit. Fresh release+debug binaries. sw-checklist 41/543, no new FAIL.