Fix BUG 3 / upstream-asks #8 / mlpl-blockers B6: deep user-function recursion overflowed the stack and aborted the process (exit 134, no MLPL diagnostic). Add a call-depth cap (env.call_depth already tracked in framed) checked in run_body: exceeding it raises a catchable EvalError naming the function, not a crash. Native 1000, wasm 250 (smaller stack). TDD on a big-stack test thread; document the limit in lang-reference.