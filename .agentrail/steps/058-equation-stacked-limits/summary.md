Fixed the abbreviated math: svg('equation') now renders big-operator limits STACKED above/below (display convention), and the demo/literate conv equations carry the paper's full bounds. Renderer rewrite: atom-based positioned layout (atoms.rs parse + render.rs positioned push_atom); a sum/product/integral + _{}/^{} gets its lower bound centered below and upper above the operator; inline x_i/y^2 unchanged. Equations completed to Zhao et al. (2018): y[r,x,y]=SUM_{q=1..Q}SUM_{u=1..Mw}SUM_{v=1..Nw} W X, with a 'where' line naming Q/Mw/Nw/R + the index-origin note (paper 1-based, MLPL 0-based: q=1..Q->channels 0..Q-1, reduce axes 0-based). Updated Convolution-from-the-equation + CNN(simple) + cnn-convolution.org (re-published; baked SVGs show 9 middle-anchored texts = 3 ops+3 uppers+3 lowers). moving-average already had 0-based bounds. Tests: equation_tests +1 (stacked structure), demo-smoke + literate_parity green. Binaries rebuilt release+debug; deployed DEV/nightly (bundle 22a0134c, commits 22a0134c+87158117+59806aab). sw-checklist 41 held, +2 warn (exception: clean atoms/render/equation split). No version bump, no stable.