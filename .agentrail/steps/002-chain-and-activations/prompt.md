Phase 2: Add chain[layer1, layer2, ...] sequential composition and atomic activation layers tanh_layer, relu_layer, softmax_layer that can appear inside a chain. chain.apply(X) threads the input through each child layer in order. RED: a 2->8->2 chain[linear, tanh_layer, linear, softmax_layer] applies correctly on a [N,2] input and produces a [N,2] probability matrix. Commit via /mw-cp.