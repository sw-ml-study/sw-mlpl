Lower the byte/text conversions: tokenize_bytes/1 (str -> rank-1 byte array), decode_bytes/1 (byte array -> str, reusing the loud-reject array_to_bytes validator), and to_int/1 (str -> int, parse failure -> err Result). Add the runtime functions (a text module in mlpl-rt-value or mlpl-rt-fsio) and register in the fncall registry. tokenize_bytes/decode_bytes take/produce CVal; to_int str->int returns a Result. TDD + gated e2e (str -> bytes -> str round-trip through a compiled program). Hold sw-checklist. This is the last byte/text feature rung before the compiler-process-semantics saga (print/eprint/exit/read_stdin + pristine stdout).