load_extension builtin + MLPL_EXTENSION_PATH discovery. resolve_extension_path (loader crate) maps a logical name to libNAME.<ext> searched across MLPL_EXTENSION_PATH (the env var a justfile sets), with a clear miss listing dirs. fncall_extload::load_extension(name_or_path): path loads directly, bare name resolves via the env var; returns the namespace Str or an err(...) Result, wired into the dispatch chain (mlpl-eval deps mlpl-extension-loader). TDD in its own test binary: set MLPL_EXTENSION_PATH, load_extension(mlpl_ext_testdylib)->testext, testext:answer()->42, missing name->err. Also fixed a rustfmt gap in the loader that was blocking demo-extensions' fmt gate. Checklist 44/523 held.