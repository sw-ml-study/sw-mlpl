Third step of extensions-dynamic-load. Wire dynamic loading into the interpreter with name-based discovery. Add: (1) discovery -- resolve a logical extension name to a shared-library path by searching MLPL_EXTENSION_PATH (colon-separated dirs) for the platform filename (lib<name>.dylib on macOS, lib<name>.so on Linux) using std env consts DLL_PREFIX/DLL_EXTENSION; a clear error if not found, listing the dirs searched. Put this in the loader crate (a resolve-by-name function next to load_c_extension). (2) an MLPL builtin load_extension(name_or_path) wired into the interpreter dispatch chain (a new fncall module in mlpl-eval, which is over module-count so a new module adds no fail): if the arg looks like a path (contains a separator or ends in the dylib ext) load it directly, else resolve via MLPL_EXTENSION_PATH; on success return the namespace as a Value::Str, on failure a Value::Result err. mlpl-eval will need a dep on mlpl-extension-loader. TDD: set MLPL_EXTENSION_PATH to the dir holding the built mlpl-ext-testdylib artifact, then from MLPL load_extension("testext") and call testext:answer() -> 42; also load by explicit path; a missing name yields an err listing the search dirs. Note the env var is what a justfile/run script sets. Keep functions under 25 lines; keep sw-checklist fails/warns decreasing. Gate, commit, complete queuing cdylib-packaging-and-contract.