Continue FAIL paydown by decomposing eval_tensor_fncall in grad.rs (86 LOC FAIL). Has 8 if-name-X branches (unary, matmul, apply, cross_entropy, patchify, concat, take, reshape) -- extract each branch to a small helper, or group into 2-3 helpers. grad.rs is at 7 fns already (Module-Function-Count borderline) so the helpers need to go to a new grad_calls.rs sibling. Strict gate: target -1 FAIL.