GAN 2D circle demo fixed: empirical tuning (radius-MSE metric) found the working recipe -- 2 hidden layers per player, D lr = G lr / 4, 2000 steps -> radius MSE 0.35 -> 0.0056 with verified full-ring coverage; demo restructured as TARGET / EARLY(100 steps, blob) / TRAINED(2000, ring) on one fixed noise batch per the user's show-bad-then-good suggestion; smoke runtime unchanged; pages deployed.