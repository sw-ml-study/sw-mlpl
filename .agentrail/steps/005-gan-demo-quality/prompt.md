User report 2026-08-04: 'GAN (2D circle)' demo produces noise, not a circle. Diagnose (tiny GAN instability at demo scale is expected: check steps, lr balance, architecture, latent dim, mode collapse); improve until the generated scatter is recognizably circular, and per the user's suggestion present the pedagogy honestly -- show the early/bad generator output AND the trained/better one side by side. Keep runtime browser-friendly. Update takeaway; smoke gates; pages deploy.