Added attention_overlay viz type to mlpl-viz: paints per-head translucent viridis attention heatmaps on top of an image, with patch grid auto-derived from the attn shape (P perfect square required). Implementation in gallery.rs (3 new fns staying under the 7-fn budget), wired into render_with_aux dispatch, 4 new tests. New demo demos/vit_attention_viz.mlpl + web playground entry 'Pets: attention overlay (per-head)' that trains a 4-head ViT for 30 steps and renders the per-head attention on a test cat image. New glossary entry, README counts bumped, Vision learning path picks up the new demo. Saga 29 closes here.