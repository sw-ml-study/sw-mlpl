Added str_concat(a,b) and str_join(parts,sep) interpreter builtins (mlpl-eval/fncall_string.rs): exact byte join, Unicode-preserved, no coercion; str_join O(total) linear, []->"". Registered in the eval_fncalls dispatch chain; documented in lang-reference + :builtins catalog. TDD via tokenize_bytes acceptance cases + error cases. sw-checklist HOLD 557/49/524. User flagged that the dispatch is still a hand-written .or_else chain -- refactor to a data-driven registry is queued next.