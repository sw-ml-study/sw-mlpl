B1: add str_concat(a, b) -> string and str_join(parts, separator) -> string interpreter builtins in mlpl-eval (components/eval; add a fncall_string.rs or similar, dispatched via the builtin dispatcher). Exact byte-for-byte join, Unicode preserved, NO coercion (a number arg is an error, not a silent to_string). str_join takes a string-list + separator and must be O(total) linear (not O(n^2)); [] -> "", [only] -> "only". TDD with the work order's acceptance table (str_concat("ab","cd")=="abcd", str_join(["a","b","c"], ", ")=="a, b, c", str_concat("e-acute","x") preserves bytes). Update docs/lang-reference.md + the :builtins catalog. Hold sw-checklist.