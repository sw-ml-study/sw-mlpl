Implicit per-step loss metric: metric-less train blocks stream their own per-step loss as the 'loss' metric (no recompute; explicit *_metric bindings suppress it). All streamed connect-mode trains -- incl. the CUDA/MLX LoRA demos unchanged -- now light up the live loss panel. Ratchet: split mlpl-serve handlers.rs into handlers_inspect + handlers_eval_task (13->11 failed, 318->316 warnings).