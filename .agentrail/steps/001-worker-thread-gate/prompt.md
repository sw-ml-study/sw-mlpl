First step of extensions-event-loop. Land the worker-thread-gate as a kept regression proving B6 gate #1: the interpreter can run on a spawned worker thread while winit owns the main thread. The spike already exists at components/eval/crates/mlpl-eval/tests/worker_thread_spike.rs (Environment and Value are Send; an eval runs on a spawned worker; an env built on main MOVES into a worker and evals there) -- verify it passes, keep it, and add a compile-time Send assertion for any other type the worker path must move (e.g. the eventual Port handle placeholder if introduced). Do NOT build the Port yet; this step only locks the Send/worker invariant so later steps can rely on it. Gate the commit (fmt/clippy/tests on mlpl-eval, sw-checklist), commit, and complete queuing the next step: port-and-channels.