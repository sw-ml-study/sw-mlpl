Landed worker_thread_spike as the kept B6 gate-#1 regression: Environment and Value are Send (compile-time assertion), an eval runs on a spawned worker (reduce_add(range(7))->21), and an env built on main MOVES into a worker and evals there (seed on main, reduce on worker->10). Proves the interpreter can run on a worker while winit owns main. Gated (fmt/clippy/tests on mlpl-eval, sw-checklist 44/522 held), committed.