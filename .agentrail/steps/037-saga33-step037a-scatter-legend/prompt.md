Add a corner index legend to the 2-D scatter_labeled viz, mirroring the existing scatter3d pattern. Currently scatter_labeled (analysis.rs path / svg/scatter.rs render) draws colored dots but no key — the reader cannot map a color back to its label. (1) Add a small legend block in the corner of the SVG output: one colored swatch per unique label, with the integer label rendered next to it. Reuse the PALETTE shared with scatter3d so colors stay consistent. (2) Update existing tests in mlpl-viz/tests/svg_analysis_tests.rs to assert the legend renders. (3) Add a follow-up TODO comment near the scatter_labeled renderer noting that named labels (scatter_labeled(pts, labels, ["cat", "dog"])) are deferred until svg() supports Value::StrList aux — file as its own step when that lands. (4) No new builtin signature: same scatter_labeled(points, labels) two-arg form. (5) PAGES REBUILD required since the viz output changes.