Saga 14 step 006 shipped. grad(expr, wrt) now works inside device("mlx") {} and matches the all-CPU path within fp32 tolerance. Design choice (option (b) hand-written, documented in module-level comment + commit message): keep the autograd tape structure exactly as mlpl-autograd builds it on CPU; after the forward pass, walk the tape in insertion (= topological) order and re-execute every non-leaf node's forward through mlpl-mlx, overwriting NodeData::value with the MLX-rounded result. The existing CPU backward formulas in mlpl-autograd::ops then operate on those MLX-rounded values; gradients match all-CPU within tolerance because the math is identical and only fp32 round-trip on forward differs. Rejected option (a) leaning on mlx_rs::transforms::grad because it expects an Fn(Array) -> Array closure, which is structurally incompatible with our Tape/NodeId graph without a wholesale rewrite, and the CPU formulas are already gradcheck-tested via Saga 9's fixtures and are the authoritative spec. New device::materialize_tape_on_mlx walks every NodeKind (Unary, Binary, MatMul, SumAll, MeanAll, Softmax, Transpose, Reshape, CrossEntropy) and routes through try_mlx_dispatch; Reshape and CrossEntropy stay CPU-cached (their inputs are already MLX-rounded by earlier loop iterations, so the cached values implicitly carry MLX data). 16 grad parity tests cover per-primitive (sum, mean, exp, log, relu, tanh, sigmoid, neg, add, mul, div, softmax, transpose, matmul) + chained ops (mean(tanh(matmul(x,w)))) + closing integration grad(cross_entropy(apply(linear, X), Y), __linear_W_0) on CPU vs MLX within 1e-4. Workspace cargo test clean default; cargo test -p mlpl-eval --features mlx clean; clippy -D warnings clean both feature modes; fmt clean; sw-checklist FAIL count unchanged at 135. Commit df5cfb8 pushed.