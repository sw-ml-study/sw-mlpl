Record field reads inside grad as constant leaves (plan.md step 4). Records are never params, so a field read is data. Add a side map for non-tensor constants to the traced scope so a record-valued u: argument binds; FieldAccess on it yields an untracked leaf. Weight records stay non-trainable -- document it. TDD: demo-decision-model's Q5 probe rewritten with a {ids, wmask} record matches the plain-array gradient.