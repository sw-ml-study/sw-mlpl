RS1-pow: pow(base, exponent) is a binary elementwise builtin and is not differentiable inside grad. Add differentiability for the common ML case of a CONSTANT exponent (x^2, x^0.5, x^-1) via a dedicated tape node (e.g. NodeKind::PowConst { parent, exp: f64 } -- NodeKind derives only Clone/Debug so an f64 field is fine), with forward x.map(|v| v.powf(k)) on the host path and backward upstream * k * x^(k-1). Wire the grad path: when pow's exponent arg is a constant scalar (not a tracked param), build the PowConst node; if the exponent is itself differentiable, either add the two-sided rule (da = b*a^(b-1), db = a^b*ln(a)) or error loudly. Note the crate mlpl-autograd-tape is at 7 modules and ops.rs at 7 functions -- do NOT add a module or grow ops.rs; put PowConst handling where the other NodeKind arms live (backward.rs / backward_shape.rs) and split first if needed. TDD: gradcheck pow(x,2), pow(x,0.5), pow(x,-1) against closed forms + finite differences; eager pow unchanged. Rebuild repl/build (release+debug).