RS1-pow: pow(x, k) with a constant positive-integer exponent (1..=64) is differentiable via exact repeated product base*base*... (reuses tape Mul backward; d/dx x^k = k*x^(k-1) exact). Verified in release repl: pow(x,2)->2x, pow(x,3)->3x^2; fractional/zero/negative/differentiable exponents error loudly pointing to x*x/sqrt/1-over-x. General constant-exponent PowConst node deferred behind the mlpl-autograd crate split (crate at module ceiling, 3 modules already failing fn-count). New grad_calls_pow module (mlpl-eval already fails crate-module-count, so free). RED->GREEN pow_grad_tests + drive-by 3.14->3.5 clippy fix on RS5 test. sw-checklist held 38/570. Binaries rebuilt repl/build release+debug, serve release.