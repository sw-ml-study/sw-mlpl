Closed extensions-dynamic-load (B3). Added user-facing docs for load_extension + MLPL_EXTENSION_PATH (lang-reference.md Native Extensions section, WHAT/HOW only), flipped A4/B3 to SHIPPED in companion-demo-extensions.md and downstream-unblock-plan.md, marked the queue entry SHIPPED and queued dlclose/manifest(A7) follow-ons, and updated the wiki (Documentation-Errata E-027 + Capability-Matrix dynamic-loading row). Also fixed an out-of-band wasm-build regression from step 3 (gated load_extension/mlpl-extension-loader to non-wasm) and deployed the footer demo-data-structures link.