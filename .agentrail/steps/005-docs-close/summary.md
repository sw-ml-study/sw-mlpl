Closed compiler-process-semantics. Documented the compiled process builtins (print/eprint/exit/read_stdin) + pristine-stdout rule in lang-reference (new 'Compiled programs and stdout' subsection), marked the rung SHIPPED in the queue, removed a duplicate compiler-control-flow bullet and corrected it (if/while already lower; for/repeat/break/continue remain), queued an optional interpreter-parity follow-on, cleared companion-demo-file-processing item 5, and added wiki E-029. Saga delivered: print/eprint, exit, read_stdin lowering + pristine compiled stdout with correct err exit codes.