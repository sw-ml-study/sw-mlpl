Tokenizers step 005 shipped, plus a bundled UTF-8 lexer fix that unblocked the BPE work. Two commits: (1) fix(parser): decode UTF-8 properly in string literals -- replaces 'b as char' (Latin-1 misinterpretation) with proper UTF-8 leading-byte decoding in lex_util::lex_string; new ParseError::InvalidUtf8 variant; 3 new tests covering Latin-1-supplement (é), CJK (日本), 4-byte codepoints (🦀). (2) feat(eval): train_bpe byte-level BPE trainer. New TokenizerSpec::BpeMerges variant with merges/vocab_size/corpus_byte_count/seed fields. Algorithm: u32 token sequence starting at 256 byte ids; count adjacent pairs, pick most frequent with lex-smallest tie-break, add new id, rewrite, repeat until vocab_size hit or no pairs remain. Deterministic given input. BPE algorithm lives in new crates/mlpl-eval/src/bpe.rs module to keep tokenizer.rs under 7-fn budget. 8 BPE tests incl UTF-8 corpus byte-count verification and determinism check. 682 tests total; sw-checklist 16 FAIL unchanged from step-003 baseline.