models-out as specced is infeasible: every cluster depends on in-hub Environment (90 fns of inherent impls) and eval_expr, so sibling crates cycle. Delivered the corrected plan instead: docs/eval-env-design.md (capability-trait design extending the existing mlpl-env-traits pattern, leaf-type moves to mlpl-eval-core, OnceLock hooks for eval_expr/dispatched_call, transient-exception protocol) and the reordered step list in the saga doc.