CA3 (doc-only): corrected the symlink wording. Docs said 'symlinks never followed' but reads/writes DO follow a symlink confined to the sandbox (canonicalize + containment); only fs_walk skips symlinked dirs (cycle-safe). Fixed glossary + lang-reference to state: symlink escaping the root refused, in-sandbox symlink followed; fs_walk does not descend into symlinked directories. No behavior change; sw-checklist unchanged (38/570).