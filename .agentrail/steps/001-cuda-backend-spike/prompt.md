Step 1 (GO/NO-GO GATE) of cuda-foundation: prove the candle CUDA backend works on THIS GPU (RTX 5060 Ti, Blackwell sm_120, CUDA 13.2 at /opt/cuda). Create a gated spike crate (e.g. components/native-rt/crates/mlpl-cuda-train as the eventual home, or a throwaway under target-scoped path) with candle-core + candle-nn behind a 'cuda' feature, triple-gated cfg(all(feature=cuda, target_os=linux, target_arch=x86_64)). TDD: write a gated test that (a) creates a Tensor on Device::new_cuda(0), (b) builds a tiny linear regression loss, (c) gets gradients via candle's autograd (Tensor::backward + GradStore), asserts they match the analytic least-squares gradient at w=0 within fp32 tol, and (d) runs a few candle-nn Adam steps and asserts the loss drops toward the closed-form solution -- all ON the GPU (verify the tensors report a Cuda device). PIN candle/cudarc versions known to support sm_120 / CUDA 13 (this is the load-bearing risk; if the pinned versions don't build for Blackwell, record the exact error and the cudarc/CUDA matrix tried, and that is still a valid spike result). Record the build incantation (CUDA_COMPUTE_CAP / nvcc path / features) in the crate README or a docs note. Keep the crate sw-checklist-clean (<=5 fns/module, <=4 modules). DISK: scope builds to this crate; cargo clean -p after. Mirrors MLX step 005. Commit .agentrail with source.