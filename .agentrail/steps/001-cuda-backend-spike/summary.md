GO/NO-GO spike PASSED: candle CUDA backend builds + runs on this host's GPU (RTX 5060 Ti, Blackwell sm_120, CUDA 13.2). New gated crate components/cuda-rt/crates/mlpl-cuda-train (own component workspace). 3 GPU tests pass: CUDA device init, autodiff gradient == closed-form least-squares gradient, Adam collapses loss on-device. Pinned candle-core/nn 0.9.2 + cudarc 0.19.7; build incantation (CUDA_COMPUTE_CAP=120) in crate README. candle (approach A) confirmed viable; the rest of the saga proceeds on it. Crate all-PASS on sw-checklist (0 new failures/warnings).