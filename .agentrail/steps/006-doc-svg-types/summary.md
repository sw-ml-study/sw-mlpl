A4 (docs): documented the six shipped-but-undocumented svg() render types in docs/lang-reference.md -- scatter3d, plotly3d, heatmap_grid, life, waffle, attention_overlay -- with shapes verified against components/viz/crates/mlpl-viz/src/svg.rs and each renderer. Broadened the svg(data,type,aux) row to name the 3-arg types reading aux. Fixed a control-flow example that used '"hello " + name' (does not run; + is not string concat) to str_concat. Added errata E-023 (resolved) to the wiki and fixed a pre-existing smart-quote there. Docs-only: sw-checklist holds at 48/523 (exception trailer).