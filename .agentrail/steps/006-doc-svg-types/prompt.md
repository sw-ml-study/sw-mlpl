A4 (docs-only): document the six undocumented svg() render types in docs/lang-reference.md -- life, heatmap_grid, waffle, scatter3d, plotly3d, attention_overlay (verify each against the actual svg renderer source for accepted args/shape). Also fix the broken string-concatenation example in the docs: '"hello " + name' does NOT run (+ is not string concat); rewrite it to use str_concat("hello ", name). Keep user-facing docs WHAT/HOW only (no saga/step/date framing). Update the wiki Documentation-Errata.md for any stale svg claims. Run sw-markdown-checker (ASCII-only) + repo-wide sw-checklist; ratchet both axes down.