B5: a string list (["a","b"] -> Value::StrList) is rejected when passed as an argument to a u: user function. The work order calls it 'looks like an oversight in one domain check, ~one line'. Find the argument-domain/type check in the u:-function call path (mlpl-eval; likely where u: args are validated/bound -- fncall/user-function eval, or an arg-kind guard) that rejects StrList, and allow it (StrList is a first-class value like Str/Array/Record). Add a repro test: def u:f(xs) { ... } called with a string list works. Read the work order's B5 section for the exact repro + downstream code it unblocks. Scope tests to mlpl-eval. Hold sw-checklist.