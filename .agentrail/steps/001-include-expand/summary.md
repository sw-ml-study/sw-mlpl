mlpl-build resolves include via mlpl-source-loader::expand() + a replicated FsProvider sandbox, flattening to stmts and lowering directly (rt=::mlpl::__rt), replacing the raw-text->macro path. Added --source-dir. e2e (MLPL_BUILD_TESTS=1) green: include compiles+prints 45; reduce_add/wasm/parse-error all pass. FAILs held at 2 (retired 2 transient LOC fails via build_and_emit/take_value); +1 warning (new source_load module).