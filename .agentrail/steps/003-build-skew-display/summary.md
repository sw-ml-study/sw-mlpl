Splash now shows UI build time AND (when connected) the server build time, warning only on a real skew (30-min tolerance -- timestamps not SHAs, since the baked SHA always lags a commit). Server: build.rs stamp + /v1/health {commit, built_at}. chrome: use_server_build hook. onboarding: build_skew tolerance helper. Rebuilt pages + deployed; server current. User-verified both connected (localhost: two dates) and public github.io (UI-only) splash states.