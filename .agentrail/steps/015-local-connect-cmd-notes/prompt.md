Public-demo regression (Workspace Introspection on github.io): unconnected ':connect list' fell through to the MLPL parser ('undefined variable: list') and ':ask' echoed its question. Add try_connect_only_note: when NOT connected, :connect/:ask/:status lines are taken with a friendly 'needs a connected mlpl-serve' note instead of leaking to eval; playwright-verify on a static host; rebuild pages.