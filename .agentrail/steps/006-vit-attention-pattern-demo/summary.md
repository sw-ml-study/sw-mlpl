Saga 29 step 006: demos/vit_attention_pattern.mlpl shipped. No-training forward demo exercising every Phase-1 builtin end-to-end on a synthetic [1, 3, 64, 64] image: patchify -> reshape -> linear patch embed -> concat(cls, patches, 0) -> + positional -> Q/K/scaled-dot-product -> softmax -> svg heatmap. 6 dedicated tests (shape, row-sums == 1, determinism, SVG non-empty). New demo registered in apps/mlpl-web/src/demos.rs ('ViT Attention Pattern (no training)') + pages/ rebuilt. README demo count 22 -> 23. sw-checklist unchanged at 150. Decoded-pets_tiny slicing is deferred until MLPL ships per-batch indexing; demo uses synthetic randn image and documents the trade-off.