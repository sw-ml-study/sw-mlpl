Saga 29 step 006 (per docs/milestone-vit.md Phase 2): demos/vit_attention_pattern.mlpl. NO training. Pure forward demo that exercises every Phase-1 builtin end-to-end on one image from pets_tiny: decode one image, patchify(x, 16), linear-embed each patch, prepend a randn CLS token via concat(cls, patches, 1), add a randn positional embedding, run one attention(128, 1, seed) head (single-head, the multi-head tape lowering lands in step 008), render the attention matrix with svg(..., "heatmap"). Test: row sums of the rendered attention matrix are within 1e-6 of 1.0; the SVG renders to a non-empty file; the same seed produces the same matrix. Same demo lands in apps/mlpl-web's demo dropdown and lessons file. Build pages/ after the web change per CLAUDE.md live-demo gate.