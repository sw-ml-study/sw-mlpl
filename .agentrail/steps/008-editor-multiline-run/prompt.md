User report: loading ../demo-algorithms/demos/deques/service-desk.mlpl into the web UI editor and running it evaluates LINE BY LINE, so multi-line def blocks fail ('unclosed { at ...', then per-line undefined variables). Fix the editor Run path to submit statement GROUPS: buffer lines until brackets/braces balance (outside string literals) so multi-line constructs evaluate whole while per-statement transcript entries and viz still render; same behavior local and connect. TDD on the grouping fn; verify with the actual service-desk.mlpl file.