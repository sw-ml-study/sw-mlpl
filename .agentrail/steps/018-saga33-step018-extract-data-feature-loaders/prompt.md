Continue loader extraction. The remaining env-dependent loader fns (eval_load, eval_load_preloaded, eval_load_images, eval_fetch_dataset) need either a HasDataDir capability trait or get-data-dir-as-closure injection. Consider whether to extract them now or wait. Also examine bpe.rs and tokenizer.rs to see if they're cleanly extractable to mlpl-tokenizer sub-crate (BPE training + byte-level tokenization). Strict gate: should be feasible since the foundation is broad.