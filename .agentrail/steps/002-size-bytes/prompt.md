Add a size_bytes(x) -> scalar interpreter builtin returning the PACKED storage footprint in bytes. For Value::Bytes -> data.len(); for a numeric Value::Array -> elem_count * 8 (f64 backing); for Value::StrList -> sum of UTF-8 byte lengths; for Value::Record -> recursive sum over field values (+ optionally the key bytes; pick one and document it). This is demo-memory's packed-layout-size HARD blocker probe (bytes-per-key claims). TDD: RED test first (size_bytes(pack([1,2,3],"u8"))==3; size_bytes(pack([1],"u32"))==4; size_bytes([1,2,3])==24). Add the builtin to the existing fncall_bytes family (keep it under the 5-function module limit -- extract a pure size_of_value helper into a named module if needed, e.g. mlpl-bytes or a size.rs). Scope tests to mlpl-eval + mlpl-bytes; clippy/fmt both workspaces; ratchet sw-checklist both axes down. Do NOT rebuild the release binary yet.