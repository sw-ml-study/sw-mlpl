Add a stable public release channel for the live demo at mlpl.softwarewrighter.com (decoupled from the auto-deploying dev demo). scripts/release-stable.sh builds the site at root base-path (--public-url /, vs the dev /sw-mlpl/ subpath), mirrors into ../mlpl-live with a CNAME file, and commits. docs/release-hosting.md documents the two-channel model + one-time GitHub Pages custom-domain + Squarespace DNS setup. ../mlpl-live already built + committed locally (user creates the GH repo + pushes).