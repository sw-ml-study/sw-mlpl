Added the stable public release channel. scripts/release-stable.sh builds the demo at root base-path (--public-url /), mirrors into ../mlpl-live with a CNAME (mlpl.softwarewrighter.com) + build-info channel=stable, and commits (idempotent). docs/release-hosting.md documents the two-channel model + one-time GH Pages custom-domain + Squarespace DNS setup. Ran it: ../mlpl-live built + committed locally (commit fe1ff55 from sw-mlpl 7f589dc2), root asset paths verified (/mlpl-web-*.wasm, no stale /sw-mlpl/ except the intentional GitHub source link). Owner creates sw-ml-study/mlpl-live on GH + pushes. sw-checklist unchanged (41/540; shell+docs).