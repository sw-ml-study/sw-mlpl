Close the compiler-text-ops saga. Document the three compiled text conversions (tokenize_bytes, decode_bytes, to_int) as compile-to-Rust capable: update docs/compiler-implementation.md (or the compiler capability doc) and any user-facing note WHAT/HOW only. Update docs/future-sagas-queue.md marking the compiler-io-parity TEXT rung SHIPPED and confirming compiler-process-semantics is the next rung. Update the wiki Documentation-Errata if a compiled-capability claim flips, and refresh docs/companion-demo-file-processing.md if the text conversions clear a gate. Hold sw-checklist (docs-only exception if it merely holds). Then agentrail complete --done.