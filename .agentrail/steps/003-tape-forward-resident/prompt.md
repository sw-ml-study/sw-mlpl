E4 step 3: tape forward stays resident. Generalize the autograd tape's value carrier: NodeData.value (and .grad) in mlpl-autograd-tape become mlpl_tensor_handle::TensorHandle (Cpu arm preserves today's exact behavior -- CPU stays the bit-exact reference; all existing DenseArray-based backward kernels keep operating on .to_dense()/Cpu values initially). Tensor::leaf/param accept DenseArray as today (wrap Cpu) plus a new resident entry point. Then route the FORWARD under device("mlx"): grad.rs eval_tensor_expr / the tape-building ops keep intermediates resident via TensorHandle dev_* calls when env.device()==mlx and a backend is registered (register_mlx_device_ops wired into the binaries' startup next to register_gpu_step: repl + serve + tests), with per-op CPU fallback (download, run DenseArray op, continue host-side) for ops without device kernels (patchify/take/stack/rotate/concat). DELETE materialize_tape_on_mlx and its 2x forward (device.rs) -- the forward IS now the MLX-rounded one. Node values that backward will read remain accessible via to_dense() during the (still-CPU) backward pass in this step; full backward residency is step 4. TDD: grad_mlx_tests + train_mlx_tests must stay green at FP32_TOL (they now exercise the resident forward); add a test pinning that a device-block grad() call leaves intermediates resident (spy via handle is_dev on the tape or count downloads) and that CPU-only builds/behavior are BIT-identical to before (no-mlx run of gradient_flow_tests etc). Watch budgets: mlpl-autograd-tape crate is at 7 modules / layers.rs & apply.rs at limits (per models-write... actually autograd component: tape.rs NodeData). Scoped gates: components/autograd, components/eval (mlx features), native-rt untouched unless the seam needs additions; sw-checklist ratchet applies.