Write docs/data-forge-design.md: the array-first data-forge surface -- generator forms, evaluator contract, rejection/ranking substrate (sample/evaluate/select/filter), curation + curriculum scheduling, dataset provenance records, and the minimal knowledge-graph representation (entity ids + [E,3] edge arrays + path ops). For each piece: what composes from existing MLPL today vs what needs a builtin, with worked MLPL examples. No implementation. Present to the user for review before any code.