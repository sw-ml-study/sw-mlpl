F4: gather_rows now differentiable on the tape (scatter-add backward via one-hot sel@table matmul, reusing the engram lookup seam -- no new autograd kernel), so from-scratch addressing/embedding tables train. Verified in release binary: grad(sum(gather_rows(M,[0,0,2])),M) == [[2,2],[0,0],[1,1]] scatter-add. kl_divergence needs no builtin -- confirmed reduce_add(P*(log P - log Q)) already works eager (0.0823) AND in grad (-P/Q == [-1.4,-0.6]); documented the idiom in lang-reference + rewrote glossary KL entry (removing a banned roadmap/Deferred claim). Implementation: grad_calls_engram +call_gather_rows/selection_onehot/gather_index (4 fns, at gate, NO new module), grad.rs +1 delegating arm, lang-reference grad-op list names gather_rows+user-fns. Tests: grad_builtin_tests 21/21 (+2), engram 4/4. sw-checklist 38/562 UNCHANGED (zero new warnings/fails -- anticipated). Rebuilt release+debug mlpl-repl/mlpl-build. Pushed.