Saga 33 step 027: unhang the Pets-quick demo. Three fixes: (1) scripts/serve.sh defaults to --release (dev WASM was ~30x slower than release; this matches what github.io ships). (2) PETS_CAT_VS_DOG_QUICK's train 30 split into 6x train 5 chunks with all_losses accumulator -- mathematically identical, but cross-line Timeout(0) yields now kick in between chunks. (3) running_message split off train_caption so small train N (N<=10) gets a chunk-aware caption instead of the 30-90 second unresponsive warning. Native: 13.89s post-fix vs 14.58s pre-fix; mean(all_losses) byte-identical at 130.80. Trunk serve verified up at localhost:9957 in release mode.