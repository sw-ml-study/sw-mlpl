Added easy-menu MLPL menu-bar to mlpl-mode AND elisp/mlpl-init-example.el one-file setup. Diagnosed why the user's manual load failed: eval'ing mlpl-mode.el alone doesn't activate the mode or load REPL/menu siblings, and reload keeps a stale keymap (mlpl-mode-map is a defvar; defvar skips already-bound vars) so C-c C-f showed undefined. Init file loads mlpl-all.el, makunbounds the map+syntax table to force a fresh rebuild, force-loads the repo mode file over the other editors/emacs copy, and re-modes open buffers. Verified from the broken state: C-c C-f binds, MLPL menu present, format works.