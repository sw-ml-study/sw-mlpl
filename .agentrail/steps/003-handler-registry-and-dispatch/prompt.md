Third step of extensions-event-loop. Build the event-dispatch loop + handler registry so MLPL registers callbacks (JS applet model). Add: (1) an env-local handler table keyed by (port slot, event-name string) storing user fn-refs; (2) builtins on(port, event_name, :u:handler) and off(port, event_name) that register/unregister; (3) a run(port) dispatch loop that, on the worker thread, repeatedly pulls the next event via the port's event channel, looks up the handler for that event's kind, and invokes it in-process with the event, threading app state as a VALUE (fold: state = handler(event, state)), until a close event; run returns the final state. TDD headless: register handlers, feed a scripted event sequence through the port's far end (an in-process sender, like port_channel_tests), assert handlers ran in order, app state evolved via the fold, and a close event stops the loop cleanly -- all on the worker side, no winit. Event kind: events are records with a 'kind' field (matches the record returns from arrays-handles). Keep functions <=25 lines by refactoring; keep sw-checklist fails/warns decreasing (reuse ExtHandle/records; the handler table can live in env.rs or a new mlpl-eval module since mlpl-eval is already over module-count -- avoid a NEW module in mlpl-eval-env which is at its 7-ceiling). Gate, commit, complete queuing ui-host-launch-inversion.