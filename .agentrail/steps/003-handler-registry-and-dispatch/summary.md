Event-dispatch loop + handler registry (JS applet model). on(port, event, :u:h) / off register/unregister into an env-local (slot, kind)->fn table; run(port, state) pulls events, reads the record kind, invokes the matching handler in-process via invoke_user_fn_values, and FOLDS app state as a value until a close event, returning final state. dispatch_one holds the per-event decision; port_util holds slot/kind/error helpers (each module <=4 fns). TDD headless: fold inc+noop(skip)+inc+close -> 2; off then fresh stream unchanged. Checklist 44/522 held; clean.