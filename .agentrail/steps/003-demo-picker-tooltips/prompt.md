Add accessible tooltips to every demo-picker group and demo. Group tooltips explain the group's purpose; enabled demo tooltips summarize the demo; disabled demo tooltips include the summary plus the precise reason it is unavailable for the current connection/device state and how to enable it. Use TDD, preserve capability gating and ordering, run scoped web-components tests/clippy/fmt plus sw-checklist, then rebuild/restart the local connected review surface before returning to the queued deploy step.