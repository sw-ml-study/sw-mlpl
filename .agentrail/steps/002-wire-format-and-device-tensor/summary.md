Saga R1 step 002: real eval-on-device + tensor wire format + Value::DeviceTensor. crates/mlpl-mlx-serve gains 3 new endpoints (POST /v1/sessions/{id}/eval-on-device replaces the step-001 501 stub; POST /v1/sessions/{id}/transfer pulls tensor bytes back; POST /v1/sessions/{id}/release-handle/{h} cleans up). Wire format module ships bincode + versioned envelope (f64-only in R1; version field reserves space for future dtypes). Per-peer handle store keyed by uuid. Value::DeviceTensor variant on the orchestrator side errors strict-fault (new EvalError::DeviceTensorFault) on cross-device ops without explicit to_device('cpu', x). 17 new tests (8 wire + 6 api + 3 device_tensor); 1031/0/3 total. Both contracts updated: serve-contract/eval-on-device.md from stub to real shipped shape, new eval-contract/device-tensor.md for the Value variant.