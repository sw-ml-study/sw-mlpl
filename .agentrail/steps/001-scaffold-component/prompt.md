Create the components/lang-core/ nested workspace scaffold. Make components/lang-core/Cargo.toml with [workspace] block (initially empty members), [workspace.package] block matching the project's edition/license/version. Create the empty components/lang-core/crates/ directory (placeholder file like .gitkeep). Verify the empty workspace itself doesn't break anything: cd components/lang-core && cargo check (should succeed -- empty workspace is valid). Verify the main workspace still builds: cargo check --workspace from sw-mlpl/ root. Commit.