Add the grad(expr, wrt) built-in. wrt may be a single param or a list of params. Returns arrays matching each param shape. Evaluator: when any input to a built-in/op is a tracked Tensor, route through the autograd tape; on grad() call, run backward and return accumulated gradients. TDD: scalar loss wrt vector param, vector loss via sum wrt matrix param. Also test zero-grad reset between calls. Commit via /mw-cp.