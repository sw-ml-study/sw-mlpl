Landed the B3 loader-gate regression: mlpl-ext-testdylib, a lib+cdylib fixture exporting sw_mlpl_extension_v1 (leaked descriptor, one arity-0 fn answer->42.0) via the same C descriptor ABI as the static path; tests/dlopen_tests locates the shared library (platform prefix/ext aware), dlopens it (libloading), resolves the entry, register_c_extension's it, and invokes testext:answer -> F64(42.0). Proves the C descriptor ABI crosses the dlopen edge unchanged in CI. Refactored the descriptor builder under 25 with a slice helper -> net-zero warns. Checklist 44/523 held.