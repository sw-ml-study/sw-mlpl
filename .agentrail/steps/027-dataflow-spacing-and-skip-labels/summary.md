Two emufpga UPSTREAM asks. Ask 6 (spacing control): optional col_gap/row_gap overrides on the nodes record, auto-widen stays a floor; threaded Dataflow->Graph->layout. Ask 5B (skip-edge label on box): every forward edge label now anchored at the center of the first column gap after its source (always clear of boxes), not the geometric midpoint that fell on an intermediate box; column_gaps sizes gaps for skip edges too. layout.rs label_anchors() from a Geo bundle stored in Positioned.label_at; rank inlined into layout to keep the function budget; render consumes label_at (edge_path points-only; EdgeStyle struct). 11 crate + 3 eval tests pass; verified e2e (col_gap 200->canvas 504; 'resident' skip label at x=200 clear of box b 248..376). Fresh release/debug binaries rebuilt. sw-checklist held 44 failed (exception; 538->540 warnings; no new FAIL -- place kept <50 via label_anchors extraction, eval reused float_array to stay at 7 fns).