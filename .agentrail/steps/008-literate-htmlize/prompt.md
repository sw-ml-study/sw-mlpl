User request: look at ../../softwarewrighter/microgpt-mlpl to see how it uses Emacs htmlize to syntax-color MLPL code snippets in its published literate (org -> html) documents, and adopt the same approach for sw-mlpl's literate html (examples/literate and any docs/literate export scripts). Reuse the elisp/ mlpl-mode (the active one) for font-lock; keep the export reproducible from a script; regenerate the committed html and check it renders colored. If the pages site ships literate html, rebuild/deploy per CLAUDE.md. Document the export command.