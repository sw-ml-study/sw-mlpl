Saga 33 step 031: pca_components(X, k) + pca_variance_explained(X, k) builtins shipped. Refactored pca_builtin.rs into pca_builtin (entry + validate) + pca_compute (pure compute helpers) so the new builtins fit under the 7-fn module ceiling. extract_components evolved to extract_components_with_eigenvalues returning both vectors so callers don't need to re-run power iteration. 8 unit tests cover shape, orthonormality, alignment with data direction, variance-explained sum-to-1 + descending order + first-dominates-anisotropic, plus arg validation. Glossary, lang-reference, inspect_groups all updated; help_completeness tests green. Net: -1 WARN (validate_pca_args trim from 48 -> 41 LOC).