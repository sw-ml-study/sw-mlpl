eval decomposition step 2 (docs/eval-env-design.md): move Environment + the 27 env_* modules into new crate mlpl-eval-env (eval workspace). dispatched_call routes through an install_dispatch OnceLock hook (mlpl-eval installs at eval entry). pub(crate) surface of the moved cluster becomes pub (documented internal API). mlpl-eval keeps module paths alive via pub use re-exports. TRANSIENT: the new crate is over the module budget until the capability peels (steps 3+) -- documented sw-checklist exception per the design doc. Scoped tests: targeted env/interrupt/experiment/optim/dispatch test files + cpu and cuda clippy + dependent-workspace compiles.