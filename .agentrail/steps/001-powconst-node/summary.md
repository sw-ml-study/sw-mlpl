PowConst node: pow(x, k) now differentiates for ANY constant exponent. Added NodeKind::PowConst{parent,exp}, Tensor::pow_const forward (host powf), prop_pow_const backward (upstream*exp*x^(exp-1)) via a propagate() arm; grad_calls_pow builds PowConst for any constant scalar exponent (dropped integer-only/MAX_POW/repeated-mul), rejects a param-dependent exponent. RED->GREEN pow_grad_tests (x^2/x^3 exact, x^0.5 analytic, x^-1/x^2.5 finite-diff, differentiable-exponent rejected, eager unchanged). Verified in release repl: x^0.5->0.5x^-0.5, x^-1->-x^-2, x^3->3x^2 all exact. Full autograd+eval grad suites green. sw-checklist 34 failed held; +2 warnings (pow_const in tensor_ops, prop_pow_const in backward_shape; exception). Binaries rebuilt repl/build release+debug, serve release.