Shipped /v1/viz content-addressed storage. New crates/mlpl-serve/src/viz_storage.rs (top-level module) with VizStore + SharedVizStore + attach_viz + result_to_sse + upload_handler + get_handler + require_known_bearer. Eval pipeline (eval_handler + spawn_eval_task) detects SVG via mlpl_cli::viz_cache::is_svg_string and stashes bytes in store + MLPL_CACHE_DIR (if set), surfacing viz_url + viz_local_path on EvalResponse and SSE done payload. mlpl-repl EvalResponse mirrors the fields and prints 'viz: <url>' / 'viz: <path>' before the value. 10 new tests (8 server, 2 repl); all cargo gates green. sw-checklist +1 over baseline (mlpl-serve module count 7 to 8) via exception lane with justification: inline-mod approach pushed lib.rs over both 7-fn and 500-LOC budgets. Contract updated.