Pivoted step 007 from model_dispatch.rs (already split in step 004) to the deeper main.rs refactor with full engineering discipline. (1) Cargo.toml lib+bin hybrid + src/lib.rs holds all pub mod declarations (main.rs no longer has mod statements at all). (2) build.rs delegates to build_env + build_main_body sub-modules; build_main_body selects main_wasm_body.rs vs main_not_wasm_body.rs by CARGO_CFG_TARGET_ARCH and emits OUT_DIR/main_body.inc.rs. (3) src/main.rs is now a 5-line stub: fn main() { include!($OUT_DIR/main_body.inc.rs) }. (4) app() phase-separated into init (use_sessions/use_ui_state/use_upload_state), side-effect (log_connect_mode), derivation (active_context), processing (build_callbacks), reactive-effect (use_scroll_effect), render dispatch. Each helper <=25 LOC across 6 sibling files using struct-of-structs (Sessions/UiState/UploadState/ActiveContext/AppCallbacks). (5) render() decomposed into RenderArgs::from_parts + build_input_callbacks + derive_modes + render_shell, with html! templates spread across render_shell_chrome/header/modebar/footer.