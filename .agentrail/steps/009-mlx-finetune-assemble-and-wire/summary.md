Assembled the full demo LoRA architecture into one traceable MLX graph in a new component mlpl-mlx-model (its own component so native-rt stays at 4 crates; 7/0/0). DemoWeights holds the frozen base; demo_forward composes embed->rms_norm->causal_attention->residual->rms_norm->lora head->cross_entropy with the 4 attention projections + head LoRA-wrapped (10 traced adapters, effective weight w+scale*(a@b) computed in-graph). A gated test trains the adapters via train_steps+MlxAdam and asserts the cross-entropy drops -- gradients flow through the WHOLE model and the optimizer reduces loss, all on GPU. This retires the last technical unknown; only interpreter glue remains.