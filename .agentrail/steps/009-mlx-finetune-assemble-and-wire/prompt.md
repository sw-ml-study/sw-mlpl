Step 009 (FINALE of the MLX full-GPU LoRA fine-tune): assemble the MLX forward primitives into the demo model and wire it into the fine-tune loop. (1) In mlpl-mlx-forward (or a new sibling crate mlpl-mlx-model if module budget is hit -- refactor up and out), add a function that runs the full demo forward as ONE traceable graph: embed(onehot(tokens), table) -> residual(rms_norm -> causal_attention) -> rms_norm -> lora_linear head -> cross_entropy(targets_onehot). Base weights (embed table, attn Wq/Wk/Wv/Wo, rms gammas, head W) are captured as constants; the LoRA adapters A/B are the traced params. (2) Build the loss closure |&[Array]|->Result<Vec<Array>> over the adapters and run it via mlpl_mlx_train::train_steps + MlxAdam. (3) Wire into mlpl-eval grad_optim.rs eval_adam: when env.device()=='mlx' AND every trainable param is a LoRA adapter (names __lora_A_*/__lora_B_*), route through this MLX path instead of the CPU tape+adam; pull base params + adapters from the Environment as mlx Arrays (mlpl-mlx-rt common::dense_to_mlx), write updated adapters back (mlx_to_dense_data) each step. Build the per-token one-hot inputs + target one-hots once from the int token ids. (4) Extend lora_mlx_demo_tests.rs to assert the MLX loss curve matches the CPU path within fp32 tol (V=16,d=8,h=1,rank=2,3 steps). (5) Relabel the 'MLX LoRA fine-tune' demo hybrid->true-GPU: update components/web-demos .../lm.rs intro/takeaway + the progress NOTE in registry.rs, and any docs that say 'forward on GPU, backward+adam on CPU'. This is the last MLX step before the demo is genuinely true-GPU; scope/split as needed. MLX value_and_grad not thread-safe -> serialize grad tests. DISK: scope to mlx + eval crates, clean after. Commit .agentrail with source.