Authored demos/lora_finetune_mlx.mlpl (fine-tune train block in device("mlx") { }, base pre-training stays on CPU) plus triple-gated integration test lora_mlx_demo_tests.rs that runs cut-down paths with identical seeds and asserts fine-tune losses + adapter values + frozen base all agree within fp32 tolerance. Added bench_lora_finetune_step Criterion group. Measured warm CPU 164us vs warm MLX 1.11ms = 0.15x (MLX slower). Added the row + a 7-line analytical paragraph to docs/benchmarks.md explaining why LoRA is a step down from non-LoRA tiny-LM ratios (extra matmul per linear, adapter dims too small to amortize kernel launches).