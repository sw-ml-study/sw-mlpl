Phase 0 part 2 (web side) of local-gpu-agentic. The server now exposes GET /v1/ollama/config (default host+model) and GET /v1/ollama/tags (model list). Wire the web/connect-mode REPL to them: (1) a ':models ollama' command that fetches /v1/ollama/tags via the connected server and lists the available models (distinct from MLPL ':models'); (2) make the web ':ask' default to the server-configured host/model from /v1/ollama/config when no ?ollama=/?model= override is present (fetch on connect, cache it). DEFER the per-:ask --model override. Then rebuild pages/ + restart the local mlpl-serve and verify via Playwright. Keep sw-checklist non-regressing. Commit .agentrail with the source.