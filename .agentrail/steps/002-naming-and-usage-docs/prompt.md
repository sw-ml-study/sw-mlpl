Document the naming trichotomy (name = builtin call; :name = quoted builtin reference for higher-order use; u:name = user-defined namespace; rationale + the future first-class-u: staging note) as a lang-reference section + glossary entry. Add a Value Kinds section to lang-reference covering all 9 kinds from value_kind(). Refresh docs/usage.md (stale since 2026-05-26): def u:/return, if/while/break/continue, records + field access, Results + ok/err accessors + projections, try/catch + ?, connect-mode commands (:ask/:connect/:status/:reset/:upload/:list/:fns), rotate/emit_frame/disp depth, demos pointers. markdown-checker; pages rebuild (both tabs are include_str).