Create the mlpl-optim crate scaffold (or mlpl-runtime::optim module -- pick one and document the choice). TDD. Add to workspace. Define an OptimizerState type stored in the Environment, keyed by param name, holding per-param tensors (e.g. momentum buffer). Add empty hooks for momentum_sgd and adam. RED: tests asserting state can be created/read/written through Environment without panicking. GREEN: minimal impl. Commit via /mw-cp.