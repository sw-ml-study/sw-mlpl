Saga 10 step 001: scaffolded optimizer state. Added OptimizerState struct (HashMap-keyed buffers + per-optimizer step counters) as a private Environment field with optim_state/optim_state_mut free-fn accessors. Wired eval_momentum_sgd/eval_adam stubs into eval.rs FnCall dispatch returning Unsupported('not yet implemented'). Inlined 3 grad.rs helpers (check_arity/const_leaf/array_lit_elems) into closures to keep grad.rs under the 7-fn-per-module budget after merging optim into it. 4 new passing tests.