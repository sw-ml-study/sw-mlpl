Saga 29 step 016 (inserted): :upload x REPL command. Today the web playground has an 'Upload Image' button that hardcodes the result as 'uploaded'. Convert this to a REPL command: when the user types ':upload x' (or any name), the existing Canvas decode + resize pipeline runs and binds 'x = Ok({pixels: [1, 3, 64, 64], h: 64, w: 64})' on success, or 'x = Err("cancelled")' when the dialog is dismissed. This is the first in-tree consumer of Value::Result (Saga 29 step 012). Update the upload success message in apps/mlpl-web/src/upload.rs to paste a copy-pasteable predict line using the caller's chosen variable name. Update docs/lang-reference.md REPL commands section. Keep the existing button working for the no-name case (binds 'uploaded' as a Result-wrapped value too, so behavior is consistent).