flatten is now differentiable in grad() (downstream note): inline dispatch arm reshapes to [tally(x)], reusing the tape's Reshape backward -- unblocks the im2col training spelling flatten(k). Inlined in eval_tensor_fncall (grad_calls_shape at ceiling). Test grad(sum(flatten(M)),M)=ones; grad suite 16 pass. Doc adds flatten. Binaries rebuilt release+debug; no pages deploy (no demo uses it). Commit f8f58813. sw-checklist 43 HELD. No version bump, no stable.