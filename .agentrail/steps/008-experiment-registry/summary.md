Tokenizers step 008 shipped. :experiments REPL command merges env.experiment_log (in-memory) with on-disk <exp_dir>/*/*/run.json records, sorts by timestamp, prints one line per run with name + timestamp + top-line metric (first alphabetically from the BTreeMap). compare(a, b) builtin returns Value::Str with side-by-side most-recent runs per name, showing each metric as 'metric: a_value vs b_value (delta +/-X)'. Missing side of a metric renders as '-' with no delta. ExperimentRecord + ParamShape gained Deserialize for the disk-read path. New read_records_from_disk, format_registry, dispatch_compare, latest_by_name, render_compare helpers in experiment.rs. inspect.rs dispatches :experiments; eval.rs dispatches compare FnCall. 12 new tests; 720 total; sw-checklist unchanged from step-003 baseline.