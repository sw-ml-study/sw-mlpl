Lower du's builtins: list_len/1, list_get/2 (StrList ops), fs_walk (sandboxed dir -> Result of StrList), and concat (array concat). Interpreter parity (list_ops.rs, fncall_fs.rs/fs_ops.rs/fs_walk_impl.rs, mlpl-runtime-array/math concat). CVal has a StrList variant already. list_len(xs)->scalar char... no: list_len(xs)->scalar count of a CVal::StrList; list_get(xs,i)->Result ok(Str)/err out of bounds. fs_walk(path, opts_record)-> ok(StrList of root-relative paths)/err, needs the compiled sandbox root (MLPL_FS_ROOT/cwd + contained, like read_bytes in mlpl-rt-fsio -- add an fs_walk to mlpl-rt-fsio mirroring the interpreter fs_walk_impl::walk, lexical order). concat: 2-arg flat rank-1 (math_try_call) and/or 3-arg axis (array_try_call) -- both already reachable via the try_call re-exports, just add registry rows. Prefer inline emits for list ops (CVal::StrList already accessible); fs_walk needs an mlpl-rt-fsio fn (re-export via facade). Add dispatch_coverage variants + CVAL_BUILTINS entries for StrList/Result producers. TDD gated e2e: list_len + list_get over a str_split result; concat([1,2],[3,4]) or concat(a,b,0); fs_walk over a temp dir with files. Hold sw-checklist (exception if ceiling forces +1). After push refresh binaries. Commit before agentrail complete.