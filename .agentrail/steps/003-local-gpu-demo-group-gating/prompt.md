Phase 1 of local-gpu-agentic: create the 'Local GPU / connect-mode' demo SECTION and the live-demo gating (saga task A). (1) Add a demo category/section (in the demo registry, see docs and components/web-paths or the demos list) that groups connect-mode / GPU demos distinctly from the core playground demos. (2) Add a 'requires_connect' flag to the demo registry so connect-only demos render VISIBLE-BUT-NOT-RUNNABLE on the public GitHub Pages demo (no server), with a clear 'needs a connected mlpl-serve' affordance; in connect mode they run normally. (3) Seed the section with a working 'ask Ollama a contextual question' demo entry that uses :ask (this works today in connect mode) -- this is demo (a) the user asked about. The download+fine-tune demo (b) is Phases 4-5 (SmolLM2 loader + arch + tokenizer), NOT this step. TDD the registry/flag logic; rebuild pages + verify the gating renders both on the public build and in connect mode. Keep sw-checklist non-regressing. Commit .agentrail with the source.