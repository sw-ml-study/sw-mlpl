Phase 1: demo capability tiers + dropdown gating + seeded connect/MLX demos. Side-table capability model (Device cpu/mlx/cuda, capability_for defaulting unlisted demos to cpu/live; 4 TDD tests). mode_bar dropdown groups by tier -- cpu/live demos keep their category; connect/MLX/CUDA get separate (connect) optgroups; connect/GPU options render disabled (visible-but-not-runnable) on the public build, enabled in ?connect= mode (cross-target is_connected). Seeded 'Ask Ollama (contextual)' (Client-server) + 'MLX LoRA fine-tune' (MLX, honestly labeled forward-GPU/backward-CPU). Verified via Playwright in both modes. sw-checklist held 14/308.