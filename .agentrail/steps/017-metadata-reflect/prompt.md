Per design: tests() (StrList of stable names, source order), test_info(name) (record: name/fn as :u: reference/tags/skip/expected_failure/timeout_ms/source/line), annotations(name) (record word -> payload, bare = 1); catalog/lang-reference/glossary/usage docs + README pin; :describe u:name metadata line if budget holds; rebuild repl+server+pages; run mlplunit check-capabilities expecting test-metadata AVAILABLE; q-and-a update.