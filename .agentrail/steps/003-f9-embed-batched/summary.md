F9: apply(embed, tokens) now accepts batched [B,T] input (the reference already documented it; the impl wrongly rejected rank!=1). Both eager and tape flatten tokens to [N], build [N,vocab] one-hot, matmul the [vocab,d] table, and reshape the [N,d] result to tokens.shape+[d]; rank-1 unchanged. tokens_to_onehot flattens any rank; apply_embedding + embedding_tape reshape back. Kept eager/tape consistent. Tests apply_embed_accepts_batched_bt_tokens + grad_through_batched_embed_trains_table; embedding suite 9/9. sw-checklist 38/562 unchanged. Rebuilt repl/build (serve batched to close). Pushed.