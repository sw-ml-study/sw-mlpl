Saga 29 step 005 (per docs/milestone-vit.md Phase 1): patchify + concat builtins with tape lowering. patchify(x, P) over [B, C, H, W] returns [B, N, P*P*C] where N = (H/P)*(W/P) -- internally reshape + transpose + reshape, but the named builtin exists so the tape has one op to lower and the demo reads cleanly. concat(a, b, axis) produces an array whose shape matches both inputs except along axis (sizes add); initial version supports two-argument concat over axis 0 or 1 (variadic + arbitrary axis are followups). Both ops on the tape: forward identical to eager; backward splits the gradient along the same axis they joined. Gradcheck parity vs finite differences on [2, 3, 4, 4] patchify and [2, 1, 4]-prepended-to-[2, 8, 4] concat fixtures. Update contracts/eval-contract for tape lowering. TDD: write the gradcheck tests first.