RS2: softmax(a, axis) is axis-aware and rank-general in grad. Root cause: grad routed softmax through the 1-arg unary dispatch (BadArity on the axis arg; only last-axis differentiated). Rewrote tape kernels softmax_forward/backward rank-generally via shared axis_strides (removed softmax_backward_2d, panicked on rank-3 before); added grad_calls_softmax::call_softmax threading the optional axis (default last) to new Tensor::softmax_axis; softmax() delegates. RED->GREEN softmax_axis_tests (eager axis-0 + rank-3 lanes sum to 1; grad axis-0 matches finite-diff; grad rank-3 flows, lanes sum to ~0; default==last-axis). Verified in release repl. Landed on already-failing tensor_reduce + new grad_calls_softmax module (checklist-neutral); extracted call_flatten to hold eval_tensor_fncall <50 LOC. sw-checklist 38 failed / 568 warnings (failed baseline; warnings DOWN 2). Binaries rebuilt repl/build release+debug, serve release.