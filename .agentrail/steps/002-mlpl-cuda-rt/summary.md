mlpl-cuda-rt + mlpl-cuda-elementwise: the core eager forward-op surface (candle analog of mlpl-mlx-rt), proven on the RTX 5060 Ti. Split into two warning-free sibling crates to respect the <=4 fn/module + <=4 module/crate budgets: mlpl-cuda-rt (device/convert plumbing, matmul mat-mat + mat-vec, reshape, transpose) and mlpl-cuda-elementwise (add/sub/mul/div scalar-broadcast + neg). 6 GPU parity tests pass vs CPU within fp32 tol. Both triple-gated; off-target export nothing. sw-checklist baseline held (0 new failures/warnings). nn surface deferred to new step 003 (mlpl-cuda-nn).