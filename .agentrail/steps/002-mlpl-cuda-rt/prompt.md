Step 2 of cuda-foundation: create components/native-rt/crates/mlpl-cuda-rt mirroring mlpl-mlx-rt -- forward ops as free functions over candle Tensors: matmul, add/sub/mul/div/neg, exp/log/relu/sigmoid/tanh, softmax/log_softmax/cross_entropy/mean/reduce_mul/argmax, transpose/reshape, plus dense_to_cuda / cuda_to_dense_data converters (analog of mlpl-mlx-rt::common). Triple-gate cfg(all(feature=cuda, target_os=linux, target_arch=x86_64)); stub (returns None / errors strict) when any gate absent. TDD: each op parity-tested vs the mlpl-array CPU implementation within fp32 tol. Respect sw-checklist (<=5 fns/module -> split reductions/activations/etc into named modules like the MLX crate; <=4 modules/crate -> if it would exceed, plan a sibling crate, do NOT cram). native-rt currently has 4 crates (at the <=4 budget) -- adding cuda-rt makes 5, so EITHER place cuda crates in a NEW component dir (e.g. components/cuda-rt/) mirroring how mlx-model got its own component, OR document the budget decision. DISK: scoped builds, clean -p after.