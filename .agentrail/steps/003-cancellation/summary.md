Shipped cooperative cancellation across all 3 layers. mlpl-eval: new Interrupt token + EvalError::Cancelled { step, partial_losses } + Environment::set_interrupt/check_interrupt + interrupt checks at every loop head (for/train/repeat) + pre-builtin dispatch + eval_train enrichment that catches bubbled Cancelled and re-emits with current step + accumulated losses via enrich_train_cancel helper. mlpl-serve: POST /v1/sessions/<id>/cancel handler + parallel InterruptMap (token + Interrupt pair, sidesteps the sessions write lock held by in-flight evals) + install_session_interrupt shared between eval/eval_stream handlers + SseEvent::Cancelled terminal frame. mlpl-repl: ClientError::Cancelled + should_double_cancel debounce + post_cancel helper + ctrlc-based SIGINT handler with 2s window double-press + render_cancellation prints loss preview. 19 new tests across 3 layers (9 eval, 5 serve, 5 repl). All cargo gates green; sw-checklist 134 held via cancel_train -> interrupt.rs move + emit_metrics extraction + install_session_interrupt extraction + trimmed Display arm. Contract documents the endpoint, mechanism, 'one op' latency floor, and event: cancelled wire schema.