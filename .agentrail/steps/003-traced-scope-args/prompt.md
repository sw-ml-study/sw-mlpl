Traced-scope argument resolution + microgpt-mlpl asks (downstream ../../softwarewrighter/microgpt-mlpl). (1) BUG: grad handlers evaluate non-differentiable args eagerly in the GLOBAL env, so a u: function parameter is 'undefined variable' inside grad. Repro: W = param[3,3]; def u:ce(x, y) { cross_entropy(matmul(x, W), y) }; grad(u:ce(X, [0, 2]), W) -> 'undefined variable: y' (inline form works). Four sites: grad_calls_basic.rs:78 (cross_entropy targets), grad_calls_shape.rs:72 (rotate k), grad_calls_pow.rs:36 (pow exponent), grad_calls_transpose.rs:33 (transpose_axes axes). Route all through ONE shared helper using the traced-scope overlay (grad_const::eval_index_expr pattern). TDD: each of the four with the value bound to a u: parameter. (2) DOC: docs/lang-reference.md causal_attention row says 'Tape-lowered for heads=1' -- stale; multi-head trains (verified: causal_attention(16,4,7) + adam, loss 119.7 -> 2.6 in 30 steps). Fix the row; add a multi-head adam regression test if none pins it. Note attention has no biases (only Wq/Wk/Wv/Wo). (3) CONSISTENCY: comparison BinOps (<, >, ==, ...) are rejected inside grad while lt()/gt()/eq() builtins already work as stop-gradient mask constants (F5). Make the operator forms behave like the builtins (constant 0/1 mask leaf), or at minimum make the error suggest lt()/gt()/eq(); decide + document in lang-reference grad section. TDD: causal mask built with c < r + 1 inside the loss. Out of scope: gen_state KV caching for hand-written u: forwards (design-sized; recompute is fine at 16 tokens) -- note it in docs/future-sagas-queue.md. Answer microgpt-mlpl in docs/q-and-a.md (their global-variable workaround can go; multi-head works; use lt() today).