Shipped --persist <path> flag for mlpl-serve session state across restart. New crates/mlpl-serve/src/persist.rs writes a versioned JSON file with slim session subset (token + timestamps + Environment.vars + params); eval_handler flushes after every successful eval; build_app_with_peers_cors loads at startup. Slim subset only (plain numeric vars); models/tokenizers/experiments survival is a follow-up. JSON-on-disk chosen over rusqlite to avoid the libsqlite3-sys build. 4 new persistence tests cover restart round-trip, no-persist legacy path, token survival, malformed file ignored. sw-checklist 138 held.