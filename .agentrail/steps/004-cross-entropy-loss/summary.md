Step 004: cross_entropy(logits, targets) fused, stable, differentiable loss. New runtime builtin + autograd tape primitive (CrossEntropy NodeKind) with analytic (softmax - one_hot)/N gradient; shared validation helper bridges forward-only eval path and grad tape path. 8 tests covering numeric match, stability at 1e3, rank-2/rank-3 equivalence, shape/index error paths, per-row grad invariant, finite-diff gradcheck, and an end-to-end adam smoke test.