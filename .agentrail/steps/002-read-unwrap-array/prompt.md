Fix the read_bytes(...)? (and any ?-unwrapped Result whose payload is an array) so the unwrapped value flows into DenseArray-position ops without a CVal-vs-DenseArray mismatch. Root cause (from the code map): Emit::Check lowers to *payload which is typed CVal, but DenseArray-consuming emitters (lower.rs BinOp operands, fncall.rs Emit::UnaryRt/Iota/Reshape/ReduceAxis, Emit::Cmp operands) pass &(#a) / call .data() assuming DenseArray. The clean fix is a lower_darr(ctx, expr) helper mirroring lower_cval: if produces_cval(expr) -> (lower_expr).arr().clone() (CVal::arr() returns &DenseArray, value.rs:34), else lower_expr; then use lower_darr at every DenseArray-position site. TDD RED first: a gated MLPL_BUILD_TESTS=1 e2e like a wc-newline-count shape -- def u:count(p) { b = read_bytes(p)? ; reduce_add(eq(b, 10)) } over a temp file with known newline bytes, asserting the count. Also cover an arithmetic-position ?-result. Hold or lower sw-checklist. Commit source before agentrail complete.