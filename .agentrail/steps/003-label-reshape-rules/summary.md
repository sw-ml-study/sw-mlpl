Saga 11.5 step 003 shipped. transpose(x) reverses labels in lock-step with dim reversal (three-line change in ops.rs, no new fns). reshape(x) continues clearing labels and is now locked in by a labeled-source test. New reshape_labeled(x, [dims], [labels]) builtin special-cased inline in eval_expr; lowers to reshape()?.with_labels()?, no new DenseArray method needed. 15 new tests green. Quality gates clean, sw-checklist unchanged from baseline.