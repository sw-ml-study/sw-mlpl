F2: user-defined functions (u:name) now differentiate inside grad/adam/train. Added grad_user module that inlines a u: call's body onto the same reverse-mode tape (params bind to traced args; body walked stmt-by-stmt; global params stay differentiable via scope layering; MAX_GRAD_FN_DEPTH guard). Wired an additive '_ if name.starts_with("u:")' arm ahead of the existing reject in eval_tensor_fncall -- backward-compatible, no builtin or prior grad expression changes. Fixed the lookup to use canonical env.get_fn(name) with the full u:-prefixed key. 3 new tests + 19/19 grad_builtin_tests green; smoke-tested in fresh release binary (grad(u:loss(W),W) => [4,6] correct). lang-reference Autograd note+example added. Rebuilt release+debug mlpl-repl and mlpl-build. Pushed.