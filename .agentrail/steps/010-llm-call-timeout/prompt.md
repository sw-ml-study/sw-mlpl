User-reported :ask failure: llm_call to Ollama timed out reading the response (ureq agent overall timeout 120s with stream:false -- a model (re)load plus a long generation legitimately exceeds it). Split the transport timeouts: short connect timeout (fail fast when Ollama is down) + long response window (generation is slow by nature). Rebuild + restart the 6464 server; verify a real :ask round-trip.