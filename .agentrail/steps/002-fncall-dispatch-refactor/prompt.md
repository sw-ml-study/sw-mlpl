Refactor lower_fncall's growing dispatch match into a chain-of-responsibility / handler-registry BEFORE byte-IO adds more builtins: a config table describing each builtin (name, arity or arity-set, lowering handler fn), a builder that assembles the registry, a dispatcher that routes name+arity to a handler, and small per-group handler fns. Adding a builtin should become adding a config/handler entry, not fattening the match. Preserve every existing lowering behavior; keep all tests green. Target: retire the fncall function-LOC / module-fn-count warnings. Then subsequent byte-IO steps (read_bytes/file_size, write/append_bytes, text ops) register handlers.