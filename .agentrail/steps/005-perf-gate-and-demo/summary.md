Step 5 part 1 shipped: MLX Tiny LM + Engram demo (native script + web entry + capability gating + pages deploy) AND the flat-loss-curve fix it surfaced -- optimizers now return the step loss (eval_grads_batch/grads_all return (loss, grads); one scalar download per resident step per the E4 sync contract). Healed: both CPU Engram demos (flat since ship) and the MLX LoRA/tic-tac-toe regressions from the gpu_step demotion. Local serve rebuilt+restarted for the user. REMAINING step-5 scope (engram bench workload + benchmarks.md E5 table + wiki notes) split to a follow-up step so a live user-reported glossary UX issue can be fixed next.