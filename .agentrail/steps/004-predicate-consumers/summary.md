Shipped EvalError::TypeMismatch{op, expected, actual, hint} variant + type_errors.rs module (3 fns: arg_tag + check_logit_consumer + check_loss_consumer) + 5 tutoring hint constants. Hooked predicates at 3 dispatch sites: eval.rs cross_entropy and adam/momentum_sgd special-cases, eval_ops.rs eval_fncall for sample/top_k. cross_entropy / sample / top_k reject Probability/LogProbability with hint pointing at raw Logit or nll; adam/momentum_sgd reject Probability/Logit with hint pointing at the missing loss function. Inline FnCalls (cross_entropy(softmax(L, 1), T)) handled via auto_tag::for_assign delegation in arg_tag, so the canonical double-softmax bug surfaces at the inline call site. Untagged arguments pass every predicate (gradual-typing additivity). 9 tests pass.