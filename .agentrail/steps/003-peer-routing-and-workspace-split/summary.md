Saga R1 step 003 (scoped down): shipped Display impl for Expr (foundation for future block forwarding; 14 round-trip tests); peers module + --peer flag + AppState integration in mlpl-serve (4 production fns + 10 peers_tests; refuses non-loopback URLs without --insecure-peers, refuses duplicate device names, builds blocking reqwest clients with 600s timeout); workspace split moved crates/mlpl-mlx-serve to services/mlpl-mlx-serve with its own [workspace] declaration (independent target tree, .gitignore'd). 1037/0/3 main + 18 services tests, sw-checklist baseline 139 fails held. The actual block forwarding through eval_device + to_device('cpu', x) materialization deferred to a new inserted step 004 (block-forwarding-and-to-device) before release; that step requires a PeerDispatcher trait on Environment + scope analysis for free-name bindings -- substantial work better suited to its own focused step.