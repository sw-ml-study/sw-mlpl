Saga 11.5 step 004 shipped. Elementwise +/-/*/div ops now propagate labels through a new merge_labels helper in mlpl-array/ops.rs: matching labels pass through, one-labeled-one-unlabeled adopts the labeled side, scalar broadcast takes the non-scalar side's labels unconditionally, two-labeled disagreement returns ArrayError::LabelMismatch. relabel(x, [...]) builtin added as an escape hatch sharing the label handler in eval.rs. 15 new tests green (8 array + 7 eval). Quality gates clean; sw-checklist unchanged from baseline (merge_labels extraction kept apply_binop under 50-line LOC budget).