Dataflow renderer Phase 2: added optional groups/node-highlight/edge-widths/edge-highlight channels (groups.rs band renderer, extended Graph+Dataflow, validate_channels) and fixed the download-button bug by emitting explicit width/height on the <svg> tag so it renders inline. fncall_dataflow extracts the 4 new record fields; mlpl-viz re-exports Dataflow; Dataflow Diagram demo showcases the channels. 4 crate tests + eval dataflow_tests + demo-smoke pass; fresh release/debug mlpl-repl+mlpl-build rebuilt and verified (width=576 height=88, 2 bands, hl edge). sw-checklist exception (44 failed held; 535->538 warnings, structural). Pages deploy next.