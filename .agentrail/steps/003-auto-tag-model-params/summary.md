Closed model-aware producer-tagging gap. (1) Param tags in model_dispatch.rs: linear -> Weight{linear_<id>, W} + Bias{linear_<id>}; embed -> Weight{embed_<id>, table}; attention -> Weight{attention_<id>, W_q|W_k|W_v|W_o}. Tags follow param NAMES so :describe-by-walk-spec works in step 005. (2) apply(model, X) result tags via new apply_tag + tail_tag + activation_tail_tag helpers in auto_tag.rs. Walks Chain/Residual to a leaf: Linear/LinearLora -> Logit; Activation(Softmax) -> Probability; Activation(Tanh|Relu) -> Activation{model-name, kind}; Embedding/Attention/RmsNorm tails -> None. for_assign now takes &Environment so apply_tag can env.get_model. auto_tag.rs at 6 fns under 7-fn budget. LinearLora adapter tagging via clone_model is intentionally deferred -- the structural-tail rule still handles LinearLora via the same Logit branch as Linear.