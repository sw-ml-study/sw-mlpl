Saga 14 step 003 shipped. mlpl-mlx now has reduce_mul/mean/argmax (Option<usize> axis) + softmax/log_softmax (usize axis) + cross_entropy(logits, targets) routed through mlx-rs's prod/mean/argmax/softmax_axis/nn::log_softmax/logsumexp_axis kernels, with parity wrappers in mlpl-rt so codegen can swap runtimes without source changes. Validation runs Rust-side (axis bounds, integer targets, target range), labels propagate per Saga 11.5 (reduced axis drops, softmax preserves, CE returns unlabeled scalar). cross_entropy double-round-trips fp32 (LSE on MLX, gather+mean on Rust) so its parity tolerance is 2 * FP32_TOL. Both crates land a new reductions.rs sibling and now sit at the sw-checklist 7-modules-per-crate ceiling. Closing test runs a structurally-identical Tiny LM forward (embed/positional/rms_norm/causal_attention/residual/rms_norm/linear/relu/linear/residual/rms_norm/linear/CE) on both runtimes via a function-pointer Backend table and asserts loss agrees within 5e-4. 28 MLX-gated tests pass, default builds stay green on non-Apple hosts, full /mw-cp checkpoint clean (cargo test workspace, clippy --all-features, fmt, sw-checklist add-no-new-FAILs). Commit f5ba9b8 pushed.