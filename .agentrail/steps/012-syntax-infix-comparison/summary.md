Shipped Tier-1 infix comparison operators < > <= >= == != on both interpreter and compiler (parity). Lexer longest-match tokens, BinOpKind variants at precedence 0 (below arithmetic), eval_binop->gt/lt/ge/le/eq/ne (added ge/le/ne runtime builtins), compiler closures + Cmp ge/le/ne, autograd rejects comparisons in grad. Retired the 3 FAILs introduced (array_parser.rs split, comparison + tensor_binop + binop_for_token extractions) -> HOLD 44 fails, +1 warning (exception). Verified interp [1 0 1 1 1 1 -5], compiled score 6, masks, tictactoe-demo trains. Docs in lang-reference. Binaries rebuilt.