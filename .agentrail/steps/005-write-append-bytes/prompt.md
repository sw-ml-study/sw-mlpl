Lower write_bytes/2 and append_bytes/2. Add them to the compiler runtime reusing the io.rs array_to_bytes validator (loud-reject) and the fsio contained() sandbox check; write_bytes truncates+writes, append_bytes create(true).append(true); both return ok(count)/err, propagating I/O errors (interpreter fs_bytes.rs/fs_append.rs parity). Register in the fncall registry (a WriteBytes/AppendBytes emission: path in CVal position + bytes as CVal). CONSIDER carving fsio into a dedicated mlpl-rt-fsio crate now (2 modules: sandbox + read/write) to give the fs functions their own budget and retire the +3 structural warnings from step 4 (as mlpl-runtime-bits was carved out). TDD + gated e2e (write bytes to a temp file, read them back; out-of-range byte errs). Hold sw-checklist (the carve-out should let you).