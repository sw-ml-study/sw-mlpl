localStorage helper (onboarding_storage.rs: splash_dismissed + last_seen_version + pure predicates), data-tour-target attrs on 6 UI elements, decomposed components.rs (6 focused files: header, mode_bar, input_row, footer, doc_dialog, welcome) and handlers.rs (3 focused files: submit pipeline, demo runner, input events) into single-concern modules. Also added Storage + DomRect web-sys features, trimmed sandbox.rs check_components.