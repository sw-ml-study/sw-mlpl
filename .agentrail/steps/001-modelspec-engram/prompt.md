E2 step 1: ModelSpec::Engram variant (mlpl-eval-core; serde/save-load compatible) + engram(hidden, ngrams, heads, slots, head_dim, seed) constructor builtin in the mlpl-eval-models family: validates via EngramSpec, creates env params (memory table [rows, head_dim] zeros; W_v [retrieved, hidden] small-scaled randn; b_v zeros; W_g [2*hidden, hidden] small; b_g at -2.0 for a near-closed gate), tags them, returns the spec. :describe on an engram model prints the accounting (mlpl-engram-core numbers). TDD; budgets per current norms.