Step 4 of cuda-foundation: create mlpl-cuda-forward (embed via one-hot matmul, rms_norm = x/sqrt(mean(x^2)+1e-8) GAMMA-FREE matching the CPU/demo, causal_attention single-head h=1 scale=1/sqrt(d_k), cross_entropy = logsumexp - picked logit mean over rows) and mlpl-cuda-model (DemoWeights + demo_forward: embed -> rms_norm -> causal_attention -> residual -> rms_norm -> lora head (x@w + (alpha/rank)*(x@a@b) + b) -> cross_entropy). These must match the CPU apply_model EXACTLY (see saga-local-gpu-agentic.md step 010 findings: gamma-free rmsnorm eps=1e-8, ONLY the head is LoRA-adapted, head carries a bias). TDD: each primitive parity-tested vs a hand-computed reference AND vs the CPU mlpl-eval primitives within fp32 tol; a gated test trains the adapters via candle autodiff+Adam and asserts cross-entropy drops (gradients flow through the whole assembled model on the GPU). Place crates to respect the <=4-modules/<=4-crates budgets (own component dir if needed, like mlx-model). candle autodiff thread-safety: if candle's grad state is global/non-reentrant like MLX's value_and_grad, serialize grad tests on a crate-local lock. DISK: scoped, clean -p after.