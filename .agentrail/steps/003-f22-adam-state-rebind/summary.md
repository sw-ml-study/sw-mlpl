F22 (important): added reset_optimizer() builtin (drops all Adam/momentum buffers + step counters for a clean slate between training runs in one process) and made rebinding a model name clear that binding's stale per-parameter moments (OptimizerState::clear/clear_param; clear_param called from the model-assign path). Fixes the silent 'second variant trains with the first's moments' footgun. Verified: reset restores fresh first-step behavior (isolated fresh == after-reset, true); Rust tests confirm buffers cleared on reset and on rebind. Note: adam's step counter is per-optimizer (shared), so reset_optimizer is the full reset between runs. Tests reset_optimizer_clears_all_state + rebinding_a_model_clears_its_stale_moments; regression train_loop 11/11, first_train_resident 3/3. Fixed a must_use-attribute misplacement caught by clippy (optim_state kept its attribute). sw-checklist 38 failed (held), 567 warnings (+1 exception: OptimizerState methods). Rebuilt repl/build/serve. Pushed.