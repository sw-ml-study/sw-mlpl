to_json(value) -> Str: general deterministic JSON encoder (json_encode.rs), the encode half of parse_json. records->objects with sorted keys, rank-1 arrays->flat lists, rank>=2->nested by shape, strings JSON-escaped exact, numbers bare-int/float, ok(x)->{ok:true,value:x} err(e)->{ok:false,error:e}; model/tokenizer/gen-state/partial/refs/device-tensor error loudly. Move push_str_json/push_number into json_encode as shared helpers; event_json delegates. Add to_json to fncall_json dispatch; catalog+lang-ref+glossary rows; help_completeness green. TDD (to_json_tests): scalars/vectors/strings/records sorted, ok/err, round-trip via parse_json, unicode, non-serializable errors. Gates; commit; push.