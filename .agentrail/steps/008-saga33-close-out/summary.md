Set up components/mlpl-session/ nested cargo workspace and extracted first sub-crate (mlpl-models-tape). Proves the cross-workspace plumbing: mlpl-eval depends via relative path; sw-checklist scans the nested workspace; build/test/clippy all green. The new crate has 7 modules (apply, attention, attention_heads, layers, linear, error, lib) each <=4 fns. Established the loose-coupling boundary: TapeError has its own variant set; mlpl-eval/src/error_from_tape.rs translates via a single From impl. Other downstream crates can define their own conversions without ever importing mlpl-eval. Decomposed linear_lora_tape (was 36-LOC WARN) into lora_adapter + bias_broadcast helpers, and attention_multi_head_context (was 45-LOC WARN) into inline scale/mask + per-head map closure.