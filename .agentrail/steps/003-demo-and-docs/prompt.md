Step 3 per saga plan: (1) General Programming web demo NEXT TO the Life demos (same group): Sudoku backtracking packaged from ../demo-algorithms/demo/backtracking (INLINE its includes -- the editor rejects include): show the initial incomplete board, sampled intermediate wrong boards, and the final solved board via blocked disp + transpose_axes re-ranking; (2) docs: lang-reference rows (transpose_axes; disp rank-3/4 note), glossary entries (+README pin), usage note; (3) rebuild repl+serve+pages, restart local server, ts-suffixed review URLs; (4) queue note: future ML demo = trained Sudoku solver (BDH/HRM/TRM). Close saga with --done after gates.