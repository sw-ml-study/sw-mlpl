Added at(v, i) vector-indexing builtin (../emufpga ask 3) on interpreter + compiler: element i of a rank-1 array as a scalar (== take(v,0,i)), via one mlpl-runtime-array dispatch::at helper delegating to take axis 0, lowered through the ArrayCall path. Collapses order[i]'s 4-nested workaround; pairs with length-1 broadcast. Interpreter + gated compiled e2e + dispatch-coverage + lang-reference. HOLD 44/532. Binaries rebuilt. Of emufpga's 4 asks: broadcast+disp(docs)+at shipped; dataflow SVG renderer remains (saga-sized).