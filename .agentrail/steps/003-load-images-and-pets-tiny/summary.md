Saga 29 step 003: load_images(dir, [H, W]) builtin + load_preloaded("pets_tiny") Record fixture. PNG+JPEG decode via png 0.17 / jpeg-decoder 0.3 (NOT image-rs) gated on image-io feature; WASM stub points at pets_tiny. Bilinear resize + u8->f64 normalize in [-1, 1]. pets_tiny.bin (2.35MB) committed under crates/mlpl-eval/data/, built offline from Oxford-IIIT Pet via build_pets_tiny example (gitignored data/oxford-iiit-pet/). 19 new tests (9 load_images + 10 pets_tiny). load_preloaded("pets_tiny") returns Record{X:[200,3,64,64], Y:[200], names:StrList} -- the step 001 (Record) + step 002 (StrList) prereqs are now consumed end-to-end. sw-checklist +1 from image_io.rs hitting 10 fns, same exception pattern as steps 001/002.