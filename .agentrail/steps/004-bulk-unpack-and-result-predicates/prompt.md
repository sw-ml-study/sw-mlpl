Downstream asks (user-approved 2026-09-22). (A) R11 from ../reasoning-from-scratch (blocking; demo-extensions E3 depends): bulk unpack(bytes, dtype) -> flat 1-D array, the inverse of pack, for every dtype reinterpret accepts (u8..f64, bf16, f16). One native loop over mlpl_bytes::read_le (keeps subnormals/inf/NaN); error when byte length is not a whole number of dtype values; dtype may default to the buffer's own dtype from reinterpret if 1-arg form is cheap. Acceptance: bf16 bytes for [1,-1,2,0.5,0,50] (build via raw u16 bit patterns + reinterpret, since pack to bf16 is decode-only) unpack exactly; a large (e.g. 10M-value) buffer decodes in well under a second in release (timing sanity test). Note memory: f64 arrays = 8 bytes/value. Put it in fncall_bytes.rs (check fn count first; split if >=5). Update future-sagas-queue typed-packed-bytes tail (unpack shipped; BE readers remain). (B) R1 from ../demo-extensions: add total predicate is_result(x) -> 1/0 (never raises); is_ok/is_err unchanged. Document the extension success/err asymmetry + is_result idiom in lang-reference. (C) R2: get_error on a string payload keeps its boundary; make the error message name err_message(r) as the way to read a string error, and document the contract. TDD all three. Answer both repos in docs/q-and-a.md (reasoning-from-scratch R11; demo-extensions R1/R2 + unpack for E3). Rebuild release+debug mlpl-repl.