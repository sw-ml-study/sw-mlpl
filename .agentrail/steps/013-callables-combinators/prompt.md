Per design rider: map_ok(f, r) / and_then(f, r) / or_else(f, r) builtins over Results + function references (function-first order per resolution 3), TDD; docs: usage (call + refs + combinators), lang-reference (three-kinds table update: :u:name now exists; call row; combinator rows), glossary entries (call, user-fn reference, combinators; count pin), README; pages+server rebuild; check-capabilities re-run; q-and-a update.