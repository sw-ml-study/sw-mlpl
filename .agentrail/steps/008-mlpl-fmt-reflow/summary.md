Added reflow to mlpl-format-buffer + scripts/mlpl-fmt.sh: explodes one-line MLPL defs/blocks/statements onto their own lines (newline after block-{, before block-}, after ;), keeping records/strings/comments inline, then indents. Provably whitespace-only (verified on real audio-spectrum applet: 7->100 lines, strip-all-whitespace IDENTICAL before/after). Script now calls mlpl-format-buffer instead of indent-only. Idempotent + --check verified.