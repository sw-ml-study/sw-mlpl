Per docs/kv-cache-design.md: docs/benchmarks.md wall-clock table (cached gen loop vs recompute at T=32/128/512 using clock_ms) + a 'KV Cache' web demo -- same prompt generated both ways with ids compared equal (bit-identical proof), gen_stats growth shown, and a per-step attended-positions cost curve (flat cached vs linearly growing recompute; deterministic so it renders in-browser). Rebuild pages+deploy. Gates; commit; push.