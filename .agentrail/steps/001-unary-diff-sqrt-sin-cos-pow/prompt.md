RS1: add tape backward rules for sqrt, sin, cos, pow (exp/log/sigmoid already exist in mlpl-autograd-tape UnaryKind). For each: add the UnaryKind variant, forward fn, and derivative in components/autograd/crates/mlpl-autograd-tape/src/ops.rs, wire the eval builtin to the tape op in the grad path, and TDD with a finite-difference gradcheck per op plus a check that eager (non-grad) values are unchanged. pow needs base and (constant) exponent handling. Hold sw-checklist at baseline; rebuild repl/build (release+debug) after.