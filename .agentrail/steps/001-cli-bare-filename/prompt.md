A1: fix mlpl-repl <bare-filename.mlpl> exiting 1. In components/cli/crates/mlpl-cli/src/include_script.rs, load_script computes root_dir via path.parent(); for a bare filename Path::parent() returns Some("") (not None), so FsProvider::new("") fails with a misleading --source-dir error. Fix: treat an empty parent as absent -- .or_else(|| path.parent().filter(|p| !p.as_os_str().is_empty()).map(Path::to_path_buf)). Add a regression test for the bare-filename case. Acceptance: mlpl-repl mini.mlpl -> Ok(1) exit 0 from the file's own dir; ./mini.mlpl, sub/mini.mlpl, /abs/mini.mlpl unchanged. Scope tests to mlpl-cli. Hold sw-checklist.