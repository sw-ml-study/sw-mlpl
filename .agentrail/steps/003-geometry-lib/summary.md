Step 3 geometry-lib: examples/viz/layout.mlpl -- pure-MLPL geometry library turning a columnar layout record into 3D block-map geometry. u:columns3 ([M,3] stacker), u:block_xyz (FlashViz 16x16-layer cell layout x=b%16/z=(b/16)%16/y=b/256), u:first_blocks/u:block_counts, u:region_geometry -> {centers[N,3],sizes[N,3],colors[N,4],ids[N]} tower overview (colors via select_rows), u:region_ids (explicit region_id else range fallback). System-agnostic (SWTOS/MLOS/MesaOS). 6 TDD tests via include_str! of the real .mlpl. Docstrings on every def; mlpl-fmt clean; eval clippy/fmt green; 38/558 unchanged. Also folded the demo-extensions consumer's contract additions into the doc+fixture (schema, provenance, explicit stable region_id, relationship edge table) validated parse_json-compatible, and recorded MLOS+MesaOS as producers.