Added 3 rendered equations to the pre-existing 'CNN (simple)' demo (conv sum, relu(z)=max(0,z), 2-D max pool) via svg(text,'equation'), so it shows the math behind its conv2d/relu/pool2d pipeline like the other CNN demos. demo-smoke + def gate green; no demo-count change. Deployed DEV/nightly (bundle rebuilt, commits 31dc40c2+e9abb3de+32a2b894); no version bump, no stable. Also DIAGNOSED a :list bug the user reported: env_user_fns_render::list_fn returns a def's stored raw source VERBATIM (one line) when present, skipping indent_source -- violating the trait's documented 're-indented' contract; fix is to run indent_source on the stored source too (queued next).