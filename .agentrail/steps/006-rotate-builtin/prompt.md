Game of Life saga step 1 (user-prioritized ahead of remaining tech-debt splits): add the rotate(x, k, axis) builtin -- cyclic shift along an axis, negative k allowed, scalar or per-call k. TDD: mlpl-eval tests first (vector rotate, matrix rotate both axes, negative k, k larger than axis length wraps, identity k=0). Implement the array op in mlpl-array (pure permutation), register in the runtime dispatch + BUILTIN_GROUPS inspect table, autograd backward = rotate by -k (gradcheck on small fixture). Keep functions <=25 LOC post-rustfmt; pair warning retirements or document exception. See docs/future-saga-game-of-life.md.