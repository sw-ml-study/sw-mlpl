rotate(x, k, axis) shipped at every layer: RotateExt array op (9 tests), runtime dispatch with signed k, NodeKind::Rotate + Tensor::rotate + finite-difference gradchecks, eval grad path, BUILTIN_GROUPS/:help/lang-reference surfacing, pages rebuilt. Blinker Life step passes through the full pipeline (the saga acceptance engine). Restructured backward propagate into a shape sub-dispatcher to stay under the 50-line FAIL bar. 4/301 -> 4/300.