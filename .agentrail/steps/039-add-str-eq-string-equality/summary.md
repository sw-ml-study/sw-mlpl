Added str_eq(a,b)->1|0 whole-string equality (upstream-asks #23, the blocker). eq rejects strings; str_find is a substring trap. Implemented in interpreter (fncall_string_ops eval_str_eq, no coercion) AND lowered to compiler (2-arg StrOp registry + CVal::Str == emit) for a clean boundary; registered in :help catalog, lang-reference, glossary, and the dispatch-coverage StrEq variant. Verified e2e (str_eq('semigroup','group')=0, the trap avoided). 7 interpreter tests + dispatch/help/glossary gates pass. Fresh release+debug binaries; dev pages deployed. Stable channel left pinned (user cuts releases). sw-checklist 41 failed (no new FAIL), 541->542 warnings (exception, +1 for 5th fn in fncall_string_ops).