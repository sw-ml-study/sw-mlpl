Fix two dataflow rendering defects the ../emufpga playground lessons exposed: (1) arrowheads scaled with stroke-width (default markerUnits=strokeWidth), so log-scale width-9 edges grew ~80px triangles that swallowed labels/boxes -- set markerUnits=userSpaceOnUse for a constant-size arrowhead. (2) node labels overflowed the fixed 128px box ('sequential store: 269 MB' clipped) -- fit each box to its label (monospace estimate, min NODE_W) with variable-width, centered columns in layout. TDD.