Make reduce/reduce_add differentiable in grad() (both spellings). Full reduce (no axis) maps to existing SumAll; axis/multi-axis reduce(:add,x,[2,3,4]) / reduce_add(x,axis) gets a new ReduceSum tape node whose backward BROADCASTS the upstream grad back over the reduced axes into orig_shape. grad dispatch for reduce/reduce_add (:add only differentiable; error for :mul/:min/:max). Restore reduce_add to the grad() doc list. TDD gradcheck. Makes reduce(:add, k*windows(...),[2,3,4]) trainable. Same autograd-ceiling exception. Then CNN training demo(s).