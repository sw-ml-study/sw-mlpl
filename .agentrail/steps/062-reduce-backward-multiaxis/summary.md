reduce/reduce_add now differentiable in grad() AND fixed C6 (downstream broadcast-backward panic) -- together they make BOTH conv spellings trainable. reduce: ReduceSum tape node (axis/multi-axis backward broadcasts grad over reduced axes; full reduce maps to SumAll; :add only). C6: the tape's Binary backward only un-broadcast scalars -> rank-broadcast operand indexed OOB (ops.rs panic the downstream filed); now broadcasts each operand to output shape via apply_binop before indexing, then unbroadcast reduces to parent. Verified: reduce_sum_gradcheck 2, grad_builtin_tests 15 (incl full elementwise-conv loss composing windows+reduce+broadcast), autograd 40, full eval 1401, zero regressions. Binaries rebuilt release+debug. Docs restore reduce_add. Commit 4a9c2ac3. sw-checklist 43 HELD (exception; ops.rs backward kept <50 LOC via tuple-match). Downstream C6 CLOSED. Minor follow-up noted: flatten unsupported in grad() (elementwise path doesn't need it; im2col can use reshape). No version bump, no stable.