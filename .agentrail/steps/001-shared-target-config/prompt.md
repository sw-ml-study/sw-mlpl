Create .cargo/config.toml at repo root with [build] target-dir = "target" (relative path). Cargo discovers .cargo/config.toml by walking up from the invocation directory, so any cargo command from anywhere in the tree resolves to the same shared target. Verify: 1) cargo check from sw-mlpl/ root; 2) cd components/mlpl-session && cargo check; 3) cd services/mlpl-mlx-serve && cargo check. Each should reuse the same target/ tree (check du -sh target/ doesn't multiply). Commit and push.