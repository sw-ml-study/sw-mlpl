Step 6 of cuda-foundation: make this Arch box a connect-mode CUDA peer and let the UI gate by the peer's real device. (1) Create mlpl-cuda-serve (analog of mlpl-mlx-serve): a server-side peer dispatcher that runs device('cuda') blocks through the cuda path, wired so 'cargo run -p mlpl-serve --features cuda' on Linux exposes it. (2) Add GET /api/devices to mlpl-serve returning the devices the running server actually offers (cpu always; cuda if built with --features cuda on Linux with a GPU present; mlx if built with --features mlx on Apple). (3) Web/connect client: on connect, probe /api/devices and gate demos -- a Cuda-tier demo becomes runnable only when the connected peer reports cuda; an Mlx-tier demo only when it reports mlx; otherwise keep the visible-but-not-runnable affordance. This realizes 'connect the UI to a server on a Mac (MLX) and/or an Arch Linux box (CUDA)'. TDD: mlpl-serve test asserts /api/devices reflects compiled features; a web-demos test asserts gating keys off the probed device set, not a static guess. Update docs/saga-cuda-foundation.md + saga-local-gpu-agentic.md device-tier table to note CUDA is now live on a Linux peer. This closes the cuda-foundation vertical slice; remaining MLX demos are a follow-up saga. DISK: scope to serve+web+cuda crates.