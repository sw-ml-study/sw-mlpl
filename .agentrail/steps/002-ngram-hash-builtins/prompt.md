Saga E1 step 2: surface the primitives in the language. Builtins: ngram_hash(ids, orders, heads, slots, seed) -> rank-3 [T, order, head] local slot indices (CPU implementation delegates to the mlpl-engram-core reference -- exactness by construction; validates integral ids and the 2^21-1 bound with clear errors), and gather_rows(table, indices) -> general flattened row gather (indices shape + [dim]). New fncall_engram dispatch module in the hub; registered in BUILTIN_GROUPS (+readme-count pins updated), glossary + lang-reference entries. TDD: eval tests pin ngram_hash output to golden_fixture_v1; gather shape/value/error tests. wasm-safe (engram-core is dependency-free).