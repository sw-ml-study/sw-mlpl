global_set(name, value): validate name (expunge's well_formed), bind NOW (kind-routed like Assign), and when call_depth > 0 record into env.global_writes (outside the snapshot); the frame helper replays recorded writes after each restore and clears the log at depth zero -- so writes survive nested frames but stale replays cannot clobber later top-level rebinds. print is already variadic (verified). TDD with their reporter-counting pattern. Docs rows + usage note. Gates; commit before complete; push.