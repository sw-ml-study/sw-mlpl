E4 step 2: the device-resident handle. New wasm-clean crate (suggest components/native-rt/crates/mlpl-tensor-handle or a types-workspace home -- it must be dependable from mlpl-autograd WITHOUT linking mlx-rs): pub enum TensorHandle { Cpu(DenseArray), Dev(Arc<dyn DeviceArray>) } plus trait DeviceArray: Debug + Send + Sync? (NOTE: mlx_rs::Array thread-safety -- we serialize via mlx_op_lock, so consider Rc-like semantics or a Send+Sync wrapper asserting the lock discipline) with shape()/elem_count()/to_dense()/as_any(); a DeviceOps registry (OnceLock, mirroring register_gpu_step) exposing upload(DenseArray)->handle and the tape op surface both forward AND backward kernels need: matmul, add/sub/mul/div/neg, exp/log/tanh/sigmoid/relu, softmax(axis)/log_softmax, cross_entropy fused fwd, mean/sum reductions (incl. axis + keepdim variants backward needs), transpose/reshape, scale-by-scalar, broadcast helpers. TensorHandle convenience methods route Cpu arms to the existing DenseArray ops and Dev arms to the registry (error if no backend registered). MLX implementation in mlpl-mlx-rt: MlxBuf(mlx_rs::Array) implementing DeviceArray; ops stay LAZY (no .eval() inside ops -- eval only in to_dense/download), all under mlx_op_lock; register_device_ops() for binaries/tests. TDD: parity tests in mlpl-mlx-rt (each op handle-vs-DenseArray within FP32_TOL, chained op sequences with ONE download at the end, lazy semantics verified), handle unit tests in the new crate compiled on all targets (Dev arm inert without backend -- clear error). Linux/wasm cleanliness: the new crate must not depend on mlx-rs; check wasm32 target builds if the wasm component pulls it transitively. Scoped gates per touched workspace; sw-checklist ratchet applies.