Compile-to-rust step 008 shipped under agentrail's slot 7 (step-order swap documented in commit: saga init mis-ordered the slots, execution follows the documented 001->002->003->004->005->006->008->007 order, so this slot holds the build-subcommand work and the next slot will hold release). New binary app apps/mlpl-build/: reads .mlpl source, eagerly lex+parse+lower for fast-fail errors with a stable 'mlpl-build:' prefix, writes a temp cargo project wrapping the source in mlpl::mlpl! { ... }, shells to cargo build --release, and copies the output. Forwards --target to cargo for cross-compile. 3 gated e2e tests (MLPL_BUILD_TESTS=1): native binary output matches, parse error fails fast without rustc cascade, wasm target produces valid WASM bytes (auto-skips if target not installed). args.rs module split keeps main.rs under the 7-fn budget. 620 tests total; sw-checklist FAIL set unchanged.