Implement file_metadata(path) in the interpreter. New module fs_meta.rs with a pure read_metadata helper + a thin eval shell (mirror fs_range.rs eval_file_size: resolve via env.fs_root + contained for symlink protection; err on no sandbox). Return ok({kind,size,modified_unix_ms}) where kind is file/dir/other, size is byte length, modified_unix_ms is the last-modified time as an exact Unix-millisecond integer (std metadata modified() -> duration_since(UNIX_EPOCH).as_millis()); err (never silent 0) when the platform has no mtime. NEVER substitute access/creation/local/now. Wire dispatch in fncall_fs.rs try_dispatch and mod fs_meta in lib.rs. TDD RED first: a temp file with a KNOWN set mtime returns that exact ms (wall-clock-independent, via std::fs::File::set_modified); a dir returns kind dir; missing file, symlink-escape, and unconfigured surface each err. Hold sw-checklist. Commit source before agentrail complete.