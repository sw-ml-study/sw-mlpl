Step 020 retired the long-standing fmt 83-LOC FAIL in mlpl-eval's error.rs. Decomposed into 3 per-arm helpers (fmt_simple, fmt_mismatch, fmt_record) in a new error_fmt.rs sibling, grouped by variant shape. fmt itself shrunk to a 3-line dispatcher using if-let fall-through. Each helper <=25 LOC = PASS. Pivoted from the queued env_*.rs bundling because bundling would have ADDED warns (per-file warns multiply) without retiring the saga's Crate-Module-Count FAIL.