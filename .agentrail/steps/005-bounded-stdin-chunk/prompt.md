Add read_stdin_chunk(max_bytes) -- the bounded, incremental raw-byte stdin source for demo-file-processing's bounded-stdin pipeline -- to BOTH the interpreter (mlpl-eval) and the compile-to-Rust path (mlpl-rt-value + lowering), with identical byte/EOF/validation/error semantics. Returns ok({bytes, eof}); eof=1 only on the terminal empty read; max_bytes validated (positive integer) before any stdin byte is consumed.