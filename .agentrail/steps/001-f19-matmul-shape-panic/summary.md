F19 (BUG): a matmul inner-dim mismatch inside grad panicked (Tensor::matmul .expect); now a clean error, the matmul analogue of F18. New pub check_matmul_compat (mlpl-array-ops-matmul) validates [m,k]@[k,n]/[k] without computing; grad's call_matmul and the linear layer's linear_tape pre-validate and map ArrayError->EvalError/TapeError. Verified in release binary: 'array error: shape mismatch: 8 vs 16 elements', no panic. Test grad_matmul_shape_mismatch_errors_cleanly_not_panic; regression grad 33/33, attention 8/8 (no valid matmul rejected). sw-checklist 38 failed (held), 566 warnings (+1 exception: validator in already-warning matmul.rs). Rebuilt repl/build. Pushed.