Step 5 of cuda-foundation (the vertical-slice finale): create mlpl-cuda-train (candle autodiff + an on-device Adam wrapper analog of MlxAdam, moments as candle Tensors) and wire it into mlpl-eval's eval_adam CUDA branch -- a new grad_optim_cuda.rs (analog of grad_optim_mlx.rs) that, when env.device()=='cuda' AND every non-frozen param is a LoRA adapter (__lora_A_/__lora_B_) AND the model matches the demo architecture (else fall back to CPU tape): extract base weights + adapters from the Environment as candle Tensors via dense_to_cuda, build x_onehot/y_onehot from token ids, run one loss-and-grads + Adam step per call (persist moments in env.optim_state like the CPU/MLX paths), write adapters back via cuda_to_dense_data + env.set. Gate mlpl-cuda-{train,model} behind mlpl-eval's 'cuda' feature so non-cuda/non-Linux builds are unaffected. Author demos/lora_finetune_cuda.mlpl (mirror lora_finetune_mlx.mlpl: device('cuda') wrapping the fine-tune block) AND the literate org doc examples/literate/cuda-lora-finetune.org (mirror mlx-lora-finetune.org), render to pages/literate/. Add a registry entry 'CUDA LoRA fine-tune' { requires_connect: true, device: Cuda } in mlpl-web-demos-types and its literate mapping. TDD/PARITY (the real test): assert the device('cuda') LoRA fine-tune loss curve matches the CPU path within fp32 tol (V=16,d=8,h=1,rank=2,3 steps) in a new cuda_lora_demo_tests.rs -- this runs on THIS GPU. DISK: scope to eval+cuda crates; clean after. Web demo source change -> rebuild pages per CLAUDE.md only if apps/mlpl-web changed.