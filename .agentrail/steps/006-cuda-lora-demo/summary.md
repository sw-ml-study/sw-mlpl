device('cuda') LoRA fine-tune runs forward+backward+Adam on the RTX 5060 Ti end-to-end, parity-tested vs CPU. mlpl-cuda-train candle kernel (loss_and_grads + stateless adam_update); eval_adam CUDA branch via grad_optim_cuda(+_step) reusing the shared demo-layout recognition; mlpl-eval+mlpl-repl cuda features. Artifacts: demos/lora_finetune_cuda.mlpl, 'CUDA LoRA fine-tune' web demo (catalog+capability+literate map), examples/literate/cuda-lora-finetune.org published on the GPU to pages/literate/cuda-lora-finetune.html (loss 0.855->0.211). PARITY: cuda_lora_demo_tests asserts the device('cuda') loss curve matches CPU within fp32 tol AND drops, on the GPU. clippy clean; sw-checklist baseline held (111/502). Deferred: WASM pages rebuild to step 7 (bundled with UI device-gating, avoids double build).