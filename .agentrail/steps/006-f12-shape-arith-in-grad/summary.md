F12: a param-value-independent subexpression (e.g. a shape-derived size reduce_mul(shape(...))) is now constant-folded inside grad instead of rejected. grad's FnCall path falls back to eager eval + constant leaf when differentiably_uses_param says the whole call is value-independent (a param counts only if it appears outside shape/rank/len/labels). reduce_mul(shape(...)) folds; reduce_mul(W) still errors (no silent gradient drop, D1 principle). Subsumes F5/F14. Refactor: moved the constant/stop-gradient/fold cluster + FnCall dispatch into new grad_const.rs, retiring the eval_tensor_expr Function-LOC FAIL the inline fold introduced (sw-checklist back to 38 failed; +1 warning for the new module -- exception, it retired a FAIL). Tests grad_folds_shape_derived_constant + grad_still_rejects_non_diff_reduction_over_a_param; 31/31. Verified in release binary. Rebuilt binaries. Pushed.