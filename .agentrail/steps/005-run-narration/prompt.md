User report: editor Run should show comment blocks demo-style (intro narration like 'What you are about to see', summary like 'What this showed'), not raw # lines in the prompt echo. Split each Run group into leading comment block + code: comments render as EntryKind::Narration prose (demo styling), code evaluates as the input line; trailing comment-only group (the summary) renders as narration without eval. group_statements emits the trailing comment-only group. Restyle docs/apl2-idioms.mlpl intro/summary to demo tone. TDD; rebuild pages; dispatch deploy (push triggers currently broken on GitHub's side).