Compile-to-rust step 001 shipped. New mlpl-rt crate at crates/mlpl-rt/: re-exports DenseArray, Shape, ArrayError, LabeledShape plus seven typed primitive functions (iota, shape, rank, reshape, transpose, reduce_add, reduce_add_axis). Strictly no dep on mlpl-eval/mlpl-runtime/mlpl-parser -- the crate is the runtime target, not a tool, so compiled MLPL binaries never ship a parser or interpreter. Label propagation from Saga 11.5 carries through for free (transpose swaps, reduce_axis drops reduced axis's label). 11 unit tests verify numeric output and label behavior. All quality gates clean; sw-checklist FAIL set unchanged.