Phase 1: mlpl-rt scaffolding. Create a new crate crates/mlpl-rt/ that compiled MLPL code will call into. Thin re-export wrappers around DenseArray, Shape, and LabeledShape from the existing mlpl-array and mlpl-core crates. No builtins yet -- just the value type, shape, labeled shape, and a handful of primitive ops chosen from the top of call_builtin: iota, shape, rank, reshape, transpose, reduce_add. The crate must compile standalone (no dep on mlpl-eval, mlpl-runtime, or mlpl-parser -- it is the runtime target, not a tool). TDD: unit tests that construct a DenseArray via mlpl-rt re-exports and call each primitive; match the same numeric output as mlpl-runtime's builtins. Update sw-checklist compliance. Do not add the codegen yet -- this step only creates the target surface.