Per docs/kv-cache-design.md: gen_clone(gs) (independent copy, returned as a GenState value to bind), gen_reset(gs) (drop cached rows back to the prompt by rebuilding + re-feeding), gen_stats(gs) ({tokens, layers, kv_rows, kv_values} record), and multi-row gen_append(gs, ids) accepting a rank-1 id vector (feed each in order -- the batched verification hook). New controls in a sibling module fncall_gen_controls.rs (fncall_gen.rs is at the fn ceiling). Catalog + lang-reference + glossary rows; help_completeness green. TDD (gen_controls_tests): clone independence, reset==fresh, stats accounting, multi-row append equals sequential singles. Gates; commit; push.