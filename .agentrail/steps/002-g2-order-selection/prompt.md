Implement grade_up/grade_down/compress per docs/data-forge-design.md in mlpl-runtime-array (watch module budgets: pick homes that avoid a crate-module FAIL), NAMES + dispatch + catalog + lang-reference + glossary (coverage test enforces) + apl2-parity-gap G2 status flip; TDD in builtin_eval_tests incl. stable-tie ordering and compress axis handling; scoped gates; pages rebuild rides with the demo step later.