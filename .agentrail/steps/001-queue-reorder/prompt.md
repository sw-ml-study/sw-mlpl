Rewrite docs/future-sagas-queue.md per user direction 2026-08-02: E5 engram-mlx stays next; MTP program (generation-state-kv-cache -> mtp-training -> mtp-self-speculation, CPU+MLX) promoted directly after E5; mHC-CPU sagas (p1, p2) parked BEHIND the MTP program (parked, not cancelled -- user confirmed); mhc-p3-mlx-resident and mhc-cuda deferred; engram-cuda stays deferred to the Linux box; agent track after the first MTP speed result. Docs-only gates: sw-markdown-checker on the file, sw-checklist, commit, push.