Step 3 of cuda-foundation: wire device('cuda') dispatch in components/eval/crates/mlpl-eval/src/device.rs. Generalize the MLX dispatch: today try_mlx_dispatch/dispatched_call only fire when env.device()=='mlx'. Add the cuda path so env.device()=='cuda' routes ops through mlpl-cuda-rt (add mlpl-cuda-rt as an optional dep behind a 'cuda' feature on mlpl-eval, mirroring the 'mlx' feature). Preserve: CPU fallback when the cuda feature/target is absent, and a one-time warning (mirror take_mlx_fallback_warning -> take_cuda_fallback_warning or generalize to a per-device flag). Prefer refactoring the two near-identical dispatch arms into one device-parameterized helper rather than copy-paste (loose-coupling: define once). TDD: extend components/eval/.../tests/device_dispatch_tests.rs and device_block_tests.rs with cuda cases (dispatch fires under cuda feature; falls back + warns without it). Keep mlpl-eval sw-checklist-clean -- if device.rs grows past budget, extract a device_dispatch.rs sibling. DISK: scope to eval+cuda-rt.