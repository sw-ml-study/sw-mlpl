F23: reshape/windows/reduce/TensorCtor dims bound to user-function arguments now resolve through the traced scope so the gradient flows. Root cause: eval_shape_dims used eager global env (eval_expr) which cannot see function-arg bindings; threaded (tape, params) and switched to eval_tensor_expr(...).value() across all 5 dim-literal sites. RED test asserts param-dims grad == literal-dims grad (4.56, both non-trivial). Verified in rebuilt release repl on probes/f23b. Binaries rebuilt: repl/build (release+debug), serve (release). sw-checklist held 38/570.