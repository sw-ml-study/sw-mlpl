Size each dataflow column gap to its edge labels: layout.rs column_gaps() widens each inter-column gap to fit the widest adjacent-edge label plate (plate width + 16px margin), floored at COL_GAP; place()/canvas() consume per-gap widths. Shared LABEL_CHAR_W/LABEL_PAD constants unify gap sizing with the render.rs plate. box_width inlined to keep layout.rs at the 7-fn ceiling. 9 crate tests pass (new: 27-char label widens canvas 400->517). Fresh release/debug binaries rebuilt; confirmed arrowheads stay fixed-size (markerUnits=userSpaceOnUse) and boxes label-fitted. NOTE: the live gh-pages CDN was still serving the pre-arrowhead-fix bundle 2e552ebb (10-min max-age); this deploy pushes a new bundle hash. sw-checklist held 44/539 (exception, no new FAIL).