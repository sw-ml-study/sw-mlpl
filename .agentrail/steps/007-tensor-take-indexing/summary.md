Saga 29 step 007 (inserted prereq): take(x, axis, idx) ships as a single-axis indexing primitive. Drops one axis at a single integer index, returns array with rank r-1, per-axis labels propagate. NodeKind::Take + scatter-back backward (PyTorch index_select-style gradient). Wired through DenseArray, Tensor, runtime, eval, grad, device MLX re-run. 10 tests including shape, labels, errors, gradcheck parity, pets_tiny round-trip. Unblocks per-image extraction in the trained ViT demo; next prereq (step 008 batch-aware attention) extends attention to rank-3 [B, T, d].