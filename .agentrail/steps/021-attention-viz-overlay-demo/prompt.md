Saga 29 step 017 (per the original milestone-vit.md Phase 4 step 010, renumbered): demos/vit_attention_viz.mlpl -- render the trained ViT's CLS-row attention as a heatmap *over* the test image. Pick one held-out cat photo (e.g. pets_tiny index 50), train a ViT on the balanced 20-image subset, extract attention_weights to get the CLS row of the [T, T] matrix (single-head) or one head's CLS row (multi-head), reshape over the 8x8 patch grid (or 4x4 for 64x64 / 16 patch input), upsample to image resolution, and render the heatmap as an alpha-blended overlay on the original image. May ship as a tail block inside the existing demos/vit_multihead_thorough.mlpl or as a standalone demo file. Add a new svg() type 'attention_overlay' if needed, or reuse heatmap + gallery layered via SVG composition.