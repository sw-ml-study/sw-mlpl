Phase 2 start: mlpl-lower-rs crate skeleton. Create crates/mlpl-lower-rs/ that imports mlpl-parser and exposes a single entry point: fn lower(ast: &[Expr]) -> Result<TokenStream, LowerError>. This step covers the scalar-arithmetic subset only: IntLit, FloatLit, UnaryNeg, BinOp(+-*/). Emit calls into mlpl_rt::scalar helpers (add to mlpl-rt in this step if needed -- scalar ops aren't covered by phase 1's array surface). Use proc-macro2's TokenStream, not std::proc_macro, so the crate stays callable from both tests and the future proc macro. TDD: a test suite that lowers '1 + 2 * 3' and asserts the generated Rust compiles + evaluates to 7. Use trybuild or just cargo expand-style snapshot testing. The test harness compiles the generated tokens into a tiny Rust source, runs it through rustc --crate-type lib, and evaluates -- document the approach in the crate README.