Tech-debt spike (CLAUDE.md 'dedicated tech-debt reduction spike between feature steps'): relieve the crate ceilings blocking the file-processing-builtins batch. BOTH mlpl-rt and mlpl-lower-rs are at their 7-module ceiling with over-budget files (fncall.rs 360-line file-LOC + lower_fncall 128-line FAIL; mlpl-rt lib.rs 6-fn facade violation). Split to create headroom: (a) mlpl-lower-rs -- extract the fncall.rs complex handlers (lower_bit_op/lower_matmul/lower_label_attach/extract_label_list/labels_of, and the take/cmp inline arms) into a sibling crate or restructure so fncall.rs is under 350 lines and lower_fncall is under 50; this needs a NEW workspace crate (e.g. mlpl-lower-emit) since the crate is at its module ceiling -- carving out a sibling crate drops mlpl-lower-rs's module count AND file/function LOC at once. (b) mlpl-rt -- move the local iota/shape/rank/tally/floor/take fns out of lib.rs (facade-only) into a named module or sibling crate, and prefer re-exporting mlpl_runtime_math::try_call / mlpl_runtime_array::try_call (like bit_try_call) for the pure math/array builtins so future ones add no local fns. Target: retire >=5 failures AND >=15 warnings net (the ratchet spike target). Keep all tests green (dispatch coverage + the gated build e2es). Update code_metrics/docs if the crate map changes. Commit + refresh binaries. Then resume the builtins batch (type_of/equal for wc).