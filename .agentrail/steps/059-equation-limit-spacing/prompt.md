Fix squished/clipped upper limits in the svg('equation') stacked-limit renderer: the upper bound sits on top of the operator and clips at the canvas top because SUP_RISE (15) < the operator cap height (~15 at FS 22). Raise SUP_RISE so the upper limit clears the operator with a readable gap, grow LINE_H + top/bottom canvas room to match, and re-verify no clipping. Rebuild binaries; re-publish literate; rebuild+redeploy DEV/nightly only.