Closeout step for the dimensionality reduction milestone (full plan: docs/milestone-dimensionality-reduction.md).

DELIVERABLES:

1. Final sw-checklist pass: net-negative on FAILs and warnings vs the milestone's first commit, OR document the carry-overs.

2. Update docs/language-status.md with the milestone close-out entry: lists the new builtins (pca_components, knn_graph, umap, mds, random_projection, plus the loadings + variance helpers), new viz type (critical_dimensions), new demos, new tutorial lessons.

3. Update docs/milestone-dimensionality-reduction.md to mark each phase as DONE with a link to the saga step.

4. Refresh CHANGES.md via scripts/gen-changes.sh.

5. Rebuild pages/ one final time so the live demo at github.io reflects the complete milestone.

6. Mark the agentrail step `--done`.

This is a pure documentation + close-out step -- no new feature work.
