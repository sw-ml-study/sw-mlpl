Moved scatter_labeled legend OUTSIDE the plot area (was overlapping data points at x=310 inside the 30..370 data region). New write_svg_open_with_size helper lets analysis_scatter_labeled request a 90px right gutter; legend renders at x=410 in the gutter. Also fixed cluster-id narrative consistency: replaced 'cluster A/B/C' references in UMAP_VS_TSNE demo + 3 lessons + .mlpl source comments with 'class 0/1/2' matching the legend (named labels still deferred until svg() supports Value::StrList aux).