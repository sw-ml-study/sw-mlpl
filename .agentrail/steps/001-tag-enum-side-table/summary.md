Shipped ValueTag enum (Tier A: Logit/Probability/LogProbability/Loss{kind}/Gradient{wrt}/Weight{layer,name}/Bias{layer}/Activation{layer,kind}/LearningRate/Labels{num_classes}/AttentionMap) in mlpl-core/src/value_tag.rs with serde derives + display_name(); plus Environment::tags HashMap side table in mlpl-eval/src/env.rs with set_tag/get_tag/clear_tag/tags_iter helpers. ValueTag lives in mlpl-core (not mlpl-eval) because trace JSON surfacing is downstream and mlpl-eval is at its module budget. ActivationKind/LossKind defined parallel to ActKind in model.rs to keep the typing layer independent of the model DSL crate. Zero language surface; auto-tagging and predicate checks land in steps 002+. 25 new tests (16 mlpl-core + 9 mlpl-eval), cargo test workspace green, clippy zero warnings, fmt clean, sw-checklist failed count held at 139.