Saga 29 step 010: gallery viz output + UX curation. New svg(images, gallery) renders [N, 3, H, W] image batches as an SVG grid of RGB thumbnails (block-averaged downsampling). 7 tests covering shape rejection, single/multi-image layout, empty batches, value clamping, dispatch routing. Also addressed three live-demo UX issues raised by the user: (1) added take-chain inspection examples to the Pets: cat vs dog (quick) demo so users can peek into the high-rank pets tensor; (2) alphabetized the demo dropdown by name (display-only sort, DEMOS source untouched); (3) added a new Vision Transformers in MLPL learning path. sw-checklist unchanged at 152.