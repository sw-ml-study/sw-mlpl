B2: add to_string(x) -> string interpreter builtin in mlpl-eval (the honest inverse of to_number, register in fncall_string.rs which now dispatches str_concat/str_join). Number -> string with shortest round-trip formatting: integral values print bare (to_string(8/2) == "4", not "4.0"); to_number(to_string(x)) recovers x exactly for finite f64 (the same formatting to_json already uses for scalars). Ship the 1-arg form only (rounding belongs in a library). Only a scalar (rank-0) number is meaningful -- decide the behaviour for arrays/strings (error or defined). TDD with the work order's acceptance (to_string(0)=="0", to_string(0-3)=="-3", to_string(1.5)=="1.5", to_number(to_string(sqrt(2)))==sqrt(2)) via tokenize_bytes. Update lang-reference + :builtins catalog. Hold sw-checklist.