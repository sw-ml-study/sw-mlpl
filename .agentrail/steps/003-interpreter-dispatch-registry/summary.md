Refactored eval_fncalls.rs's builtin dispatch from the hand-written ~18-link .or_else chain into a data-driven DISPATCHERS registry (const slice of a uniform Dispatcher fn type; find_map, first-match). The 16 span-less families gained an ignored _span param so all conform to one handler contract; config/dispatch/handlers separated. Behaviour-preserving: full mlpl-eval suite (~900 tests, release) zero failures; first-match order preserved. sw-checklist DOWN 1 warning (557/49/523) -- the short find_map retired eval_fncalls' Function-LOC warn. Family-level (per-family match stays); per-name flatten of 184 builtins is a separate effort.