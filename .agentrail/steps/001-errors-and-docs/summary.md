Step 1 (resumed) errors-and-docs: documented the unified axis-naming forms and retired the stale claims. glossary.md + lang-reference.md now state that reduce's axis arg and label/relabel/reshape_labeled's name arg accept a bracketed name list, an equivalent comma-string, or a computed value interchangeably (reduce also integer positions); removed the 'not a list of strings / not a value MLPL has' claims (MLPL has StrList; the bracketed form ships). wiki Documentation-Errata marked the axis-naming gap RESOLVED. The typed AxisError already unifies resolution errors in axis_adapter; reduce_add's remaining single-axis labeled path is deferred to surface-sweep. Docs-only (markdown-checked); sw-checklist unchanged.