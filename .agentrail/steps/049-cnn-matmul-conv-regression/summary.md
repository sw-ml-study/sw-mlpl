Pinned the weighted-conv capability as a regression (mlpl-eval/tests/conv_eval_tests.rs, commit b915341f): matmul(reshape(windows(img,[kh,kw]),[oy*ox,C*kh*kw]), reshape(kernel,[C*kh*kw])) -> correct conv values [268,296,352,380] with NO rank broadcast; windows axis order [out_y,out_x,C,kh,kw]; multi-channel box-filter reduce -> [52,60,76,84]; and a boundary test documenting the elementwise kernel*windows spelling still errors (delete when C2 lands). 4 tests green. Test-only -- no interpreter source changed, so no binary/pages rebuild needed. sw-checklist held 41/545. This corrects the earlier (wrong) advice that the conv demo was gated on C2.