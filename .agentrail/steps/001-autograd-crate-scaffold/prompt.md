Create the mlpl-autograd crate skeleton. TDD. Add crate to workspace. Define Tensor { value: DenseArray, grad: RefCell<Option<DenseArray>>, node: NodeId, requires_grad: bool } and a Tape holding Nodes. Implement leaf construction and a no-op backward pass. Tests: constructing a param tensor, calling backward on a leaf is a no-op, graph IDs are unique. Update Cargo.toml workspace members. No language-surface changes yet. Commit via /mw-cp.