E5 step 2 per plan: DeviceOps grows concat(axis) + split for its backward (MlxOps via mlx concatenate/split_axis); TensorHandle dev_concat/dev_split; resident forward attempt for Concat tape nodes + resident backward; no-download pinning test in resident_seam_tests pattern; engram warm-step fallbacks should drop 3 -> 1 (CE only) and the attention path also benefits. Parity tests, scoped gates, commit.