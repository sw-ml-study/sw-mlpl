Host transport per docs/q-and-a.md assessment: (1) mlpl-repl --test-events <path> writes each validated event as one JSON line (synchronous append; exact text; ordered) -- env carries the path, emit serializes the record (strings, scalar numbers, string lists, nested records) after validation and before sink delivery; runner errors on unwritable path; (2) connect mode: env-buffered event lines drained per eval and emitted as a test_event SSE type by mlpl-serve; (3) docs (lang-reference rows, glossary entries + README pin, usage note, catalog rows) and rebuild repl+serve+pages. TDD both transports.