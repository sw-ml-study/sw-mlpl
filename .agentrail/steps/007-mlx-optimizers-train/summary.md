Saga 14 step 007 shipped. Optimizer + train-loop parity on MLX demonstrated by 5 new tests in tests/train_mlx_tests.rs, no source changes needed. adam/momentum_sgd both call eval_grad internally, which step 006 wired to re-materialize tape forward values through mlpl-mlx; their parameter-update arithmetic then stays on CPU as pure elementwise math over MLX-rounded gradients. Control flow (train N, experiment, last_losses) is pure dispatch in mlpl-eval::eval and sees no device-specific behaviour. OptimizerState buffers (Adam m/v, momentum_sgd v) stay CPU-resident DenseArray values -- design choice made explicit in commit + test docstring: moving them to MLX tensors is a perf optimization, not correctness, and the values they hold are computed from MLX-rounded gradients regardless. Tests cover: (1) one adam step on linear W update within 1e-4 CPU vs MLX, (2) one momentum_sgd step same tolerance, (3) train 3 { adam(...) } on Tiny LM-shaped slice (chain(embed, rms_norm, linear) -> CE) produces same last_losses within 1e-4 -- the step prompt's closing gate, (4) cosine_schedule/linear_warmup inside device(mlx) produce correct scalars (pure scalar math, fall through dispatched_call to mlpl_runtime), (5) experiment loss_metric capture inside device(mlx) records one run with expected value. Quality gates: cargo test --workspace clean default, cargo test -p mlpl-eval --features mlx + cargo test -p mlpl-mlx --features mlx clean on Apple Silicon, clippy both feature modes clean, fmt clean, sw-checklist FAIL count unchanged at 135. Commit 498fcaa pushed.