Dataflow honest log-width channel: opt-in width_scale ("linear" default, clamped | "log" natural-log-normalized into a [1,9] px band) on the edges record, so extreme byte ratios (269MB vs 4KiB, 65000:1) render as an orders-of-magnitude contrast instead of blowing out or vanishing. New widths.rs stroke_widths(); Dataflow/Graph gain width_log; render.rs consumes precomputed widths; fncall_dataflow extracts width_scale (clean error on bad value). 5 crate tests + eval dataflow_tests pass; verified e2e in fresh release binary (log->1,9; linear clamps both to 9; raw 269000000 never leaks). Design doc updated. Fresh release/debug mlpl-repl+mlpl-build rebuilt. sw-checklist held 44/538 (exception; widths.rs is a cohesive 7th one-concern module, no new warning).