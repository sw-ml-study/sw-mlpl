1. Add detection for '<var> = attention_weights(' producing rank-3 [H, Q, K] tensor. Set viz.kind='attention', viz.attention.layout = { rank: 'head_qk', head: H, q: Q, k: K }. 2. Refactor renderAttentionHeatmap to dispatch on layout.rank. For 'head_qk', add a <select> dropdown 'Head 0..H-1' above the SVG; onchange re-renders the body with the new head index. Default head 0. 3. Track current head as a module-level inspectorHeadIdx; reset on each openInspector call. 4. Demo proof: vit_attention_pattern_multihead.mlpl, click A, see 17x17 heatmap with head dropdown; change head, see different patterns.