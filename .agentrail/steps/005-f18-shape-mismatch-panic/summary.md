F18 (BUG): a shape mismatch inside grad panicked the process (tape push_binary .expect); eager errors cleanly. This is the general case of F10's panic site. Fixed by pre-validating broadcast+label compat on forward values before building the tape node: new pub check_binop_compat (merge_labels + broadcast_shape, no compute) in mlpl-array-ops-element; grad's checked_arith (split out of tensor_binop) calls it for +/-/*// and maps ArrayError->EvalError. Uses the same broadcast logic the tape would, so rejects exactly what would panic and nothing valid. Verified in release binary: clean 'array error: shape mismatch: 4 vs 3 elements', no panic. Tests grad_shape_mismatch_errors_cleanly_not_panic; regression grad 29/29, array-ops-element 11. sw-checklist 38/562 unchanged (checked_arith split keeps LOC under gate). Rebuilt repl/build/serve (serve now current with all followups-2 evaluator changes). Pushed.