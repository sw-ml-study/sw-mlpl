Native GatherRows autograd node (plan.md step 3): forward copies rows, backward scatter-ADDs into only touched rows (duplicates accumulate), O(n*D) instead of the dense one-hot [n,V] matmul in grad_calls_engram::call_gather_rows. Also rewire the one-hot embedding in mlpl-models-tape/src/layers.rs. Host fallback OK for resident/mlx if no kernel -- note it. TDD: gradcheck vs old one-hot result incl. duplicate ids; large-vocab timing sanity.