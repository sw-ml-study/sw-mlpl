Path-resume bug fix: dropped path_state.set(None) from cb_path_open_lesson + cb_path_run_demo so the walker position survives navigation. paths_active now requires !tutorial_active so the lesson pane wins the screen while path_state is held for resume. Added 'Back to path' button to TutorialPanel that clears lesson_idx and flips the walker back on. 4 new pure tests on compute_modes covering the state-pair cases. Pages rebuilt.