Implement per docs/combinators-design.md: Value::Partial (callable name + bound Vec<Value>), env partials table + assignment/clear_binding/ScopeSnapshot/bind_value/serve+wasm arms + repr/equal forms; applicative call in eval_call (under/exact/excess-left-assoc; zero-arg identity; builtin under-application tutoring error); shared resolve-callable helper adopted by call, UDF args, each/table/atop/over, map_ok/and_then/or_else, bracket. TDD: staged K/B, left-assoc equivalence, SK-basis derivation of I, mockingbird, all four storage flows, error cases. Gates; ratchet; commit; push.