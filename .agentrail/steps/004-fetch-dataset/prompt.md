Saga 29 step 004 (per docs/milestone-vit.md): fetch_dataset("oxford_iiit_pet"). Native-only builtin that downloads the upstream Oxford-IIIT Pet tarball (~792 MB) to $MLPL_DATA_DIR on first use, verifies sha256 (recorded value: 67195c5e1c01f1ab5f9b6a5d22b8c27a580d896ece458917e61d459337fa318d for the version downloaded in step 003), extracts to data/oxford-iiit-pet/images/, then decodes via the load_images path from step 003 at the demo's 128x128 resolution. Returns the same Record shape as load_preloaded("pets_tiny") but at full size: {X: [N, 3, 128, 128], Y: [N], names: StrList}. WASM stub raises clean error pointing at load_preloaded. Tests: a small synthetic 4-image tarball fixture drives the unit test; the live ~792 MB download is gated behind --ignored. Reuses the same png + jpeg-decoder feature gate (image-io). Update contracts/eval-contract for the new builtin. Disk: the tarball already lives in the gitignored data/oxford-iiit-pet/ from step 003, so the fixture build is essentially free if MLPL_DATA_DIR points there.