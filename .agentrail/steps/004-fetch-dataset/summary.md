Saga 29 step 004: fetch_dataset(name) live-download pipeline for Oxford-IIIT Pet. Resolves data root from $MLPL_DATA_DIR or Environment::data_dir, downloads via ureq, sha256-verifies, gunzip+tar extract, decodes via step 003 image_io at 128x128, returns same Record{X, Y, names} shape as load_preloaded(pets_tiny) but N=~7393. Pre-populated checkouts bypass HTTP. WASM stub points at preloaded. New deps (tar/sha2/flate2/ureq) feature-gated under image-io; WASM dep tree verified clean. 12 new tests (9 inline + 3 dispatch + 1 ignored live integration). +1 sw-checklist exception for fetch_dataset module fn count, same structural pattern as steps 001-003.