Authored demos/embedding_viz.mlpl: trains a standalone embed(12, 8, 0) toward a 3-cluster target via MSE for 50 adam steps, extracts learned table via apply(emb, iota(V)), runs it through the Saga 16 viz pipeline (tsne to 2-D scatter; column-selector matmul + 3-D scatter; knn for nearest-neighbor reporting). Manual run confirms trained embeddings recover cluster structure (all tokens in cluster X have knn neighbors also in cluster X). 4 cut-down integration tests (V=9, d=4, 3 train steps) pin shapes, finiteness, knn invariants, and svg("scatter3d") renders with axis gizmos. Added Demo 9 entry to docs/demos-scripts.md.