Cut the 0.21.0 release: bump all component-workspace package versions 0.20.0 -> 0.21.0 across ~52 Cargo.toml and sync the mlpl-* entries in all Cargo.lock (surgical, external deps like roxmltree untouched), validated by a --locked build. New minor: str_eq builtin, run_script value_raw, all-unit broadcast fix, comment-span fix, recursion-depth cap. Then rebuild+deploy dev pages and cut the stable release.