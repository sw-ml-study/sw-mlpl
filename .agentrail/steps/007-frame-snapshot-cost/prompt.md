microgpt-mlpl issue (e), the blocking half: calling ANY u: function costs time proportional to the total size of ALL globals (u:ts() { to_string(6) }: 0.003 ms with small globals, 0.64 ms with a 2.28M-element global; they expunge big globals as a workaround). Cause: eval_user_fn.rs call_user_fn does env.snapshot_scope() on entry and restore_scope() on exit, and snapshot_scope (mlpl-eval-envc-bindings/src/env_scope.rs, for_each_scope_table!) deep-clones all 13 scope tables including every array's Vec<f64>. Fix: make the frame snapshot O(names written), not O(all globals) -- e.g. an undo log: record the PRIOR binding (or absence) of each name the frame binds/assigns, across every scope table, and restore just those on exit; keep existing semantics exactly (frame-local writes vanish, global_writes / set_trained_param persist, nested frames, early return / ? / try-catch / errors unwind correctly, grad_const::eval_const_arg + other snapshot users keep working). Design it with the for_each_scope_table! single-source macro so tables cannot drift. TDD: a timing test that u: call cost does not scale with a 2M-element global (compare small vs big within a generous ratio); semantic tests for shadowing, nested calls, recursion, error unwinding, and optimizer writes inside u: bodies; the full mlpl-eval release suite is the real safety net. Report before/after numbers vs microgpt's (docs/benchmarks.md style) in docs/q-and-a.md -- after this lands their expunge calls can go. Per-READ copy cost of big globals is out of scope (cow-values saga).