User lens question verified that running_sum/running_product compose with the take/flatten lenses, but exposed a footgun: rank>1 input silently scans the row-major ravel (contradicts the rank-1 doc and APL axis semantics). Add a rank-1 guard to both with an error naming the two lenses (take(a, axis, i) for a row/column focus, flatten(a) for the explicit whole-array scan); tests for the error + the lens compositions; lang-reference lens hint. Scoped gates.