Implemented the non-attention MLX forward primitives for the LoRA fine-tune model in a new crate mlpl-mlx-forward (native-rt now 4 crates, at budget; 6 passed/0/0): embed (one-hot matmul), rms_norm (x/sqrt(mean(x^2)+eps)*gamma), cross_entropy (stable logsumexp_axis - picked logit). Each parity-tested vs a hand-computed f32 reference. Verified mlx_rs reduction API (logsumexp_axis/sum_axis/mean_axis). Forward-value tests only (no value_and_grad) so they run parallel. Attention + full assembly + eval_adam wiring + demo relabel remain (steps 008-009).