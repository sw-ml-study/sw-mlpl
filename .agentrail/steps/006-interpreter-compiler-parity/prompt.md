Phase 6: parity test harness + one demo port. (a) New parity-tests integration crate that iterates over a curated subset of mlpl-eval's existing pure-expression tests, runs each source through both the interpreter (eval_program) and the compile path (lower + rustc + execute), and asserts the numeric outputs match bit-for-bit for deterministic ops. (b) Pick one on-disk demo (suggest demos/moons_mlp.mlpl since it exercises model DSL, autograd, and optimizers -- but if the model DSL is out of compile scope, pick a simpler one like demos/kmeans.mlpl) and ship a compiled version: ; measure interpreter-vs-compiled runtime and record the ratio in the step summary. Expect 2-10x speedup on reasonable workloads, not a hundred-x -- the interpreter already amortizes over array size. If a demo cannot compile yet because it uses an unlowered primitive, document which primitives are missing and defer that demo.