Phase 1b of local-gpu-agentic: make the LoRA fine-tune loop actually run on the Apple GPU (today device("mlx") accelerates only the forward pass; backward + adam run on CPU -- see docs/saga-local-gpu-agentic.md 'CRITICAL reality' section). Work: (1) extend mlpl-mlx-rt with the backward ops the autograd tape needs (matmul backward, elementwise grads, softmax/cross_entropy backward); (2) make the autograd path (mlpl-autograd / mlpl-eval grad.rs) dispatch backward to MLX when device==mlx instead of materializing on CPU; (3) make the adam optimizer (mlpl-eval grad_optim.rs) keep moment buffers + weight updates on-device (no per-step CPU round-trip). Goal: a device("mlx") fine-tune where forward, backward, AND optimizer run on GPU, parity-tested vs CPU within fp32 tolerance. This is a big lift requiring the MLX toolchain build (Mac/aarch64, mlx feature) -- scope carefully, likely multiple steps; consider agentrail insert to split (mlx-rt backward, autograd dispatch, adam on-device). Then upgrade the 'MLX LoRA fine-tune' demo label from hybrid to true-GPU. Keep sw-checklist non-regressing. Commit .agentrail with source.