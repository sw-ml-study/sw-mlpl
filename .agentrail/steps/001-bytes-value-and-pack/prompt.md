Add a Value::Bytes { dtype: ByteDtype, data: Vec<u8> } variant to mlpl-eval-types (ByteDtype enum: u8,i8,u16,i16,u32,i32,u64,i64,f32,f64). Update value_kind ("bytes") and Display (<bytes: DTYPE[N]> where N = element count). Wire every exhaustive Value match the compiler flags (bind_arg in eval_user_fn, etc.). Add a pack(array, "dtype") -> bytes interpreter builtin: pack an f64 array into a canonical little-endian packed buffer of the given dtype (reject non-representable values, e.g. fractional for int dtypes or out-of-range, with a clear EvalError). TDD: RED test first (pack a small array, check element count via a later size probe or a temporary accessor; check the dtype tag; check a domain error). New logic in named files per code_metrics (model.rs for ByteDtype, pack.rs for packing), pure free functions. Scope tests to mlpl-eval + mlpl-eval-types. Ratchet sw-checklist both axes down (a new module/crate may add ~1 warning; retire elsewhere or use exception trailer with justification). Do NOT rebuild the release binary until a later step -- keep this step focused.