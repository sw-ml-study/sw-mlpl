Step 004: Simple CNN demo.

New demo 'Simple CNN' that builds a 2-layer CNN pipeline on tiny synthetic 8x8 images: conv2d -> relu -> pool2d -> conv2d -> relu -> pool2d -> reshape -> linear -> softmax. Show the shapes shrinking through the pipeline. Pages rebuild required.