Step 4 render-and-drive: two runnable surfaces from the columnar contract. (a) examples/viz/memory_map_2d.mlpl -- self-contained 2D orthographic memory-map (no native extension): bucketize blocks->region via broadcast-compare + row reduce, pad to whole rows, render the block grid with the shipped svg heatmap; verified it emits a valid SVG. (b) examples/viz/render_native3d.mlpl -- end-to-end 3D reference: read storage-layout.json, build geometry, drive native3d via the proposed set_boxes/set_labels/render primitives; geometry always builds (verified 8 boxes from the fixture with explicit region_id), push gated on an arg + native3d on MLPL_EXTENSION_PATH. 2 TDD tests pin the bucketize + grid. Docstrings + mlpl-fmt clean; eval clippy/fmt green; 38/558 unchanged; no new builtin so no binary rebuild.