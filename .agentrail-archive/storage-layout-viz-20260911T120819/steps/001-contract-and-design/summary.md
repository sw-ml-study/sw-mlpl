Step 1 contract-and-design: wrote docs/storage-layout-viz.md -- the coordination artifact for the three-repo storage-layout 3D demo. Defines the COLUMNAR storage-layout.json contract (space_* and region_* parallel homogeneous arrays, index-aligned), sw-mlpl's array-programming role/boundary, the reference-script architecture (parse -> block->xyz layout math -> classify -> assemble [N,3]/[N,4]/[N] -> drive native3d), capability status (parse_json + record_get + list ops + mod/floor/div + reshape/concat + extension rank-2 marshaling all SHIPPED; one gap = vectorized string->RGBA classification), the native3d invocation surface, phasing, and who-provides-what. Shipped examples/viz/storage-layout.json and validated with the release binary that parse_json ingests the columnar shape (string cols -> StrList, numeric cols -> arrays) and column arithmetic works.