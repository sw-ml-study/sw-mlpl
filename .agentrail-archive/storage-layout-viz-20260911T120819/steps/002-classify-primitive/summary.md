Step 2 classify-primitive: added select_rows(table, keys) -- a vectorized keyed row lookup that turns a categorical string list into an [N,C] attribute matrix (e.g. region kinds -> [N,4] RGBA). Confirmed the gap was real: the manual while/list_get/str_eq loop is verbose AND take/3 cannot row-select. New eval module fncall_select_rows (in DISPATCHERS), 5 TDD tests, :help catalog entry. eval clippy/fmt green. 38 failed / 558 warnings: no new fail; +1 module-fn warning (expected clean-new-builtin cost). Also added an MLOS (sw-os-ml) coordination note to docs/storage-layout-viz.md -- it is a second producer of the same columnar contract; the viz code is system-agnostic.