Step 5 coordinate-and-close: shipped storage-layout-viz. Added a handoff/coordination-status section to docs/storage-layout-viz.md (what sw-mlpl built, the sample-artifact sha256 b5a328a6..., and what each other repo owns), updated the capability 'gap' note to reflect select_rows shipped, updated the wiki Capability-Matrix (select_rows + the JSON->geometry->native3d pipeline, pushed separately), and marked the saga SHIPPED in docs/future-sagas-queue.md (no active saga; next candidate is resuming axis-naming-unification). sw-mlpl's part of the multi-repo demo is complete: locked columnar contract (schema/provenance/region_id/relationships), select_rows classification primitive, geometry lib, and 2D+3D runnable surfaces. Remaining work is other repos' (producers emit the JSON; demo-extensions implements native3d).