Step 005 (FINAL, use --done): UDF demo + glossary + help + language-status + saga close.

Create a UDF demo showing def + recursion + pi(). Add glossary entries (User-Defined Function, Scope, Recursion). Update help text with def syntax. Update language-status.md. Demo smoke test. Mark --done.