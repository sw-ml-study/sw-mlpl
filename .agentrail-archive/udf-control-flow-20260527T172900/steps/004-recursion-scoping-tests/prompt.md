Step 004: Recursion + scoping integration tests.

Fibonacci, factorial, mutual recursion. Verify lexical scoping: inner function reads outer vars but does not mutate them. Test that builtin names cannot be shadowed by def. Test colon-namespace enforcement end-to-end.