Step 004: LSTM sequence demo.

New demo 'LSTM (sequence memory)' showing LSTM on a longer sequence where the simple RNN would fail (vanishing gradient). Compare RNN vs LSTM on the same task if feasible, or show LSTM handling a longer dependency.

Run demo smoke test. Pages rebuild required.