Extend the Combinators (the birds) demo with a fixed-point / Y-combinator section: (a) self-application recursion (fact_step takes self -- the mockingbird move, no named recursion); (b) the fixed-point combinator def u:fix(f, v) { call(f, call(:u:fix, f), v) } with a CLEAN step (fact_body/fib_body never name themselves), factorial + fibonacci; (c) frame it -- 'Y Combinator the brand is named after this fixed-point idea: make something from itself'. Note the strict-language subtlety in the takeaway: the partial call(:u:fix, f) delays, which is exactly why this works eagerly (the classic lazy Y would diverge). Add an idioms-doc section, a glossary entry, and a Rust test (y_combinator_tests) pinning fix over fact + fib. Verify the demo runs; release smoke; docs pins. Gates; commit; push.