Shipped CNN C2: general NumPy/APL trailing-axis broadcasting in apply_binop (new broadcast.rs). Subsumes all prior single-element cases (incl. [[0]]*1->[1,1] BUG 1) and adds rank broadcast: rank-3 kernel [C,kh,kw] * rank-5 patches [oy,ox,C,kh,kw] works; [3,1]+[1,4] is a [3,4] outer sum; incompatible axes still error. Labels align right (merge_labels broadcast branch). Shared by interpreter AND compiled runtime (mlpl-rt) -- both gained it. Verified: elementwise reduce(:add, k*windows,[2,3,4]) == matmul conv [268,296,352,380]. Tests: broadcast_shape_tests +4; conv boundary test rewritten to pin the requirement (equal-to-matmul, per demo-ml-utils probe-trap lesson). FULL mlpl-eval 1396 pass + native-rt 33 pass, ZERO regressions on the shared path. Binaries rebuilt release+debug; deployed DEV/nightly (bundle f4f87e0e, commits f4f87e0e+a490680d+b99fabf5). sw-checklist 41 held, +1 warn (exception: new broadcast module). No version bump, no stable. Saved memory on capability-tests-pinning-requirements + string-lists-second-class. NOTE demo-ml-utils: C4 shipped upstream (their workaround now redundant); C2 done though they'd demoted it (general broadcasting is a language win beyond conv); C1-C5 all complete.