CNN Phase 2b (demo-ml-utils feedback on C1): change windows() output axis order from [unwindowed-leading, positions, window] to [positions, unwindowed, window] so a multi-channel [C,H,W].windows([kh,kw]) is [out_y,out_x,C,kh,kw] -- the non-windowed axes sit next to the window axes, so a kernel [C,kh,kw] aligns by trailing position (C2 rank broadcast) with no transpose. Matches demo-ml-utils probes/convolution-reference.mlpl. Also: update the J-inspired moving-average live demo to present the direct windows(x,[w]) form alongside the prefix-sum identity, and refresh windows layout docs (lang-reference, glossary, catalog). Must land before 0.22.0 ships.