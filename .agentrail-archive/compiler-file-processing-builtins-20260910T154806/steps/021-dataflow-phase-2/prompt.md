Dataflow renderer Phase 2 per docs/dataflow-renderer-design.md: add optional channels (nodes.groups id array -> faint group bands; nodes.highlight -> accent node; edges.widths -> per-edge stroke width; edges.highlight -> accent edge+arrowhead). ALSO fix the reported bug that the dataflow SVG shows only a download button: the open() SVG tag lacked explicit width/height so the inline SVG collapsed -- add width/height. Extend mlpl-viz-flow (Dataflow struct + Graph fields + groups.rs band renderer + render.rs), fncall_dataflow.rs extraction, mlpl-viz re-exports, and enhance the Dataflow Diagram demo to showcase the channels.