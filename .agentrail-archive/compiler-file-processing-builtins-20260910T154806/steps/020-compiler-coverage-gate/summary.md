Added the compiler-coverage gate + docs/compiler-coverage.md. coverage_boundary_tests.rs pins a representative INTERPRETER_ONLY builtin set and asserts disjointness from supported_builtin_names() (boundary drift fails the test), plus a registry-non-empty floor. The doc records what compiles (array+bytes+file+stdin+process+control-flow core), what is interpreter-only (category A mechanical gaps vs category B by-nature: ML/autograd, viz, GPU, ports), and the plan. HOLD 44/535.