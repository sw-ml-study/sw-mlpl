Cut the 0.22.0 release (maintainer-authorized 2026-09-10). (1) Commit pending edits: conv demo takeaway cross-ref to the Train a Convolution Filter demo (demos.toml), the "not just runnable but LEARNABLE" paragraph in examples/literate/cnn-convolution.org, the mlpl-viz-math Cargo.lock additions (types/web/web-demos), and the fn_source_tests.rs fmt. (2) Republish the literate CNN doc. (3) Bump all 52 component-workspace versions 0.21.0 -> 0.22.0 and refresh the mlpl-* Cargo.lock entries in lockstep. (4) Rebuild + deploy nightly (build-pages.sh + deploy-pages.sh, gh-pages / sw-ml-study.github.io/sw-mlpl/). (5) Promote to STABLE (release-stable.sh -> ../mlpl-live, mlpl.softwarewrighter.com). This 0.22.0 carries the differentiable CNN work: windows/reduce/flatten backward, trainable convolution (adam recovers a teacher kernel to machine precision), the svg equation renderer, the convolution-from-the-equation + Train a Convolution Filter demos, the literate CNN derivation, and the full doc-accuracy sweep. Refresh CHANGES.md at the end.