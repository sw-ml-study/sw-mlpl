Fixed upstream-asks #16: multi-line playground entries rendered code inside the comment span. render_input_line applied split_inline_comment (per-LINE) to the whole statement GROUP, so a leading full-line # comment swallowed the code after it. Added split_group_lines (per-line split) to the tutorial crate; render_input_line renders one input-line per line, first with the mlpl> prompt. Kept in tutorial crate (entry.rs at 7-fn ceiling). 4 comment_split tests + clippy clean; no new FAIL (41/543). Web-only; pages deploy batched with #10/#20.