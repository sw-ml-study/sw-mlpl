Lower the grep string family: str_concat/2, str_find/2, str_len/1, str_slice/3, str_split/2 on CVal::Str. Interpreter parity (mlpl-eval fncall_string.rs / fncall_string_ops.rs / string_search.rs): str_concat(x,y)->Str(x+y); str_find(s,needle)->scalar char index or -1 (empty needle->0); str_len(s)->scalar char count; str_slice(s,start,len)->Str of len chars from char index start (clamped); str_split(s,sep)->CVal::StrList (empty sep splits into chars). Implement rt fns in an ALREADY-WARNING mlpl-rt-value module (e.g. text.rs, already 4 fns) or add carefully -- mlpl-rt-value is at its 7-module ceiling so NO new module; prefer inline emits where trivial to avoid rt fns. str_len/str_concat can be CvalIo-shaped (1-CVal-in) or a small emit; str_slice(3-arg)/str_split need small emits. Add rt fns to the mlpl-rt facade + CVAL_BUILTINS for the Str/StrList producers. dispatch_coverage variants. TDD gated e2e: str_len("hello")->5, str_concat("ab","cd")->"abcd", str_find("hello","ll")->2, str_slice("hello",1,3)->"ell", str_split("a,b,c",",") shows the parts. Hold sw-checklist. After push refresh release+debug binaries. Commit source before agentrail complete.