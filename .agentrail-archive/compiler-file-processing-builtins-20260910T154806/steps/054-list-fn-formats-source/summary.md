Fixed :list not pretty-printing flat one-line defs (user-reported on u:conv_im2col). env_user_fns_render::list_fn returned stored source verbatim; now runs mlpl_eval_core::indent_source on SINGLE-LINE source (multi-line stays verbatim, preserving authored layout + # comments per the existing list_shows_source_with_comments contract). TDD: new one_line_def_is_pretty_printed_on_list (RED then GREEN); fn_source 5 / inspect 12 / colon_ux 19 all pass. Verified on binary (:list u:cube explodes to indented multi-line). Binaries rebuilt release+debug; pages rebuilt+deployed (browser REPL shares the fix). Commits 69057dc8+85720e53+fefa4ddd. sw-checklist 41 held. DEV/nightly only; no version bump, no stable.