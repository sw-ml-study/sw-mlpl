Write docs/unifying-plan.md: a type-safe, staged plan to unify axis-naming (and string-list) semantics so label(), reduce(), reshape_labeled(), transpose_axes(), etc. accept the same axis-selector forms across interpreter and compiler. Grounded in the real types: Value::StrList / CVal::StrList already exist; label validates at AST level (literal-only) while reduce evaluates and rejects StrList; compiler has static known_labels + static matmul label-checks but no named reduce. Propose a shared AxisSelector/AxisNames type + single resolver (define-once-invoke-many), phase-aware compile-time vs runtime handling, a backward-compatible staging, and a parity test that pins the requirement. Planning doc (plans/stages allowed); ASCII-only markdown.