Add str_eq(a, b) -> 1|0 whole-string equality (demo-abstract-algebra upstream-asks #23, the blocker). eq is an array op and rejects strings; str_find is a substring trap ('semigroup' matches 'group'). Implement in the interpreter (fncall_string_ops) AND lower to the compiler (str_ family is already lowered) for consistency; register in :help catalog, lang-reference, glossary, and the dispatch-coverage gate. TDD.