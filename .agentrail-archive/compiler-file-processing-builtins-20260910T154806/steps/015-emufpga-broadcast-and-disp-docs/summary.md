Shipped the safe subset of ../emufpga's upstream asks: (1) length-1 arrays broadcast like scalars in apply_binop (elem_count()==1), NumPy/APL parity on both surfaces, additive (only previously-erroring pairs affected) -- [2]*[1,2,3]->[2,4,6]; (2) fixed the misleading disp doc + hello-world comment WITHOUT a runtime change (disp writes-to-stdout would double-break ~230 downstream files with mlplunit baselines; disp RETURNS a display string, print is the stdout verb). Regression test + verified. Remaining asks (at(v,i) indexing, dataflow SVG renderer) scoped separately.