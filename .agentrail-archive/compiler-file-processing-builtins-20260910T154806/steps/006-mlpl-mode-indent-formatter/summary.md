Fixed elisp/mlpl-mode.el indentation: replaced the buggy relative-delta indenter with an absolute idempotent syntax-ppss brace-depth indenter (dedents closing braces, ignores strings/comments) so if/else/while/repeat/def blocks indent correctly and indent-region is safe. Added mlpl-format-buffer (C-c C-f) and scripts/mlpl-fmt.sh (cargo-fmt analogue: batch Emacs reindent + trailing-whitespace strip, --check mode, Emacs auto-location). Verified byte-compile clean, nested-block formatting correct, idempotent, --check both directions.