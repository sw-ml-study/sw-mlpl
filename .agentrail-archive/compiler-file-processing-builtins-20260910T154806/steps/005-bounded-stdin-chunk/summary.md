Shipped read_stdin_chunk(max_bytes) on both surfaces (interpreter mlpl-eval + compile-to-Rust mlpl-rt-value) with identical byte/EOF/validation/error semantics: ok({bytes,eof}), eof=1 only on terminal empty read, positive-int budget validated before consuming stdin. Lowered via CvalIo shape + CVAL_BUILTINS + dispatch-coverage variant. Gated compiled e2e proves exact byte counts over piped stdin (14), immediate EOF (0), budget rejection without consuming; interpreter validation via tests/. Retired the 3 structural FAILs introduced (merged ctor.rs into value.rs; relocated tests to tests/) for a clean HOLD at 44/530. Rebuilt release+debug mlpl-repl+mlpl-build; verified interpreter parity (14/0). Docs: glossary + lang-reference + future-sagas-queue + wiki errata E-030. UNBLOCKS ../demo-file-processing bounded-stdin pipeline.