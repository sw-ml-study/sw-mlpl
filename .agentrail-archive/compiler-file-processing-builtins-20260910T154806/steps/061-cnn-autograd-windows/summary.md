windows is now differentiable in grad() (CNN Phase 5): scatter-add backward for the overlapping sliding-window gather, so the im2col conv matmul(reshape(windows(x,[kh,kw]),...),flatten(k)) is trainable. 6 sites mirroring take/rotate: NodeKind::Windows, windows_backward kernel, Tensor::windows, propagate_dense arm, grad dispatch + call_windows. Verified: gradcheck 3 (1-D overlap + 2-D rank-changing central-diff), grad(sum(windows(x,[3])),x)->[1,2,3,2,1] e2e; full eval 1397 pass, autograd 38 pass, ZERO regressions. Docs: grad() supported list now names windows + the already-lowered structural ops, drops false reduce_add (coming next). Also fixed a stale equation_svg_tests assertion from the stacked-limits step (limits now stacked <text>, not inline sub). Binaries rebuilt release+debug (no demo uses grad(windows) yet, so no pages deploy this step). sw-checklist 41->43 (exception: grad_kernels+tensor_reduce 7->8, both at their correct home; the deferred autograd-crate-split saga retires these). No version bump, no stable.