Pin the weighted-convolution capability that works TODAY (no C2). Add a regression test in mlpl-eval that asserts: (1) matmul(reshape(windows(img,[kh,kw]),[oy*ox, C*kh*kw]), reshape(kernel,[C*kh*kw])) computes a correct multi-channel conv -- verify img=reshape(range(18),[2,3,3]), kernel=range(8) -> [268,296,352,380]; (2) the reduce-only patch-sum (all-ones kernel) form still holds; (3) the windows axis order stays [out_y,out_x,C,kh,kw]. TDD, rebuild binaries, redeploy DEV/nightly only (no version bump, no stable touch).