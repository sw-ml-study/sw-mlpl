upstream-asks #10: run_script's value is a lossy display rendering (string child came back quoted, unrecoverable). Added value_raw carrying the child's actual final value unmodified (additive -- value unchanged, existing callers/tests keep working). err -> payload; error/exit -> empty. Verified e2e (value_raw='hello' bare string, type string). 6 script_exec tests pass; lang-reference updated. #20 (WasmSession.eval_with_values wasm-bindgen export) is the same root cause at the browser boundary and remains -- browser-only, not blocking CLI regression. sw-checklist 41/543, no new FAIL.