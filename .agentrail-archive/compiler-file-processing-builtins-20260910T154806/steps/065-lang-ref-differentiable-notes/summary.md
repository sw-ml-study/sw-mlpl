Full pre-release doc-accuracy sweep (expanded from the windows/flatten table-row note per 'check all docs'). A read-only audit + the downstream's pre-release check found stale claims across every surface; all fixed: lang-reference Broadcasting-Rules section (said arrays must match shape -- rewrote to trailing-axis, fwd+bwd), pool2d arity ([kh,kw] vector not scalar), windows/flatten rows + grad list (differentiable notes); glossary CNN entry (conv2d ships + trainable, dropped 'not in MLPL'), U-Net, windows/flatten/reduce/reduce_add differentiability, v0.21 stamp dropped; catalog :help windows/flatten/reduce; README + usage broadcasting (trailing-axis). Wiki updated (Language-Tour + Capability-Matrix broadcasting) + errata entry (sw-mlpl.wiki 868a593). Binaries rebuilt (catalog->:help); pages rebuilt+deployed (browser ? modal + :help current -- resolves blog-agent flag #2). Clarified C4: multi-named reduce is a comma-string, not a string array (no false vector claim in docs). Commits c58c3da4 (docs) + db57660b (pages). sw-checklist 43 held. No version bump, no stable.