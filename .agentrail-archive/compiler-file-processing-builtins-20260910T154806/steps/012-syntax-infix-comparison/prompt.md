Tier-1 language compactness: add infix comparison operators < > <= >= == != (lower precedence than arithmetic), lowering to gt/lt/le/ge/eq/ne semantics on BOTH interpreter and compiler. Unary minus already works. Add ge/le/ne runtime builtins (mlpl-runtime-math), BinOpKind variants, lexer tokens (1- and 2-char), parser precedence (prec 0), eval_ops name mapping, compiler lower.rs closures. TDD across lexer/parser/interp/compiler. Refresh binaries after.