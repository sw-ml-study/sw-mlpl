Add recently-shipped language primitives to the LANGUAGE-tour demos so learners see them (not just the ML demos). In 'Thinking in Arrays': add a windows + multi-axis/named reduce idiom (e.g. label the windowed axes then reduce by name, and reduce([positions]) for multi-axis). In 'Compact Idioms': add a str_eq string-equality idiom. Keep each def with a docstring; demo-smoke + def gate + readme_counts (no new demo, only lines added to existing demos -- count unchanged); rebuild+redeploy DEV/nightly only; no version bump, no stable.