Make the svg('equation') renderer and the CNN/conv demo equations mathematically specific, matching the paper (Zhao et al. 2018, MDPI Algorithms 11(10):159). RENDERER: a big operator (sum, product, integral) followed by _{...}/^{...} must render the limits STACKED above and below the operator (display/limits style), centered -- not inline to the right. Keep inline sub/superscript for ordinary vars (x_i, y^2). Widen the canvas/line-height to fit the stacked limits. TDD (assert the stacked-limit SVG structure). DEMOS+LITERATE: replace the abbreviated conv sums with the paper's actual bounds -- y[r,x,y] = sum_{q=1}^{Q} sum_{u=1}^{Mw} sum_{v=1}^{Nw} W[r,q,u,v] X[q,x+u,y+v] -- name Q/Mw/Nw/R, and add the index-origin note (paper counts from 1; the array language from 0, so q=1..Q -> channels 0..Q-1, and reduce axis numbers are 0-based). Update Convolution-from-the-equation + CNN (simple) + moving-average + cnn-convolution.org. Rebuild binaries; republish literate; rebuild+redeploy DEV/nightly only; no version bump, no stable.