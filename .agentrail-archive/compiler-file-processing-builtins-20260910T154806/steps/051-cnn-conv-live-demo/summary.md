Shipped the 'Convolution from the equation' Vision demo (DEMOS 96->97). First line renders the defining conv equation via svg(text,'equation'); code computes it with the im2col spelling (windows -> reshape -> matmul, no rank broadcast), following demo-ml-utils' probes/convolution-reference.mlpl im2col construction. Verified exact parity vs the built-in conv2d oracle on a 1-channel edge filter (-6 everywhere) and a 2-channel case (max diff 0). README demo count 96->97; also fixed a pre-existing 481->482 glossary-count drift. Gates green: demo-smoke, def-docstring, readme_counts; no interpreter change so no binary rebuild. Deployed DEV/nightly (bundle a6cf42e73b26c9ca, commits 426b4e37+a904969b); no version bump, no stable. NOTE: authored from demo-ml-utils' reference construction, NOT vendored -- they have no canonical demos/cnn source yet (only the probe); the agreed vendor+sync-check pin is deferred until they publish one, and they should adopt/refine this as canonical.