Ship the convolution-from-the-equation live demo (was 'Phase 6'), now UNBLOCKED (works today via matmul; NOT gated on C2). Per the agreed ownership split, demo-ml-utils owns the canonical demos/cnn/*.mlpl sources + docs; sw-mlpl VENDORS the accepted source into the web-demos pipeline with a sync-check test pinning vendored==upstream (readme_counts pattern). Add the demo to demos.toml (docstrings + mlpl-fmt + demo-smoke gates), rebuild + redeploy DEV/nightly only. Coordinate with the demo-ml-utils agent for the accepted source before vendoring.