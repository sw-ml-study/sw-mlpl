CNN-from-equations Phase 1 (demo-ml-utils C5): compress drops axis labels -- a defect (rotate/take preserve them). compress removes slices along one axis, changing no axis identity, so labels must survive. Fix: propagate a.labels() to the result in mlpl-runtime-array compress. TDD. Downstream consumes via rebuilt CLI binaries (commit-identified); version bump + web deploy batched at the C1/0.22.0 boundary.