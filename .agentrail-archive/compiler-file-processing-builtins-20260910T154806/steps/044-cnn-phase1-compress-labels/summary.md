CNN Phase 1 (demo-ml-utils C5): compress now preserves axis labels (it removes slices, changing no axis identity, so labels survive like rotate/take). Fixed in mlpl-runtime-array compress by propagating a.labels(). Verified e2e: labels(compress([0,1,1], M[image_y,image_x])) = image_y,image_x (was ','). Test compress_preserves_axis_labels. Fresh release+debug binaries so downstream consumes by commit. Version bump + web deploy batched at C1/0.22.0. sw-checklist 41 failed (no new FAIL; +2 warnings are the uncommitted Phase-2 windows.rs in the tree).