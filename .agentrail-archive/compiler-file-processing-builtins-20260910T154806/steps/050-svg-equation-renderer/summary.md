Built svg(text,'equation') high-contrast math renderer. New crate mlpl-viz-math (equation.rs layout + spans.rs sub/superscript parsing), re-exported via mlpl-viz umbrella; eval_svg branches on the equation type (string arg0). Unicode math passes through; _x/^x and _{}/^{} become tspans. Showcased in the J-inspired Moving Average demo (renders MA_w[i]=(1/w) sum_j x[i+j]). Verified end-to-end via native REPL cache-SVG (well-formed, dark bg/light glyphs, summation+subscript). Tests: 5 renderer units + 2 eval + demo-smoke green; clippy/fmt/markdown clean; sw-checklist 41 failed HELD, warnings +2 (exception, new-crate clean split). Docs: lang-reference equation type. Rebuilt binaries release+debug. Deployed DEV/nightly only (bundle 65ee181cd826ceb7, commits ef9d9213+9ada2145); no version bump, no stable. Tier-0 Unicode-in-comments confirmed usable (ASCII rule is md-only).