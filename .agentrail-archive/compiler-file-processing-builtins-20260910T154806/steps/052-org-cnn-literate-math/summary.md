Added examples/literate/cnn-convolution.org -- a literate CNN derivation whose 3 equations render as INLINE IMAGES via MLPL's own svg(text,'equation') (no LaTeX/MathJax/network, per user's chosen approach). Discovered ob-mlpl already inlines svg output (viz-path -> <svg> -> #+begin_export html), so ZERO code changes needed. Code blocks are live: windows->reshape->matmul im2col conv, checked block-by-block vs the native conv2d oracle (-6 edge filter; exact multi-channel parity). Published via emacs -Q --batch (MLPL_REPL_CMD=target/release/mlpl-repl, Emacs.app). Added [[literate]] mapping + index card linking it to the Convolution demo. Gates: literate_parity green (fixed initial 9-vs-6 block drift by switching equation blocks to :exports both). Deployed DEV/nightly (bundle at 5616ab61, commits 98234c51+53f286f7); no version bump, no stable. Files: cnn-convolution.org (source), pages/literate/cnn-convolution.html (deployed, 3 inline eq SVGs), examples+pages index.html, demos.toml [[literate]].