Add an emacs-org literate CNN doc (examples/literate/cnn-convolution.org) deriving the 2-D convolution from the equation via the im2col MLPL (windows->reshape->matmul, verified vs conv2d). APPROACH (user chose, no LaTeX install): render the math as inline images using MLPL's own svg(text,'equation') renderer inside mlpl babel blocks with :results html -- each equation exports as a self-contained inline SVG in the HTML (no LaTeX toolchain, no MathJax/CDN). Verify ob-mlpl supports :results html; publish via examples/literate/publish.sh (locate emacs -- Emacs.app not on PATH); copy the html into pages/literate/, rebuild+deploy DEV/nightly only. No version bump, no stable.