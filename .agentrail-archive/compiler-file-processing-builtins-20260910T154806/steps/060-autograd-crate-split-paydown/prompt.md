Tech-debt spike: split the saturated autograd crates so windows-autograd (and future ops) land cleanly, AND retire FAILs/warnings. Targets: mlpl-autograd-tape is at 7 modules (warn) with grad_kernels at 7 fns (warn); mlpl-autograd is at 7 modules with backward_shape at 8 fns (FAIL) and tensor_shape at 7 fns (warn). Plan: extract the pure gradient kernels into a new sibling crate (grad_kernels split into shape-kernels + arith-kernels), dropping the tape crate's module count and grad_kernels fn count; split backward_shape by responsibility to retire its FAIL and make room; ensure tensor-method modules have headroom. Retire >=1 FAIL and several warnings. Rewire deps; full autograd + eval regression must stay green (1396 eval tests, autograd gradchecks). Then reopen cnn-autograd-windows.