Fixed reflow infinite-loop on a lone '}' (restore point to the brace when bolp, else it re-processes the same brace forever -- hit any 'stmt; }' block). Applied the working formatter to the two dense hand-authored files (tictactoe-demo 58->228, tictactoe-minimax 50->153); both whitespace-only vs HEAD, minimax include_str! value test passes, demo runs clean. Left other .mlpl as-is (already-commented demos, long-expression demos with no blocks, generated web-demos).