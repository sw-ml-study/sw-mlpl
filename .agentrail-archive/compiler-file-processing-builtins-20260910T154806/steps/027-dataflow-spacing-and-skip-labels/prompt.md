Two ../emufpga UPSTREAM.md asks. Ask 6: caller spacing control -- optional col_gap/row_gap overrides on the nodes record (auto-widen for long labels stays a floor). Ask 5B: a skip-layer edge label landing on the intermediate box -- place EVERY forward edge's label at the center of the first column gap after its source (always clear of boxes), so a skip-edge label can never land on a box it routes past; column_gaps sizes gaps for skip edges too. layout.rs computes per-edge label anchors (Geo bundle); render consumes p.label_at. TDD.