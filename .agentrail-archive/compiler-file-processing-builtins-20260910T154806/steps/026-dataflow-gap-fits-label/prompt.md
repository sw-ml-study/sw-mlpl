Size each dataflow column gap to its edge labels (../emufpga ask: the renderer measures text for node boxes via CHAR_W but used a fixed COL_GAP=96, so a 20-char edge label overflowed the gap onto the boxes). layout.rs column_gaps() widens each inter-column gap to fit the widest adjacent-edge label plate that sits in it (plate width + margin), floored at COL_GAP; place() and canvas() consume per-gap widths. Shared LABEL_CHAR_W/LABEL_PAD constants used by both the gap sizing and the render.rs plate. TDD.