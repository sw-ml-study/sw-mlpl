Add rendered math equations (svg(text,'equation')) to the pre-existing 'CNN (simple)' demo in demos.toml so it shows the math behind its conv2d/relu/pool2d pipeline, matching the 'Convolution from the equation' and moving-average demos. Add equation lines for the convolution sum, relu(z)=max(0,z), and 2-D max pooling at the right points. demo-smoke + def-docstring gates; rebuild+redeploy DEV/nightly only; no version bump, no stable.