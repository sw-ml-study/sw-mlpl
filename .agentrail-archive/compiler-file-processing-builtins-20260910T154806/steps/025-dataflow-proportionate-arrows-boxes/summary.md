Fixed two dataflow rendering defects from the emufpga lessons: (1) arrowheads scaled with stroke-width (default markerUnits=strokeWidth) so log-scale width-9 edges grew ~80px triangles -> set markerUnits=userSpaceOnUse for constant 10px arrowheads; (2) node labels overflowed the fixed 128px box -> box_width() fits each box to its label (chars*CHAR_W+2*BOX_PAD, min NODE_W) and layout lays out variable-width centered columns; Positioned carries per-node node_w consumed by render.rs (boxes+edge endpoints) and groups.rs (bands). Short labels keep 128px min so existing diagrams/coords unchanged. 8 crate tests pass; verified e2e in fresh release binary on the residency graph (269MB box now 220px wide, no monster arrowhead). Fresh release/debug mlpl-repl+mlpl-build rebuilt. sw-checklist held 44 failed (exception; back_edges inlined adjacency to keep layout.rs at the 7-fn ceiling, 538->539 warnings, no new FAIL).