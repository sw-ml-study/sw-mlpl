Lower type_of/1 and equal/2 to the compile-to-Rust path (wc's remaining pair; equal/2 is the downstream's CURRENT blocker). Both are eval-layer on Value in the interpreter -- reproduce on CVal in mlpl-rt-value: type_of(v) -> CVal::Str of the value kind (Arr->"array", Str->"string", StrList->"string-list", Record->"record", Result->"result"; match mlpl-eval-types value_kind strings); equal(a,b) -> scalar DenseArray 0.0/1.0 via a structural CVal equality (mirror mlpl-value-structural value_equal). Add rt fns in mlpl-rt-value (a text/struct module) + re-export via mlpl-rt facade. type_of is 1-arg CVal->CVal (join the CvalIo registry row -- no new emit). equal is 2-arg CVal,CVal->DenseArray: add a small Emit (both args lower_cval, returns DenseArray). Add dispatch_coverage variants. TDD RED first: gated e2e -- type_of([1,2])->"array", type_of("hi")->"string"; equal([1,2,3],[1,2,3])->1, equal([1,2],[1,3])->0, equal("a","a")->1. Hold sw-checklist. After push refresh release+debug mlpl-repl+mlpl-build. Commit source before agentrail complete.