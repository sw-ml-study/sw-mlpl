Fourth step of extensions-dynamic-load. Document the provider cdylib packaging convention and prove the FULL value boundary crosses dlopen, not just a scalar. (1) Extend the fixture (or add a second) so the dynamically loaded provider exercises arrays IN and record/handle returns OUT across the dlopen edge -- reuse the shipped array/record/handle marshaling; a TDD test loads it dynamically and asserts an array arg sums and a returned record reads by field, proving parity with the static path. (2) Write the packaging convention doc: a provider crate sets crate-type cdylib, exports sw_mlpl_extension_v1, names its library libNAME.<ext> so load_extension(NAME) discovers it; note MLPL_EXTENSION_PATH and that the interpreter/compiled paths share the loader. Update docs/extensions-dynamic-load-design.md and add a user-facing note. Keep functions under 25 lines; keep sw-checklist fails/warns decreasing. Gate, commit, complete queuing docs-close.