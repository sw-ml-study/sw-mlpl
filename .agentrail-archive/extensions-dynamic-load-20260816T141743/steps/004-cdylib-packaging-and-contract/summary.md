cdylib packaging convention + array-across-dlopen proof. Documented the provider packaging convention in the design doc (crate-type cdylib, export sw_mlpl_extension_v1, libNAME.ext for discovery, MLPL_EXTENSION_PATH, shared loader). Extended mlpl-ext-testdylib with a sum(array) function (split into a provider module + a func descriptor helper to stay within budget), and load_extension_tests now evals testext:sum([10,20,30,40])->100 through the dlopen'd provider -- a dense f64 array crosses IN via the same host marshaling record/handle returns use (proven statically). Checklist 44/523 held net-zero.