Second step of extensions-dynamic-load. Extract the dlopen-and-register logic from the gate test into a REUSABLE loader function the interpreter (and later the compiler/runtime) call. Add a loader (e.g. load_c_extension taking a path, returning Result of the registered namespace name or a clear error string) in a NEW small crate (say mlpl-extension-loader) -- NOT in mlpl-extension-cabi, which is at its 7-module ceiling. The loader uses libloading to dlopen the path, resolves the sw_mlpl_extension_v1 entry symbol, calls it, validates + register_c_extension's the descriptor (reuse the existing struct_size and abi_version validation), returns the namespace on success, and HOLDS the Library handle for the process lifetime (leak or store in a process-global; no dlclose in v1) so the extension code stays mapped. TDD: point it at the mlpl-ext-testdylib artifact, assert it returns the namespace and that testext:answer then resolves and invokes to 42.0; a bad path, a missing entry symbol, and a bad descriptor each return a clear Err, not a crash. Keep libloading confined to the loader crate. Keep functions under 25 lines; keep sw-checklist fails/warns decreasing (a new small crate about 1 warn is acceptable with the exception trailer). Gate, commit, complete queuing discovery-and-builtin.