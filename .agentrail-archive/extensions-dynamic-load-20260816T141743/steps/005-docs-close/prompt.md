Final step of extensions-dynamic-load. Document and close the saga. Add user-facing docs for the load_extension builtin + MLPL_EXTENSION_PATH (a short section, WHAT and HOW only, no saga/step/date framing per the user-facing-docs rule) -- likely in docs/lang-reference.md or a small user doc, cross-linking docs/extensions-dynamic-load-design.md for the provider packaging convention. Update the demo-extensions upstream-contract note / downstream-unblock-plan: dynamic loading (B3) SHIPPED for the interpreter (cdylib provider, dlopen loader, MLPL_EXTENSION_PATH discovery, load_extension builtin, value boundary across dlopen); note the provider builds as a cdylib and the small-compiled-app path still needs the compiler-parity follow-on (lowering load_extension + extension/Port calls). Update the wiki Documentation-Errata. Queue follow-ons in docs/future-sagas-queue.md: extensions-compiler-parity (compile the app that dlopens the extension), dlclose/unload, manifest/trust resolver (A7), and the use-facade (B1). Keep sw-checklist fails/warns decreasing. Then run agentrail complete with --done to close the saga.