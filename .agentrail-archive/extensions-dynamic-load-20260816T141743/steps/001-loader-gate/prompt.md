First step of extensions-dynamic-load. Land a KEPT regression of the B3 spike (which lived in scratchpad and is gone): prove dynamic loading works in CI. Add (a) a small in-repo TEST cdylib crate (crate-type = ["cdylib"], edition 2024, in an appropriate workspace -- likely a new crate under components/runtime-core/crates/, e.g. mlpl-ext-testdylib) that path-depends on mlpl-extension-cabi and exports a #[no_mangle]/#[unsafe(no_mangle)]  returning a leaked descriptor with one arity-0 function 'answer' returning F64 42.0; (b) a test that builds/locates that cdylib artifact and, using libloading, dlopens it, resolves the entry symbol, register_c_extension's the descriptor, then mlpl_extension_registry::lookup("<ns>:answer") + call_contained -> assert F64(42.0). Handle the artifact path robustly (the shared repo-root target/, platform extension .dylib/.so). Keep gate #1 result (register_c_extension already validates struct_size + abi_version). Keep functions <=25 lines; keep sw-checklist fails/warns decreasing (new crate ~1 warn is expected -- use the exception trailer per the ratchet-reality memory). Gate, commit, complete queuing dynamic-loader.