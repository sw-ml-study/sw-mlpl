Initialized extensions-dynamic-load (B3) saga. Scoped dynamic extension loading: dlopen a cdylib provider exporting sw_mlpl_extension_v1, register via the existing C descriptor ABI, so a small compiled MLPL app loads a large extension (winit/wgpu native3d) at run time rather than one giant binary. Gate PROVEN by a spike (cdylib + dlopen + register + invoke -> 42.0 on macOS; the C ABI already crosses dlopen unchanged). Wrote docs/extensions-dynamic-load-design.md.