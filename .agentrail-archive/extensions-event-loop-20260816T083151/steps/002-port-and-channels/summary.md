General Port primitive: an opaque handle (reusing Value::ExtHandle, mint-on-open, non-forgeable) backed by a command channel (MLPL->far) + event channel (far->MLPL), share-nothing owned Values (no Arc<Mutex>). Env-local ports table + register_port/resolve_port. Builtins port_send/port_recv/port_poll (poll drains to a {count,items} batch). Dropped Environment's unused Clone derive so it can own a Receiver (Send preserved). TDD: command round-trips through a spawned echo worker (cross-thread); poll drains then empties. Checklist 44/522 held; regression net (binding/scope/extension suites) green.