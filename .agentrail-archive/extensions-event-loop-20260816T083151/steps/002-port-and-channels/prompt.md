Second step of extensions-event-loop. Build the general Port primitive: a native handle (Value-level) backed by a command channel (MLPL->far end) and an event channel (far end->MLPL), share-nothing with OWNED messages (no Arc<Mutex>, Rust Send-enforced). Add builtins port_send(p, value) (send a command), port_poll(p) (non-blocking try_recv, returns a record BATCH of queued events), and port_recv(p) (blocking receive of one event). The far end in tests is an in-process ECHO worker (a std::sync::mpsc pair on a spawned thread) -- NO winit, NO provider yet. TDD: MLPL sends commands and receives events back through the port with no shared mutable state; port_poll on an empty queue returns an empty batch; the echo worker and MLPL run on different threads. Keep the Port a first-class opaque handle that MLPL passes around but cannot forge (mint-on-open, like the extension native handles). Keep functions correctly formatted under 25 lines by refactoring; keep sw-checklist fails/warns decreasing. Gate, commit, complete queuing handler-registry-and-dispatch.