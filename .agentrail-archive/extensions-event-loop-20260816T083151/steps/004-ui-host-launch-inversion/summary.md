'Parked main' launch inversion: run_applet_with_host runs the interpreter on a worker (port bound as 'port') while a UI host runs on the calling/main thread feeding events + draining commands -- the browser model, share-nothing channels, gate-#1 Send preserved. require_ui_host_thread policy guard: launcher marks its env UI-capable; connect/serve worker evals fail with a clear err. TDD headless: applet folds inc/inc -> 2, submits commands [1,2], clean quit on close; guard errors by default. Checklist 44/523 held (trimmed PortEndpoints doc to keep env.rs under file-LOC; mlpl-eval-env at module ceiling, split queued).