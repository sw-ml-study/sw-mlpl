Fourth step of extensions-event-loop. Build the 'parked main' UI-host launch inversion so the interpreter runs on a worker while the main thread is free for a native UI event loop -- with a HEADLESS host double for CI (no winit). Provide: (1) a launch entry that runs the MLPL interpreter on a spawned worker thread and keeps the main thread available to a UI host; (2) a main-thread UI-host abstraction (a trait or fn) the provider plugs into -- for tests, a headless host that is handed the far ends of a port's channels, forwards a SCRIPTED event stream into the event channel, and drains commands from the command channel into a Vec; (3) a policy guard: opening a native-UI port requires the local main-thread launch path -- in connect/serve (worker-thread eval) it returns a clear err. TDD (headless, no winit): an MLPL applet defines handlers + run(port); the harness runs the interpreter on a worker, the headless host on the main test thread feeds events + collects the commands the handlers submitted, and asserts the applet folded state + emitted the expected commands + quit cleanly on close. Keep gate #1's worker-thread Send invariant. Keep functions <=25 lines; keep sw-checklist fails/warns decreasing (env-env is at its 7-module ceiling -- put new host code in mlpl-eval or a NEW small crate, not mlpl-eval-env). Gate, commit, complete queuing provider-abi-and-contract.