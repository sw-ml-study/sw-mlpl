Bounded event delivery + provider contract. port_poll(port, limit) returns at most limit events (pull-path bounded delivery); drain moved to port_util::event_batch. Documented the provider contract in docs/extensions-event-loop-design.md: provider is the UI host (FnOnce(cmd_rx, ev_tx) on main, owns winit + non-blocking pump, forwards {kind,...} events, drains commands), native-UI local-only via require_ui_host_thread. TDD headless: provider-shaped host feeds key/pointer/key/close, per-kind handlers fold (12) + submit ordered commands, clean quit; bounded poll capped at limit. Rust provider plugs in as the host closure -- no new C-ABI, no winit in sw-mlpl. Checklist 44/523 held.