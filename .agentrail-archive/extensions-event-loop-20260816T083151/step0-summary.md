Initialized extensions-event-loop (B6) saga. Scoped the browser-model architecture (two threads, share-nothing message passing: winit/wgpu UI host on main, MLPL interpreter + single-threaded dispatch loop on a worker, a general Port primitive connecting them; callbacks fire on the worker not the UI thread). Wrote docs/extensions-event-loop-design.md. Proved gate #1 with a worker-thread spike (Environment/Value Send + eval on a spawned worker + env moved main->worker).