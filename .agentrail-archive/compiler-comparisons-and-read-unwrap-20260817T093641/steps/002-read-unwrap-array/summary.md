Fixed read_bytes(...)? -> array flow: a lower_darr bridge (CVal->DenseArray via CVal::arr()) applied at every DenseArray-position site, plus Ctx cval_bindings tracking (in lower_assign AND fndef body-lowering) so a CVal-typed binding like b = read_bytes(p)? bridges correctly and a CVal final binding yields itself. produces_cval gained an Ident arm + pub(crate). Gated e2e: a wc-newline-count shape (2) and byte-sum (230); all 24 build e2es pass. Net-zero sw-checklist. Follow-ons noted: compiled user-fn CVal params + top-level ? unsupported.