Close the compiler-comparisons-and-read-unwrap saga. Document the lowered gt/lt/eq comparisons (interpreter parity; ne/le/ge/not/and/or are NOT builtins -- compose via gt/lt/eq + arithmetic: ge(a,b)=eq(lt(a,b),0), not(x)=eq(x,0), and=mul) and the read_bytes(...)? -> array-op flow as compile-to-Rust capable (lang-reference and/or compiler capability doc, WHAT/HOW only). Update docs/future-sagas-queue.md: this rung SHIPPED; the remaining GNU-clone gates are the streaming saga (chunked stdin + loop-to-EOF bounded reads) and, for path-driven tools, compiled user-fn CVal parameters + top-level ? / unwrap. Refresh docs/companion-demo-file-processing.md (comparisons + read-unwrap cleared; file-based bounded-memory tools now expressible via while-loops over read_bytes(off,len)). Update the wiki errata if a compiled-capability claim flips. Also queue the fncall.rs handler/dispatch split tech-debt (lower_fncall is a 120-line FAIL; the crate is at its 7-module ceiling). Hold sw-checklist. Then agentrail complete --done.