Lower tally/1 to the compile-to-Rust path (the downstream reports 'unsupported construct: fncall tally/1'). Match the interpreter's tally semantics exactly (confirm from the interpreter dispatch: tally is a structural builtin, likely the count of major cells = shape[0] returned as a scalar). Add a runtime primitive if needed (mirror the local shape/rank fns in mlpl-rt) or reuse a DenseArray method; wire it into the fncall REGISTRY with the right Emit shape (DenseArray arg via lower_darr -> scalar DenseArray result, so it composes and accepts a read_bytes(p)? result). Add the dispatch_coverage_tests Builtin::Tally variant. TDD RED first with a gated MLPL_BUILD_TESTS=1 e2e: tally over a rank-1 array (and a read-derived byte array) prints the element count. Hold or lower sw-checklist (exception if test-only holds). After the push, rebuild target/release + target/debug mlpl-repl AND mlpl-build (per keep-release-debug-binaries-fresh). Commit source before agentrail complete.