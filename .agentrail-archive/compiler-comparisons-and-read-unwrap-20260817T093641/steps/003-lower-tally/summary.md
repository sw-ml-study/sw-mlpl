Lowered tally/1 (downstream blocker). tally(a) = leading-axis length (shape[0], scalar 1 for rank-0), byte-for-byte the interpreter's mlpl-runtime-array tally. Local mlpl-rt::tally mirrors rank; added to the UnaryRt registry row (so it accepts a read_bytes(p)? result via lower_darr) + dispatch-coverage Tally variant. Gated e2e: tally(iota(4))=4, [10,20,30]=3, read_bytes(...)?=5. Net-zero sw-checklist. Refreshed release+debug mlpl-build per the keep-binaries-fresh policy.