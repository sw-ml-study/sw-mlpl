Lowered gt/lt/eq comparisons to the compile-to-Rust path (the eq/2 blocker). Interpreter-parity scope: gt/lt/eq are the only elementwise comparison builtins (mlpl-runtime-math binary_cmp); ne/le/ge/not/and/or aren't builtins and weren't invented -- those three + arithmetic are complete for 0/1 logic. New Emit::Cmp mirrors the arithmetic binop lowering (ApplyBinopExt::apply_binop closure, eq within f64::EPSILON); inlined to hold fncall.rs at 7 functions. dispatch-coverage Gt/Lt/Eq + gated e2e (scalar, elementwise broadcast, composed ge). Net-zero sw-checklist.