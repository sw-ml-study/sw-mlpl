Added ModelSpec::LinearLora variant and lora(m, rank, alpha, seed) builtin. Clones the source tree, replaces every Linear with a LinearLora that owns base W/b + adapter A [in,rank] + adapter B [rank,out]. A = randn(seed+i) * 1/sqrt(in) with running counter i for independent adapters; B = zeros (LoRA zero-init-B). Auto-marks cloned base W/b frozen via env.frozen_params so adam(loss, lora_m) only moves adapters; users can unfreeze(lora_m) for full fine-tuning. Device tags propagate. Nested lora() rejected; rank-zero and rank-too-large errors surface. Used structs (LoraCtx, AdapterInit) to keep arg counts clean per design review. Exhaustive-match obligations handled in apply_model, apply_model_tape, clone_spec, inspect::render_spec. apply() on a lora-wrapped model errors with a clear "step 003" pointer until the forward pass ships. 13 tests cover shapes, init values, auto-freeze, chain walking, disjoint names, and every error path.