Added "LoRA Fine-Tuning" web tutorial lesson to apps/mlpl-web/src/lessons.rs with a tiny interactive variant (V=8, d=4, rank=2) that runs a 10-step fine-tune in WASM. Walks through freeze/unfreeze/lora, the zero-init-B identity property, and the auto-freeze semantics. Wrote docs/using-lora.md (~230 lines) covering the three-builtin surface, initialization, auto-freeze (including the step 004 embed+attn amendment), device propagation, both demo files, measured MLX numbers (0.15x warm ratio), the three-environment matrix with Saga 21 pointer, parity testing across the four test files, and the not-shipped follow-up list (QLoRA, selective attachment, merge_lora, multi-adapter, real checkpoints, nested lora). Rebuilt pages/ via scripts/build-pages.sh and committed both source + WASM bundle.