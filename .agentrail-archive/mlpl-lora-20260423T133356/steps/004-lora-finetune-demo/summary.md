Authored demos/lora_finetune.mlpl: pre-trains a Tiny LM on Shakespeare (100 adam steps), wraps with rank-8 LoRA adapters via student = lora(base, 8, 16.0, 0), fine-tunes 50 steps on a synthetic Q/A instruction corpus tokenized with the shared BPE. Manual run: final adapter-only fine-tune loss ~2.18 cross-entropy. Two integration tests (lora_finetune_tests.rs) pin: (a) every non-adapter param bit-identical across fine-tune, every adapter moved, B starts at zero, losses finite; (b) source base binding untouched by student training (clone + auto-freeze). Added Demo 8 entry to docs/demos-scripts.md. Also amended step 002 auto-freeze to cover embedding tables + attention projections + MLP base linears, not just Linear W/b -- required for the demo to have correct LoRA semantics on any model containing embed/attention.