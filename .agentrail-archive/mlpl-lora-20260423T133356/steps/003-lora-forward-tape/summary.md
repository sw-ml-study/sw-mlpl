Implemented LinearLora forward in apply_model (CPU) + apply_model_tape (autograd) paths: y = X @ W + (alpha/rank) * X @ A @ B + b. Replaced both "not yet implemented" stubs. Threads through existing matmul/mul/add dispatch so MLX picks it up for free. Bias broadcast matches the Linear pattern (ones([n,1]) @ b) so the autograd tape composes with unchanged backward formulas. 7 tests: forward identity (B=0 makes adapter a no-op), hand-constructed formula correctness, alpha/rank scaling, adam isolation (W/b frozen bit-identical, A/B move), unfreeze round-trip, chain composition, triple-gated CPU-vs-MLX parity within 1e-3.