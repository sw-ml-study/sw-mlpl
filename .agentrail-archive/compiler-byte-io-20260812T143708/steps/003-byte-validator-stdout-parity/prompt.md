Add a loud-reject array->bytes validator to the compiler runtime (mirror mlpl-eval fs_bytes.rs array_to_bytes: rank<=1, each cell an integer in 0..=255 else a descriptive error) and rewire compiled write_stdout to validate (reject, not truncate via x as u8) and PROPAGATE write/flush errors as an err Result instead of the current discard (let _ = ...). This closes the two interpreter-parity gaps for stdout and gives write_bytes/append_bytes their validator. Register nothing new in fncall yet (write_stdout already dispatches); the change is in the runtime (mlpl-rt-value io.rs) + a validator module. TDD + gated e2e; hold sw-checklist.