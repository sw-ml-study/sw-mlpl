Lowered tokenize_bytes/1 (str -> byte DenseArray), decode_bytes/1 (byte array -> str, reusing array_to_bytes; invalid bytes hard-error), to_int/1 (str -> ok(int)/err). New mlpl-rt-value text.rs; all lower via CvalIo. rt-value unit tests + gated e2e (round-trip Hi; to_int(42)->ok(42)); full 17-test e2e green. This completes the byte/bit/text lowering surface of compiler-byte-io (read/write/append bytes, file_size, bit ops, byte/text conversions all lower with interpreter-parity validation + Results). Remaining compiler-parity work (process-semantics: print/eprint/exit/read_stdin + pristine stdout; capstone) is separate sagas per docs/downstream-unblock-plan.md. sw-checklist 557/49/523.