Refactored lower_fncall from a growing (name,arity) match into a data-driven registry + dispatcher (chain of responsibility): a builtins! macro builds a REGISTRY of [names]@arity=>Emit rows; the pub(crate) dispatcher interprets the Emit tag (simple shapes inline, bit/label/matmul delegate). Recovered compile-time exhaustiveness via dispatch_coverage_tests (exhaustive Builtin enum match + reverse registry check + mlpl-runtime-bits::NAMES cross-check) and a supported_builtin_names() introspection API. Behavior-preserving: full lower-rs suite + all 10 gated e2e green. sw-checklist HOLD 574/2/386; fncall.rs 288 lines (headroom for byte-IO rows).