run_script(path, {source_dir, data_dir, capture}) per their section 8: evaluate the file in a FRESH Environment (fs_root + source provider from opts or defaults; test-event buffer enabled when capture: 1) via the chunked include runner so include and file-accurate spans behave; return ok(record) with status ("ok"/"err"/"error"/exit code), value repr, events_kind: "test_events" + events string list when captured; hard errors inside the script become structured data, not this eval's failure. TDD against real temp scripts incl. one emitting events and one with include. Then run their gate. Docs rows. Gates; commit; push.