Sandboxed filesystem builtins per their section 8: fs_walk(root, {recursive, kind, pattern}) -> lexical string list; read_text / write_text / remove_text... remove_path; ALL Result-returning, sandbox-contained (generalize the include FsProvider canonicalized-root seam; sandbox root = --source-dir or script dir; REPL without script: cwd), exact text + Unicode, explicit symlink policy (not followed by default), glob pattern = simple * wildcard. TDD incl. traversal rejection + round-trip. Docs rows. Gates; commit; push.