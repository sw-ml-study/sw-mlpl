Step 004: Show values in the detail panel.

When clicking a sculpture that has values, the detail panel shows:
- First 20 values (formatted as a table for matrices)
- Min, max, mean, std statistics
- Histogram (rendered as a simple ASCII bar chart or CSS bars)
- For large arrays: the summary stats + histogram

Pages rebuild required.