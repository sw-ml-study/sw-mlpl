Add the little-endian typed reader family: read_u8/read_u16_le/read_u32_le/read_u64_le, read_i8/read_i16_le/read_i32_le/read_i64_le, read_f32_le/read_f64_le -- each (bytes, offset) -> scalar (f64). Read  bytes at  from a Value::Bytes buffer and decode as the named dtype (little-endian), widening to f64 for the return (u64/i64 lose precision above 2^53 -- acceptable, document it). Bounds-check: offset+width must be <= data.len(), else a catchable Unsupported error; non-buffer first arg errors. This unblocks demo-ml-utils' read_u32_le(bytes, 0) header idiom. DESIGN for the module-function budget: define the whole family ONCE via a macro or a data table keyed by (name -> dtype), dispatched by one handler eval_read_typed(name, args) in fncall_bytes (so it stays ~5 functions -- consider moving handlers to a bytes_read.rs module if needed). Put the pure decode (bytes+offset+dtype -> f64) in mlpl-bytes as read_le(data, offset, dtype) -> Option<f64>. TDD: read_u32_le(pack([1,0,0,0],"u8"),0)==1; read_u16_le at offset 2; read_f32_le round-trips 1.5; OOB offset errors. Scope tests mlpl-eval + mlpl-bytes; clippy/fmt both workspaces; keep net-zero debt (or split a module if clean). After this, the NEXT step (big-endian + unpack) then docs+release-binary rebuild so downstream sees the whole family.