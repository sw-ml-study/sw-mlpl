Resume the typed-packed-bytes byte-io features: add reinterpret(bytes, "dtype") -> bytes that RE-VIEWS the same underlying bytes under a new element dtype WITHOUT numeric conversion (serves demo-ml-utils #2). Constraint: the buffer's byte length must be divisible by the new dtype's width (else a catchable Unsupported error); the raw Vec<u8> is copied unchanged, only the dtype tag changes. TDD: RED first -- reinterpret(pack([1,0,0,0],"u8"),"u32") displays <bytes: u32[1]> and read-backs to 1 (after typed readers land) / for now check the dtype tag + byte length preserved; reinterpret of a 3-byte u8 buffer to u32 errors. Add to fncall_bytes (watch the 5-function module limit -- extract a pure reinterpret helper into mlpl-bytes if needed). Scope tests mlpl-eval + mlpl-bytes; clippy/fmt both workspaces; ratchet both axes down (or continue the module-split spike inline if a clean win exists).