Step 004 (reinterpret): added reinterpret(bytes,"dtype") -> re-views the same packed Vec<u8> under a new element dtype without numeric conversion (demo-ml-utils #2 Safetensors/GGUF idiom); byte length must divide the new dtype width else catchable error; non-buffer arg errors. Extracted parse_dtype + expect_bytes into a bytes_args module (fncall_bytes stays 4 fns). 15 bytes_tests green, clippy/fmt clean. Net-zero debt (44/521); paydown lives in the spike track.