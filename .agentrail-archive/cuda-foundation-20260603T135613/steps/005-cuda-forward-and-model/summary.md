mlpl-cuda-forward (traceable candle primitives: embed/rms_norm gamma-free eps=1e-8/causal_attention single-head/cross_entropy one-hot/lora_linear) + mlpl-cuda-model (DemoWeights + demo_forward assembling the demo architecture) in a new components/cuda-model/ workspace. All ops fully candle-traceable (no CPU gather) so autograd flows through the whole graph; formulas match CPU apply_model exactly. GPU TDD: primitives parity-tested vs hand-computed refs; a gated test trains the head adapters via candle AdamW and cross-entropy drops -- gradients flow through the assembled model on the RTX 5060 Ti. Warning-free; baseline held (111 failed / 502 warnings). clippy clean cuda+stub.