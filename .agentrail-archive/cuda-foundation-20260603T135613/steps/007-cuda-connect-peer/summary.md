This Arch box is a connect-mode CUDA peer. mlpl-serve + mlpl-repl gained a cuda feature passthrough; mlpl-serve --features cuda --bind 0.0.0.0 serves the playground AND runs device('cuda') in-process on the GPU (the GPU box IS the server -- no separate peer). GET /v1/devices reports the real device set (test asserts it reflects the build). UI probes /v1/devices (fetch_devices + use_peer_devices hook) and gates via pure tested demo_disabled(cap,connected,peer_devices): CUDA demo enabled + MLX disabled on this peer (and vice versa on Apple). build-pages.sh made Linux-portable; WASM playground rebuilt. Verified end-to-end on the LAN: CUDA LoRA fine-tune runs on the RTX 5060 Ti through the connect server. cuda-foundation vertical slice COMPLETE. clippy clean; failures held at baseline (handlers FAIL retired via devices.rs extraction); +1 warning (new-module artifact, exception).