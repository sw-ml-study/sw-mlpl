Build-matrix spike: make the cuda feature compile as a no-op stub on non-Linux hosts, mirroring how the mlx feature stubs on Linux. Today mlpl-cuda-rt's cuda feature pulls candle-core/cuda -> cudarc, whose build.rs hard-requires nvcc, so any --features cuda (or --all-features) build fails on macOS. Move the candle-core dependency behind a target-specific table -- [target.'cfg(all(target_os = "linux", target_arch = "x86_64"))'.dependencies] -- in every crate that pulls it: mlpl-cuda-rt, mlpl-cuda-elementwise, mlpl-cuda-nn, mlpl-cuda-train (components/cuda-rt), plus audit mlpl-cuda-eval / mlpl-cuda-model / mlpl-serve and any other Cargo.toml referencing candle or cudarc. Keep the feature flags themselves host-independent; the crates' internal cfg gates already stub the code paths, but the off-target manifest must not RESOLVE the candle dep at all. Adjust any #[cfg(feature = "cuda")] code that assumes candle types exist off-target (pair the cfg with target_os/target_arch where needed). TDD where practical: a Mac-side check is 'cargo clippy --all-targets --all-features -- -D warnings' succeeding in components/eval and components/cuda-rt; add a comment-documented CI expectation instead of a test where compile-time behavior cannot be unit-tested. Acceptance: on macOS, --all-features clippy/build succeeds across touched workspaces (cuda stubs, mlx active); behavior on the Linux/CUDA box must be UNCHANGED -- if Linux verification is not possible this session, say so explicitly in the step summary so it gets verified after the move back. Then update .claude/commands/mw-cp.md + the CLAUDE.md /mw-cp list: the per-host clippy matrix collapses back to --all-features everywhere once Linux-verified (leave a 'pending Linux verification' note until then). sw-checklist ratchet applies.