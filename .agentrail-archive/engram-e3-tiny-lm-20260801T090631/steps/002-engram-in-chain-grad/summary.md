Tape path for in-chain Engram: apply_model_tape Chain arm captures the chain's original input as eager token ids and lowers Engram children through engram_tape (new engram_arm in apply.rs); bare-Engram tape error now mirrors the eager wording; in-chain errors carry the chain context. Frozen-base training works with zero optimizer changes (Chain::params already recursed). 4 new tests: chain-vs-manual grad bit-equality, frozen-base train (base bit-identical, only addressed rows move, loss -10x), fd gradcheck through the chain, loud bare-engram error. Paydown: split model_eval_engram.rs into init/values modules; warnings 342->340.