ASCII-only markdown spike: transliterated all non-ASCII in the hand-owned failing files (10 docs/ + books files; meaning-bearing APL/UI glyphs got prose rewrites); gen-changes.sh now transliterates historical commit subjects so CHANGES.md passes; operative gate docs standardized on the sw-markdown-checker binary name (upstream repo name noted, archival docs untouched). Repo-wide failing files 27 -> 12 real + AGENTS.md alias, all out of scope: .agentrail-archive (autogenerated), vendor/, CLAUDE.md managed block (em dashes emitted by 'agentrail instructions apply' -- UPSTREAM AGENTRAIL FIX NEEDED, flagged to user). Gotcha found + fixed in follow-up commit: perl -i materialized 4 intentional symlinks (AGENTS.md, books prefaces); restored + warned in the playbook.