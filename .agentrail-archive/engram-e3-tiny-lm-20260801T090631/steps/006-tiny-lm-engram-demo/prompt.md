E3 final step: the Tiny LM + Engram demo with stats panel (plan step 6). In demos.toml (category Engram) add a 'Tiny LM + Engram' demo: build the standard tiny LM base chain(embed(V,d,0), residual(chain(rms_norm(d), causal_attention(d,1,1))), rms_norm(d), linear(d,V,4)) on a small byte corpus (tokenize_bytes + shift_pairs like the existing tiny_lm demos), train it briefly, then freeze(base pieces), e = engram(d, [2], heads, slots, head_dim, seed), m2 = chain with e inserted after the attention block (in-chain threading from steps 1-2), train N more steps engram-only (adam on m2), and show: baseline-vs-engram loss improvement, engram_stats fields before/after (s.unique_rows, s.collisions, s.nonzero_rows growing from 0, s.gate_mean rising from ~0.119) as the stats panel narrative. Add [[progress_notes]] entries for the two train blocks; bump the pinned counts in mlpl-web-demos/tests/metadata_codegen.rs if capabilities/literate/progress tables change; keep the demo CPU-feasible in-browser (small V via byte tokens, short train budgets -- follow the existing Tiny LM train demo's sizes; release-profile demo smoke test must stay in budget). Verify with the web-demos release suite (cargo test --release in components/web-demos incl. the demo smoke), then docs refresh + ./scripts/build-pages.sh + commit pages/ + push (Pages deploy gate). If a local mlpl-serve is running, follow the server rebuild/restart gate from CLAUDE.md since engram_stats is a new builtin on the connect path (ask the user before restarting a server they are using). Completing this step finishes saga E3 -- use --done, and note that E4 (mlx-persistent-tensors) is next per docs/engram-sagas-plan.md.