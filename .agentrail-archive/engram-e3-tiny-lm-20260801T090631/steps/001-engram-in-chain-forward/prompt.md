E3 step 1: eager in-chain Engram forward. Wire ModelSpec::Engram into apply_model (components/eval/crates/mlpl-eval-models/src/model_apply.rs) so an engram layer can sit inside a Chain: the chain threads its ORIGINAL input (the token ids consumed by the leading embed layer) to any Engram layer, which computes apply_engram(e, h, ids) with h = the running hidden state. TDD: tests in mlpl-eval (new engram_chain_tests.rs) proving (a) apply(chain(embed, residual(chain(rms_norm, causal_attention)), engram_e, rms_norm, linear), ids) equals the manually composed pipeline using explicit apply_engram; (b) with a fresh (near-identity) engram the chain output matches the same chain WITHOUT the engram layer exactly; (c) an Engram layer in a chain whose input is not a plausible id vector (rank/float checks) or an Engram used bare via apply() yields a clear error, not a panic. Keep model_apply.rs a facade -- new logic goes in a named file (e.g. model_apply_engram_chain.rs or extend model_apply_engram.rs). Scoped tests: cargo test -p mlpl-eval --test engram_chain_tests --test engram_apply_tests plus clippy/fmt in components/eval; sw-checklist ratchet applies.