Create components/web-viz-ir/ workspace + mlpl-web-viz-ir crate per docs/viz-ir-plan.md. Sub-modules: kind.rs, axis.rs (AxisDim), node.rs (VizNode), attention.rs (AttentionViz + AttentionLayout + Token), sankey.rs (SankeyViz + SankeyNode + SankeyEdge). lib.rs facade only. All types derive Serialize (passes through serde-wasm-bindgen to JS). NOTE: VizNode does NOT carry stats/values/elements (those stay on ShapeInfo to avoid a viz3d <-> viz-ir cycle); VizNode carries only the semantic payload. Wire to viz3d: add 'pub viz: Option<VizNode>' to ShapeInfo + add viz-ir as a viz3d dep. JS dispatch site: stage3d.js renderInspectorBody recognizes userData.viz.kind, falls through to current behavior when absent. Build pages + push.