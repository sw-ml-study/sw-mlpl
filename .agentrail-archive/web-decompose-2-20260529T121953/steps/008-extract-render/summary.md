Extracted render cluster (15 files, 27 -> 10 modules in mlpl-web) into components/web-render/ as 4-crate sub-component: types (state + app_callbacks + args) at level 0, aux (entry + editor_panel + plotly_panel + resize_handle + tutorial) at level 1, core (callbacks + modes + panel) at level 2, shell (render + shell + chrome + footer + header + modebar + overlays) at level 3. Resolved the core<->shell cycle by moving shared structs to types and the render() entry fn to shell. Threaded BUILD_* env vars from mlpl-web through RenderArgs since only mlpl-web's build.rs emits them. ONE step from retiring the Crate Module Count FAIL on mlpl-web.