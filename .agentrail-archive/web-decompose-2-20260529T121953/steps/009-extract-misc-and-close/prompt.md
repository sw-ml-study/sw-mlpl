Finish saga 82. Split render_entry (74 LOC FAIL) by responsibility. Extract any leftover mlpl-web src modules (scroll.rs, readme_counts.rs, app_active.rs, app_hooks.rs, app_log.rs, app.rs, lib.rs, main*.rs, build*.rs, the static help.rs that may still be there) to bring mlpl-web below the 7-module crate-module-count limit. Sub-component name: web-misc + web-app. Run sw-checklist and report the saga close delta.