flatten builtin shipped + iota deprecated: naming policy (meaningful names canonical + arity-locked; APL glyphs heritage-only) landed in lang-reference; audit found only iota+pi as Greek builtin names (pi = math constant, stays). iota still evaluates (regression-pinned) but is gone from NAMES/catalog/completion/help/docs and EVERY example surface incl demos.toml, all .mlpl scripts, literate .org + published .html twins (literate_parity gate), and test fixtures; compile-to-Rust lowering now accepts range (it previously only knew iota). Drive-by: parser precedence loop while-let fix (new clippy lint). Pages rebuilt+deployed. SECOND commit ef9dbe04: docs/apl2-parity-gap.md (composition/HOF ASCII designs as user priority, grade/sort, strings-as-data, nested arrays, general-purpose runtime; classic-app litmus tests; APL-in-early-ML history; ML-first ordering) + Track 7 (4 parity sagas) in future-sagas-queue.md. Warnings held 340.