User-directed naming cleanup (2026-08-01): (1) Add flatten(a) builtin -- ravel semantics, reshape to rank-1 [elem_count] -- in mlpl-runtime-array (NAMES + dispatch + transform impl within budgets), catalog entry in inspect_groups, docs/lang-reference.md row, completion surface; replace the reshape(X, [reduce_mul(shape(X))]) idiom in demos/examples where it improves readability. (2) Deprecate iota: it stays dispatching to range(n) so existing scripts keep working, but the catalog + lang-reference mark it 'DEPRECATED alias of range(n); use range' and it disappears from every example surface: demos.toml (pages rebuild gate!), demos/*.mlpl, examples/*.mlpl, web help.rs, prompt_text.rs, completion suggestions, parser/eval/web test fixtures (switch to range), keeping exactly ONE test that pins the deprecated alias still evaluates. Audit note: full builtin sweep found only iota+pi as Greek names; pi() is the math constant and stays. Naming policy line for docs: meaningful names are canonical and arity-locked one-to-one; APL glyph names are heritage aliases only. Scoped gates per touched workspace; web change => build-pages + commit pages/; sw-checklist ratchet applies.