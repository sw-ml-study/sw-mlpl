Resident tape forward shipped: NodeData.value is a TensorHandle; resident tapes (flag set by enable_resident_tape under device(mlx)) upload leaves once and keep elementwise/matmul/softmax/reductions/reshape/transpose/fused-CE forwards on the device as ONE lazy graph; structural ops fall back per-op and the chain re-uploads after. DELETED materialize_tape_on_mlx and its 2x forward. Tensor::value() materializes via to_dense so every caller compiled unchanged; grads stay host DenseArray (step 4). All 23 eval mlx parity suites green on the resident forward; 9 CPU suites bit-green; new resident_tape_tests pin is_dev residency, structural-fallback rejoin, and non-resident bit-exactness. Warnings 337->336. ALSO (user request, separate commit c8c4571e): docs/future-sagas-queue.md records 4 roadmap sagas (wiki errata cleanup, mHC feature per docs/mHc-research.txt now committed, engram-cuda E10, mhc-cuda) -- both commits belong to this step.