E4 step 4: backward stays resident. Make NodeData.grad a TensorHandle and run the backward formulas on the device for the core op set when the tape is resident: matmul (upstream @ b^T, a^T @ upstream via BinKind::Matmul + UnaryKind::Transpose), elementwise binaries (mul/add/sub/div backward via dev binary/unary composition), unary activations (op.backward composed from dev ops: e.g. tanh' = 1 - y^2 needs scalar constants -- add a DeviceOps::full/scalar-fill or upload-scalar helper if missing), sum/mean broadcast-fill, softmax backward (y * (up - sum(up*y, axis, keep)) via dev ops), cross-entropy backward (softmax(logits) - one_hot: needs a one-hot or scatter helper -- consider DeviceOps::one_hot(targets, v) or compute via CPU fallback initially and measure). accumulate() must handle handle+handle (dev add), handle+host, host+host. Structural backward (patchify/concat/stack/take/rotate/unbroadcast) may download-fallback. grad() reads at the seam (Tensor::grad, eval_grad's return, grad_optim's per-param g) materialize via to_dense as today. Keep CPU path bit-exact (Cpu arms use existing kernels verbatim). TDD: extend resident_tape_tests with per-op backward parity vs the CPU tape at FP32_TOL incl a full tiny-LM-shaped chain; grad_mlx/train_mlx/thicket suites stay green; CPU suites bit-green. Budgets: backward.rs/backward_shape.rs/ops.rs will grow -- plan splits early (autograd crate is at 7 modules: rebalance INTO mlpl-autograd-tape (6 modules) if needed). sw-checklist ratchet applies.