E4 step 7: the deliverable gate. (1) Rerun cargo bench -p mlpl-bench --features mlx --bench mlx_vs_cpu and record the after-table in docs/benchmarks.md next to the step-1 Metal baseline (before: tiny_lm_train_step 0.008x). Investigate the tiny_lm_train_step + lora_finetune_step + neural_thicket numbers: the train benches should now hit the resident path (verify the bench harness actually trains under device(mlx) with the generic path, not gpu_step -- if gpu_step intercepts, bench both). ACCEPTANCE per the saga plan: tiny_lm_train_step MLX/CPU warm ratio > 1.0; if missed, profile where the remaining time goes (per-op lock overhead? attention's stack/take CPU fallbacks forcing mid-graph downloads? per-step scalar sync?) and fix the top bottleneck in-step if bounded (e.g. Take/Stack/Concat device kernels via mlx split/concat ops), else document the honest number + the identified bottleneck as the next step's scope. (2) Parity hardening: a 30-step tiny-LM base-model train trajectory test MLX vs CPU with a documented tolerance (state never returns to f64 between steps); engram-in-chain 30-step ride-along. (3) docs/benchmarks.md narrative update (which of the four documented costs are now gone: per-op round-trip for tape ops, no-fusion within a step, 2x forward, host moment round-trip). sw-checklist ratchet applies (spike owed).