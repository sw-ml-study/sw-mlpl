E4 step 1: enable Metal. Flip mlx-rs features from [accelerate] to [accelerate, metal] in the four crates that pin it (mlpl-mlx-rt, mlpl-mlx-forward, mlpl-mlx-train, mlpl-mlx-model -- grep for 'mlx-rs' across Cargo.tomls to catch all; triple gates unchanged). The Metal toolchain is present on this host (Xcode 26.6; xcrun --sdk macosx --find metal succeeds) -- update the stale 'metal needs full Xcode, later step' comments in mlpl-mlx-rt/Cargo.toml. Verify at runtime that MLX now executes on the GPU: a small test or example printing mlx_rs default device / Device::gpu availability (mlx-rt tests are the natural home; keep the assertion tolerant so a CI Mac without Metal skips rather than fails). Run the existing mlx parity suites (mlpl-mlx-rt tests, mlpl-eval train_mlx_tests + grad_mlx_tests + neural_thicket_mlx_demo_tests with --features mlx) -- f32 GPU results must stay within the documented FP32_TOL. Then rerun 'cargo bench -p mlpl-bench --features mlx --bench mlx_vs_cpu' and add a 'Metal-only delta' baseline table to docs/benchmarks.md (same four benchmarks; note this is BEFORE any persistent-tensor work, so the remaining gap is the per-op round-trip + no-fusion + 2x-forward costs). Watch disk (bench pulls Criterion; ~580G free at saga start). Scoped gates: the mlx crates' workspaces (native-rt, mlx-model) + components/eval mlx test files; clippy --all-features + fmt in touched workspaces; sw-markdown-checker on benchmarks.md; sw-checklist ratchet applies.