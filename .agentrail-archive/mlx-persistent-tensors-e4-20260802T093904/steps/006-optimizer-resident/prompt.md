E4 step 6: optimizer + weights stay resident across the train loop. THE step the deliverable hangs on: today grad_optim's momentum_sgd/adam rebuild a FULL tape per parameter per step (eval_grad per param -- N params = N forwards!) and round-trip every weight and moment through f64 host maps. Rework for device(mlx): (a) ONE tape per step: build the loss once, backward once, then read every param's grad from that single tape (grad.rs already seeds all env.params() as leaves -- add an eval_grads_all(loss_expr, param_names, env) that returns handles; keep per-param eval_grad for the public grad() builtin); (b) params bound under device(mlx) upload ONCE into a resident param store (env-side: a HashMap<String, TensorHandle> parallel to vars, or TensorHandle values inside OptimizerState) and stay resident across train N iterations -- the tape's leaf seeding pulls the resident handle instead of re-uploading the DenseArray each step; (c) adam/momentum moments live as TensorHandle in OptimizerState.buffers (type change mirrors NodeData.grad); the update math runs via dev ops (fill for scalars/bias-correction, binary compose; sqrt -- ADD UnaryKind::Sqrt + mlx impl + parity test); (d) explicit sync: device-block exit + into_array/display reads + last_losses per-step scalar download only; env.set of the updated weight keeps the resident handle, materializing lazily on any host read. Keep CPU path bit-exact and adam's GPU fast-path (gpu_step) untouched. TDD: train_mlx_tests trajectory parity ACROSS 3+ steps (state never returns to f64 between steps -- tolerance may need documented widening), engram-in-chain frozen-base train parity ride-along, resident_tape_tests extension asserting weights+moments is_dev across loop iterations and exactly one download per step (the loss scalar). Budgets: grad_optim.rs is fat already -- plan a grad_optim_resident.rs sibling; OptimizerState lives in wasm-reachable mlpl-eval-state (TensorHandle is wasm-clean ok). sw-checklist ratchet applies (spike owed acknowledged).