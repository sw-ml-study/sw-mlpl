TensorHandle seam shipped: wasm-clean mlpl-tensor-handle crate (TensorHandle {Cpu, Dev}, DeviceArray trait with to_dense as THE sync point, 6-method DeviceOps behind kind enums, OnceLock registry, explicit placement policy: >=1 resident operand runs on device, two host operands = NotResident) + mlpl-mlx-handle backend (MlxBuf over a LAZY mlx_rs::Array, zero evals inside ops, dims from eager shape inference, everything under mlx_op_lock, unsafe Sync documented by the lock discipline). Gotcha found by the parity suite: as_slice on a transpose VIEW reads physical order -- downloads now materialize via flatten-reshape. 7 parity tests (all op families at FP32_TOL, fused CE, 5-node lazy chain with one download) + backend-free tests green on every target incl wasm32. Warnings 338->337 with 6 structural warnings paid down by 7 retirements.