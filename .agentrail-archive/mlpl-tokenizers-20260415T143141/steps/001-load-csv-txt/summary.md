Tokenizers step 001 shipped. First std::fs contact from the interpreter. load('rel.csv') reads comma-delimited files with header auto-detection (non-numeric first row is stripped) and returns a DenseArray; load('rel.txt') returns Value::Str with file contents. load_preloaded('name') reads from a compiled-in registry (one entry: tiny_corpus) for the web REPL path. Sandbox via Environment::data_dir + new --data-dir flag on mlpl-repl; unset data_dir blocks load() with a pointer to load_preloaded. Path validation walks components manually rejecting absolute, rooted, and parent-traversal components without dereferencing symlinks. CSV label attachment dropped from step scope: MLPL's one-label-per-axis model doesn't fit per-column names cleanly; documented as a future load_labeled variant. 12 new tests; 632 total; all quality gates clean; sw-checklist unchanged.