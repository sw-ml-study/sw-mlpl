Saga 12 (mlpl-tokenizers) COMPLETE as v0.9.0. All 9 steps shipped across file IO + dataset prep + streaming iter + byte-level BPE (train/apply/decode with byte-lossless round-trip) + experiment tracking (experiment form + :experiments + compare). Byproduct fixes: UTF-8 lexer (replaced b-as-char Latin-1 with proper UTF-8 leading-byte decode) and string-valued variable bindings (new Environment::strings map + Assign path + Ident lookup) -- the latter surfaced from smoke-testing the tokenizer tutorial lesson. Release step: workspace 0.8.0 -> 0.9.0, banner strings updated, three new tutorial lessons (Loading Data, Tokenizing Text, Experiments) smoke-tested via mlpl-repl -f, docs flipped across saga.md/status.md/plan.md/are-we-driven-yet.md with downstream version-target bumps, pages rebuilt. Tag v0.9.0-tokenizers created and pushed. 720 tests pass; sw-checklist FAIL set stable at 16 (one over pre-saga-12 baseline, noted across the saga commits). Deferred: experiment source-text capture, compare rendered as chart, load sandbox symlink canonicalization.