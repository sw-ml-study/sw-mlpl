Tokenizers step 004 shipped. Byte-level tokenizer + Value::Tokenizer scaffolding. New TokenizerSpec enum with a single ByteLevel variant; new Value::Tokenizer variant parallel to Value::Model; Environment gains tokenizers HashMap + set_tokenizer/get_tokenizer accessors; Assign path learned to store tokenizer bindings. Three new builtins: tokenize_bytes(str) returns rank-1 array of UTF-8 byte indices; decode_bytes(tokens) inverse with fractional/out-of-range/higher-rank errors; tokenizer() returns a ByteLevel tokenizer. :describe reports kind as 'byte-level tokenizer (vocab=256)'. Central dispatch() helper in tokenizer.rs keeps eval.rs under 500-line file-LOC cap. Tests include an explicit-UTF-8-byte decode verifying decoding is correct independent of the MLPL lexer's long-standing Latin-1 string-literal behavior. 13 new tests; 668 total; sw-checklist 16 FAIL (same as step 003, +1 from pre-saga baseline).