Tokenizers step 007 shipped. New experiment "name" { body } scoped form: runs body, on exit scans for _metric-suffixed scalar vars, snapshots param shapes, appends ExperimentRecord to env.experiment_log. When env.exp_dir is set (terminal REPL --exp-dir), also writes run.json to <exp_dir>/<name>/<timestamp_ns>/run.json via serde_json. Web REPL leaves exp_dir unset so records live in memory only. New Token::Experiment keyword, Expr::Experiment AST variant (name must be a string literal), parse_experiment method in stmts.rs reusing parse_braced_body. New experiment.rs module with ExperimentRecord + ParamShape (serde::Serialize). Environment gains exp_dir + experiment_log + vars_iter accessors. mlpl-lower-rs and grad.rs both reject Expr::Experiment as Unsupported. 14 new tests covering parser surface, metric capture variants, empty/non-scalar edge cases, on-disk json write, in-memory log-only path, and lower-rs rejection. 708 tests total; sw-checklist unchanged. Source-text capture deferred to a follow-up (needs source threading through eval).