Tokenizers step 006 shipped. apply_tokenizer(tok, text) and decode(tok, tokens) builtins close the tokenizer round-trip invariant. ByteLevel path delegates to tokenize_bytes/decode_bytes; BpeMerges path walks merges in training order via new bpe::apply_trained (reusing the greedy left-to-right merge routine from training for exact semantic parity) and bpe::decode_token (recursive expansion of merged ids back to bytes). First-arg resolution accepts bound idents or inline tokenizer() calls. Apply/decode dispatch lives in bpe.rs so tokenizer.rs stays at 7-fn budget. 12 new tests including byte-level parity with simple primitives, ByteLevel ASCII + UTF-8 round-trip, BPE compression evidence (shape shrinks, ids >= 256 appear), BPE round-trip on training corpus, UNSEEN text, and UTF-8. 694 tests total; sw-checklist unchanged.