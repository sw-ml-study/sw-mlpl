Shipped estimate_train(model, steps, batch, seq [, dtype_bytes]) -> [5] feasibility builtin. Pure-math over ModelSpec tree: params, vram (fwd + grad + Adam + 4x activation safety factor), disk per checkpoint, flops (per node type), wall-seconds (flops / gflops; default 50, overridable via mlpl_device_throughput_gflops env key for step 002's calibrate_device to write). 11 tests green (tiny linear exact, chain additivity, LoRA freeze, embedding scaling, attention flops/params bump, dtype halving, throughput override, 4 error paths). Module stays at 6 fns within 7-fn budget by using ModelSpec::params() for the sum pass and a separate walk for hidden/depth stats. sw-checklist baseline (102/139) held.