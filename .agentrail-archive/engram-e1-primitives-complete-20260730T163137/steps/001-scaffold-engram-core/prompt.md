Saga E1 step 1 (docs/engram-sagas-plan.md): create components/engram/ with mlpl-engram-core -- EngramSpec/HashSpec types, validation (empty ngrams, orders < 2, zero heads/slots/dims, overflow, mismatches), derived accounting (table_rows, retrieved_width, parameter_count, bytes_for), and the f64-exact mul-add-mod rolling-hash REFERENCE implementation with golden fixtures the MLX backend must later match bit-for-bit. Pure crate, no tensor deps. TDD throughout; budgets clean.