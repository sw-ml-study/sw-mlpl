User question 2026-08-04: s.train failed because keywords are rejected as record FIELD names -- a parser limitation, not a necessity (JS allows obj.class; Rust/Python do not). Fix mlpl-parser so keyword tokens are accepted as identifiers in the two unambiguous positions: after '.' in field access and as record-literal keys ({train: 1}). TDD in parser tests + eval record tests (s.train, s.if, {train: 1}.train); keep keywords reserved everywhere else; lang-reference note. Scoped gates.