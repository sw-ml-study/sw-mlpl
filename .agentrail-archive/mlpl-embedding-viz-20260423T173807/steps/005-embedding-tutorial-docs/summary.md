Added "Embedding exploration" web tutorial lesson with a 6-point 3-D fixture (two clusters) that renders instantly in WASM; walks the user through pairwise_sqdist, knn, svg(scatter3d), tsne, svg(scatter). Wrote docs/using-embeddings.md (~280 lines) covering the three builtins + scatter3d viz type + the PCA composition pattern + demo walkthrough + parity testing + the deferred follow-up list (UMAP, pca builtin, RAG, interactive 3-D, MLX tsne, embed_table builtin, Barnes-Hut). Rebuilt pages/. Also refactored: extracted the three "advanced" lessons (Neural Thickets, LoRA, Embedding) out of lessons.rs into a new lessons_advanced.rs module to stay under the 500 LOC budget; lessons.rs goes 513 -> 467, lessons_advanced.rs is 62.