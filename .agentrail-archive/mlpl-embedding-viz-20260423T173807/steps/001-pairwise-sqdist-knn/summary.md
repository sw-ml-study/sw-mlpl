Added pairwise_sqdist(X) + knn(X, k) builtins in a new crates/mlpl-runtime/src/embedding_builtins.rs module. pairwise_sqdist: [N,D] -> [N,N] squared Euclidean distances with symmetry + zero-diagonal + empty-input guarantees. knn: [N,D] + scalar k -> [N,k] integer indices, stable sort by ascending distance with self-exclusion and lower-index tie-break. Direct O(N^2 * D) loops today; MLX dispatch via matmul-composition is a deferred optimization. 13 tests cover exact values, symmetry, self-exclusion, and every error path. Two contracts pin the surface.