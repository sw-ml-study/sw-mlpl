Colon commands over /eval: eval_handler branches before parse for ':'-prefixed programs; run_eval calls mlpl_eval::inspect on the session env (Some(out) -> string response; None -> 'unknown command' error). Client: when connected, route the inspect-command family through connect_eval so :fns/:list/:vars reflect the SERVER session; local mode unchanged. TDD: serve api test (:fns after def over /eval; :list returns verbatim source), plus client eligibility test. Rebuild + restart the local server per the split-brain gate.