User direction: (1) server inspect route -- colon introspection commands (:fns, :list, :vars, :describe, :models, :tokenizers, :tags, :untag, :wsid, :experiments, :introspect) work over /eval so connect-mode sessions (where prefers_connect demos define functions server-side) can be inspected; client routes the inspect family to the server when connected. (2) The help-tab markdown pipe tables render as real HTML tables (the <pre> dump misaligns columns) via a small table-aware segmenter in doc_dialog + CSS.