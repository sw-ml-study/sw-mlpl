Step 4 downstream-relay-and-close: closed axis-naming-unification. Verified ../demo-ml-utils/probes/named-axis-reduce.mlpl passes (result 0) with the rebuilt binary -- caught+fixed a real bug the probe exposed: the labeled reduce family must accept a VECTOR of axis names (reduce_add/reduce_mul now multi-axis, matching reduce; argmax/softmax stay single-axis). Docs (glossary/lang-reference) document the shared axis forms; wiki errata marked resolved earlier. Marked the saga SHIPPED in docs/future-sagas-queue.md and refreshed CHANGES. Interpreter binaries rebuilt (release+debug). The label-vs-reduce inconsistency is fully resolved across interpreter + compiler.