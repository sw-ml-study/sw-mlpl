test_event_sink(:u:f): register ONE user-fn callback (builtin refs rejected; returns ok(1)). emit_test_event(record): validate envelope -- version scalar 1 (incompatible loud), kind in the 9 documented kinds (unknown loud, listing known), suite+name strings, status in {passed,failed,skipped,expected_failure} for *_end; unknown ADDITIVE fields pass through untouched. Deliver the record to the sink via invoke_user_fn_values; a sink err/hard error becomes emit's err; no sink = validate-only ok(1). TDD (events_core_tests). Scoped gates; ratchet or exception; commit before complete; push.