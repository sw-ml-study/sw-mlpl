Game of Life saga step 4: live frame_trace finale. Add a 'frame' SSE event (name, step, shape, flat values) alongside 'metric' in mlpl-serve sse + eval metric-sink plumbing; a generation-keyed frame_trace store in mlpl-web-eval (loss_trace pattern); a live grid panel polling it (LiveLossPanel pattern) so a connect-mode repeat-loop shows generations as they arrive; the SMIL value persists as the final entry (live-then-persist like the loss curve). Demo last act updated to run the crowned variant connect-side. Pages rebuild.