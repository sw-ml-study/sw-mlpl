Live frame_trace finale shipped: MetricSink::emit_frame default + emit_frame(name, step, x) builtin (no-op locally, streams when a sink is installed); SseEvent::Frame end-to-end through /eval_stream (sse_tests prove 3 emissions arrive); SseFeed::with_generation -> generation-keyed frame_trace store (latest-frame, chunk-split + pruning tested natively); LiveLifePanel polls it and repaints with the same life renderer as the SMIL widget; Capability::prefers_connect routes the Life demo server-side when connected so its loop streams generations live; grid cells sized up 2.5x per feedback; index.html footer retired 4 metadata warnings. 4/297 -> 4/294. Pages deployed.