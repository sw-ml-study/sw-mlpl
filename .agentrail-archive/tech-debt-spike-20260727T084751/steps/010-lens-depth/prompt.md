Upgrade the lens demonstration per user feedback ('too simple -- what about lensing into a complex multi-dimensional structure, and how are indexing errors surfaced?'). (1) Structure Zoo lens act upgraded: composed lens into the rank-3 [2,2,3] tensor (get outside-in via chained take; put inside-out via mask matmul or the put builtin if landed), disp narration at each depth. (2) u:get_safe: bounds-checked get returning ok(cell) / err({kind: 'index', message: ...}) -- contrast with the raw hard error from take. (3) get_value/get_error Option projections (0-or-1-element arrays, zilde flavor) as builtins per docs/option-result-design.md. (4) Commit docs/option-result-design.md + glossary entries. Validate all demo lines; erroring lines asserted to error in the smoke test.