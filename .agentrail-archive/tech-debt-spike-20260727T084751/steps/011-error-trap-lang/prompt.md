Error-handling language features (design: docs/option-result-design.md 'Control-flow adapters'). (1) try/catch expression blocks: 'try { body } catch e { handler }' -- catches HARD EvalErrors only, binds e to the canonical error record {kind, message} (Dyalog DMX precedent); err() values flow through untouched; whole form yields body-or-handler value. finally deferred (no user-visible resources yet). (2) Result propagation: postfix '?' inside def u: bodies (ok unwraps, err early-returns the Result via the existing return machinery; top-level ? behaves as unwrap) -- interim spelled form check(expr) acceptable if parser cost is high. (3) Canonical error record builder shared with safe-lens work. TDD at mlpl-eval level; keep functions <=25 LOC post-fmt.