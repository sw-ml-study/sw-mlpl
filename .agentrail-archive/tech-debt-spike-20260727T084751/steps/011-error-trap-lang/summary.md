try/catch + ? shipped TDD: TryCatch expression (hard errors only; e = {kind, message} via new error_kind tags; err values + signals flow through), postfix ? desugaring to check() with ReturnSignal early-return and loud top-level UnwrapOnErr, u: fns accept Result/string/record args. Surfaced in :help/:ask/lang-reference; error-handling doc updated. Fixed 5 pre-existing stale lower-rs assertions. 13 new tests + 146 parser + 40 codegen green. 4/293 -> 4/292. Pages rebuilt.