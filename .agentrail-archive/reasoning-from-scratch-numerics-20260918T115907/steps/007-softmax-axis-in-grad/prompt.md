RS2: softmax(a, axis) differentiable in grad + rank-3. The tape softmax machinery exists (softmax_forward/softmax_backward/NodeKind::Softmax{axis}) but Tensor::softmax is last-axis/rank<=2 only and the surface builtin does not thread an axis arg. Either do this after autograd-partition (clean), or add an axis-parameterized softmax method to the already-FAILING tensor_reduce.rs (checklist-neutral, no NEW fail) and thread the axis from the eval grad path. TDD: softmax(x, axis) grad matches a manual reference; last-axis default unchanged; rank-3 supported. Rebuild repl/build release+debug + serve.