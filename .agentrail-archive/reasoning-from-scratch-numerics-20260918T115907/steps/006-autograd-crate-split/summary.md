autograd-crate-split SPIKE: analyzed the split and found the whole autograd component is at the module ceiling -- mlpl-autograd and mlpl-autograd-tape both have 7 modules, and mlpl-autograd has 3 modules already FAILING function-count (tensor_ops 8, tensor_reduce 9, backward_shape 8). No intra-crate split avoids a new Crate-Module-Count FAIL, so a clean split is a multi-crate re-architecture, not one step. Backward pass is nearly tape-only (mlpl-autograd-backward extraction viable; cross_entropy kernels move down to the tape crate). Deliverable: queued autograd-partition saga (docs/future-sagas-queue.md, commit aad1367b) with the concrete extraction plan. Interim finding: remaining RS grad items (softmax-axis, transpose, pow-general) can be added to the already-FAILING modules checklist-neutral if the partition is deferred. No code change (pure spike).