RS4: rank-3 (batched) matmul now errors with an actionable message ('Batched (rank-3+) matmul is not supported') instead of a misleading 'index has 3 components' error, on both the eager (eval_matmul) and grad-tape (call_matmul) paths, via a shared matmul_2d_guard. F19 inner-dim check still runs for rank-2 mismatches. Verified in release repl. RED->GREEN matmul_rank_tests (rank-3 clear error eager+grad, valid 2-D + matrix-vector unaffected). sw-checklist held 38/570 (LOC warning from the inline guard retired by the shared-helper extraction). Binaries rebuilt repl/build release+debug, serve release.