RS4: matmul is 2-D only and rank-3 input PANICS with a misleading error inside grad (backward .expect() on matmul). Add a clean structured error for rank>2 (or a [k]/[k,n] mismatch) in the grad matmul path (matmul analogue of F19's check_matmul_compat) so attention code gets an actionable message instead of a panic; document 2-D scope. Batched matmul itself is a later step. TDD: rank-3 matmul errors cleanly in and out of grad; valid 2-D matmul unaffected. Rebuild repl/build release+debug + serve.