RS6: bf16 and f16 dtypes for reinterpret + read. Added Bf16/F16 to ByteDtype (width 2, parse, name); read_le decodes bf16 (exact top-16-of-f32) and f16 (manual IEEE half incl. zero/subnormal/normal/inf/NaN) to f64; pack rejects them as decode-only with a clean error. Extracted push_float_le to hold the LOC gate. RED->GREEN bf16_f16_tests (reinterpret accept/reject, bf16+f16 known patterns, f16 inf/NaN/subnormal, pack-rejected); bytes_tests unchanged. Verified in release repl (bf16 1,2; f16 1,-2). sw-checklist 38/568 held. Binaries rebuilt repl/build release+debug, serve release.