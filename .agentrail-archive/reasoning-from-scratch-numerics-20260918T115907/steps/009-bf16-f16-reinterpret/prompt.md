RS6: bf16/f16 dtype decode in reinterpret, for loading real model weights. Find the reinterpret builtin + its dtype list (likely mlpl-runtime or mlpl-eval). Add bf16 (1 sign, 8 exp, 7 mantissa) and f16/fp16 (1 sign, 5 exp, 10 mantissa) decode from raw bytes/u16 to f64. TDD: known bit patterns decode to expected f64 (e.g. bf16 0x3F80 -> 1.0, 0x4000 -> 2.0, 0xBF80 -> -1.0; f16 0x3C00 -> 1.0, 0x4000 -> 2.0), plus a NaN/inf/subnormal case. Note the 8x transient-memory caveat in docs (each byte becomes an f64) -- a vectorized bit-level decode is the consumer's path. Rebuild repl/build release+debug (+ serve if the decode is reachable over the connect path).