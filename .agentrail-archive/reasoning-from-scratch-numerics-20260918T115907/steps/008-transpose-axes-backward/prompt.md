RS3: transpose_axes(x, perm) general-permutation is not differentiable in grad. The tape transpose (NodeKind::Transpose, transpose_backward) is reverse-axes only; Tensor::transpose reverses all axes. Add a general axis-permutation transpose to the tape (a permutation-carrying transpose node, or generalize the existing one) with backward = transpose by the inverse permutation, and wire the eval grad path so transpose_axes(x, perm) differentiates. mlpl-autograd is at the module ceiling -- add to the already-failing modules (tensor_shape/backward_shape) checklist-neutral, or defer to autograd-partition. TDD: grad through a non-trivial rank-3 permutation matches a manual reference; existing reverse-transpose unchanged. Rebuild repl/build release+debug + serve.