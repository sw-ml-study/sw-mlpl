RS3: transpose_axes(x, perm) general axis permutation is differentiable in grad. Generalized NodeKind::Transpose to carry Option<perm> (None=reverse unchanged, Some(p)=general perm, output axis i = input axis p[i]); added Tensor::transpose_axes with CPU forward and inverse-permutation backward (inv[p[i]]=i); grad dispatch via new grad_calls_transpose module validating perm is a permutation of 0..rank (clean error otherwise). Reverse-axes transpose unchanged. RED->GREEN transpose_axes_grad_tests (rank-3 [2,0,1] grad matches finite-diff; reverse-transpose grad all ones). Verified in release repl (shape [4,2,3], grad abs-sum 276). Landed on already-failing tensor_reduce + new mlpl-eval module (checklist-neutral) pending autograd-partition. sw-checklist 38/568 held. Binaries rebuilt repl/build release+debug, serve release.