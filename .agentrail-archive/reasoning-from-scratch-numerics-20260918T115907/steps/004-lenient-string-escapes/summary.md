RS-escape: MLPL string literals no longer hard-error on unknown backslash escapes. escape_char returns Option<char> (Some for " \ \n \t \r); handle_escape keeps an unknown escape's backslash literally and reprocesses the following byte through the UTF-8 path (multibyte-safe), so LaTeX/math text (\frac, \in, \boxed, \pi) and the downstream's \; all lex. Known escapes unchanged; no valid string changes meaning. Verified in release repl ("\frac{1}{2} \in S" literal, str_len("\int")=4). RED->GREEN lex-string escape_tests + mlpl-eval string_escape_tests; lexer/parser suites green. sw-checklist held 38/570. Rebuilt repl/build release+debug.