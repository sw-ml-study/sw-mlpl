Add a new demo that tokenizes a short text via train_bpe + apply_tokenizer, calls decode_each to build a StrList of per-token strings, embeds the ids, computes attention_weights, and emits a heatmap. The labels list must be emitted BEFORE the attention call so pendingTokenLabels rides across. Place the demo in components/web-demos/crates/mlpl-web-demos-basic/src/ (text-token demo fits the basic group). Demo proof: pick the new demo from the dropdown, click the attention sculpture, see BPE pieces on the heatmap axes.