User report: the Usage Guide REPL-commands table is missing :history, :tokenizers, :3d reset, and the colon-call form (:name(args), e.g. :disp(M)); the glossary lacks entries for roughly half the colon commands. Add the missing table rows, a colon-forms trichotomy section to docs/usage.md, glossary entries for every REPL colon command (:2d/:3d, :ask/:connect, :builtins, :clear, colon call, :fns/:list, :help, :history, :reset, :status, :trace, :version), update the pinned glossary count in README, rebuild pages.