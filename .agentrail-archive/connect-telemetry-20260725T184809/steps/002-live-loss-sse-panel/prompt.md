Build the in-place live loss panel for connect mode: append a point per SSE `metric` frame while a train block runs, train+val series on shared axes, chunked-repaint fallback on the local path, frame coalescing per animation frame. TDD per docs/saga-connect-telemetry.md step 2: reducer + panel-state unit tests in mlpl-web-eval / mlpl-web-render-aux, plus a connect-mode test driving a scripted metric stream. Respect code_metrics gates; rebuild pages/ only when web source lands.