Auto-emit the train loop per-step loss as an implicit "loss" metric when the block defines no explicit *_metric binding (eval_blocks.rs already computes scalar_loss; emit_metrics gains it as a fallback). Result: every streamed connect-mode train -- including the existing CUDA/MLX LoRA demos with zero content changes -- feeds the live loss panel with real training loss at no extra compute. TDD: serve sse_tests + web streaming_train_tests cover the implicit path and confirm explicit *_metric bindings suppress it.