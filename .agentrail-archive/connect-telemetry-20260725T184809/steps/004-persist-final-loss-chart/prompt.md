UX fix pulled forward at user request: when a streamed connect-mode train completes, the result entry currently demotes the live 2D loss chart to a text sparkline that lands ABOVE where the chart was -- disconcerting reversal of the during-training layout. Persist the final loss chart INTO the result entry (render loss_panel_svg from the trace at completion), placed before/above the live-loss text line: output = value + backend-load line + embedded chart + loss line. Mechanism: single-line <svg embedded in the entry output + a render_result_body composite branch (PLOTLY_MARKER precedent), split/render helpers in mlpl-web-render-live so entry.rs stays inside fn budgets. TDD native tests for the split + report builder.