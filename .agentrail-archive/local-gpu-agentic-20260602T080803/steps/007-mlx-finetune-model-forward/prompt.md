Step 007 (approach A, the bulk): express the full MLX LoRA demo model forward in mlx_rs::Array ops and wire it into the fine-tune loop. The interpreter forward is EAGER (each op materializes to a CPU DenseArray; device(mlx) only rounds post-hoc via materialize_tape_on_mlx) so it CANNOT be traced by value_and_grad -- the forward must be rebuilt as mlx ops. Demo model: chain(embed(V,d,0), residual(chain(rms_norm(d), causal_attention(d,h,1))), rms_norm(d), linear(d,V,2)), loss = cross_entropy. Work: (1) in mlpl-mlx-train (or a new sibling crate mlpl-mlx-forward if it grows the module budget -- refactor up and out), implement the forward primitives as mlx_rs::Array ops: embed via one-hot matmul, linear, lora_linear (done), causal_attention (Q/K/V/O linears + masked softmax), rms_norm (x/sqrt(mean(x^2)+eps)*gamma), cross_entropy (logsumexp - gather-target via one-hot dot; needs sum/max along the vocab axis -- verify mlx_rs sum_axes/max_axes/logsumexp API). (2) Assemble these into a loss closure |&[Array]|->Result<Vec<Array>> over the LoRA adapter params, with frozen base weights captured as constants (read from the Environment via mlpl-mlx-rt common::dense_to_mlx). (3) Wire into eval_adam (grad_optim.rs): when env.device()=='mlx' AND all trainable params are LoRA adapters, route through train_steps+MlxAdam instead of the CPU tape+adam; convert updated adapters back to DenseArray (mlx_to_dense_data) and write to the Environment each step. (4) Extend lora_mlx_demo_tests.rs to assert the MLX loss curve matches the CPU path within fp32 tol (V=16,d=8,h=1,rank=2). (5) Relabel the 'MLX LoRA fine-tune' demo hybrid->true-GPU (components/web-demos .../lm.rs and the progress NOTE). Keep sw-checklist non-regressing (split into a new crate if module budget is hit). MLX value_and_grad is not thread-safe -- serialize grad tests. DISK: scope builds to mlx crates, clean after. Commit .agentrail with source.