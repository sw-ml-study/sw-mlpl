Added single-head causal self-attention as a traceable mlx_rs forward in mlpl-mlx-forward/attention.rs (crate stays 4 modules/0 warnings): causal_attention(x,wq,wk,wv,wo,mask) = softmax((QK^T)/sqrt(d_k)+mask)V then output projection, plus causal_mask(t) builder. Single-head h=1 (matches demo). Parity-tested vs a hand-computed identity-weight case (T=2). All demo-model forward primitives now exist in MLX ops (embed, rms_norm, causal_attention, lora_linear, cross_entropy).