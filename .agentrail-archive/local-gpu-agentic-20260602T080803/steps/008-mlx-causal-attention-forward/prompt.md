Step 008: implement causal_attention as a traceable mlx_rs::Array forward in mlpl-mlx-forward (or split a sibling crate if module budget is hit -- refactor up and out). The demo uses causal_attention(d, h, seed): per the CPU path it's Q=X@Wq, K=X@Wk, V=X@Wv (each a linear, [T,d]@[d,d]), scores = Q@K^T / sqrt(d_k), apply a causal (lower-triangular) mask by adding -inf above the diagonal, softmax_axis over the key axis, ctx = attn@V, out = ctx@Wo. For h heads, slabs of width d_k=d/h (the CPU path uses selector matrices S_h: [d,d_k]; mirror that or do per-head slicing via mlx). Use mlx_rs softmax_axis (verified present) and build the causal mask as a constant additive Array (0 on/below diag, large-negative above). Parity-test vs the CPU mlpl path or a hand-computed reference for a tiny case (T=3, d=4, h=1) within fp32 tol. Keep the crate clean (0 warnings; new crate if needed). No value_and_grad in these forward-value tests -> no lock. Then step 009 assembles embed+rms_norm+causal_attention+linear/lora_linear+cross_entropy into the train_steps loss closure, wires eval_adam's MLX-LoRA path, parity-tests the loss curve vs the CPU demo (lora_mlx_demo_tests.rs), and relabels the demo hybrid->true-GPU. DISK: scope to mlx crates, clean after. Commit .agentrail with source.