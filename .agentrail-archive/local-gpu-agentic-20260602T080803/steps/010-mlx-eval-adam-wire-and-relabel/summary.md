FINALE: wired eval_adam to a true-GPU LoRA fine-tune and relabeled the demo. When device(mlx) and the model is the demo architecture (head-only LoRA), eval_adam routes to the GPU: demo_forward (embed/rms_norm/attention/lora-head/cross_entropy in mlx ops) differentiated by value_and_grad, adapters updated by a stateless MLX adam, moments persisted in env.optim_state; any other model/device falls back to the CPU tape. New gated modules grad_optim_mlx{,_demo,_step}. Shared AdamHp struct (removed a clippy-allow). Parity-tested: device(mlx) loss curve matches CPU within fp32 tol (loosened 1e-3->5e-3 for true-fp32 autodiff, justified), frozen base bit-identical, MLX path learns. Demo relabeled hybrid->true-GPU + pages rebuilt + saga doc CRITICAL-reality marked RESOLVED. Forward+backward+optimizer all on the Apple GPU. Global sw-checklist holds 12/309.