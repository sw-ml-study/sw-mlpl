Step 010 (TRUE FINALE of MLX full-GPU LoRA): wire the assembled mlpl-mlx-model demo_forward into mlpl-eval's eval_adam (grad_optim.rs:145) and relabel the demo. (1) Add an MLX branch in eval_adam: when env.device()=='mlx' AND every non-frozen param in collect_params is a LoRA adapter (name starts with __lora_A_/__lora_B_), route to a new eval_adam_mlx (put it in a new module grad_optim_mlx.rs to respect the eval crate's fn/module budget -- refactor up and out; mlpl-eval may need mlpl-mlx-model + mlpl-mlx-train as deps gated behind a feature so non-mlx/non-Apple builds are unaffected). (2) In eval_adam_mlx: extract the base weights + the 10 adapters from the Environment as mlx_rs::Array via mlpl-mlx-rt common::dense_to_mlx (make those converters pub or add a small pub helper); build the x_onehot/y_onehot from the demo's int token ids; construct a DemoWeights + an MlxAdam persisted across steps (store it in env.optim_state or a new field keyed by 'adam'); each call: loss_and_grads over the adapters with a demo_forward closure, MlxAdam.step, write the updated adapters back to the Environment via mlx_to_dense_data + env.set so the CPU side stays in sync. NOTE the demo_forward currently hardcodes the demo architecture -- guard the MLX branch to only engage for that architecture (else fall back to the CPU tape path) so other models still work. (3) Extend lora_mlx_demo_tests.rs: assert the device('mlx') LoRA fine-tune loss curve matches the CPU path within fp32 tol (V=16,d=8,h=1,rank=2,3 steps) -- this is the real parity test. (4) Relabel the 'MLX LoRA fine-tune' demo hybrid->true-GPU: components/web-demos .../lm.rs intro/takeaway + the progress NOTE in registry.rs, and update docs/saga-local-gpu-agentic.md 'CRITICAL reality' section to say full-GPU training now works. Then the saga's MLX track is DONE. Note: this is Apple-only glue; given the planned Linux/CUDA pivot, confirm with the user whether to do it now or call the MLX stack proven and move on. MLX value_and_grad not thread-safe -> serialize grad tests. DISK: scope to mlx+eval crates, clean after. Commit .agentrail with source.