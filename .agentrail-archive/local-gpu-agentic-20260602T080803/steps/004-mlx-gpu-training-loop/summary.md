Scoped Phase 1b (MLX full-GPU training): mapped mlx-rt (forward-only), the String-device forward dispatch, autograd's 19-fn CPU-f64 backward.rs (no device info), and Vec<f64> CPU-round-trip adam. Surfaced the architecture fork (A: mlx_rs value_and_grad autodiff [recommended] vs B: hand-port 19 backward formulas) and the sw-checklist trap (splitting backward.rs needs a sibling crate, not incidental paydown). Recovered 39GB disk (shared target/ 40GB->gone, free 24->63 GiB). Recorded all in docs/saga-local-gpu-agentic.md and inserted step 005 to resolve the fork + define implementation follow-ons.