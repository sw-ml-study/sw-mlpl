Phase 0 web wiring: connect-mode REPL now talks to the server's /v1/ollama/* endpoints. New WASM module ollama_fetch (fetch_ollama_models, prime_ollama_default, ollama_default). ':models ollama' lists the server's Ollama models (verified via Playwright: 16 models rendered). ':ask' defaults to the server-configured host/model (ask_endpoint: ?ollama=/?model= override > primed server default > built-in). Pages rebuilt + restarted; sw-checklist held 14/308.