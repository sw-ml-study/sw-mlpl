Phase 0: server-owned Ollama config on mlpl-serve. New config module (OllamaConfig nested in ServeConfig nested in RunConfig) with flag>OLLAMA_HOST-env>default precedence + host allow-list; CLI flags --ollama-host/--ollama-model/--ollama-allow; new endpoints GET /v1/ollama/config and GET /v1/ollama/tags (allow-listed /api/tags proxy, 403 on disallowed host). Composite-struct refactor collapsed run() 7 args->1 and build_app_with_peers_cors 5->3; router_layers module extracted to keep the builder under 50 LOC. 6 new TDD tests, all 56 serve tests pass.