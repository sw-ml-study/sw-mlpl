Phase 2 of local-gpu-agentic: agentic, tool-using :ask. Server-side /api/chat loop with tools so the model REQUESTS the context it needs (get_recent_history, get_workspace_vars, describe_variable, get_selected_sculpture, get_builtin_help, get_demo_source) and answers from what it retrieved (RLM-style). Surface the tool-call trace in the UI. See docs/agentic-ask-plan.md. NOTE platform pivot: the MLX full-GPU track (Phases 0-1b) is DONE on this Mac; Phases 4-5 (SmolLM2 loader + headline download->fine-tune demo) are now candidates for CUDA on the Linux box (reuse the approach-A pattern: traceable forward + library autodiff + on-device optimizer, mirroring mlpl-mlx-* with a CUDA crate). Phase 2 (agentic :ask) and Phase 3 (RLM demo) are platform-agnostic (CPU/connect + Ollama) and can proceed on either host. Confirm direction with the user before starting given the pivot.