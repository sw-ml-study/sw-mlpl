Replaced delayed native title popups with explicit immediate CSS tooltips for group labels and every demo row. Disabled demos use focusable wrappers so their reason is visible despite true disabled button semantics. Increased all tooltip text to 14px and group explanations to 15px with an up/right offset. Added markup/CSS contract tests, passed scoped tests/clippy/fmt, rebuilt the local Pages bundle, and supplied a cache-busted connected URL.