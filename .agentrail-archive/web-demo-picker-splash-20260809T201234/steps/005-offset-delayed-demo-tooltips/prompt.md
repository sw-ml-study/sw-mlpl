Change demo picker tooltips from inline menu-expanding blocks to clearly distinct floating callouts offset to the right of each group/demo target. Use a contrasting theme surface/accent border and approximately 250ms hover/focus reveal delay. Preserve disabled-wrapper accessibility, add CSS contract assertions for offset/color/delay, run scoped gates, rebuild pages, and then return to final deployment.