Lower tokenize_bytes/1 (str -> rank-1 byte array) in the compile-to-Rust path. Add a runtime function in a text module of mlpl-rt-value (UTF-8 bytes of the string as a CVal byte array, f64 cells in 0..=255) and the fncall lowering arm in mlpl-lower-rs/src/fncall.rs. Match the interpreter (mlpl-eval tokenizer.rs). TDD: RED failing test first, GREEN minimal lowering, refactor. Add a gated end-to-end test that compiles a tiny program turning a string literal into its byte array and checks the cells. Hold or lower sw-checklist. Commit source before agentrail complete.