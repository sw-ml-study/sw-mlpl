Harden the decode_bytes acceptance in the compiled path. The lowering shipped in a7d1a3c3 (runtime decode_bytes reuses the loud-reject array_to_bytes validator in mlpl-rt-value io.rs, then UTF-8-decodes). Add a compiled e2e (MLPL_BUILD_TESTS=1) proving the LOUD-REJECT parity: a compiled program calling decode_bytes on an out-of-range / non-integer byte cell must FAIL (non-zero exit / panic), never silently truncate, matching the interpreter fs_bytes array_to_bytes semantics. Keep the existing round-trip assertion. Verify the happy path still passes. Hold or lower sw-checklist; if only a test assertion is added, use the exception trailer with justification. Commit source before agentrail complete.