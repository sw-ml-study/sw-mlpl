Closed compiler-text-ops. Discovered the three text conversions (tokenize_bytes/decode_bytes/to_int) were already lowered in a7d1a3c3 (saga/git drift: the byte-io saga's queued 006-text-ops was done in git but never recorded). Verified via cval/dispatch/build e2e, then hardened the compiled e2e with the three missing acceptance cases (raw byte cells, decode_bytes loud-reject aborts non-zero, to_int err-branch). Docs-close: marked compiler-read-bytes + compiler-bit-ops + compiler-text-conversions SHIPPED in the queue, flagged compiler-process-semantics NEXT, and queued a new file-metadata-timestamp saga for the downstream ../demo-extensions + ../demo-file-processing blocker (confined file_modified_ms primitive).