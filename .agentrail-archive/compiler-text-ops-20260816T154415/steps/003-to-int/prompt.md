Harden the to_int acceptance in the compiled path. The lowering shipped in a7d1a3c3 (runtime to_int in mlpl-rt-value text.rs: trim + parse::<i64>, ok(int) / err(msg)). The e2e already asserts to_int("42") -> ok(42). Add the err-branch: a compiled program running to_int on a non-numeric string (e.g. to_int("xyz")) must print an err( Result the program can branch on -- NOT a panic (to_int returns a CVal::Result, unlike decode_bytes). Assert the output starts with err( and names the bad input. Verify the happy path still passes. Hold or lower sw-checklist; test-only additions use the exception trailer with justification. Commit source before agentrail complete.