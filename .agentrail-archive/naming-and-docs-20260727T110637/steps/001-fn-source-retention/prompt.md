TDD: def u: functions retain their raw source text (incl. # comment lines) so :list prints the definition as written, APL2-listing style. Environment::set_pending_source; FnDef eval arm slices the def span from it; UserFn gains source: Option<String>; list_fn prefers raw source (fallback: AST reconstruction). Thread set_pending_source at the eval entry points: mlpl-wasm eval_impl (both call sites), mlpl-repl eval_line + script_mode, mlpl-serve spawn_eval + sse spawn task. Tests at eval level + one repl-side. Keep budgets post-fmt.