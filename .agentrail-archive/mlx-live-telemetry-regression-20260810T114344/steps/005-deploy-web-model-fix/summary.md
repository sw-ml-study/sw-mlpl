Rebuilt pages at HEAD and deployed to gh-pages so the WASM browser eval carries the user-fn frame model/tokenizer/gen-state snapshot fix (bare model identifiers now resolve in non-connect demos). Live verified at commit b157767c / bundle a66f6c07 (also carries fns-over-connect, the custom demo dropdown, and the splash UTC-Z build time). Telemetry fix is server-only, no web deploy needed.