In connect mode, :fns/:vars/:describe/:list/:models/etc now MERGE the local browser env's answer (labelled [this browser]) with the server's ([connected server]), so a browser-defined u:fn from a CPU demo shows up instead of 'no user functions yet'. Implemented in try_connect_eval via is_inspect_command (connect.rs). The connect module is #![cfg(target_arch=wasm32)] so it has no host cargo-test seam; validated on a real connected CUDA system (user-confirmed).