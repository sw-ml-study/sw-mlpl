First IR renderer: SVG attention heatmap dispatched via the saga A scaffold. detectViz JS-side picks up '<var> = softmax(' on rank-2 square tensors; renderer draws viridis cells + row-sum bars matching the existing svg('heatmap') builtin's palette. Hover tooltips, axis labels, headline + stats block + hint pointing to sagas C/F. Demo proof: attention.mlpl -> click A -> 6x6 heatmap. Rust-side port deferred to saga F (D3 upgrade) where it would be rewritten anyway.