Phase 2: Give users a way to attach labels. Add label(x, ["batch", "time", "dim"]) built-in that tags an existing array. Add annotation syntax on assignment: x : [batch, time, dim] = randn(7, [8, 16, 32]). The annotation is optional; omitted means positional. Parser impact is contained: the colon in assignment is new. TDD: test labels round-trip through label() and annotation syntax; verify unlabeled arrays are unchanged.