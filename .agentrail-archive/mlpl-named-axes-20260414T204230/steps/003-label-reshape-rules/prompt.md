Phase 2 cont.: Label propagation through shape-changing ops. transpose(x) / x.T preserves labels but swaps their order. reshape(x, ...) clears labels by default (reshape loses semantic axis identity). Add reshape_labeled(x, new_shape, new_labels) opt-in for explicit re-labeling. TDD: transpose keeps label set with swapped order; reshape drops labels; reshape_labeled requires matching rank.