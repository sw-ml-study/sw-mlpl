Phase 3: Elementwise ops propagate and validate labels. +, -, *, / require matching labels when both operands are labeled; union in the usual broadcasting order. Scalar broadcast works unchanged. Row-vector-vs-matrix broadcast works when only one side is labeled; two labeled sides with compatible dims but different labels raise a structured error. Add an explicit relabel(x, [...]) escape hatch. TDD: label match succeeds, mismatch errors, unlabeled-unlabeled unchanged, mixed labeled/unlabeled adopts labels from the labeled side.