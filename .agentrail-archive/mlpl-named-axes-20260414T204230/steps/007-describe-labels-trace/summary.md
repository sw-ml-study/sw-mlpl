Saga 11.5 step 007 shipped. format_shape in inspect.rs now takes &DenseArray and dispatches to LabeledShape Display for labeled arrays, so :vars and :describe render as '[seq=6, d_model=4]' (full labels), '[6, d_model=4]' (partial), or '[6, 4]' (unlabeled, unchanged). TraceValue::Array gained a serde-skip-when-None labels field populated from DenseArray::labels(); JSON trace round-trips axis labels for annotated assignments and emits nothing for unlabeled arrays. 6 new tests green. Model per-layer input/output label pinning was deferred -- it would require Environment signature tracking on first apply, out of scope for the step's tested acceptance criteria.