Phase 6: Add a "Named Axes" tutorial lesson to apps/mlpl-web/src/tutorial.rs immediately after "Matrices" but before "Linear Algebra", so every subsequent lesson can use labels. Update the Model Composition lesson and the moons/transformer demos to label their inputs and outputs. The Model DSL itself should report [seq, d_model] inputs and outputs on apply(mdl, X). Rebuild pages/ via scripts/build-pages.sh. Commit via /mw-cp.