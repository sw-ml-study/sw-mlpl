Splash/header badge v2 per plan: RUNNING page identity from its own mlpl-build meta; SERVED origin from same-origin build-info.json; REPO state from raw.githubusercontent main pages/build-info.json (best effort). Verdicts: current / stale page (reload fixes) / deploy pending (repo newer than served; reload will not help yet) / unknown-offline default. Pure verdict TDD; real build-info.json pinned like the index pin. Rebuild + deploy.