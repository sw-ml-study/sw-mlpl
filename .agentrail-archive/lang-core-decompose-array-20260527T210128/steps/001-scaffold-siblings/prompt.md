Scaffold 5 sibling crates in components/lang-core/crates/: mlpl-array-ops-matmul, mlpl-array-ops-reduce, mlpl-array-ops-compose, mlpl-array-ops-shape, mlpl-array-ops-element. Each gets a Cargo.toml (depending on mlpl-array via path = '../mlpl-array' and mlpl-array's transitive deps as needed) and a minimal src/lib.rs with a 'pub mod prelude {}' (empty for now). Add all 5 to components/lang-core/Cargo.toml members list. Verify all four workspaces still build (main, lang-core, mlpl-session, mlpl-mlx-serve). No production code changes -- this is pure scaffolding so subsequent steps have homes for the extension traits.