Extract ops_matmul.rs from mlpl-array into the sibling crate mlpl-array-ops-matmul as extension traits. Steps: 1) Read components/lang-core/crates/mlpl-array/src/ops_matmul.rs and identify each pub method on impl DenseArray (likely matmul, dot, maybe others). 2) In components/lang-core/crates/mlpl-array-ops-matmul/src/lib.rs, define one extension trait per method (MatmulExt with matmul, DotExt with dot) plus their impls for DenseArray. The bodies are copy-pasted from ops_matmul.rs but must use only the public API of DenseArray (e.g. .data(), .shape(), .labels()). If they currently use private pub(crate) fields, you must either: (a) add public accessor methods on DenseArray in dense.rs/shape.rs OR (b) leave the operation in mlpl-array. Prefer (a). 3) Add 'pub use {MatmulExt, DotExt};' to mlpl-array-ops-matmul's prelude. 4) Delete ops_matmul.rs from mlpl-array. Remove 'mod ops_matmul;' from its lib.rs. 5) Find all call sites of .matmul( and .dot( in the workspace: grep -rln '\.matmul\(\|\.dot\(' crates/ apps/ services/ components/. For each, add 'use mlpl_array_ops_matmul::prelude::*;' at the top of the file (or merge into existing 'use mlpl_array_ops_matmul::*;'). 6) Verify cargo check --workspace from main + cargo check from each component/service workspace. 7) cargo test on representative crates. 8) cargo fmt + clippy clean. 9) Commit. While doing this, ALSO refactor the matmul function body to be under 25 lines if currently 50 (split into helper functions if needed). Same for dot. Goal: retire mlpl-array's matmul 50-LOC warning along with the structural split.