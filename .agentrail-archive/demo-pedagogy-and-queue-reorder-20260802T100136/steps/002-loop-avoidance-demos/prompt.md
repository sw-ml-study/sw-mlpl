Loop-avoidance pedagogy (user direction 2026-08-02): (1) Life demo takeaways in components/web-demos/crates/mlpl-web-demos/demos.toml gain the data-vs-time distinction -- name the surviving generation loop (grid(t+1)=step(grid(t))) vs the vanished cell loops. (2) New APL2-category demo 'Thinking in Arrays: loops you keep, loops you lose' -- one task solved twice (for-loop vs array expression), then a train/repeat recurrence explaining why time loops survive, plus the scan/associativity borderline (cumprod). (3) One-sentence additions to the attention demo intros: every loop over position pairs is one matmul. (4) apl2-parity-gap.md G1 note: the scan saga carries the associative-recurrence pedagogy. Then: demo smoke coverage (every_quick_demo_runs must pass with the new demo), ./scripts/build-pages.sh + commit pages/ + push (demos.toml changed), wiki errata update per discipline.