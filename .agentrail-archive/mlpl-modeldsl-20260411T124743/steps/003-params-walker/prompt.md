Phase 2 cont.: Implement params(model) that walks a model value's parameter tree and returns a flat list of param identifiers usable as the second argument to momentum_sgd / adam. The list should be order-stable so optimizer state keys remain consistent across calls. RED: params(chain[linear(2,8), tanh_layer, linear(8,2)]) returns 4 param names (2 W's + 2 b's), and adam(loss, params(model), ...) drives a quadratic loss to zero. Commit via /mw-cp.