Phase 4: Add an attention[d_model, heads] multi-head self-attention layer composed from linear projections + softmax(QK^T / sqrt(d_k)) + output projection. Use the existing tensor matmul/transpose/softmax ops so it remains autograd-friendly. params(attention) must return all four projection matrices' params. RED: attention layer applied to a [seq, d_model] input returns a [seq, d_model] output with non-trivial gradients flowing through grad() to each projection's W. Commit via /mw-cp.