Phase 3: Add residual[block] (y = x + block(x)) and a normalization layer (pick layer_norm(dim) or rms_norm(dim) or both). residual must register the inner block's params under params(). norm layers should expose a learnable scale/shift if you pick layer_norm. RED: residual identity holds when the inner block is initialized to zero (output equals input); norm output has the expected mean/std (layer_norm) or unit RMS (rms_norm). Commit via /mw-cp.