Phase 1: Add a Value::Model variant (or equivalent representation) holding a callable layer plus a parameter tree, and implement the linear(in_dim, out_dim, seed) atomic layer built-in. linear() should declare two params (W [in,out], b [1,out]) initialized via the existing randn-style seeding and return a model value that, when applied to an input X, evaluates matmul(X, W) + matmul(ones, b). Decide on the apply surface (model(X) function-call form vs apply(model, X) built-in) and document it in a brief comment. RED: a model value can be created, has the right param shapes, and applies correctly to a sample input. Commit via /mw-cp.