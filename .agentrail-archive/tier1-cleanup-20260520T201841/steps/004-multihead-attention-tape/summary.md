Audit finding #19 was stale. Empirical verification: vit_multihead_quick (heads=4, 100 adam steps, 20 samples) reaches accuracy 1.0; the browser config (8 samples, 30 steps) drives loss from 2064 to 0 and reaches accuracy 1.0. The multi-head tape was already correctly lowered in saga 29 step 013 (reshape + take + per-head SDPA + Tensor::stack); the audit text was never refreshed. Added a multi_head_trains_end_to_end_loss_decreases regression test pinning the behavior so any future regression of the per-head tape catches loudly. Rewrote the audit finding with SHIPPED status and a 'historical claim (now refuted)' paragraph. Updated docs/language-status.md to flip #19 to shipped.