The Tour's path-card step targets the first PATHS entry, but the default path order changed and 'A Chronological History' is now position 0. The Tour body still says 'Let's open the Dimensionality Reduction path', so the highlighted card and the narration disagree. Re-anchor the path-card marker to the Dimensionality Reduction card by title so future PATHS reordering doesn't break the Tour again.