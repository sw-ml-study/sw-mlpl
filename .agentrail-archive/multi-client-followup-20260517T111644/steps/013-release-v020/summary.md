Saga 21.5 step 013 release-v020. Bumped main workspace 0.19.0 -> 0.20.0 (Cargo.toml + Cargo.lock); bumped services/mlpl-mlx-serve 0.18.0 -> 0.20.0 so the orchestrator and MLX peer ship at the same version going forward. Rewrote CHANGELOG.md Unreleased section as a v0.20.0 entry that pairs the Saga 21.5 shipped scope (SSE streaming, cancellation, viz storage, persistence, reattach, web connect mode, f32/u8 wire) with the post-v0.19.0 polish work; fresh empty Unreleased section on top. Drive-by rustfmt fix on services/mlpl-mlx-serve/src/wire.rs that step 011 left behind (caught by services-workspace fmt --check which the main workspace fmt sweep doesn't reach). Committed (7d25e18), tagged v0.20.0 annotated, pushed commit + tag. Refreshed CHANGES.md via scripts/gen-changes.sh in a follow-up docs commit (c338c21). Quality gates green: 1285 main-workspace tests + 26 services-workspace tests pass; clippy -D warnings clean both workspaces; fmt clean both workspaces; sw-checklist held at 139 failed; markdown-checker clean for the touched docs. Saga 21.5 ships.