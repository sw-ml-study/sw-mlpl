Shipped mlpl-repl --stream / MLPL_REPL_STREAM flag routing connect-mode eval through /v1/sessions/<id>/eval_stream (Saga 21.5 step 001's SSE endpoint). New connect_stream.rs module with pure-fn parse_connect_args (ConnectMode/ConnectArgsError) + SSE wire path (eval_remote_stream + consume_sse_stream + handle_frame) + render_metric progress line. connect_repl.rs's read_loop now branches per --stream and uses new eval_and_print to surface metric frames live. 7 new integration tests cover same-value parity, 5 metric frames for train 5 {}, --stream-without-connect error, env-var equivalence + falsy values, and render formatting. All cargo gates green; sw-checklist 134 held.