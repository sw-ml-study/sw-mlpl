Shipped session re-attach: server GET /v1/sessions/<id> returns metadata (session_id, created_at, last_eval_at Unix timestamps, workspace summary); Session gains the two timestamp fields, eval_handler stamps last_eval_at. Client adds --session/--token flags, MLPL_REPL_SESSION_FILE persistence, fetch_session_meta validation, Re-attached to banner. New connect_reattach.rs module keeps connect_stream.rs + connect_repl.rs at <=7 fns. 13 new tests pass. sw-checklist 138 via +1 exception (new module FAIL trades against two would-be fn-count FAILs).