Shipped VizFormat detection table (SVG/HTML/PNG/JPEG/JSON) with content_type + extension lookups. New crates/mlpl-cli/src/viz_format.rs module (split out to defend the per-module fn-count budget after the enum + 3 methods + new write_bytes_to_cache would have pushed viz_cache.rs over 7 fns). is_svg_string kept as one-line back-compat wrapper. New write_bytes_to_cache picks the right .ext from the detected format and returns (path, VizFormat). 14 new format tests + 5 new cache tests + 1 new server PNG round-trip test; all cargo gates green; sw-checklist held at 135.