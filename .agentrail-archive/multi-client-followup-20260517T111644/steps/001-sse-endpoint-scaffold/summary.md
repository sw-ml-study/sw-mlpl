Shipped POST /v1/sessions/<id>/eval_stream returning text/event-stream with ready/metric/done/error framing. Added MetricSink trait in mlpl-eval that eval_train invokes per iteration for _metric-suffixed scalars; ChannelMetricSink adapter routes those into a tokio mpsc channel the SSE response streams. Eval runs on spawn_blocking with session moved out of the map; backpressure via bounded (64) channel. Inline pub mod sse in lib.rs matches the existing tls inline-mod precedent to stay within the sw-checklist 7-modules-per-crate budget; 7 integration tests cover happy path, train block metric ordering, /eval payload parity, auth/404/lex-error, and runtime error frames. Contract doc updated. All quality gates green, sw-checklist held at 134 failed.