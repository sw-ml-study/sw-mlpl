editors/emacs/mlpl-mode.el per the q-and-a sketch: font-lock (keywords; the three name kinds distinctly -- call/:ref/u:name; @annotations; strings; # comments; numbers), brace indentation, imenu over def u:, C-c C-c run-buffer via mlpl-repl (compilation buffer), C-c C-t run with --test-events into a results buffer listing events with jump-to-source, autoload for .mlpl. Byte-compile check as the gate (emacs --batch). README Tooling note + docs/emacs-mode.md quickstart. Then close step: deploy, wiki, q-and-a incl. NEW mlplunit gates assessment (in-language-event-reporting: sink state persistence; language-native-runner: fs + isolated script execution -- read their docs section 8 + fixtures first).