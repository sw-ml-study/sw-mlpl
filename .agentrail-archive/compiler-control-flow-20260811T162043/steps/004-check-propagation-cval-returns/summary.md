Lowered the ?/check propagation operator and unified user-function returns on CVal. A pre-pass marks functions that build records / call ok-err / use ? as CVal-returning; they lower to -> CVal with tail+return wrapping; check unwraps an ok payload or early-returns the err. The full demo pattern (f = u:fit(n)?; f.slope where u:fit returns ok(record)) compiles to a native binary and runs. Value-model gate for demo-file-processing is CLEARED; next compiler gates are byte/bit lowering + process semantics (separate sagas).