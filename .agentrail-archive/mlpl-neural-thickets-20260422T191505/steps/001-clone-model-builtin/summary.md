Added clone_model(m) builtin that deep-copies a ModelSpec tree with fresh param names, copies tensor values + device tags, and registers new params as trainable. Handles Linear/Chain/Residual/Activation/RmsNorm/Embedding/Attention variants. 10 tests cover name disjointness, forward parity, mutation isolation, nested clones, trainable registration, device propagation, and both error paths.