Authored demos/neural_thicket_mlx.mlpl (variant loop + ensemble wrapped in device("mlx") { ... } with to_device(base, "mlx") + to_device(val_X, "mlx") prologue; base training stays on CPU). Added a triple-gated integration test neural_thicket_mlx_demo_tests.rs that runs the cut-down on both CPU and MLX and asserts losses[16] + ensemble logits agree within 1e-3 fp32 tolerance, heatmap [4,4] shape, argtop_k returns 4 distinct in-range indices on both paths. Added a Criterion group neural_thicket_variant_loop to mlx_vs_cpu.rs; measured warm CPU 767us vs warm MLX 3.01ms = 0.25x (MLX slower), essentially identical to the Saga 14 tiny_lm_train_step 0.26x ratio. Updated docs/benchmarks.md with the new row and a paragraph explaining the cost is per-op kernel launch + f32 round-trip rather than tape rematerialization, since this workload is inference-only.