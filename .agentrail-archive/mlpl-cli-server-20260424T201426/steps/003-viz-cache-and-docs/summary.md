Shipped CLI viz cache strategy + Saga 21 retrospective docs. crates/mlpl-cli/src/viz_cache.rs (5 fns: is_svg_string, cache_path_for_content, write_to_cache, transform_value, resolve_cache_dir helper) replaces mlpl-repl's old raw <svg> XML print path with content-addressed paths under MLPL_CACHE_DIR (default dirs::cache_dir().join('mlpl')). Wired into mlpl-repl eval_line + connect_repl read_loop; svg_out.rs simplified to a thin --svg-out flag holder. 18 new tests. docs/using-cli-server.md retrospective + user guide; docs/configurations.md refreshed with new rows + footnote [8] explicitly distinguishing MVP-shipped from LLM-proxy-still-deferred.