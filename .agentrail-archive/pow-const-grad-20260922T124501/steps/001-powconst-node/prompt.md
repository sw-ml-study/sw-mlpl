Add NodeKind::PowConst { parent, exp: f64 } to mlpl-autograd-tape/src/tape.rs (NodeKind derives Clone/Debug, f64 fine). Add Tensor::pow_const(k: f64) in mlpl-autograd tensor_ops.rs (elementwise; host-only: bump CpuFallback if resident, value = self.value().map(|v| v.powf(k)), build NodeKind::PowConst). Add the backward in mlpl-autograd-backward: a propagate() match arm NodeKind::PowConst { parent, exp } => crate::backward_shape::prop_pow_const(tape, parent, exp, &upstream), and prop_pow_const in backward_shape.rs (to_dense x + upstream; grad[i] = upstream[i]*exp*x[i]^(exp-1); accumulate). Rewire grad_calls_pow::call_pow: keep the differentiable-exponent guard, resolve the constant exponent as any real scalar, return base.pow_const(k) -- drop the integer-only/MAX_POW restriction and repeated-mul. TDD: gradcheck pow(x,0.5)/pow(x,-1)/pow(x,2.5)/pow(x,3) vs finite differences; x^2 exact; eager unchanged. sw-checklist: hold failed at 34. Rebuild repl/build release+debug + serve.