F10 (BUG): fixed a Rust panic in the autograd tape. A labeled sinusoidal_encoding ([time,dim]) added to an embedding ([time,_]) inside a residual attention block panicked (LabelMismatch hit .expect in the binary op) during adam. Root cause: merge_labels' same-rank case used whole-vector equality (la==lb), so a None axis counted as a mismatch vs a Some label; the different-rank broadcast path already unified per-axis. Fixed same-rank to unify PER AXIS (None is a wildcard; only two different explicit labels conflict) via new unify_axis_labels, consistent with merge_broadcast_labels. The reproducer now TRAINS (loss 7.12->6.33, no panic); genuine conflicts still error. Tests: label_merge_tests (new); regression dense 46/46, builtin_eval 70/70, array-ops-element 9/9. sw-checklist 38/562 unchanged. Rebuilt release+debug repl/build; rebuilding mlpl-serve too (S1). Pushed.