Move mlpl-core into the lang-core component. Steps: 1) git mv crates/mlpl-core components/lang-core/crates/mlpl-core. 2) Add 'crates/mlpl-core' to components/lang-core/Cargo.toml members. 3) Remove 'crates/mlpl-core' from the root Cargo.toml members. 4) Update every Cargo.toml that references mlpl-core via path: change path = '../mlpl-core' to path = '../../components/lang-core/crates/mlpl-core' (for crates in sw-mlpl/crates/), path = '../../../lang-core/crates/mlpl-core' (for crates in components/<other>/crates/), or path = '../../components/lang-core/crates/mlpl-core' (for apps/ and services/). Use grep to find all referrers first: grep -rln 'mlpl-core = { path = "\.\./mlpl-core"' crates/ apps/ services/ components/. 5) cargo check --workspace from main + cargo check from components/mlpl-session/ + cargo check from services/mlpl-mlx-serve/ -- all must succeed. 6) Run cargo test on a few representative crates to verify behavior preserved. 7) cargo fmt, cargo clippy --workspace --all-targets --all-features -- -D warnings. 8) Commit + push.