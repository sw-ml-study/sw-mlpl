Move mlpl-array (the biggest fan-out: ~30 referrers). Same recipe as move-mlpl-core: git mv crates/mlpl-array components/lang-core/crates/mlpl-array, update components/lang-core/Cargo.toml members, remove from root Cargo.toml members, update all path references in dependents (use grep to find them), verify cargo check from all three workspace roots, fmt + clippy + test, commit + push. Note that mlpl-array already depends on mlpl-core which is now in the same component, so its internal path reference becomes path = '../mlpl-core'.