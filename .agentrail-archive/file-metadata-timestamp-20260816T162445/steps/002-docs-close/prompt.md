Close the file-metadata-timestamp saga. Add file_metadata to the builtin catalog (mlpl-builtin-catalog groups_lang.rs, beside file_size) AND a glossary.md entry (the builtin_coverage test requires every catalogued builtin be mentioned) AND a lang-reference.md fs-table row (WHAT/HOW only): file_metadata(path) -> ok({kind,size,modified_unix_ms})/err, exact UTC Unix-ms modification time, err when unavailable, sandboxed like file_size. Run the builtin_coverage + readme_counts tests to confirm no count/coverage regressions. Update docs/future-sagas-queue.md marking file-metadata-timestamp interpreter-side SHIPPED and queuing the compiled-path lowering follow-on. Refresh docs/companion-demo-extensions.md + docs/companion-demo-file-processing.md (primitive shipped; downstream demonstrates/consumes). Update the wiki Documentation-Errata + a Capability-Matrix row. Hold sw-checklist (docs exception if it holds). Then agentrail complete --done.