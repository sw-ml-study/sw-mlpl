Implemented the interpreter file_metadata(path) primitive: new fs_meta.rs returns ok({kind,size,modified_unix_ms})/err with exact UTC Unix-ms modification time (never atime/ctime/local/now), err when unavailable, and the same env.fs_root + contained symlink protections as file_size; wired into fncall_fs dispatch + lib.rs. TDD with 5 tests including a wall-clock-independent exact-mtime pin, dir kind, missing-file err, no-sandbox err, and unix symlink-escape refusal. Net-zero sw-checklist (retired the module's two initial warnings in-place).