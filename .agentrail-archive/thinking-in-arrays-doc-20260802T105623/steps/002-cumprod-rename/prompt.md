BLOCKED until the user picks the new name for cumprod (candidates: running_product [recommended], prefix_product, product_scan, cumulative_product). Then: iota-precedent deprecation -- add new builtin name in mlpl-runtime-array (NAMES + dispatch + lowering if present), keep 'cumprod' dispatch arm with 1-line deprecation comment but remove from NAMES/docs; sweep demos.toml, demos/diffusion_2d.mlpl, lang-reference.md, glossary.md, apl2 docs, thinking-in-arrays.md, tests; literate parity if org files mention it; scoped gates + pages rebuild if demos changed.