Add shl(x,k,width)/shr(x,k)/bmask(x,width)/bits(x,width)/from_bits(v) to mlpl-runtime-bits in a new module (shift.rs) per docs/bit-ops-design.md: width-aware shl masks to width; shr logical; bmask keeps low width bits; bits expands a scalar to a [width] LSB-first 0/1 vector; from_bits packs a 0/1 vector to a scalar (inverse). Register in NAMES + try_call; catalog + lang-ref + glossary rows + README pin; help_completeness green. TDD (bit_shift_tests). Gates; commit; push.