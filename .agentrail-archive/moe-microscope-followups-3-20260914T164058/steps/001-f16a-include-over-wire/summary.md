F16a: eval request accepts an optional includes map (virtual path -> source text); server resolves the include tree via the existing mlpl_source_loader MemoryProvider + expand (sandbox rules built in: relative-only, no escape) and flattens chunks to one stmt list. Empty map -> plain path (backward-compatible). Wired both /eval (via parse_or_expand, preserving :command fallthrough) and SSE eval_stream (expand_program, which falls back to parse). New dep mlpl-source-loader. Test eval_resolves_includes_from_the_request_map (include-defined u:double->42); api_tests 20/20, colon 3/3. sw-checklist 38/564 unchanged (helpers in already-failing handlers.rs; handlers kept under LOC gate). Rebuilt mlpl-serve. Pushed.