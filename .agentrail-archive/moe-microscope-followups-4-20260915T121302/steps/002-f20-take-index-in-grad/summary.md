F20 (BUG): take's axis/index now resolve through the traced scope (tape_scalar_usize uses eval_tensor_expr -> local bindings first, then env), so an index bound to a function argument works inside an inlined user fn and a local shadows a same-named global (no silent global resolution). Threaded tape+params through the 6 grad shape-op call sites. call_take validates the index against the axis extent -> clean 'out of range' error instead of the tape take panic. Verified: the F20 probe (take(g,1,e) with e a fn arg) runs, no undefined-var/panic. Tests grad_through_take_index_from_user_fn_arg + grad_take_out_of_range_index_errors_cleanly_not_panic; regression grad 35/35, take 2/2, gradient_flow 10/10. sw-checklist 38/566 unchanged. Rebuilt binaries. Pushed.