F21: adam/momentum inside a user function now trains the GLOBAL params instead of frame-local copies. framed() restores the var scope and replays only tracked global_writes; the optimizer wrote weights via plain env.set (reverted). New set_trained_param records the write in global_writes when call_depth>0 (like global_set) so it survives the frame restore; all 3 optimizer write sites (momentum, adam, resident) use it. Verified: F21 probe now prints 'param moved 1 model moved 1'. Test adam_inside_user_function_updates_global_param; regression train_loop 12/12, first_train_resident 3/3. Fixed a clippy needless-borrow. sw-checklist 38/567 unchanged. Rebuilt repl/build/serve. Pushed.