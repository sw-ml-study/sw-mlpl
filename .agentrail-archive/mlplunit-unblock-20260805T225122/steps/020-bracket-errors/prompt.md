Per docs/finally-design.md: capture hard evaluator errors at the use and teardown boundaries as the structured {kind, message} record (same shape try/catch produces) so teardown always runs even on hard use errors; both-fail merge attaches teardown_error to the primary err payload (record gains field; non-record wraps to {message, teardown_error}); ? composition test; then docs (lang-reference, glossary+README pin, usage, catalog, wiki matrix, monads.md rec 3 shipped), rebuild repl+server+pages, run mlplunit check-capabilities expecting fixture-lifecycle's guaranteed-finally prerequisite satisfied, q-and-a update.