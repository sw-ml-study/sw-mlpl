Design guaranteed-finally/bracket (mlplunit contract item 6, the SOLE remaining blocker their agent reports for fixture-lifecycle; docs/monads.md rec 3). Produce docs/finally-design.md: surface options (try/finally statement vs bracket(setup, use, teardown) over references), semantics with ? early-return and hard errors (teardown ALWAYS runs; teardown errors vs primary error precedence), interaction with try/catch and include, TDD step plan. Answer open questions in the doc; get user review before implementation steps.