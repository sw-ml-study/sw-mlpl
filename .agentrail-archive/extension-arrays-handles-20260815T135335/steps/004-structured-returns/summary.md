Structured record returns: ExtValue::Record + AbiField/AbiRecordView (ValueTag::Record=8) wire a provider-returned record across the boundary; read_record_view copies field names and recurses per field, from_ext maps to Value::Record via record_map. Nested records (record-of-records) give the list-of-rows shape MLPL reads with b.e0.x. 3 TDD record tests + all prior extension suites green. Moved abi_error_to_ext to register.rs (cohesion) to offset the new function. Checklist 44 fail / 522 warn held; abi_to_ext 2 lines over, crate at 7-module ceiling, split queued.