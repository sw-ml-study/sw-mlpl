Fourth step of extension-arrays-handles. A provider returns a RECORD (or list of records) so an event batch like poll_events can hand back {kind, x, y} rows. Wire a record/struct return across the boundary: extend ExtValue with a Record (field-name -> ExtValue) kind, marshal it in the cabi receive path and ext_marshal from_ext into Value::Record, and add a repr(C) provider return shape for it. TDD: a provider returns a record whose fields MLPL reads, and a provider returns a list of records MLPL can iterate. Keep functions correctly formatted and under 25 lines by refactoring, never whitespace tricks; keep sw-checklist fails/warns decreasing (extension-cabi crate split is queued if the module ceiling blocks clean dispatch).