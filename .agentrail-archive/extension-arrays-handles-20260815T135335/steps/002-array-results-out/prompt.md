Open the RETURN direction (B4 receive): a provider that RETURNS a dense array must round-trip back to an MLPL Value::Array. In mlpl-extension-cabi marshal.rs, abi_to_ext currently errors on the DenseArray tag (6). Read a provider-returned AbiArrayView: validate dtype (u8/i64/f32/f64), rank (1..=8), element count vs the shape, and the data slice length; decode the little-endian dtype bytes back to f64 elements; build an ExtValue::Array. Put the pure decode (bytes+dtype -> Vec f64) as an ExtDtype::decode_le method in mlpl-extension-abi (mirror encode_le), keeping abi_to_ext small. Copy the shape (the AbiArrayView shape ptr is provider-owned; copy immediately, like the Utf8/Bytes span contract). ext_marshal::from_ext already maps ExtValue::Array -> Value::Array, so the interpreter side is done. TDD: a repr(C) provider make_range that returns an f64 [2,3] AbiArrayView (values 0..6); assert MLPL shape and values come back (e.g. reduce_add of the returned array == 15, or index checks). Reject: a returned view whose shape product mismatches the data length. Keep every function correctly formatted and under 25 lines by refactoring (dtype methods, helpers) -- never bypass rustfmt. Scope tests to runtime-core + mlpl-eval; clippy/fmt; repo sw-checklist net-zero or better. Do NOT rebuild the release binary yet.