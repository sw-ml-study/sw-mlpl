Step 1 softmax-arity (moe F1): softmax(x) now defaults to the last axis in eager evaluation, matching the tape's unary softmax, so the same softmax(x) loss evaluates AND trains. Fixed the runtime softmax (mlpl-runtime-ml validate_softmax_args accepts 1 or 2 args via a slice match, staying under the LOC budget) + the device_dispatch device path; softmax(x, axis) unchanged. 2 TDD tests (sums-to-1/monotone; softmax(x)==softmax(x,last)). log_softmax has a separate dispatch path, out of F1 scope. eval+runtime-ml clippy/fmt green; 38 failed / 560 warnings (no new fail/warning). Binaries rebuilt.