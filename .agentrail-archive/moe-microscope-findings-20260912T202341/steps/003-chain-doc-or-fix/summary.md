F3: confirmed chain does NOT share weights (documentation fix, as predicted). Empirically verified param_count(linear(4,4,7))==20 vs param_count(chain(blk,blk,blk))==60 (3x copy), and that the weight-sharing spelling -- reuse one block via nested apply or (post-F2) a user function -- accumulates gradient across every use (grad(sum(w+w+w),w)==[3,3] vs [1,1]). Updated lang-reference chain entry + glossary Chain(Model DSL) entry to state each chain arg is an independent block whose params are summed, and point to nested apply / user fns for sharing. Added 2 pinning tests to model_dsl_tests (no new module): chain_copies_blocks_and_does_not_share_weights, shared_param_gradient_accumulates_across_repeated_use. 13/13 green, clippy --tests clean, markdown clean. No interpreter change -> no binary rebuild. Pushed + CHANGES refreshed.