Added to_number(s), to_int(s), env(name) builtins. All three return Value::Result. Implementation in crates/mlpl-eval/src/result_ops.rs (placed there because mlpl-eval is already at the sw-checklist module-count cap and these are thematically Result ops). 12/12 new tests cover happy/error/non-string/arity paths plus the canonical unwrap_or(env(...), default) pattern. Closes audit findings #25 and #26.