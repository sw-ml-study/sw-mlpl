Added if cond { then } else { else } as an expression. First surface conditional in MLPL. Closes audit #22. Truthy on non-zero scalars and Ok(_) Results; else required; branches can return any Value type. 14 new tests. Also retired the gallery.rs clippy::too_many_arguments FAIL by refactoring render_thumbnail to take tuple-bundled args. sw-checklist ratchet-down: -1 fail, -1 warning. CLAUDE.md updated per user feedback to clarify the ratchet rule is REDUCE not HOLD.