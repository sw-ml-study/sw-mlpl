User questions 2026-08-02: (1) Diagrams tab order -- 01-38 is a documented pedagogical tour (classical -> NN -> attention -> transformer -> LLM -> training -> alignment -> phenomena): KEEP it, but 39-45 are an unlabeled MLPL-mechanics appendix -- give the index two labeled sections (ML Concepts tour + MLPL Mechanics) instead of alphabetizing. (2) Completeness: add hand-authored SVG diagrams for recent work: 46 engram addressing (ids -> ngram_hash -> selection matmul gather -> value/gate -> scatter-ADD backward), 47 device-resident tensor seam (host f64 <-> lazy device handles; uploads/downloads/submits/fallback counters; to_dense as THE sync point), 48 loops you keep vs lose (data loop vanishes into array op; time recurrence survives; associative scan borderline). Registry entries + blurbs; note remaining candidates (speculative/MTP, KV cache, rejection funnel, distillation, Pareto) in the queue doc for the tracks that own them. Web gates; pages rebuild + deploy.