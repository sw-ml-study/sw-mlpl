dev-concat landed: DeviceShapeOps supertrait (concat/split2) + MLX impl + resident Concat forward/backward on the tape. Warm engram step: fallbacks 3->1 (CE only), downloads 30->27, uploads 23->19; seam-test bounds tightened; counter-asserting test files serialized on per-file locks after diagnosing order-dependent pollution. All parity suites green (engram drift 0.000000, tiny-LM 1e-6). Ledger 337->339 (+2 structural from the new trait surface); mlpl-tensor-handle nominated for the paydown-spike crate split.