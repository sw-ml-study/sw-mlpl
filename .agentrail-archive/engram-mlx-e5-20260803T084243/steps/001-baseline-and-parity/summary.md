Baseline + parity landed AND a real E4 latent bug found+fixed: the resident optimizer's device_ops gate raced first-use registration, so the process's first train step ran the CPU optimizer and the resident path reseeded moments from zero (7e-2..8e-2 trajectory drift on first runs only -- invisible to suites whose first device use was another test). Fixed via register-before-gate + host-moment import (cpu->device continuity). New pinning tests: first-train parity (single-test binary), moment continuity, engram hash-contract bit-exactness, 10-step engram trajectory (drift 0.082 -> 0.000000), warm engram seam baseline 23/30/330/3 (concat fwd+bwd + CE bwd are the ledger for steps 2-3).