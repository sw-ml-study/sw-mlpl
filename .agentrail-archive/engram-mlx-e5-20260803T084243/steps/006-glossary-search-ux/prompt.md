User report 2026-08-02: glossary type-to-jump search only scrolls to the FIRST match with no way to reach later matches. Replace scroll-to-first with a FILTERED VIEW: typing filters the visible entry list to all matches (title first, substring), clearing restores the full list; keep it keyboard-friendly. Update the README glossary tour text that documents the old behavior. components/web-glossary (search + UI crates); web workspace gates; pages rebuild + deploy; local static refresh so the user can try it.