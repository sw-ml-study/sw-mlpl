User question 2026-08-02 exposed that the readme_counts test suite (mlpl-web) pins README's demo/lesson/glossary counts against the real registries -- it was stale before the jargon sweep (63 vs 66 demos) and the sweep then removed the counts, breaking all three tests. Restore EXACT counts to the README tour, keep the test as the enforcement, and note the enforcement rule in CLAUDE.md (counts in user docs only when a test pins them). Web workspace gates.