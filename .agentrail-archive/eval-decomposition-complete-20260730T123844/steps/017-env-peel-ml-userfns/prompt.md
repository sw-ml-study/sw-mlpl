eval decomposition step 3b: peel the ML-state and user-fn capabilities from mlpl-eval-env into mlpl-eval-envc-ml (EnvModels, EnvParams, EnvFrozen, EnvTokenizers, EnvTags + EnvMlIters overflow trait for the iterator accessors -- trait decls double counted fns, max 3 methods per file) and mlpl-eval-envc-userfns (UserFn type + EnvUserFns + EnvUserFnsRender). env_api prelude grows; compiler-driven imports across workspaces; env crate shrinks 26 -> ~19 modules.