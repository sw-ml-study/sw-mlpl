eval decomposition cluster peel 1 (now unblocked): extract the 19 model_* modules from mlpl-eval into mlpl-eval-models -- a NEW COMPONENT (components/eval-models) because the eval component sits at its 7-crate cap. eval_expr reaches the crate through a second OnceLock hook beside dispatch_hook (installed at eval entry); dispatched_call routes through the existing hook (made pub). Facade re-exports keep mlpl_eval:: paths; scoped tests + dual-feature clippy; mlpl-eval drops 69 -> 50 modules.