Same-origin auto-connect shipped: a serve-hosted page with no ?connect= probes its own origin pre-mount and adopts it (replaceState, no reload); static hosts stay browser-local. ?connect=off is the disconnect sentinel; the Connect panel offers the hosting server back for the disconnect->reconnect recovery path. docs/connect-topology.md records the design (CORS is server-declared, multi-backend stays server-side via --peer). 12/12 playwright connect scenarios green.