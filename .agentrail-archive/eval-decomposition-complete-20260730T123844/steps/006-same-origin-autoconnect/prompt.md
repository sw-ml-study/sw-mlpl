Default connect: when the page has NO ?connect= param, probe the page's own origin for /v1/devices; if it answers, auto-connect to the page origin (the common one-server case needs no param at all). ?connect= stays as the override for split UI/server topologies. Also write docs/connect-topology.md capturing the design discussion: same-origin default; explicit ?connect= override; multi-backend federation stays SERVER-side via --peer (UI connects to one mlpl-serve which delegates device blocks to CUDA/MLX peers) rather than multiple browser connect params; and multi-Ollama resolution (each serve has one configured Ollama host; preference order when a peer chain offers several: explicit --ollama-host on the connected serve wins, else its default; document a future --ollama-prefer flag rather than implementing).