Gate connect demos on real reachability: cpu-tier connect demos (Ask Ollama) currently enable on mere ?connect= URL presence. Make ALL connect demos require the /v1/devices probe to succeed, and gate the Ask Ollama demo on the server's Ollama actually being alive: /v1/devices response gains an ollama liveness flag (server checks its configured Ollama host with a short timeout), client maps it into the peer-device set (Device::Ollama), Ask Ollama's capability becomes device=ollama. TDD server+client, rebuild+restart server, rebuild pages, playwright verify.