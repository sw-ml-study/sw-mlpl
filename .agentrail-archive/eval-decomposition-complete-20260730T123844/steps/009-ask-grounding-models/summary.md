Grounded :ask against the two observed hallucination modes: keyword arguments (none exist -- positional only, with the exact anti-example) and models built/trained inside def u: bodies (verified empirically via live-server evals: models are top-level workspace objects; u: fns cannot create, receive, or train them). Cheat sheet gains the canonical chain/linear + train/adam pattern and the no-mse note. Prompt text moved to a non-wasm-gated prompt_text module with 4 native pin tests; pages redeployed. Working Life-model recipe verified at 86.8% next-state accuracy: m = chain(linear(400, 128, 0), relu_layer(), linear(128, 400, 1)); train 40 { adam(mean((apply(m, X) - Y) * (apply(m, X) - Y)), m, 0.01, 0.9, 0.999, 0.00000001); mean((apply(m, X) - Y) * (apply(m, X) - Y)) } with X/Y rows = flattened board pairs from u:life.