Regression fix: connect-mode /v1/devices probe is one-shot at ModeBar mount; a transient server outage at page-load time leaves peer_devices empty forever, so GPU (CUDA/MLX) demos stay disabled while cpu connect demos light up (user hit this on large12). Make use_peer_devices retry the probe with a bounded backoff until it yields a non-empty device set (pure retry-policy helper unit-tested natively), rebuild pages, verify with playwright CLI against the local server.