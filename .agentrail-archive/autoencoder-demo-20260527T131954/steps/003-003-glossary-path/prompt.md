Step 003: Glossary entries + path entries.

Add glossary entries: Autoencoder, Latent space, Bottleneck, Reconstruction error. Add the autoencoder steps to the Architecture Zoo path (if path exists) or note them for future path assembly. Update help text. Pages rebuild required.