Saga 14 step 004 shipped. New device("target") { body } scoped form: parser keyword + Expr::Device { target, body, span } AST node, evaluator routes ops inside the body through mlpl-mlx when env.device() == "mlx" and the new mlpl-eval mlx feature is compiled in (which itself triple-gates on macOS + aarch64 + mlpl-mlx/mlx). On non-MLX hosts the block falls back to the CPU runtime with a one-time warning. device("cpu") { ... } is always a no-op and works on every host. Nesting works in either direction (experiment around device, device around experiment) -- inner device target wins via Environment::device_stack push/pop. Labels and shapes propagate verbatim across the boundary. Dispatch helper (try_mlx_dispatch) lives in the new device.rs module and covers matmul, add/sub/mul/div/neg, exp/log/relu/sigmoid/tanh, transpose/reshape, softmax/log_softmax/cross_entropy, reduce_mul/mean/argmax (with Option<axis> for the latter three). 15 device-block tests pass on the mlx feature path; 14 pass on the default cpu-only path. Workspace cargo test clean both ways, clippy --all-targets and clippy --features mlx clean, fmt clean, sw-checklist FAIL count unchanged from step 003 (135).