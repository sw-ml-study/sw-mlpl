Saga 14 step 005 shipped. apply(model, X) and the Saga 11 models (linear, chain, residual, rms_norm, attention, causal_attention, embed, activations) now route every matmul/softmax/add/transpose through a single device::dispatched_call helper, so a device("mlx") { apply(m, X) } forward actually executes on MLX. New to_device(x, "mlx"|"cpu") builtin stamps Environment::tensor_device per binding and walks the ModelSpec param tree for model identifiers. Cross-device calls are a typed EvalError::DeviceMismatch with a structured {op, expected, actual} message. Model constructors stamp params with env.device() at allocation time, so building inside device("mlx") { } transparently tags params without a per-param to_device call. apply() cross-checks the model's params against the input's device tag and raises DeviceMismatch on disagreement -- no panics. Parity tests on Apple Silicon with the mlx feature: linear, chain(linear, tanh_layer, linear), and causal_attention forwards match CPU within 1e-3 fp32 tolerance; a Saga 13-shaped Tiny LM (embed + residual(rms_norm + causal_attention) + rms_norm + linear) produces equivalent logits on both devices for the same seed -- the end-to-end gate for this step. Workspace cargo test clean default; cargo test -p mlpl-eval --features mlx (9 new dispatch tests) and cargo test -p mlpl-mlx --features mlx clean; clippy both feature modes clean; fmt clean; sw-checklist FAIL count unchanged (135, same as step 004 baseline). Commit 8dbf2c8 pushed.