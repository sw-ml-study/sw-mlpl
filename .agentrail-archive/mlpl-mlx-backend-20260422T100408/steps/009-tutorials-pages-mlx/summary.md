Saga 14 step 009 shipped. Made Saga 14 user-facing: new 'Running on MLX' web REPL tutorial lesson walks from device(cpu){randn} -> device(mlx){randn} -> build model inside mlx scope -> to_device + apply. Lesson phrased around source-level parity since WASM browser is CPU-only and device(mlx) is a no-op there; calls out cargo run -p mlpl-repl --features mlx as the native path, links to docs/benchmarks.md for measured numbers. docs/using-mlx.md rewritten from 'Status: planned' sketch to shipped-API reference: strips disclaimer, aligns every API example with shipped surface (device(mlx){body}, to_device, EvalError::DeviceMismatch), new Performance section with the step-008 measured table and four-bottleneck breakdown, new 'Retrospective (what shipped vs the sketch)' section listing where sketch held up vs where reality was messier. docs/status.md moves Saga 14 from Planned to Delivered + adds Saga 20 (Neural Thickets) row. docs/saga.md adds Saga 14 retrospective in Saga 13 pattern (ten-step summary, measured numbers, bottleneck breakdown, honest 'correctness intact, speedup deferred' close). docs/plan.md marks Saga 14 COMPLETE v0.11.0. pages/ rebuilt via scripts/build-pages.sh so the live demo gets the new tutorial lesson; source + built artifact commit together per the rebuild-pages-after-web-ui-change invariant. Quality gates green: workspace tests clean default, mlx-feature tests clean, clippy -D warnings clean, fmt clean, markdown-checker clean on every doc touched (using-mlx.md, status.md, saga.md, plan.md), sw-checklist FAIL count unchanged at 135 (kept tutorial.rs at exactly 500 LOC). Commit e4f96c9 pushed.