Saga 14 step 002 shipped. mlpl-mlx now exposes forward-pass primitives for add/sub/mul/div/neg, exp/log/tanh/sigmoid/relu, reshape/transpose, each cfg-gated behind target_os=macos + aarch64 + feature=mlx and mirrored by infallible/Result-typed signatures on mlpl-rt for codegen parity. Binary ops validate shapes on the Rust side, merge labels via a local mirror of mlpl-array::merge_labels, then dispatch through shared fp32 convert helpers in common.rs. transpose falls back to DenseArray::transpose because mlx-rs 0.25.3 exposes transpose as a lazy strided view and hides mlx_contiguous, so as_slice on a view still returns the original row-major buffer. Both crates split across elementwise/activations/shapes/transforms modules to stay under the sw-checklist function-count budget. 17 MLX parity tests pass; default build + non-MLX cargo test stay green on non-Apple hosts.