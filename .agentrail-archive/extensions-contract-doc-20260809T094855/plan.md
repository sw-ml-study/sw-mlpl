# Saga: extensions-contract-doc

Record the demo-extensions upstream contract (extracted from
../demo-extensions/docs) as a companion planning doc in sw-mlpl,
and refine the future-sagas-queue entry with the recommended
minimal first slice. Docs/planning only -- no code. Captures the
registry/import/compiler-hook asks (A1-A8), sequencing, the
do-not-build-twice overlaps (builtins.rs dispatch chain, builtin-
catalog, include, modules-namespaces, compile-to-Rust), and the
recommended first saga (static-provider registry + use import +
help, interpreted/REPL, scalar V1).

## Steps
1. write-doc -- docs/companion-demo-extensions.md + refined queue
   entry; markdown gate.
2. close -- --done.
