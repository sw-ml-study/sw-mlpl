Surfaced ValueTags in REPL introspection. New tag_render.rs (8 fns) renders per-tag header_line + body_lines. inspect.rs sig changed to &mut Environment to support :untag (env.clear_tag). Added :tags listing (sorted by name, '(no tagged bindings)' empty message) and :untag (clears + 'untagged X' confirm, or 'X had no tag' on miss). format_describe replaces 'X -- array' with 'X -- <header_line>' when tagged and appends body lines after values preview. format_vars adds tag column at end of each line. Probability bodies include row-sum invariant verification (1e-5 tolerance). 17 tests pass. 24 callers updated for &mut env. sw-checklist failed count held at 139 (briefly hit 140 from oversized inspect fn; refactored to handle_untag helper to stay under 50-LOC max).