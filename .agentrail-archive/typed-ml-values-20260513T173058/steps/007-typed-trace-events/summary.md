Extended TraceEvent schema with input_types: Vec<Option<ValueTag>> and output_type: Option<ValueTag>, both serde skip_serializing_if defaulted so untagged programs serialize byte-identically to pre-step-007 JSON. New auto_tag::for_trace_event(expr, env) helper populates fields for Expr::Assign (input types from FnCall args looked up by Ident name; output type from env.get_tag(binding)). Three special-case trace push sites (grad/matmul/adam) populate empty defaults; type plumbing there is a follow-up. Skip-when-all-none policy keeps input_types vec terse: empties when no input is tagged. mlpl-trace depends on mlpl-core (already does, for Span); ValueTag serde derives serialize unit variants as strings and struct variants as single-key objects. Contract doc at contracts/eval-contract/typed-trace.md documents schema + round-trip + out-of-scope. 5 new tests + 4 mlpl-trace tests updated for new fields. sw-checklist 139 baseline held (briefly hit 140 from oversized eval.rs fn count; refactored into auto_tag.rs at 6 fns under warn>4 threshold).