Compile-to-rust step 005 shipped. Two new crates: crates/mlpl-macro/ (proc-macro=true) wraps mlpl-lower-rs and exposes the mlpl! macro; crates/mlpl/ is the user-facing facade that re-exports both the macro and the runtime via a hidden __rt alias. mlpl-lower-rs gained LowerConfig { rt_path } and lower_with_config() so the macro can emit ::mlpl::__rt:: paths through the facade while direct lower users still get ::mlpl_rt:: defaults. 5 end-to-end tests gated by MLPL_MACRO_COMPILE_TESTS=1 verify: scalar arithmetic, multi-stmt with ';' separator, labeled matmul succeeding, mismatched-contraction matmul producing compile_error!, and parse errors surfacing at compile time. Newline fix: TokenStream::to_string() emits '\n' in some bracketed-group formatting, which collided with MLPL's '\n' statement separator; macro rewrites all '\n' to ' ' before lexing and users separate statements with ';' inside the macro (documented). docs/compiling-mlpl.md updated. All quality gates clean, sw-checklist FAIL set unchanged (615 tests).