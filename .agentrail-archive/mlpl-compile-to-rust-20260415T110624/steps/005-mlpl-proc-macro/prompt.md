Phase 3: mlpl! proc macro. Create crates/mlpl-macro/ (proc-macro = true) that wraps mlpl-lower-rs. The macro accepts a block of MLPL source, lexes via mlpl-parser, lowers via mlpl-lower-rs, and expands to the generated Rust TokenStream. Span preservation: every ParseError or LowerError maps to syn::Error with spans pointing at the offending MLPL tokens inside the macro input -- rustc should show carats in the MLPL source, not at the macro invocation site. Re-export as pub use mlpl_macro::mlpl; from a new mlpl crate (crates/mlpl/) so users write  once and get both the macro and the runtime. TDD: a proc-macro-tests harness with passing and failing trybuild cases for (a) trivial scalar arithmetic, (b) matmul round-trip, (c) span-preserved parse error, (d) span-preserved label mismatch from phase 2d.