Compile-to-rust step 003 shipped. Extended mlpl-lower-rs to cover array literals (including nested), variable bindings (Assign emits let, Ident emits clone), and FnCall lowering for the 6 phase-1 primitives (iota/shape/rank/transpose/reshape/reduce_add flat+axis). Every MLPL expression now lowers to a DenseArray-valued Rust expression; programs wrap in a block with let bindings and yield the last expression. Added array_lit primitive to mlpl-rt in its own module (mirrors eval_array_lit semantics). 17 new string-match tests + 2 new end-to-end compile-and-run tests (all three e2e cases verified: '1+2*3'->7, 'reduce_add(iota(10))'->45, 'x=3;y=x*4;y+1'->13). All gates clean; sw-checklist unchanged.