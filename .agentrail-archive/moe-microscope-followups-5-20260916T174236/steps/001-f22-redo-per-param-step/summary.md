F22 REDO (full fix): Adam's bias-correction step counter is now PER PARAMETER (keyed 'adam:<param>', bumped in the per-param loop) instead of shared per-optimizer-name. Two variants under different names never share a step; clear_param (on rebind) drops the param's step so a rebound model restarts at step 1; reset_optimizer clears all. Single-model training unchanged (params advance in lockstep). The moe probe's reused==fresh now holds (rebind alone suffices). Test rebound_model_first_adam_step_matches_a_fresh_model; regression optim_state/train_loop 13/first_train_resident 3/tiny_lm_train 8/train_val_curve 3. sw-checklist 38/570 unchanged. Rebuilt repl/build/serve. Pushed.