F24: apply_engram's ids argument bound to a user-function parameter now resolves through the traced scope so the gradient reaches the memory table. Root cause: call_apply_engram resolved ids with the eager global env (eval_expr), blind to function-arg bindings; switched to grad_const::eval_index_expr (params overlaid then restored), the same fix used by gather_rows and F23. RED test asserts param-ids grad == global-ids grad (0.0357, non-trivial). Verified in rebuilt release+debug repl on probes/f24. Binaries rebuilt: repl/build (release+debug), serve (release). sw-checklist held 38/570.