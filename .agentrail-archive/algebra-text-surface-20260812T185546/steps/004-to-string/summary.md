Added to_string(x): scalar number -> shortest round-trip string (reusing json_encode::push_number, the same formatting to_json gives a scalar), integral values bare (8/2 -> "4"); to_number(to_string(x)) recovers x. Non-scalar/non-number is a hard error. Registered in fncall_string; documented in lang-reference + :builtins catalog. 5 tests incl sqrt(2) round-trip. sw-checklist 557/49/524 (+1 fncall_string module-fn warn, net-neutral with the refactor's -1). This completes the work order's 'if you only do three' -- A1, B1, B2 all shipped.