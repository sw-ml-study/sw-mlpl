Close the algebra-text-surface saga: queue the remaining demo-abstract-algebra work-order items into docs/future-sagas-queue.md -- B3 (str_len/str_slice/str_find/str_split string ops), C1 (labelled grid renderer), A2 (recursion-depth cap), A3+D1 (comment-block rendering), and any minor items. Do NOT implement them; just record them as a queued saga with the downstream source. Then run agentrail complete --done. Docs-only; ratchet holds (exception) unless a code warning is cheaply retireable.