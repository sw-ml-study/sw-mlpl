Fixed A1: mlpl-repl <bare-filename.mlpl> exited 1 (bare filename's Path::parent() is Some("") -> empty sandbox root -> FsProvider::new("") fails). Extracted resolve_root_dir in mlpl-cli/include_script.rs (empty parent treated as absent -> resolves to "."); load_script + the run_script hook both use it. Verified on the rebuilt mlpl-repl binary: bare -> Ok(1) exit 0, other forms unchanged. 2 unit-test regressions + full mlpl-cli suite green. Key gotcha recorded: mlpl-repl is the BINARY crate, mlpl-cli the LIBRARY -- rebuild -p mlpl-repl, not -p mlpl-cli.