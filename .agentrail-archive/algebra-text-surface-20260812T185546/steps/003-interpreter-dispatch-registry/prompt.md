Refactor eval_fncalls.rs's builtin dispatch from the hand-written .or_else chain of ~18 crate::fncall_*::try_dispatch calls into a data-driven dispatcher registry (the interpreter analog of the compiler's fncall registry). Config = a slice/list of family dispatchers (unify their signatures -- most take (name,args,env,trace); a few also take span -- add an ignored span param so all share one fn-pointer type); dispatch = iterate the slice with find_map, command forwarded until a handler returns Some; no .or_else chain. Consider a builtins!-style macro to build the list succinctly. Preserve dispatch ORDER (first match wins) and every existing behavior; keep the 900+ mlpl-eval tests green. This is family-level (per-family match stays); a full per-name HashMap flatten of all 184 builtins is a bigger separate effort. Hold sw-checklist (or note the exception).