expunge(name | [names]) builtin per q-and-a design: 1 = name free afterwards (idempotent), 0 = malformed; clears all value tables via clear_binding, removes u: functions and their test-registry rows. :erase REPL command (multi-name). Update docs/usage.md REPL table (pinned by usage_table_tests) + lang-reference/glossary/catalog rows + README pin. TDD. Gates; commit before complete; push.