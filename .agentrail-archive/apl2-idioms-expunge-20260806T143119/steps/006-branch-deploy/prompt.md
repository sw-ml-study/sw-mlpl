User ask: different builder for Pages after GitHub runner failures. Switch to deploy-from-branch (build_type legacy, branch gh-pages): scripts/deploy-pages.sh does subtree split of pages/ + force-push to gh-pages (generated artifact branch); remove the Actions workflow; update CLAUDE.md deploy section + memory; cancel the stuck queued run; deploy and verify live serves the current bundle.