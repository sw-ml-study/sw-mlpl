Checklist paydown spike: retired 5 Module-Function-Count fails (43->38) via responsibility-based module splits, tests/clippy/fmt green per crate. runtime-bits logic(10)+shift(8) -> bit_domain/logic_binary/logic/shift/bit_views; runtime-umap umap_layout(9) -> umap_ab_fit/umap_forces/umap_layout; parser record_parser(8) -> record_parser/decl_parser; runtime-math array_ops(8) -> schedule/constructors/array_util. Chose targets that don't create new fails (avoided crates already at the 7-module ceiling). Cost: +2 crate-module warnings (small runtime crates with no module slack); umap/parser cost zero. No behavior change.