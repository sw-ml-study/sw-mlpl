cuda-demo-parity step 3: CUDA tic-tac-toe demo shipped + verified live. CUDA_TICTACTOE_FINETUNE demo (board-policy MLP Chain[LinearLora,relu,LinearLora] fine-tuned under device(cuda) via the step-2 fast path), dropdown entry + Cuda capability + progress notes. Found + fixed the UX issue: self-play minimax was 42s CPU (nvtop idle); cut to 8 games (~4s) and raised the GPU fine-tune to 6000 steps (~11s sustained GPU, nvtop visible). Verified end-to-end on the live 0.0.0.0 server: self-play 4s, GPU train 11s, learns before[31,6,3]->after[12,7,21], GPU 31/38 samples>0%. Added scripts/build-all.sh (sequential serve+pages build, avoids the shared-target deadlock).