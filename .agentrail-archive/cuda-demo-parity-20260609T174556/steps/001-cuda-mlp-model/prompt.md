Step 1 of cuda-demo-parity: add mlp_forward + MlpWeights to mlpl-cuda-model (candle analog of mlpl-mlx-model::mlp.rs). MlpWeights{w1,b1,w2,b2,scale1,scale2} frozen bases; mlp_forward(w, adapters[A1,B1,A2,B2], x, y_onehot): h=relu(lora_linear(x,w1,A1,B1,scale1)+b1); logits=lora_linear(h,w2,A2,B2,scale2)+b2; cross_entropy(logits,y_onehot). All candle-traceable (reuse mlpl-cuda-forward lora_linear + cross_entropy; relu via candle Tensor::relu; +b via broadcast_add). Gated test: train the 4 adapters via candle AdamW (A nonzero, B zero start), assert cross-entropy drops -- on the GPU. Keep <=4 fn/module + <=4 module/crate (mlp.rs is a new module in mlpl-cuda-model alongside model.rs -> 2 logic modules, fine). clippy clean cuda+stub. DISK: scope to cuda-model, clean after.