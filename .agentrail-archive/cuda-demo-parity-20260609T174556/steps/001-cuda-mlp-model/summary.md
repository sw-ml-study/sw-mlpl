cuda-demo-parity step 1: mlpl-cuda-model mlp_forward + MlpWeights (candle analog of mlpl-mlx-model::mlp) -- the board-policy MLP (Chain[LinearLora,relu,LinearLora], both layers LoRA'd) as one traceable graph. Gated GPU test trains the 4 adapters via candle AdamW; cross-entropy drops on the RTX 5060 Ti. clippy clean; sw-checklist held. Also committed the future-saga planning docs (multi-backend-connect + speculative-decode-cpu/-multigpu) + the cuda-demo-parity plan.