Move the scatter_labeled corner legend OUTSIDE the plot area (user request from step 037 chat). Currently the legend renders at x = W - PAD - 60 = 310 which is INSIDE the SVG's data area (PAD..W-PAD = 30..370). The user wants the legend in a dedicated right-side gutter, not overlapping data points. APPROACH: (1) add a write_svg_open_with_size(out, w, h) helper that emits the same viewBox + background + axis lines as write_svg_open but with caller-specified canvas width/height; keep the axis-line drawing at the existing PAD..W-PAD / PAD..H-PAD coords (so the data area is unchanged) and let the legend live in the new W..W+gutter region. (2) Update analysis_scatter_labeled to call the new helper with width = W + 90, render legend at x = W + 10 (inside the new right gutter). (3) Update svg_analysis_tests asserts where needed (viewBox width changes; circle count stays the same). (4) Pages rebuild required. (5) Visually verify by inspecting the produced SVG cached file. Net: the data plot area stays the same size; legend lives outside it. Do not change other viz functions (loss_curve, hist, etc.) -- they keep the original W/H.