Survey mlpl-eval/src/ for the remaining model_* / eval_* files and the env_* impl files. Count what's left vs what's moved. The mlpl-eval module count is the saga's main FAIL target -- need to drop it below 7 to retire the Crate-Module-Count FAIL. Possible directions: (1) extract Value+EvalError to mlpl-eval-core now that TokenizerSpec is there, unblocking the analyze functions that still wrap eval_expr; (2) bundle multiple env_trait_impls_*.rs files into one or two larger files to reduce module count; (3) bundle multiple env_*.rs (per-feature impl Environment) files into one. Pick the path with the biggest impact.