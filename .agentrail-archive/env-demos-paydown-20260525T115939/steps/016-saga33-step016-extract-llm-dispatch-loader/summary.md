Step 016 extracted llm_call to mlpl-models-llm sub-crate (7th domain crate). Tiny single-module library: LlmError + llm_call_inner that takes 3 pre-resolved Strings and forwards to mlpl_runtime::call_ollama. The boundary is at String (not Value or Expr) so the sub-crate needs zero env capabilities. mlpl-eval's wrapper does Expr->String resolution and Value::Str re-wrapping. Loader extraction deferred -- it has multi-variant Value entanglement that needs more design.