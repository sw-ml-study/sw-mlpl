Continue analyze extraction. Two remaining model_* files in mlpl-eval: llm_dispatch.rs (LLM-specific eval entry points) and loader.rs (preloaded dataset/corpus loaders). Extract both. May need new traits for HasDataDir + HasTokenizers + HasRecords. Watch the env-traits module count -- consider bundling new traits into existing files. Also bpe.rs (BPE tokenizer training) and tokenizer.rs (byte-level tokenizer eval) could be extracted to mlpl-tokenizer sub-crate if simple enough.