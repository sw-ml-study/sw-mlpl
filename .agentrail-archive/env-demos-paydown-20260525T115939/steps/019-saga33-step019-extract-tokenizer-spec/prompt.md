Examine tokenizer.rs (206 LOC, has TokenizerSpec enum + dispatch + eval_tokenize_bytes + eval_decode_bytes). The TokenizerSpec enum is a leaf data type that could move to mlpl-eval-core (alongside ModelSpec). The byte-level eval fns are env-tangled (use Value variants). Strategy: just move TokenizerSpec to mlpl-eval-core in this step (unblocks future moves of Value/EvalError that currently depend on it via Value::Tokenizer). Or extract the pure helpers in tokenizer.rs to a tokenizer-helpers sub-crate. Strict gate: should be achievable.