Quick docs-sync: the Dimensionality reduction learning path in apps/mlpl-web/src/paths.rs still describes a pre-MDS world. Two stale strings: (1) Dim-reduction zoo Step::Demo's why text says 'three side-by-side projections (PCA / t-SNE / UMAP)' -- now there are FIVE projections (MDS + RP added in step 041). Update to reflect the new lineup including 'JL random projection as sanity baseline'. (2) The 'What's next' Note::body says 'Phase 5 of the dim-reduction milestone adds MDS and random projection' -- Phase 5 shipped in step 041. Replace with a forward-looking note (permutation-sensitivity heatmaps for t-SNE/UMAP referenced in the Reading critical-dimensions lesson are still future; the dim-reduction-milestone close-out is upcoming). (3) Optional: insert Glossary steps for Multidimensional Scaling and Johnson-Lindenstrauss Lemma between the UMAP cluster of steps and the Dim-reduction zoo demo, giving readers a reference for the new methods. (4) Pages rebuild required. (5) No new tests needed -- existing path/lesson tests will fail if any referenced lesson title or glossary term is missing.