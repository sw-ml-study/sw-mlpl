Shipped the public umap(X, n_neighbors, min_dist, iters, seed) -> [N, 2] builtin composing fuzzy simplicial set construction (per-row sigma binary search + fuzzy union) with SGD layout optimization (cross-entropy + negative sampling, a=1/b=1 Student-t curve). 3 new modules in mlpl-runtime-umap (umap_simplicial, umap_layout, umap_builtin) + 7 unit tests (shape, determinism, bounded, gradcheck, 2-cluster separation) + glossary UMAP entry + lang-reference row + inspect_groups entry. sw-checklist 129 fail held (structural builtins.rs), 447 warn (+2 anticipated, full justification in commit trailer).