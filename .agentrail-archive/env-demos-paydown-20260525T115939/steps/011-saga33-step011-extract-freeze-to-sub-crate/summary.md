Step 011 extracted freeze_inner/unfreeze_inner to mlpl-models-freeze sub-crate using the C+D pattern: generic E: HasModels + HasFrozen, generic Err: From<FreezeError>, caller-supplied resolve closure. New crate has 4 modules (lib + error + inner + resolve), each <=3 fns, all PASS. mlpl-eval model_freeze.rs slimmed to 3 thin wrappers that pass eval_expr_resolver closure. Consolidated error_from_tape.rs + new freeze conversion into a single error_from_components.rs so future extractions add an impl block (not a new module) -- monotonic shrink of mlpl-eval module count guaranteed from here on.