Add an :introspect REPL slash-command that bundles the existing inspector commands (:version, :wsid, :describe, :fns, :vars, :models, :experiments, and any other relevant :inspect helpers) into one large concatenated output. The user can run it at the end of any demo (optional) to dump the full workspace state in one shot. (1) Find the existing :command dispatcher (likely in mlpl-eval's REPL command path; grep for ':version' / ':vars' arms). (2) Add ':introspect' that walks every relevant inspector in a fixed order, printing each section under a clear ## header so a long scroll is still readable. Sections in order: :version, :wsid, :fns (or :builtins), :describe <each user-defined fn> if any, :vars, :models, :experiments, plus any other inspectors that exist. Look at the existing inspect_groups + inspect_*.rs modules to enumerate everything. (3) Add :introspect at the END of the Workspace Introspection demo (apps/mlpl-web/src/demos_basics.rs WORKSPACE_INTROSPECTION) as the final line, so running that demo emits the full bundle. (4) Add help_completeness + inspect_helpers tests if they exist; otherwise file an inline test in mlpl-eval/tests/ asserting :introspect produces non-empty output containing each section header. (5) Update lang-reference.md and glossary.md (':introspect' entry) and inspect_groups if needed. (6) Pages rebuild required (demo source changed).