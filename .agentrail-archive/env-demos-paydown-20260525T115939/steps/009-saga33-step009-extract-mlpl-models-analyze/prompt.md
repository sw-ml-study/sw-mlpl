Continue the components/mlpl-session/ extraction with the second sub-crate: mlpl-models-analyze. Move crates/mlpl-eval/src/{model_clone, model_embed_table, model_estimate, model_feasibility, model_freeze, model_lora, model_perturb}.rs into components/mlpl-session/crates/mlpl-models-analyze/src/. These are mostly pure functions over ModelSpec; the crate should pass File-LOC + Crate-Module-Count (target <=7 modules, ideally <=4). Each file probably needs its own dedicated error type (AnalyzeError) following the loose-coupling pattern from mlpl-models-tape. Add From<AnalyzeError> for EvalError in mlpl-eval. Strict gate: net-negative on BOTH fails AND warnings vs HEAD~1; structural-only commits should use 'sw-checklist: exception' trailer.