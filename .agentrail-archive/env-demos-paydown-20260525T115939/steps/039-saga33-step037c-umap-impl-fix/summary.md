UMAP impl fix: fitted (a, b) curve params from min_dist via Gauss-Newton (replaces hardcoded a=1, b=1), widened coord bound from +-4 to +-100, raised INIT_SCALE from 2 to 10 -- pre-fix UMAP piled points against the clamp walls; post-fix UMAP recovers the two-moons manifold structure (verified visually + via new knn-purity > 0.9 test). Removed the honest-caveat note from the umap_vs_pca demo + UMAP lesson. all_demos_smoke passes (375s).