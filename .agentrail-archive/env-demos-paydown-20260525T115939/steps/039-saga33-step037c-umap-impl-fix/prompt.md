Fix the simplified UMAP layout so it actually preserves manifold structure (responding to user feedback from step 037: the umap_vs_pca demo's PCA output shows clear moon crescents but the UMAP output is blockier with coordinates piled against the coord clamp). Three load-bearing changes: (1) Replace the tight coordinate clamp at +-INIT_SCALE*2.0 with a much wider safety bound (e.g. +-100) or remove entirely -- only catch truly degenerate edges that produce NaN. (2) Fit a, b from min_dist instead of hardcoding a=1, b=1. Reference impl uses scipy curve_fit on phi(x) = (1 + a*x^(2b))^(-1) to approximate 1 if x <= min_dist else exp(-(x-min_dist)/spread). A simpler analytical/numerical fit is acceptable as long as min_dist actually controls the attractive curve shape. (3) Optional: add a spectral init path (top-k eigenvectors of the symmetrized normalized Laplacian of the fuzzy graph) to give the SGD a better starting point on manifold data. (4) Verify visually by inspecting the umap_vs_pca demo SVG output and confirming UMAP produces something more arc-like than blockier-pile. (5) Once verified, remove the caveat from the UMAP_VS_PCA Demo entry / UMAP_MODERN_DEFAULT lesson try_it / demos/umap_vs_pca.mlpl. (6) Add a unit test that asserts manifold preservation on a synthetic curved manifold, not just cluster_separation. (7) Rebuild pages.