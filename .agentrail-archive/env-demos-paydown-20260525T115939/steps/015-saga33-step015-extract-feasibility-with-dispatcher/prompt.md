Extract model_feasibility to mlpl-models-feasibility sub-crate. Requires defining a new Dispatcher trait in mlpl-env-traits that abstracts crate::device::dispatched_call (the device-aware matmul/etc dispatch in mlpl-eval). The trait surface is: fn dispatch_call(&self, env: &E, op: &str, args: Vec<DenseArray>) -> Result<DenseArray, ...>. mlpl-eval impls Dispatcher on a zero-sized struct that delegates to the existing device::dispatched_call. mlpl-models-feasibility takes &dyn Dispatcher + &E: HasStrings. Add From<FeasibilityError> to error_from_components.rs. Strict gate: should be net-neutral or slight negative.