Added corner index legend to scatter_labeled viz (one PALETTE swatch + integer label per unique class). Also added an honest caveat to umap_vs_pca demo + UMAP lesson after user reported the demo's stated conclusion did not match the rendered output -- PCA showed clear moon arcs while my simplified UMAP (a=1, b=1 + tight coord clamp + no spectral init) produced blockier patterns. Verification gap admitted.