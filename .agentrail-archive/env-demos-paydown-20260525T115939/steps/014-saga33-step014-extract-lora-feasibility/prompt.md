Continue analyze extraction with model_lora + model_feasibility. lora needs HasModels + HasVars + HasParams + HasFrozen + HasTensorDevices + HasModelIds (uses clone_spec from mutate, then walks the spec to wrap Linear layers as LoraLinear). feasibility uses HasStrings (mlpl_device_throughput_gflops). Both also call eval_expr -- inject as closures. Move to a new mlpl-models-tune sub-crate. Add From<TuneError> to error_from_components.rs. Bundle wrappers in model_inspect.rs or new model_tune.rs.