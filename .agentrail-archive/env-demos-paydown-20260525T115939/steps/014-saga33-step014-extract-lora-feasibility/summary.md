Step 014 extracted lora to new mlpl-models-tune sub-crate (4 modules, all PASS no warn). Scoped to lora only -- feasibility deferred to step 015 due to its crate::device::dispatched_call dependency that needs a Dispatcher trait. lora_inner is generic over HasModels + HasVars + HasParams + HasFrozen + HasTensorDevices + HasModelIds + Err: From<TuneError>; clone_spec's MutateError converts via local impl From<MutateError> for TuneError.