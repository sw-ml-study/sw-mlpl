Tackle loader.rs. It has these pieces: eval_load (env.data_dir + fs + parse_csv), eval_load_preloaded (in-memory PRELOADED registry + pets_tiny), eval_load_images (cfg=image-io), eval_fetch_dataset (cfg=image-io), resolve_in_sandbox (pure path helper), parse_csv (pure parser). Strategy: extract the pure-data helpers (resolve_in_sandbox + parse_csv) to a small mlpl-loader-helpers sub-crate first. Save the env-dependent eval_* functions for a follow-up step that introduces HasDataDir trait. Strict ratchet should be achievable since the pure helpers add no warns and the deleted helpers reduce loader.rs's LOC.