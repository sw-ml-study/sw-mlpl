Swapped REPL completion trigger from Tab (broken: focus traversal won over preventDefault) to Shift+Space (per user direction: reserve Tab entirely for browser nav, avoid Ctrl+Space which collides with OS/browser bindings). Renamed handle_tab -> handle_completion. Glossary 'Completion popup (REPL)' entry rewritten; 'Tab completion' redirect updated. Predicate test renamed + asserts only Shift+Space fires (and that Tab is NOT a trigger). README glossary count bumped 294->295 for the new Completion-popup entry.