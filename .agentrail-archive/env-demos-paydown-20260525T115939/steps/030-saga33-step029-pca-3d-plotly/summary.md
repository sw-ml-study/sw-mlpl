Saga 33 step 030: interactive Plotly 3D viz shipped. New  viz type in mlpl-viz emits a self-contained HTML/JS payload (Plotly.newPlot + plotly_click handler) detected by the web playground via the <!-- mlpl-plotly3d --> marker. PlotlyPanel Yew component re-executes inline scripts after innerHTML insertion (standard WHATWG workaround). PCA_3D demo in new demos_dim_reduction.rs (the home for future UMAP / MDS / random-projection demos as the dim-reduction milestone progresses). 6 viz-side unit tests cover marker, single+multi-trace emission, cluster-map back-mapping, shape validation, dispatch. CDN Plotly added to index.html. Pages rebuilt + pushed; deployed to github.io.