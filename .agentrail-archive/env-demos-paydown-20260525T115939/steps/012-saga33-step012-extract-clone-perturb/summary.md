Step 012 added HasVars + HasTensorDevices + HasModelIds traits to mlpl-env-traits and extracted clone_model + perturb_params to new mlpl-models-mutate sub-crate. Both ops are now generic over the trait set + injected resolver closure + generic Err with From<MutateError>. Bundled the mlpl-eval-side wrappers into a single model_mutate.rs file (replaces model_clone.rs + model_perturb.rs). Consolidated From<MutateError> impl into error_from_components.rs (per the step 011 architecture).