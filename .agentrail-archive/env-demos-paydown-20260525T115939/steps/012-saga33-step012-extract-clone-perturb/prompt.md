Continue analyze extraction with model_clone and model_perturb (similar shape to freeze: take ModelSpec from a model identifier, modify env params). Move both to components/mlpl-session/crates/mlpl-models-mutate/ (single sub-crate for both since they share the model-resolve pattern). They need HasModels + HasVars + HasParams + HasTags + HasDevices (for set_tensor_device). May need to add HasVars + HasTags + HasDevices traits to mlpl-env-traits if they don't exist yet. Strict gate: subsequent extractions should net-negative since error_from_components.rs absorbs the conversion impls.