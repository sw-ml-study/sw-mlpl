Saga 33 step 025: fixed the Moons MLP decision-boundary y-flip regression. Root cause was in analysis_boundary_2d + svg::decision_boundary::draw_surface: both renderers iterated raw[r*cols+c] with r=0 at image top, but their overlaid training points use math-y-bottom convention, vertically mirroring the surface relative to its own dots. Fix: flip r to read raw[(rows-1-r)*cols+c]. Lifted boundary_2d input validation to its own module to keep the renderer under 25 LOC. Added 4 new tests across two crates: synthetic-grid orientation tests in mlpl-viz that pass-after / fail-before the fix; end-to-end Moons MLP p1-grid axis-variance + corner-class tests in mlpl-eval. Diagnostic file kept for future investigations. Bisect proved the bug predates saga 33 entirely -- latent since boundary_2d was first added.