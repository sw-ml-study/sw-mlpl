Implemented svg() built-in with scatter type. mlpl-viz crate is now real (was placeholder): svg::render dispatcher + svg::render_scatter for Nx2 matrices with Catppuccin Mocha colors, axes, and auto-scaled bounds. mlpl-eval gains EvalError::{ExpectedString, BadArity, VizError} and eval_expr special-cases Expr::FnCall{name:'svg',..} routing through eval_svg which validates arg types and returns Value::Str. 9 new tests (7 viz unit + 2 wasm end-to-end) all passing.