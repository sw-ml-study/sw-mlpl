loadMathJax() returns a cached promise that lazy-loads from CDN on first call. mlplToLatex() converts common MLPL function calls to LaTeX via iterative innermost-first regex replacement (transpose, sqrt, matmul, softmax, generic funcName -> text). renderDerivation wraps cleanRhs in \(...\). openInspector + refreshInspectorBody await MathJax then typesetPromise the body. Demo proof: attention.mlpl A inspector shows derivation as real math.