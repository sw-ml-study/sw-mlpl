MathJax-rendered derivation steps landed in 1cc337e (loadMathJax CDN lazy-loader, mlplToLatex innermost-first regex conversion, openInspector/refreshInspectorBody await+typesetPromise, renderDerivation wraps RHS in \(...\)). Prior session committed the implementation but skipped agentrail complete. This session verified the implementation end-to-end (attention.mlpl 'W0 = softmax(matmul(Q0, transpose(K0)) / sqrt(d_k), 1)' converts to \text{softmax}(Q0 \cdot K0^T / \sqrt{d_k})), confirmed pages/ in sync, and fixed the now-stale derivation hint that still claimed plain-text/follow-up (feceb68).