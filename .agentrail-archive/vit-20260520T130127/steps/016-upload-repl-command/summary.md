Saga 29 step 016: :upload x REPL command, first in-tree consumer of Value::Result (step 012). Web-only slash command opens the file picker programmatically in the user-gesture sync chain; binds the chosen photo as a caller-named Result. Success: <name> = Ok({pixels: [1, 3, 64, 64], h: 64, w: 64}). Dismissed: <name> = Err('cancelled'). Decode failures: <name> = Err('decode failed'). The legacy Upload Image button takes the same Result-binding path with the hardcoded name 'uploaded'. Identifier-shape validation rejects spaces / digits-first / hyphens cleanly before reaching the session. Tests: 4 handlers.rs unit tests for the parser + identifier validation, 2 wasm session tests for the bind_upload_result_ok/err round-trip through is_ok/is_err/unwrap/err_message. Docs: glossary :upload entry, lang-reference Result section refreshed, REPL help text + 4 budget-driven refactors (eval_one_line, handle_upload_command, upload::messages submodule, mlpl-wasm/src/upload.rs submodule) kept sw-checklist at 157 (== parent). Pages rebuilt.