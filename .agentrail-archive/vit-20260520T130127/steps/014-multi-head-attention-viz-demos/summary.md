Saga 29 step 014: multi-head attention viz demos. Added svg(data, 'heatmap_grid') viz primitive (mlpl-viz/src/svg/heatmap_grid.rs) that renders [N, R, C] tensors as an N-cell grid of heatmaps with per-cell colormaps. Two new demos: vit_attention_pattern_multihead.mlpl (no training, baseline) and vit_multihead_quick.mlpl (trained 4-head ViT with post-train per-head attention maps). Added both to apps/mlpl-web/src/demos.rs (the trained variant is in SKIP_DEMOS). README demo count bumped 25 -> 27. Tests: vit_multihead_viz_tests.rs (3 tests, end-to-end + SVG structure + #[ignore]'d trained variant). CLAUDE.md updated with 'Release-profile tests for demo-heavy crates' subsection (180s release vs 15+min dev for all_demos_smoke). Quality gates: clippy + fmt + markdown-checker clean, sw-checklist 157 (== parent). Pages rebuilt and pushed (mlpl-web-4ed94d2fe03d8e21).