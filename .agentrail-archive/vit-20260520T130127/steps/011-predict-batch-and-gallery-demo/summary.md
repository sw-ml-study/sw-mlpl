Saga 29 step 011: predict_batch builtin + 3-arg gallery overlay + per-image demo. predict_batch(model, X) returns argmax over the trailing axis as integer class indices (single forward + argmax in one call). svg(images, gallery, overlay) extended to accept [N] or [N, K<=4] integer overlay, rendering values as '/' -separated text under each thumbnail. demos/vit_predict_gallery.mlpl trains the quick-demo ViT on 16 images and renders the labeled gallery (actual / predicted per thumbnail). Web demo entry, ProgressNote for the slow train block, learning-path update. 7 new tests (4 predict_batch + 3 gallery overlay) on top of existing. sw-checklist unchanged at 152.