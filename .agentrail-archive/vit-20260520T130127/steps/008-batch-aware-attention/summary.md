Saga 29 step 008 (inserted prereq): batch-aware-attention. apply_attention + compute_attn_weights + attention_single_head_tape now accept rank-3 [B, T, d_model] input alongside rank-2 [seq, d_model]. Forward path loops over batch axis 0 via DenseArray::take and stacks outputs. Tape path emits B rank-2 chains and stitches via take + reshape + chained concat -- no new tape primitive needed (reuses step 007 take + step 005 concat). attention_weights returns [B, T, T] for rank-3. Multi-head h>1 + rank-3 still rejects cleanly (lands in step 010). 6 tests including gradcheck parity vs finite differences. +2 sw-checklist exceptions, same structural pattern. Trained ViT demo (step 009) is now fully unblocked.