Saga 29 step 019: added legends + axis scales to every viz that lacked them. heatmap_grid now shows a viridis strip + per-cell min/max labels under each per-head cell (the magnitudes differ between heads, so per-cell scales are essential). scatter / line / bar pick up corner xmin/xmax/ymin/ymax labels via the new write_corner_scale_labels helper in svg/mod.rs. decision_boundary picks up a vertical colorbar with lo/mid/hi labels matching the heatmap legend pattern. 6 new tests across the viz crate (69 total pass). Pages rebuilt.