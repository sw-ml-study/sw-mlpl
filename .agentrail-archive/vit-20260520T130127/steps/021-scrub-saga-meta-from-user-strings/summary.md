Saga 29 step 021: scrubbed developer-process metadata from user-facing strings. The user reported that the Vision path's :upload glossary entry was unreadable (wall-of-text with no formatting) AND meta-referenced 'Saga 29 step 016 / 017 / 012' -- the playground user is learning ML, not the repo's dev process. Audited every user-facing string: docs/glossary.md (53 saga refs -> 0), paths.rs (3 -> 0), demos.rs (7 -> 0), help.rs (1 -> 0), lessons.rs + lessons_advanced.rs (10 -> 0). Rewrote the :upload glossary entry as two short scannable paragraphs grounded in user actions (pick a photo, get Ok or Err, inspect with is_ok / unwrap / err_message). Developer-facing docs (architecture, plan, milestone-*, CLAUDE.md, .agentrail/, commit messages) correctly retain saga refs. Pages rebuilt.