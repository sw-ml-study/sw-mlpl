Saga 29 step 024: glossary hyperlinks. New [[term]] sigil in the path_body markdown-ish renderer becomes a clickable button that dispatches a window-level mlpl-glossary-open CustomEvent. A new GlossaryPopupHost component (mounted at the app root) listens for that event and renders a modal overlay with the matched glossary entry, re-using the same renderer so nested [[term]] cross-references work recursively. Demo Narration bodies now route through the renderer too, so demo intros/takeaways/ProgressNotes get clickable links + markdown formatting. CSS for the link + modal styled to match. First wire-up: the Vision path's BYO Note now links four key terms. sw-checklist 157 (== parent). Pages rebuilt.