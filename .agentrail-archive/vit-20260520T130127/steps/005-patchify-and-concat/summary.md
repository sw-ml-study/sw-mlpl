Saga 29 step 005: patchify(x, P) + concat(a, b, axis) builtins with full tape lowering. patchify rearranges [B,C,H,W] -> [B,N,P*P*C] in channel-outer order. concat extends the legacy 2-arg form to a 3-arg axis-aware variant (axis 0/1 in this release). New NodeKind::Patchify + NodeKind::Concat with forward + backward. Wired through DenseArray ops, autograd Tensor methods, mlpl-runtime builtin dispatch, eval-grad tape integration, and the MLX re-run match. 12 tests including gradcheck parity vs closed-form on [2,3,4,4] patchify and CLS-prepended [2,1,4]+[2,8,4] concat fixtures. Help-completeness regression coverage retired. +7 sw-checklist exception, same structural pattern as prior steps.