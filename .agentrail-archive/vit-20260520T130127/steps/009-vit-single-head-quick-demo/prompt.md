Saga 29 step 007 (per docs/milestone-vit.md Phase 2 + step 005 in milestone-vit.md): demos/vit_single_head_quick.mlpl. Full training loop. CPU-only. Single-head attention(128, 1, ...), rms_norm, relu_layer. 100 training steps on the load_preloaded("pets_tiny") fixture. Validation accuracy must beat the 60% threshold for a fixed seed. The web REPL tutorial gains a 'Vision Transformers' lesson that runs this demo verbatim. The demo dropdown gets a 'Pets: cat vs dog (quick)' entry. Tests: integration test in crates/mlpl-eval/tests/ runs the demo to completion and asserts the val-accuracy threshold; WASM build runs the same source. NOTE: this step needs the train{} block, apply(model, X) over a [B, T, d] batch (currently attention requires [seq, d_model] rank-2 -- may need a batch-aware path OR per-image training loop), adam + grad over the whole stacked Phase-1 model. If batch>1 attention can't land cleanly, scope down to looping over single images with apply(model, single_image_seq) inside a train{} block and accumulating gradients manually. Build pages/ after the web change.