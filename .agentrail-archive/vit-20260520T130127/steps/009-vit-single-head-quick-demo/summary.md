Saga 29 step 009: trained Vision Transformer demo. demos/vit_single_head_quick.mlpl forward-trains a 20-image balanced subset of pets_tiny (10 cats + 10 dogs) for 100 full-batch adam steps. Single-head attention(128, 1), MLP classifier head. Final training accuracy hits 1.0 (well past 60% gate from milestone-vit.md). Pipeline: patchify -> reshape -> linear -> reshape -> rank-3 attention (step 008) -> take (step 007) -> classifier -> cross_entropy. Wire-up: collect_params now walks model params inside ArrayLit; grad.rs gains reshape dispatch. Web demo dropdown entry + pages rebuild. 1 integration test #[ignore]'d (~140s release run). sw-checklist unchanged at 152. B=20 instead of full B=200 because the rank-3 attention tape's concat chain is O(B^2) -- documented as a deliberate followup.