Saga 29 step 023: extended Decision Boundary demo to walk AND/OR/NAND/NOR (all linearly separable, all converge) + XOR (the canonical non-linearly-separable failure case). Added a second new demo 'Decision Boundary: XOR (with MLP)' that solves the same XOR data with chain(linear, tanh_layer, linear) trained via adam; reaches training accuracy 1.0. The linear demo's XOR takeaway points the reader at the MLP demo. Demo count 27 -> 28. sw-checklist 157 (== parent). Pages rebuilt.