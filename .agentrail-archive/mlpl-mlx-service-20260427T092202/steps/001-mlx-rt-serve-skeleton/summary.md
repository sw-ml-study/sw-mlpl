Saga R1 step 001: pure refactor. Renamed crates/mlpl-mlx -> crates/mlpl-mlx-rt via git mv (preserves history); the crate is now the FFI shim only. New crates/mlpl-mlx-serve binary + library reuses mlpl-serve's auth + session storage primitives via path dep + pub imports (no code duplication). 3 endpoints: GET /v1/health (returns device:mlx), POST /v1/sessions (delegates to mlpl_serve::sessions::create_session), POST /v1/eval-on-device (501 stub with forward-pointer to step 002). 4 new tokio api_tests. New contracts/serve-contract/eval-on-device.md documenting the endpoint shape + bincode versioned-envelope wire format that step 002 will implement. NO behavior change visible to MLPL programs -- the existing device('mlx') in-process dispatch keeps working through the renamed feature flag.