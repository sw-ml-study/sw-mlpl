Fix the generated main so a process-effect program does NOT get a spurious trailing Display line after binary/stdout output. Today mlpl-build project.rs write_main_rs always does println!("{}", result) -- so a program ending in write_stdout(bytes) prints ok(N) text after the binary bytes, and print(x) double-prints. Decide the rule from the interpreter's -f script output behavior (mlpl-repl script mode: does it auto-print the final expression?) and match it. Likely: do NOT auto-print when the program's final value is a process effect already emitted (write_stdout / print / eprint), OR more simply suppress the trailing echo for Result/effect-typed finals -- pick the rule that matches the interpreter and keeps a value-returning program showing its result. TDD RED first: a gated MLPL_BUILD_TESTS=1 e2e asserting write_stdout([65,66,67]) yields byte-exact stdout 'ABC' with NO trailing ok(3)/text line, while a value program (e.g. iota(3)) still shows its result. Update the existing write_stdout e2e if it encoded the old trailing-line behavior. Hold or lower sw-checklist. Commit source before agentrail complete.