Close the compiler-process-semantics saga. Document the compiled process builtins (print, eprint, exit, read_stdin) and the pristine-stdout rule as compile-to-Rust capable in docs (lang-reference and/or the compiler capability doc; WHAT/HOW only): a compiled binary's stdout is pristine -- print/write_stdout own the output, a final err goes to stderr and exits 1, a final ok is suppressed (render with disp to show it), a plain value is echoed. Note the interpreter-vs-compiled echo divergence plainly (present tense, no plan framing). Mark compiler-process-semantics SHIPPED in docs/future-sagas-queue.md; compiler-control-flow is the remaining rung to the file-processing hexdump/WAV capstone (note if/while already lower; for/repeat/train do not). Queue an OPTIONAL follow-on for interpreter/compiler stdout parity (silent-success, option 1) if worth it. Refresh docs/companion-demo-file-processing.md item 5 (process semantics cleared). Update the wiki errata + capability row if a compiled-capability claim flips. Also correct the earlier note that control flow is unsupported -- if/while DO lower. Hold sw-checklist. Then agentrail complete --done.