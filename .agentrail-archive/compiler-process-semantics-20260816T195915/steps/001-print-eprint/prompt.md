Lower print/1 and eprint/1 in the compile-to-Rust path. Add runtime functions in a process module of mlpl-rt-value that write the CVal Display to stdout/stderr respectively and RETURN the value unchanged (composable, matching the interpreter's return-the-arg output primitives in eval_intercepts.rs/eval_script.rs). Wire the fncall lowering in mlpl-lower-rs/src/fncall.rs (likely a CvalIo-shaped Emit since the arg is a CVal and the return is a CVal). TDD RED first: a gated MLPL_BUILD_TESTS=1 e2e where a compiled program prints a value to stdout and eprint to stderr, and x = print(v) still yields v. Verify interpreter parity. Hold or lower sw-checklist (exception if test-only holds). Commit source before agentrail complete.