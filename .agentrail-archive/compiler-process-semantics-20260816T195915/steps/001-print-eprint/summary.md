Lowered print/1 and eprint/1 to the compile-to-Rust path: new mlpl-rt-value proc.rs (write CVal Display to stdout/stderr, return the value), CvalIo registry + produces_cval entries (data-driven CVAL_BUILTINS const), dispatch-coverage Print/Eprint variants. Gated e2e proves stdout/stderr routing + return-the-arg composability. Net-zero sw-checklist (retired the produces_cval LOC warning via the const refactor).