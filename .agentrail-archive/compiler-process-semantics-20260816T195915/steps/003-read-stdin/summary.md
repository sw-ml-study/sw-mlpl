Lowered read_stdin/0 to the compile-to-Rust path: mlpl-rt-value proc.rs read_stdin() -> CVal::Str reads all of stdin (documented divergence: a compiled CLI reads whatever is piped, unlike the interpreter's terminal refusal); nullary Emit::ReadStdin, CVAL_BUILTINS entry, dispatch-coverage variant. Gated e2e pipes stdin through a compiled disp(read_stdin()) and asserts the echo. Net-zero sw-checklist (inlined exit's validator to keep proc.rs at 4 functions).