Lower read_stdin/0 to the compile-to-Rust path. Interpreter parity (eval_script.rs read_stdin_to_string via eval_read_stdin): read ALL of stdin into a string (CVal::Str), empty on immediate EOF; the interpreter errors if stdin is a terminal (is_terminal) -- decide whether the compiled binary mirrors that or just reads (a compiled CLI is normally piped; document the choice). Add a runtime fn in mlpl-rt-value proc.rs (read_stdin() -> CVal::Str) reading std::io::stdin to a String; wire the fncall lowering. read_stdin takes 0 args and returns a CVal -- likely a dedicated Emit (like Args) emitting rt::read_stdin(); produces_cval must LIST read_stdin (it yields a CVal::Str). Add the dispatch-coverage ReadStdin variant. TDD RED first: a gated MLPL_BUILD_TESTS=1 e2e piping stdin into a compiled program that echoes it (e.g. disp(read_stdin())) and asserting byte-for-byte. Hold or lower sw-checklist (exception if test-only holds). Commit source before agentrail complete.