CA5: added a polymorphic len(x) -- string-list item count OR array leading-axis length (rows, like tally); bare string errors clearly. There was no len builtin before (only list_len/tally). Generalized eval_list_len to serve both list_len and len (no new function). Tests len_counts_string_list_items/len_of_array_is_leading_axis_length/len_of_a_bare_string_errors_clearly; list_len unchanged. lang-reference documents len. sw-checklist 38 failed (held), 568 warnings (+1 exception: eval_list_len 31 LOC serving both names). Rebuilt repl/build/serve. Pushed.