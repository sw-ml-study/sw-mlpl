CA4: added make_dir(path) -- sandboxed directory creation (with missing parents), so write_text/write_bytes into a new subtree work after make_dir. New fs_mkdir module (kept out of the already-FAILing eval_fs); relative-only, contained() to env.fs_root, refused outside. Tests make_dir_creates_nested_directories_and_write_text_then_works + make_dir_refuses_outside_the_sandbox; fs_api 7/7. lang-reference documents make_dir + write_text-no-parents note. sw-checklist 38 failed (held), 570 warnings (+1 new module, exception). Rebuilt repl/build/serve. Pushed.