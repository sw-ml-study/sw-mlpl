Step 003: Save/download.

Save button collects REPL input history (input lines only, filtering narration), joins with newlines, triggers browser download as session.mlpl via Blob + URL.createObjectURL + click-on-anchor.

Copy to Editor button loads the REPL input history into the editor textarea for editing and re-running.

Pages rebuild required.