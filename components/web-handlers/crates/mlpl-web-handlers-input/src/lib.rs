//! Keyboard / input event handlers: `make_keydown` (history +
//! popup keys), `make_oninput` (textarea state sync), the
//! autocomplete popup keydown dispatch (popup.rs), and the
//! generic `toggle_bool` button-callback helper.

pub mod input;
pub mod popup;
pub mod toggle;
