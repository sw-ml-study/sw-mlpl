//! Vision / attention / vision-transformer demos. Each module
//! exposes `pub const` `Demo` entries threaded into the global
//! `DEMOS` list by the facade crate's aggregator.

pub mod attention;
pub mod cnn;
pub mod vit;
