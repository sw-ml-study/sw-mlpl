[package]
name = "mlpl-lower-rs"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
mlpl-parser = { path = "../../../syntax-parser/crates/mlpl-parser" }
proc-macro2 = "1"
quote = "1"

[dev-dependencies]
# Source of truth for the bit-op family; the dispatch-coverage test
# asserts the fncall registry stays in sync with it.
mlpl-runtime-bits = { path = "../../../runtime-element/crates/mlpl-runtime-bits" }
