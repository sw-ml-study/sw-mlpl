[package]
name = "mlpl-lexer-token"
edition.workspace = true
license.workspace = true
version.workspace = true

# Token types for the MLPL lexer.

[dependencies]
mlpl-core = { path = "../../../types/crates/mlpl-core" }
